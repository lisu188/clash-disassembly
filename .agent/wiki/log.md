# Agent Log

## 2026-04-16

- Continued the executable-regeneration startup/runtime slice on `codex/full-game-bootstrap-args`.
- Kept the current default route on `main -> App_WinMain`; host-side probe switches and menu-probe env selectors remain removed.
- Restored the retained log-file creation side effect: `createLogFiles` now gates on `logEnabled`, truncates/creates `clash.log` and `battle.log`, closes both handles, and clears `dword_526A20`.
- Restored `logEnabled = 1` before the runtime slab setup and the `createLogFiles(0, 0, 0)` handoff after the still-deferred `CSS_Init` point.
- Revalidated the recovered C baseline, bootstrap executable, C++ core, and C++ executable-regeneration targets; both executables still enter the recovered live route and require external timeout kills.
- Updated the durable sidecars so the live frontier is finite full-route verification, the skipped `CSS_Init` table, and the broader playable session milestone.

## 2026-04-13

- Reloaded the durable repo memory again after the `PlayGame` batch, reused the read-heavy subagents on `WorldMap_RunHumanTurnLoop`, and confirmed from asm/map evidence that the next retained blocker stayed in recovered-C human-turn control flow below SDL, compat, and `src_cpp`.
- Repaired the first exact human-turn slice directly in `clash95.c`: `WorldMap_RunHumanTurnLoop` now zero-initializes its entry flags from asm and restores the mission-success `sub_4623C0("arama1", "kon_por1")` tail instead of forwarding a fabricated local.
- Re-ran `clash95_recovered`, `clash95_bootstrap`, and `clash95_cpp_regen`, plus the retained `PlayGame_Dispatch` and `PlayGame` standalone probes and the exact traced contained post-confirm menu probes after the human-turn-loop fix. All green targets stayed green, the direct retained `PlayGame_Dispatch` and `PlayGame` probes still link and exit `124`, and the contained runtime split is still `oddzial` with broader rules versus `MAIN` without them.
- Advanced the durable memory again: the retained frontier is no longer the `PlayGame` handoff or the bare `WorldMap_RunHumanTurnLoop` entry, but the deeper register-loss/usercall surface inside `WorldMap_RunHumanTurnLoop`, with `sub_451F70` still secondary and the contained class/bload `oddzial` / `MAIN` split still separate.


- Reloaded the durable repo memory in the required order, rechecked the newest `.codex-loop` stderr files, and confirmed they are only stale launcher/rules failures rather than evidence about the live retained frontier.
- Reused the read-heavy subagents immediately on the post-mission `PlayGame` frontier; they converged that the next honest retained target is the human-turn entry `WorldMap_RunHumanTurnLoop`, with SDL, compat, and `src_cpp` still secondary or below the line.
- Repaired the first post-mission retained slice directly in recovered C: `Scenario_LoadMissionByIndexAndPlay` now keeps its real 27-byte campaign-state save/restore explicit and returns into `PlayGame` without the fabricated `v5` / `v7` temporaries, while the `PlayGame` prologue now restores the real `backgr1/2/3.s32` and `treemas1/2/3.s32` resource names and initializes the player-reveal scan counters from asm.
- Re-ran `clash95_recovered`, `clash95_bootstrap`, and `clash95_cpp_regen`, plus the retained `PlayGame_Dispatch` and `PlayGame` standalone probes and the exact traced contained post-confirm menu probes. All green targets stayed green, the new direct `PlayGame` retained probe now also links and exits `124`, and the contained runtime split is still `oddzial` with broader rules versus `MAIN` without them.
- Advanced the durable memory from the generic “broader gameplay/session surface after PlayGame” wording to the concrete next retained symbol `WorldMap_RunHumanTurnLoop`, while keeping the contained class/bload `oddzial` / `MAIN` split separate from that retained gameplay/session lane.

- Reloaded the durable repo memory in the required order, checked the newest `.codex-loop` files again, and resumed directly from the stored retained mission-loader frontier at case `18` / `p_mapa9j.map`.
- Reused the read-heavy subagents immediately on case `18`; runtime, SDL, and class seam checks all reconfirmed the live work stayed in recovered-C mission setup below compat, SDL, and `src_cpp`, while the boot-path evidence pinned the exact `p_mapa9j.map` arm and its neighboring `case 19` / `p_map10z.map` lane.
- Recovered case `18` / `p_mapa9j.map` directly in `Scenario_LoadMissionByIndex`, including the five-player `Raylin` / `Tubius` / `Lord Gorio` / `McDan` / `Drebegen` setup, the `Stone Bell` `BUILDING_RECORD(+438) -= 100` cut, the `Fhur Tao` `BUILDING_RECORD(+438) += 200` boost, the raw slot-state mutation bands on the `+28` lane, and the preserved absence of any post-init camera override or `Rules_LogAssigned*` tail.
- Re-ran `cmake -S . -B build`, the three green build targets, the retained `PlayGame_Dispatch` standalone probe, and the exact traced contained post-confirm menu probes after case `18`; all green targets stayed green, the retained probe still links and exits `124`, and the contained runtime split is still `oddzial` with broader rules versus `MAIN` without them.
- Corroborated that the already-present case `19` / `p_map10z.map` body matches the retained `mapP10` asm lane closely enough to treat the whole 20-case mission switch as covered, while also confirming that mission-conditional UI switches should not be deduplicated just because the switch reuses adjacent mission aliases.
- Advanced the durable memory off the mission-loader switch entirely, so the next `continue` resumes from the broader gameplay/session surface after `Scenario_LoadMissionByIndexAndPlay` / `PlayGame` instead of re-auditing the already-covered `p_mapa9j.map` or `p_map10z.map` lanes.

## 2026-04-12

- Reloaded the durable repo memory in the required order, re-read the newest `.codex-loop` entries, and resumed directly from the stored retained mission-loader frontier at case `14` / `p_mapa5l.map`.
- Reused the read-heavy subagents immediately on case `14`; all five converged that the live work stayed in recovered-C mission setup below SDL, compat, and `src_cpp`, and the boot-path mapper pinned the exact `p_mapa5l.map` case order plus the next jump-table target `loc_45E070` / case `15` / `p_mapa6l.map`.
- Recovered case `14` / `p_mapa5l.map` directly in `Scenario_LoadMissionByIndex`, including the three-player `Raylin` / `Uraken` / `Wodar` setup, the `Weghetown` post-castle `BUILDING_RECORD(+430) & 0xF000` clear, the `Timbran` forced-garrison lane with its raw 12-slot `& 0xFC | 1` loop, the two `Building_OnGarrisonChange` handoffs, the three prisoner-slot writes, the `Fraggmeon` / `Eufurhon` `BUILDING_RECORD(+438) += 200` boosts, the final `Rules_LogAssignedCastleFact(..., 14)` tail, and the preserved absence of any post-init camera override.
- Re-ran `cmake -S . -B build`, the three green build targets, the retained `PlayGame_Dispatch` standalone probe, and the exact traced contained post-confirm menu probes; all green targets stayed green, the retained probe still links and exits `124`, and the contained runtime split is still `oddzial` with broader rules versus `MAIN` without them.
- Advanced the durable memory from case `14` / `p_mapa5l.map` to case `15` / `p_mapa6l.map`, then stayed on the same retained mission-loader lane instead of stopping after the first green batch.
- Recovered case `15` / `p_mapa6l.map` directly in `Scenario_LoadMissionByIndex`, including the mission-local byte clear, the four-player `Raylin` / `Frederic` / `Sir James` / `Agordeh` setup, the player-4 intelligence write, the `Defambrion` `BUILDING_RECORD(+438) += 200` boost, the `Ghih Up` `BUILDING_RECORD(+438) = 1000` and `BUILDING_RECORD(+18) = -1` writes, the `Guluali` `BUILDING_RECORD(+438) = 2000` write, the final `Rules_LogAssignedPlayerFact(4, 15)` tail, and the preserved absence of any post-init camera override.
- Re-ran the three green build targets, the retained `PlayGame_Dispatch` standalone probe, and the exact traced contained post-confirm menu probes again after case `15`; all green targets stayed green, the retained probe still links and exits `124`, and the contained runtime split is still `oddzial` with broader rules versus `MAIN` without them.
- Advanced the durable memory from case `15` / `p_mapa6l.map` to case `16` / `p_mapa7j.map`, so the next `continue` resumes from the next real jump-table arm instead of re-auditing the already-recovered `p_mapa6l.map` lane.
- Reloaded the durable repo memory again, reused the read-heavy subagents on case `16`, and confirmed from asm/map evidence that the live retained blocker stayed pure recovered-C mission setup below compat, SDL, and `src_cpp`; the only live transcription hazard was not to misread the saved `push ebx` as a `createUnit` vararg.
- Recovered case `16` / `p_mapa7j.map` directly in `Scenario_LoadMissionByIndex`, including the mission-local byte clear, the three-player `Raylin` / `Kalev` / `Ianos` setup, the `Gwadat` `BUILDING_RECORD(+444)` masked write, the four raw slot-state mutation bands, the player-1 camera override after `Game_InitPlayerViewState`, and the preserved absence of any `Rules_LogAssigned*` tail.
- Re-ran `cmake -S . -B build`, the three green build targets, the retained `PlayGame_Dispatch` standalone probe, and the exact traced contained post-confirm menu probes after case `16`; all green targets stayed green, the retained probe still links and exits `124`, and the contained runtime split is still `oddzial` with broader rules versus `MAIN` without them.
- Advanced the durable memory from case `16` / `p_mapa7j.map` to case `17` / `p_mapa8j.map`, so the next `continue` resumes from the next real jump-table arm instead of re-auditing the already-recovered `p_mapa7j.map` lane.
- Reloaded the durable repo memory again, reused the read-heavy subagents on case `17`, and confirmed from asm/map evidence that the live retained blocker stayed pure recovered-C mission setup below SDL, compat, and `src_cpp`; the main transcription hazards were the four leading treasure retractions, the `Dark Town` `BUILDING_RECORD(+438) -= 100` cut, and the four raw slot-state mutation bands.
- Recovered case `17` / `p_mapa8j.map` directly in `Scenario_LoadMissionByIndex`, including the mission-local byte clear, the four `Rules_RetractTreasureFact` calls, the four-player `Raylin` / `Lord Ruwe` / `McGregor` / `Crowley` setup, the `Stormus` / `Dark Town` / `Treg Rock` / `Bodeon` / `Girock` / `Leweburg` / `Defambrion` castle lane, the `Dark Town` `BUILDING_RECORD(+438) -= 100` cut, the four raw slot-state mutation bands, and the preserved absence of any post-init camera override or `Rules_LogAssigned*` tail.
- Re-ran `cmake -S . -B build`, the three green build targets, the retained `PlayGame_Dispatch` standalone probe, and the exact traced contained post-confirm menu probes after case `17`; all green targets stayed green, the retained probe still links and exits `124`, and the contained runtime split is still `oddzial` with broader rules versus `MAIN` without them.
- Advanced the durable memory from case `17` / `p_mapa8j.map` to case `18` / `p_mapa9j.map`, so the next `continue` resumes from the next real jump-table arm instead of re-auditing the already-recovered `p_mapa8j.map` lane.

## 2026-04-11

- Reloaded the durable repo memory again, checked the stale `.codex-loop` errors, and resumed the retained mission-loader widening from `mapK6` / case `5`.
- Reused the read-heavy subagents on `mapK6`; the runtime, SDL, and class explorers all reconfirmed the frontier stayed in recovered-C mission setup, while the boot-path review pinned the exact localized player-name fork and the raw post-`Ghih Up` 10-slot status loop.
- Materialized `mapK6` / case `5` directly in `Scenario_LoadMissionByIndex`, including the `gameData + 140021` mission-local flag clear, the `Furd` / `Dulimam` versus `Wetus` / `Riludius` split, the player-2 religion flag clear, the player-1 minimap reveal, the player-1..3 intelligence writes, the `Defambrion` / `Katha Gha` / `Ghih Up` building-field mutations, the raw 10-slot status OR loop through `gameData + 564880`, and the final `Rules_LogAssignedPlayerFact(3, 5)` tail.
- Re-ran the three green build targets, the retained `PlayGame_Dispatch` link probe, and the exact traced contained post-confirm menu probes, and confirmed the protected split is unchanged:
  - broader rules still reaches `class-lookup-no-table name=oddzial`
  - `CLASH95_LOAD_MENU_PROBE_BROADER_RULES=0` still fails earlier on `symbol-lookup-missing-table MAIN`
- Advanced the durable memory from `mapK6` to `mapK7`, so the next `continue` resumes from case `6` instead of re-auditing the already-recovered `k_mapa6j.map` lane.
- Reloaded the durable repo memory, validated the three green build targets plus the retained `PlayGame_Dispatch` probe, and resumed the retained mission-loader widening from `mapK5` / case `4`.
- Reused the read-heavy subagents on `mapK5`; the SDL/runtime/class explorers all reconfirmed the frontier stayed in recovered-C mission setup, while the fresh explorer pinned the exact `mapK5` body and caught the absence of minimap writes plus the exact early `Totaweon` spawn arguments.
- Materialized `mapK5` / case `4` directly in `Scenario_LoadMissionByIndex`, including the three-player setup, the `Totaweon` / `Hopenberg` spawn lists, the `BUILDING_RECORD(castle_index) + 18 = 9` preseed, the `Building_UnitGetInto` handoff through the raw stack word at `gameData + 0x8A9A4`, the 12-slot raw OR loop, the prisoner-slot writes, and the final `Rules_LogAssignedCastleFact(*(WORD *)(gameData + 0x8A9A0) - 0x8000, 4)` tail.
- Re-ran the three green build targets, the retained `PlayGame_Dispatch` link probe, and the exact traced contained post-confirm menu probes, and confirmed the protected split is unchanged:
  - broader rules still reaches `class-lookup-no-table name=oddzial`
  - `CLASH95_LOAD_MENU_PROBE_BROADER_RULES=0` still fails earlier on `symbol-lookup-missing-table MAIN`
- Advanced the durable memory from `mapK5` to `mapK6`, so the next `continue` resumes from case `5` instead of re-auditing the already-recovered `k_mapa5j.map` lane.

## 2026-04-09

- Reloaded the durable repo memory and corrected the stale assumption that the live frontier was still `gfx\\backgr1.s32`; the latest durable state was already inside the contained load-menu corridor.
- Reproduced the current widened row-resource crash and found the first failure in `DLXSpriteSet_Load("cache\\...")` after a repo-local cache false positive.
- Repaired the compat `Render_LoadResourceSprite_v4` cache gate so cache reuse only happens when the mounted-query layer can reopen the same file.
- Reproduced the next crash in `DLXSpriteSet_DrawText` and matched the original x86 contract: source font colors come from companion `*.pfn` assets, target colors come from the live menu palette.
- Added `Compat_LoadFontPaletteTable` to load `*.pfn` palettes through the mounted-query layer and feed the proper source palette into the text remap.
- Interrupted the first widened row-draw stall in gdb and confirmed `sub_40BD40` had the classic decompiler scar where non-newline characters never advanced the cursor.
- Repaired `Render_LoadResourceSprite_v3` in the quarantine seam with the asm-backed cursor advance.
- Reproduced the next crash in `sub_40BC00` and repaired the recovered C function to match the x86 active-slot record and glyph-step contract.
- Validated that the widened contained probe now loads `Render_LoadResourceSprite_v4(18/21)`, completes all ten row draws, survives the `back` exit, and also survives contained slot-hover selection with `selected_slot = 0`.
- Reworked the contained load-menu probe so a combined slot-and-confirm run clicks the save-slot strip first, which lets the authentic `dword_5441E0` / `sub_44A110` corridor fire on a real occupied save slot.
- Validated that the widened contained confirm probe now exits with `selected_slot = 0`, `confirm = 1`, and `screen = 5`.
- Tried to widen the bootstrap probe directly into `sub_444490`, confirmed that this reopens the deeper unresolved runtime/parser link band, and reverted that direct call to keep the bootstrap surface green.
- Reproduced the new post-confirm world-map-init stop and found a live recovered-C bug in `sub_4163F0`: the decompiler had left an uninitialized-index loop where the asm shows a finite 15-iteration RNG seeding pass.
- Repaired `sub_4163F0` to match the x86 loop and confirmed that the contained authentic load path now reaches `load-menu-post-confirm-load-save`.
- Took an escalated gdb backtrace on the new save-load crash and pinned it to `sub_4443C0 -> sprintf_` while formatting `save\\%d.dat`.
- Repaired `sub_4443C0` and `sub_4443D0` so the save-path builders take real `char *` buffers instead of truncating stack addresses to low 32-bit ints.
- Validated that the contained authentic `Load Game` lane now survives `WorldMap_Initialize`, survives the `.dat` / `.fac` load in `sub_444490`, reaches `load-menu-post-confirm-after-save`, and returns to `main-after-menu-probe`.
- Tried to widen the contained post-confirm probe by seeding the broader rules symbol/number tables through `sub_482260` before `sub_444490`.
- Confirmed that the experimental widening exposes a deeper recovered allocator/runtime blocker in `sub_472E40 -> sub_472D70 -> sub_472620 -> sub_4827B0 -> Str_Intern -> sub_482260`.
- Captured crash-state evidence showing the allocator pool tail already pointing into the first hash-table allocation when that broader rules bootstrap is enabled.
- Rolled the experimental widening back out of the live probe path so the repo keeps the green `load-menu-post-confirm-after-save` foothold.
- Reconciled the stale durable memory against the live worktree and confirmed the current broader contained probe no longer stops at the old allocator crash note.
- Materialized `unk_50293C` plus the adjacent `bload` loader strings in `clash95.c` so the retained `sub_47C850` slice no longer dies on missing loader data.
- Recovered `unknown_libname_4` as the allocator-failure callback swap and repaired `sub_47CBF0` to match the binary-loader allocate / retry / restore contract.
- Added a `CSyncObject_Unlock` C ABI bridge in `src_cpp/csync_object.cpp` so the retained startup-prelude probes can resolve the lock seam through `clash95_cpp_core`.
- Confirmed a direct-object retained probe for `Rules_ShowBanner_StrategicClash` now links successfully.
- Confirmed the archive-backed retained probe for `Rules_ShowBanner_StrategicClash` now links too, so the remaining retained blocker has moved to the broader `sub_451E46` unresolved set.
- Re-ran the broader contained load-menu probe and confirmed the live crash frontier is now `parse-make-instance-before-class-lookup` on `oddzial`, not the earlier allocator note in stale sidecars.
- Added guarded traces in `sub_4AB0B0` / `sub_4B0480` and proved the wider post-confirm `oddzial` crash is a null defclass registry: `class-lookup-no-table name=oddzial`.
- Re-ran the same probe with `CLASH95_LOAD_MENU_PROBE_BROADER_RULES=0` and confirmed the earlier failure is still `symbol-lookup-missing-table MAIN`.
- Tried an env-gated direct `PlayGame` handoff from `bootstrap_main.c`, confirmed it immediately widened the bootstrap link surface into the unresolved gameplay/session band, and reverted that change to keep the executable footholds green.
- Traced `dword_51AD68` back to `sub_4B0940`, confirmed retained probes for `sub_4B0940` and `sub_499990` link cleanly, and confirmed retained `sub_4996D0` still fans into the deeper AST / locale / `JUMPOUT` band.
- Materialized `dbl_502FDC` in `clash95.c` as exact `0.5`, removing it from the retained `sub_451E46` unresolved set.
- Recovered `AST_FreeNode` directly from the asm body and confirmed the retained probes no longer carry it as a blocker.
- Recovered `unknown_libname_7` / `unknown_libname_8` as the `dword_51A1EC` / `dword_51A928` setter-swaps, and corrected the `sub_47D0E0` tail so it now points at retained `.fn_init` (`sub_49A0E0`) instead of the unrelated runtime setter.
- Recovered `mblen_`, `mblen__0`, and `sub_4D88F0` from `clash95.exe`, including the missing token-table globals at `unk_51B488` through `unk_51B698`.
- Re-ran the retained probes and confirmed `sub_4996D0` now narrows to `unk_508D50` plus nearby `JUMPOUT` scars, while `sub_451E46` now honestly exposes `sub_49A0E0` alongside `sub_496643`, `ftime_`, `system_`, `JUMPOUT`, and deeper parser/math helpers.

## 2026-04-10

- Verified the live tree before editing and confirmed `sub_49A0E0` was already materialized in `clash95.c`, while `dword_51ACC4` still needed to be tied to the high half of `qword_51ACC0`.
- Re-ran the retained linker probes and confirmed the honest next retained blocker had moved off `unk_508D50` and onto the local `JUMPOUT` scar cluster around `Compiler_MarkAndEmit`, `sub_4D0660`, `sub_4D0710`, and `sub_4D2AC0`.
- Corrected `unk_508D50` to the exact `5,6,7,"CLIPS",0,0` bytes and collapsed the first retained `JUMPOUT` cluster back into normal control flow.
- Re-ran the retained probes and confirmed `sub_4996D0` now linked cleanly while the broader `sub_451E46` slice narrowed to `sub_496643`, `ftime_`, `system_`, and a second local `JUMPOUT` band.
- Recovered `sub_496643` into `compat/decomp_runtime_stubs.c` as the asm-backed Watcom signal-table helper with the original default handler table and ctrl-handler gating.
- Reconstructed the `ftime_` and `system_` callsites in `sub_47D360` / `sub_47D3D0`, then added quarantined CRT wrappers for those helpers in `compat/decomp_runtime_stubs.c`.
- Recovered the second local `JUMPOUT` band in `sub_47F480`, `sub_48E1A0`, and `sub_491790`, including the lost outer/inner enumeration updates in `sub_47F480`.
- Re-ran the retained probes and confirmed `sub_451E46` is now blocked by the deeper parser/class/math band led by `sub_4B6DD0`, `Lexer_ParseSlotConstraint`, `Lexer_ParseFieldSpec`, `sub_4BDD40`, `unknown_libname_13`, `ismbdprint_`, and the `IF_*` / `__FYL2X__` helpers.
- Re-ran the traced contained menu probes under `timeout 2s` and reconfirmed the live split: broader rules still reach `parse-make-instance-before-class-lookup` and `class-lookup-no-table name=oddzial`, while `CLASH95_LOAD_MENU_PROBE_BROADER_RULES=0` still logs `symbol-lookup-missing-table MAIN` earlier in the same corridor.
- Materialized the retained parser-export alignment slice in `clash95.c`: exact `aJ_0 = "+j"`, `unknown_libname_13`, `ismbdprint_(logical_name)`, `sub_4B6DD0`, and `sub_4BDD20` / `sub_4BDD40`.
- Rebound the already-recovered local parser bodies to their map-backed exports: `sub_48BD50 -> Lexer_ParseSlotConstraint`, `sub_48BE80 -> Lexer_ParseFieldSpec`, `sub_4C2710 -> Lexer_ValidateMessageHandler`, `sub_4CC6C0 -> Lexer_ParseDefglobal`, `sub_4D9C40 -> Lexer_ParseRuleRHS`, and `sub_4D9FD0 -> Lexer_ParseDeclareOptions`.
- Re-ran the retained probes and confirmed `sub_4996D0` still links while `sub_451E46` now narrows past the cleared parser-export layer into the deeper slot/parser/math/runtime band led by `Lexer_BuildSlotNode`, `Lexer_FindSymbolIndex`, `Lexer_CheckValueList`, `Lexer_EmitSlotBinding`, `Lexer_FindTemplateSlot`, `Lexer_OutputFieldRange`, `Lexer_ParseModifyOrDuplicate`, `Lexer_WarnImpliedTemplate`, `unknown_libname_2`, `MoveFileA`, `fgets_`, `sscanf_`, `ceil_`, `floor_`, the `IF_*` / `__FYL2X__` helpers, and `JUMPOUT`.
- Re-ran the short smokes and the full post-confirm contained menu probes after the parser-export wave; the build targets stayed green, and both timeout-driven menu-probe variants again reproduced the same split under the timeout/core-dump band: broader rules reached `class-lookup-no-table name=oddzial`, while `CLASH95_LOAD_MENU_PROBE_BROADER_RULES=0` failed earlier on `symbol-lookup-missing-table MAIN`.
- Rebound the remaining retained slot/parser exports in place: `sub_47A3F0 -> Lexer_EmitSlotBinding`, `sub_48BDD0 -> Lexer_BuildSlotNode`, and `sub_48D7B0 -> Lexer_FindSymbolIndex`.
- Replaced the placeholder `unknown_libname_2` bridge with the exact asm-backed signed decimal parser and added narrow compat wrappers for `MoveFileA`, `sscanf_`, and `fgets_`.
- Recovered the current `fgets_` call shapes in `AI_CalcStrategicPriorityScore`, `sub_4A5000`, `sub_4A5460`, `sub_4A5AB0`, and `sub_4A60A0` so the retained startup-prelude slice no longer depends on the decompiler's dropped buffer argument.
- Re-ran the retained `sub_451E46` probe and confirmed the broader startup-prelude widening is now reduced to the remaining x87-heavy math/runtime helpers `IF_DACOS`, `IF_ASIN`, `IF_DCOSH`, `IF_DSINH`, `IF_DTANH`, `__FYL2X__`, `__FPREM__`, `__F2XM1__`, `__FSCALE__`, `floor_`, `ceil_`, and `IF_DPOW`.
- Reused the read-heavy subagent set to re-audit the retained frontier and confirmed again that the x87 band belonged to recovered rules/runtime math, not to the SDL seam or the C++ class seams.
- Recovered the retained `sub_4A3FF0` through `sub_4A4E70` rules math builtin band directly in `clash95.c`, replacing the decompiler's x87 pseudo-symbols with evidence-backed libc math while preserving the original `sub_4A3C50` / `sub_4A3CB0` / `sub_4A3D10` error paths.
- Re-ran the retained `sub_451E46` probe and confirmed the old x87-heavy math/runtime blocker set is gone entirely; the retained `sub_451E46` link probe now succeeds and its standalone probe binary stays alive for `timeout 1s`.
- Broadened the retained standalone probes and confirmed `sub_460490` and `UI_StartAnims` now link too, matching the corroborated startup order around `init_clips()`, `mouse.s32`, `map.pal`, `StartMenu`, and the intro AVI handoff.
- Took the next honest retained widening to `PlayGame_Dispatch` and confirmed the frontier has moved to the broader front-end/gameplay link surface led by `_wcpp_4_static_init__`, `sub_43D100`, `unk_512008`, `off_5123CC`, `rand_`, `memmove_`, `strlwr_`, `Locale_DrawInteger`, `Rules_LinkArmyFinalize`, `Rules_UnlinkArmyFact`, `Render_DrawSprite_v3`, `UI_LoadTurnBannerGfx`, `WCIsvListBase_*`, and nearby `JUMPOUT`.
- Re-ran the short smokes plus the full traced contained post-confirm menu probes and confirmed the protected SDL-backed load-menu lane is unchanged: broader rules still reaches `parse-make-instance-before-class-lookup` then `class-lookup-no-table name=oddzial`, while `CLASH95_LOAD_MENU_PROBE_BROADER_RULES=0` still fails earlier on `symbol-lookup-missing-table MAIN`.
- Reused the live read-heavy subagents and confirmed the first retained `PlayGame_Dispatch` misses belonged in recovered C/data rather than the SDL seam, the C++ class seams, or `compat/decomp_runtime_stubs.c`.
- Materialized the front-end cursor/overlay descriptor slab around `unk_519718` through `unk_519920` as exact 40-byte records consumed by `sub_460D80`.
- Rebound the already-recovered local bodies behind the first retained front-end/world-map aliases: `UI_LoadTurnBannerGfx`, `Locale_DrawInteger`, `Rules_UnlinkArmyFact`, `Rules_LinkArmyFinalize`, `Unit_DebugDumpFormationSizes`, `Render_DrawSprite_v3`, and `UI_CheckEndTurnHotkey`.
- Rebound the next retained helper/export aliases in place: `sub_412000`, `sub_4120B0`, `Unit_SetFlag`, `Map_IsTilePlacable`, `Unit_FindById`, `Building_AutoFillOrUseGarrison`, `UI_DrawUnitStatsValues`, and `Building_DrawGarrisonRow`.
- Materialized the battle/shot/port/queen debug string slab plus `dword_517B48`, `g_PortReinforcementUnitTypePool`, and `word_5191F0` directly from `clash95.asm`.
- Re-ran the retained `PlayGame_Dispatch` probe and confirmed the broader unresolved band is now reduced to `UI_CheckDialogAccepted`, `UI_CheckConfirmQuit`, `unit_stats`, the deeper queen birth/departure arrays and buffers, and the remaining `JUMPOUT` scars.
- Re-ran the full traced contained post-confirm menu probes after the retained alias/data pass and confirmed the protected SDL-backed load-menu lane is still unchanged: broader rules still reaches `parse-make-instance-before-class-lookup` then `class-lookup-no-table name=oddzial`, while `CLASH95_LOAD_MENU_PROBE_BROADER_RULES=0` still fails earlier on `symbol-lookup-missing-table MAIN`.
- Re-ran the exact traced post-confirm menu probes with the real `CLASH95_MENU_PROBE_AUTO_CLICK=load` lane and reconfirmed the same contained split: broader rules still reaches `class-lookup-no-table name=oddzial`, while `CLASH95_LOAD_MENU_PROBE_BROADER_RULES=0` still fails earlier on `symbol-lookup-missing-table MAIN`.
- Rebound `UI_CheckConfirmQuit` and `UI_CheckDialogAccepted` onto the already-recovered local bodies at `sub_460270` and `sub_4602F0`, then replaced the `sub_4602F0` default shared-epilogue `JUMPOUT` with the asm-backed `return 0`.
- Materialized the queen departure-event DGROUP slab around `g_QueenMsgBuf`, including `g_QueenDepartureEventMessageBuffer`, `g_QueenDepartureTexts`, `g_QueenCastleTreasuryTheftTexts`, `g_QueenCastleWellPoisoningTexts`, `g_QueenCastleArsonTexts`, `off_519350`, and `off_51935C`, and rebound the reached `unit_stats` byte lane to the existing local metadata table.
- Recovered `Map_RebuildCastleSiteAnchorCache` from the asm-backed even-slot anchor-cache loop and removed that retained `JUMPOUT` scar from the live `PlayGame_Dispatch` probe.
- Re-ran the retained `PlayGame_Dispatch` probe and confirmed the broader UI/data/runtime band is gone: the surviving unresolved is now only `Scenario_LoadMissionByIndex` / `sub_460360`.
- Reused the read-heavy subagents again and confirmed the narrowed retained blocker is pure recovered C mission-loader work, not SDL seam work, not `compat` runtime glue, and not a `src_cpp` class seam.
- Reloaded the durable repo memory again, revalidated the three green build targets, and reran the full contained post-confirm load-menu probes before touching the retained mission-loader frontier.
- Reused the read-heavy subagents against the live retained frontier and confirmed the menu-reachable selectors are only `0` and `10`, the current blocker still lives in recovered C mission setup, and SDL / `src_cpp` remain out of scope for this slice.
- Recovered `createUnit` and `createCastle` as real vararg helpers with explicit `-1`-terminated unit lists and updated the existing local callsites to stop depending on x86 stack accidents.
- Threaded the real selector through `Scenario_LoadMissionByIndexAndPlay` into `Scenario_LoadMissionByIndex`, carrying the live floating-point argument into the first recovered mission cases just as `sub_460370` does in asm.
- Materialized the first menu-reachable mission-loader cases from `sub_460360` directly in `clash95.c`:
  - case `0`: `k_mapa1l.map`, `Alan`, `Bochuwit`, `Cantbelly`
  - case `10`: `p_mapa1z.map`, `Raylin`, `Gaalaad`, `Timbran`
- Re-ran the retained `PlayGame_Dispatch` link audit with `bootstrap_main.c.o` included and confirmed the old `Scenario_LoadMissionByIndex` `JUMPOUT` blocker is gone; the retained widening now stops at `sub_40D330` and `sub_44C2A0`.
- Re-ran the full contained post-confirm menu probes and confirmed the protected split is unchanged:
  - broader rules still reaches `parse-make-instance-before-class-lookup` and `class-lookup-no-table name=oddzial`
  - `CLASH95_LOAD_MENU_PROBE_BROADER_RULES=0` still fails earlier on `symbol-lookup-missing-table MAIN`
- Reused the read-heavy subagents on the retained mission-loader helper band and confirmed the remaining `sub_40D330` / `sub_44C2A0` gap was already local recovered C: `MiniMap_CreateSurface` and `Game_InitPlayerViewState`, not SDL, not `compat`, and not `src_cpp`.
- Rebound the first recovered mission-loader cases onto `MiniMap_CreateSurface` / `Game_InitPlayerViewState`, re-ran the retained `PlayGame_Dispatch` link audit, and confirmed the direct retained probe now links and stays alive under `timeout 1s`.
- Materialized `mapP2` / case `11` directly in `Scenario_LoadMissionByIndex` from `clash95.asm` (`p_mapa2z.map`, `Raylin`, `Wetus`, `Gatgally`, `Guluali`) without widening SDL or compat seams.
- Corrected the stale post-confirm verification command by setting `CLASH95_LOAD_MENU_PROBE_AUTO_CLICK=confirm`, then revalidated the contained split with exact logs: broader rules still reaches `class-lookup-no-table name=oddzial`, while `CLASH95_LOAD_MENU_PROBE_BROADER_RULES=0` still fails earlier on `symbol-lookup-missing-table MAIN`.
- Reloaded the durable repo memory again, reran the three green build targets plus the corrected contained post-confirm probes, and confirmed the protected `oddzial` / `MAIN` split still holds before touching the next mission case.
- Reused the read-heavy subagents against `mapK2` and corroborated the exact retained case-1 body: `k_mapa2l.map`, `Alan`, `Ianos`, `Stormus`, `Drakefly`, the case-local `BUILDING_RECORD(castle_index) + 18 = -1` plus `Building_OnGarrisonChange` handoff, and the manual camera override after `Game_InitPlayerViewState`.
- Materialized `mapK2` / case `1` directly in `Scenario_LoadMissionByIndex`, kept the post-castle garrison refresh explicit in recovered C, and revalidated that the retained `PlayGame_Dispatch` probe still links and stays alive while the contained menu probes still reproduce `oddzial` with broader rules and `MAIN` without them.
- Reused the read-heavy subagents again on `mapK3` and confirmed the live retained blocker was still pure recovered-C mission setup: `runtime_glue_mapper`, `sdl_seam_mapper`, `class_seam_mapper`, and `external_corroborator` all stayed below compat, SDL, and `src_cpp`, while `boot_path_mapper` corroborated the exact case-2 order from `clash95.asm`.
- Recovered `mapK3` / case `2` directly in `Scenario_LoadMissionByIndex`, including the player-2 intelligence write, the `Treg Rock` post-castle `BUILDING_RECORD(castle_index) + 18 = -1` plus `Building_OnGarrisonChange` handoff, and the post-`Game_InitPlayerViewState` camera override.
- Re-ran the three green build targets plus the retained `PlayGame_Dispatch` probe and confirmed the retained executable-regeneration surface still links cleanly after `mapK3`.
- Re-ran the exact traced contained post-confirm menu probes and reconfirmed the same protected split under the timeout/core-dump band:
  - broader rules still reaches `parse-make-instance-before-class-lookup` and `class-lookup-no-table name=oddzial`
  - `CLASH95_LOAD_MENU_PROBE_BROADER_RULES=0` still fails earlier on `symbol-lookup-missing-table MAIN`
- Advanced the durable memory from `mapK3` to `mapK4`, so the next `continue` resumes from case `3` instead of re-auditing the already-recovered `k_mapa3l.map` lane.
- Reused the read-heavy subagents again on `mapK4` and confirmed the live retained blocker was still pure recovered-C mission setup: `runtime_glue_mapper`, `sdl_seam_mapper`, and `class_seam_mapper` all stayed below compat, SDL, and `src_cpp`, `external_corroborator` only corroborated the shipped `k_mapa4j.map` / `McDonowan` / `Sir Wenom` / `Lord Gorio` / `Ughuata` names secondarily, and `boot_path_mapper` pinned the exact case-3 order from `clash95.asm`.
- Recovered `mapK4` / case `3` directly in `Scenario_LoadMissionByIndex`, including the four-player activation/name setup, the `Ughuata` post-castle `BUILDING_RECORD(castle_index) + 18 = -1`, `BUILDING_RECORD(castle_index) + 438 = 300`, the explicit `Building_OnGarrisonChange` handoff, and the three case-local raw stack mutation bands, while preserving the absence of a post-`Game_InitPlayerViewState` camera override.
- Re-ran the three green build targets plus the retained `PlayGame_Dispatch` probe and confirmed the retained executable-regeneration surface still links cleanly after `mapK4`.
- Re-ran the exact traced contained post-confirm menu probes with the corrected `CLASH95_LOAD_MENU_PROBE_POST_CONFIRM=1` lane and reconfirmed the same protected split under the timeout/core-dump band:
  - broader rules still reaches `parse-make-instance-before-class-lookup` and `class-lookup-no-table name=oddzial`
  - `CLASH95_LOAD_MENU_PROBE_BROADER_RULES=0` still fails earlier on `symbol-lookup-missing-table MAIN`
- Advanced the durable memory from `mapK4` to `mapK5`, so the next `continue` resumes from case `4` instead of re-auditing the already-recovered `k_mapa4j.map` lane.
- Reloaded the durable repo memory again, reused the read-heavy subagents on `mapK7`, and confirmed from asm/map evidence that the live retained blocker was still pure recovered-C mission setup below compat, SDL, and `src_cpp`.
- Recovered `mapK7` / case `6` directly in `Scenario_LoadMissionByIndex`, including the three-player `Alan` / `Walter` / `Drebegen` setup, the `Dragmounth` / `Akserion` / `Ghettan` / `Bhua Rock` / `Jolarion` castle lane, the raw single-stack byte OR plus 10-slot status OR loop, the two `Rules_SyncArmyFactStrength` handoffs, the `Ghettan` prisoner-slot write, the `Bhua Rock` / `Jolarion` `BUILDING_RECORD(+438)` writes, and the post-`Game_InitPlayerViewState` camera override.
- Re-ran `cmake -S . -B build`, the three green build targets, the retained `PlayGame_Dispatch` standalone probe, and the exact traced contained post-confirm menu probes; all green targets stayed green, the retained probe still links and exits `124`, and the contained runtime split is still `oddzial` with broader rules versus `MAIN` without them.
- Advanced the durable memory from `mapK7` to `mapK8`, so the next `continue` resumes from case `7` instead of re-auditing the already-recovered `k_mapa7z.map` lane.
- Reloaded the durable repo memory again, reused the read-heavy subagents on `mapK8`, and confirmed from asm/map evidence that the live retained blocker was still pure recovered-C mission setup below compat, SDL, and `src_cpp`.
- Recovered `mapK8` / case `7` directly in `Scenario_LoadMissionByIndex`, including the four `Rules_RetractTreasureFact` calls, the `Alan` / `Uraken` / `Wodar` / `Richard V` setup, the `Weghetown` / `Henrion` / `Moon Town` / `Gate Stone` / `Canoowar` / `Trungeon` / `Leweburg` / `Defambrion` castle lane, the three raw slot-byte OR loops, the final `& 0xFC | 2` slot-byte carry loop, and the preserved absence of post-castle building writes or a post-init camera override.
- Re-ran `cmake -S . -B build`, the three green build targets, the retained `PlayGame_Dispatch` standalone probe, and the exact traced contained post-confirm menu probes; all green targets stayed green, the retained probe still links and exits `124`, and the contained runtime split is still `oddzial` with broader rules versus `MAIN` without them.
- Advanced the durable memory from `mapK8` to `mapK9`, so the next `continue` resumes from case `8` instead of re-auditing the already-recovered `k_mapa8z.map` lane.
- Reloaded the durable repo memory again, reused the read-heavy subagents on `mapK9`, and confirmed from asm/map evidence that the live retained blocker was still pure recovered-C mission setup below compat, SDL, and `src_cpp`.
- Recovered `mapK9` / case `8` directly in `Scenario_LoadMissionByIndex`, including the player-0 queen relationship writes, the `Alan` / `Sir James` / `Ruryk` / `Riludius` setup, the `Totaweon` / `Gordmouth` / `Timbran` / `Ghettan` / `Hopenberg` / `Katha Gha` / `Werneom` castle lane, the `Gordmouth` `BUILDING_RECORD(+438) -= 100` cut, the three raw slot-byte OR loops, the two direct `& 0xFC | 2` slot-byte rewrites, and the preserved absence of a post-init camera override.
- Re-ran `cmake --build build --target clash95_recovered clash95_bootstrap clash95_cpp_regen -j`, the retained `PlayGame_Dispatch` standalone probe, and the exact traced contained post-confirm menu probes; all green targets stayed green, the retained probe still links and exits `124`, and the contained runtime split is still `oddzial` with broader rules versus `MAIN` without them.
- Advanced the durable memory from `mapK9` to `mapK10`, so the next `continue` resumes from case `9` instead of re-auditing the already-recovered `k_mapa9z.map` lane.
- Reloaded the durable repo memory again, reused the read-heavy subagents on `mapK10`, and confirmed from asm/map evidence that the live retained blocker was still pure recovered-C mission setup below compat, SDL, and `src_cpp`.
- Recovered `mapK10` / case `9` directly in `Scenario_LoadMissionByIndex`, including the five-player `Alan` / `Twogor` / `Drebegen` / `Mieszko` / `Chester` setup, the `Gorendberg` / `Timbran` / `Ghettan` / `Bhua Rock` / `Katha Gha` / `Stormus` / `Guluali` castle lane, the case-local `BUILDING_RECORD(+438)` cuts/assignments/addition, the direct masked `BUILDING_RECORD(+444)` writes, and the preserved absence of any raw stack loops or post-init camera override.
- Re-ran `cmake --build build --target clash95_recovered clash95_bootstrap clash95_cpp_regen -j`, the retained `PlayGame_Dispatch` standalone probe, and the exact traced contained post-confirm menu probes; all green targets stayed green, the retained probe still links and exits `124`, and the contained runtime split is still `oddzial` with broader rules versus `MAIN` without them.
- Re-read the retained jump table and confirmed the mission-loader switch still has remaining cases beyond the now-recovered `0..11` slice; the next unrecovered named case is `mapP3` / case `12`, not a fabricated `mapK11`.
- Advanced the durable memory from `mapK10` to `mapP3`, so the next `continue` resumes from case `12` instead of re-auditing the already-recovered `k_map10l.map` lane.
- Reloaded the durable repo memory again, kept the three green build targets plus the retained `PlayGame_Dispatch` probe intact, and finished publishing the already-compiled `mapP3` / case `12` recovered-C batch.
- Reused the read-heavy subagents on `mapP3` and confirmed from asm/map evidence that the live retained blocker was still pure recovered-C mission setup below compat, SDL, and `src_cpp`.
- Recovered `mapP3` / case `12` directly in `Scenario_LoadMissionByIndex`, including the two-player `Raylin` / `Gaalaad` setup, the player-1 human/minimap/religion writes, the player-1 no-castle unit lane, the `Sarturia` `BUILDING_RECORD(+438) = 5000` write, and the post-`Game_InitPlayerViewState` camera override back onto player `1`.
- Re-ran `cmake --build build --target clash95_recovered clash95_bootstrap clash95_cpp_regen -j`, the retained `PlayGame_Dispatch` standalone probe, and the exact traced contained post-confirm menu probes; all green targets stayed green, the retained probe still links and exits `124`, and the contained runtime split is still `oddzial` with broader rules versus `MAIN` without them.
- Advanced the durable memory from `mapP3` to case `13` / `p_mapa4l.map`, so the next `continue` resumes from the next real jump-table arm instead of re-auditing the already-recovered `p_mapa3z.map` lane.
- Reused the read-heavy subagents again on case `13` / `p_mapa4l.map` and confirmed from asm/map evidence that the live retained blocker was still pure recovered-C mission setup below compat, SDL, and `src_cpp`.
- Recovered case `13` / `p_mapa4l.map` directly in `Scenario_LoadMissionByIndex`, including the four-player `Raylin` / `Leryks X` / `Glazur` / `Sir John` setup, the single `Ungught` castle lane, the random per-slot `Rng_RandRange(5, 20)` loop on the stack at `TILE_INDEX(25, 28)`, the three no-castle unit lanes for players `2..4`, and the preserved absence of any post-castle building writes or post-`Game_InitPlayerViewState` camera override.
- Re-ran `cmake --build build --target clash95_recovered clash95_bootstrap clash95_cpp_regen -j`, the retained `PlayGame_Dispatch` standalone probe, and the exact traced contained post-confirm menu probes; all green targets stayed green, the retained probe still links and exits `124`, and the contained runtime split is still `oddzial` with broader rules versus `MAIN` without them.
- Advanced the durable memory from case `13` / `p_mapa4l.map` to case `14` / `p_mapa5l.map`, so the next `continue` resumes from the next real jump-table arm instead of re-auditing the already-recovered `p_mapa4l.map` lane.

- Reloaded the durable repo memory again after the full 20-case `Scenario_LoadMissionByIndex` switch was confirmed, reused the read-heavy subagents on the first deeper `WorldMap_RunHumanTurnLoop` loop-body band, and confirmed from asm/map evidence that the next retained blocker was still pure recovered-C gameplay/session logic below compat, SDL, and `src_cpp`.
- Recovered the next `WorldMap_RunHumanTurnLoop` slice directly in `clash95.c`, including the zero-arg loop-entry helper lane (`UI_ReadCheatString`, `DD_Pump`, `sub_407B20`, `WorldMap_RedrawFrame`, `WorldMap_HandleTopMenuBar`), the held-key `DD_Pump` loops, the queued-path AP compare from the last move-track step, and the saved render-hook/resource-handle debug block.
- Re-ran `cmake --build build --target clash95_recovered clash95_bootstrap clash95_cpp_regen -j`, the retained `PlayGame` and `WorldMap_RunHumanTurnLoop` probes, and the exact traced contained post-confirm menu probes; all green targets stayed green, both retained probes still link and exit `124`, and the contained runtime split is still `oddzial` with broader rules versus `MAIN` without them.
- Advanced the durable memory from the earlier human-turn-loop entry/tail wording to the remaining deeper `WorldMap_RunHumanTurnLoop` loop body, so the next `continue` resumes from the next live lost-register/usercall band instead of re-auditing the already-repaired call-shape lane.

- Reloaded the durable repo memory again, reused the read-heavy subagents on the next human-turn helper band, and confirmed from asm/map evidence that the live retained blocker was still pure recovered-C gameplay/session logic below compat, SDL, and `src_cpp`.
- Recovered `WorldMap_HandleTopMenuBar` directly in `clash95.c`, including the menu-sprite draw call, the mission `3`/`13` versus `4`/`14` turn-counter text, the temporary menu-surface destroy call, and the saved render-hook/resource-handle restore block from `sub_40E8B0`.
- Recovered `UnitStackSelection_HandleInput` directly in `clash95.c`, including the zero-arg `Render_Begin` calls, real adjacent-tile move coordinates into `Unit_MoveSelectionFromGroupToTile`, `dword_526F78` zero-on-success, and the `Render_LoadResourceSprite_v2` plus selection-panel redraw success tail from `sub_423860`.
- Re-ran `cmake -S . -B build`, the three green build targets, the retained `PlayGame` and `WorldMap_RunHumanTurnLoop` standalone probes, and the exact traced contained post-confirm menu probes; all green targets stayed green, both retained probes still link and exit `124`, and the contained runtime split is still `oddzial` with broader rules versus `MAIN` without them.
- Advanced the durable memory from the earlier top-menu/split-panel helper band to the deeper `WorldMap_HandleTileHoverAndClick` / `sub_4084A0` surface, so the next `continue` resumes from the next live lost-register/usercall band instead of re-auditing the already-repaired helper slices.
- Reloaded the durable repo memory again and kept the retained frontier on `WorldMap_HandleTileHoverAndClick` / `sub_4084A0`, below compat, SDL, and `src_cpp`.
- Recovered the next `sub_4084A0` tranche directly in `clash95.c`, including the real forwarded-coordinate wrappers for `MapTile_HasOwnOrVisibleEnemyUnitStack` and `MapTile_HasBuilding`, the lost-surface `UnitStack_ShowSelectionDialog(..., 1)` tail, the zero-arg `UnitStack_ExecuteQueuedPath` call shapes in the temple/building/self-move execute lanes, and the `Render_LoadResourceSprite_v2` resource-reload tail for own-stack merge and reselection.
- Re-ran `gcc -std=gnu89 -w -I. -fsyntax-only clash95.c`, `git diff --check`, `cmake --build /tmp/clash95-cmake-build --target clash95_recovered clash95_bootstrap clash95_cpp_regen`, the retained `WorldMap_RunHumanTurnLoop` standalone probe, both traced contained post-confirm menu probes, and one-second smoke runs for `clash95_bootstrap` and `clash95_cpp_regen`; all green targets stayed green, the retained probe still exits `124`, and the contained runtime split is still `oddzial` with broader rules versus `MAIN` without them.
- Advanced the durable memory from the earlier `sub_4084A0` wrapper/execute-tail tranche to the deeper enemy-building / enemy-stack attack band, so the next `continue` resumes from the next live lost-register/usercall surface instead of re-auditing the already-repaired tails.
- Reloaded the durable repo memory again and kept the retained frontier on the deeper `WorldMap_HandleTileHoverAndClick` / `sub_4084A0` band, below compat, SDL, and `src_cpp`.
- Recovered the next `sub_4084A0` tranche directly in `clash95.c`, replacing the decompiler-only `v50` / `v58` / `v67` tile-offset ghosts with direct `TILE_INDEX(v1, v78)` hovered stack-id reads in the own-stack merge/add-to-group and visible enemy-stack attack lanes.
- Re-ran `gcc -std=gnu89 -w -I. -fsyntax-only clash95.c`, `git diff --check`, `cmake --build /tmp/clash95-cmake-build --target clash95_recovered clash95_bootstrap clash95_cpp_regen`, the retained `WorldMap_RunHumanTurnLoop` standalone probe, both traced contained post-confirm menu probes, and one-second smoke runs for `clash95_bootstrap` and `clash95_cpp_regen`; all green targets stayed green, the retained probe still exits `124`, and the contained runtime split is still `oddzial` with broader rules versus `MAIN` without them.
- Advanced the durable memory from the earlier `sub_4084A0` hovered stack-id band to the remaining enemy-building `Unit_AttackBuilding` call-shape plus own-stack reselection clear-mask lane, so the next `continue` resumes from that live lost-register/usercall surface instead of re-auditing the already-reduced tile-id reads.
- Reloaded the durable repo memory again and kept the retained frontier on the deeper `WorldMap_HandleTileHoverAndClick` / `sub_4084A0` band, below compat, SDL, and `src_cpp`.
- Recovered the next `sub_4084A0` tranche directly in `clash95.c`, replacing the bogus reselection call-site argument with the real global selection-mask surface and fixing `UnitStackSelection_ClearMask` itself to match asm’s hardcoded `dword_526F78` clear semantics.
- Re-ran `gcc -std=gnu89 -w -I. -fsyntax-only clash95.c`, `git diff --check`, `cmake --build /tmp/clash95-cmake-build --target clash95_recovered clash95_bootstrap clash95_cpp_regen`, the retained `WorldMap_RunHumanTurnLoop` standalone probe, both traced contained post-confirm menu probes, and one-second smoke runs for `clash95_bootstrap` and `clash95_cpp_regen`; all green targets stayed green, the retained probe still exits `124`, and the contained runtime split is still `oddzial` with broader rules versus `MAIN` without them.
- Advanced the durable memory from the earlier `sub_4084A0` clear-mask lane to the remaining enemy-building `Unit_AttackBuilding` / building-interaction band, so the next `continue` resumes from that live lost-register/usercall surface instead of re-auditing the already-reduced reselection helper path.
- Reloaded the durable repo memory again and kept the retained frontier on the deeper `WorldMap_HandleTileHoverAndClick` / `sub_4084A0` band, below compat, SDL, and `src_cpp`.
- Recovered the next `sub_4084A0` tranche directly in `clash95.c`, hoisting the hovered building id into the shared building-interaction branch so the enemy-building attack predicate and own-building approach branch stop re-reading the tile entry through raw offset expressions.
- Re-ran `gcc -std=gnu89 -w -I. -fsyntax-only clash95.c`, `git diff --check`, `cmake --build /tmp/clash95-cmake-build --target clash95_recovered clash95_bootstrap clash95_cpp_regen`, the retained `WorldMap_RunHumanTurnLoop` standalone probe, both traced contained post-confirm menu probes, and one-second smoke runs for `clash95_bootstrap` and `clash95_cpp_regen`; all green targets stayed green, the retained probe still exits `124`, and the contained runtime split is still `oddzial` with broader rules versus `MAIN` without them.
- Advanced the durable memory from the earlier `sub_4084A0` building-id read scars to the remaining enemy-building `Unit_AttackBuilding` call-shape plus own-building approach-path lane, so the next `continue` resumes from that live lost-register/usercall surface instead of re-auditing the already-reduced tile lookup band.
- Reloaded the durable repo memory again and kept the retained frontier on the deeper `WorldMap_HandleTileHoverAndClick` / `sub_4084A0` band, below compat, SDL, and `src_cpp`.
- Recovered the next `sub_4084A0` tranche directly in `clash95.c`, anchoring `Building_GenerateApproachTrack` on `a1` for its selected-stack lookup and keeping the same selected stack index live across the opening `Unit_AttackBuilding` sprite-draw and move-to-building prelude instead of forwarding the bogus `v12` / `v5` / `v6` / `v9` locals.
- Re-ran `gcc -std=gnu89 -w -I. -fsyntax-only clash95.c`, `git diff --check`, `cmake --build /tmp/clash95-cmake-build --target clash95_recovered clash95_bootstrap clash95_cpp_regen`, the retained `WorldMap_RunHumanTurnLoop` standalone probe, both traced contained post-confirm menu probes, and one-second smoke runs for `clash95_bootstrap` and `clash95_cpp_regen`; all green targets stayed green, the retained probe still exits `124`, and the contained runtime split is still `oddzial` with broader rules versus `MAIN` without them.
- Advanced the durable memory from the earlier `sub_4084A0` selected-stack-forwarding prelude to the remaining mid-body `Unit_AttackBuilding` garrison/combat-resolution lane, so the next `continue` resumes from that live lost-register/usercall surface instead of re-auditing the already-reduced building attack opening.
