{
  "phase": "lowercase_r_finite_app_winmain_shutdown_route",
  "current_frontier": "Lowercase r is now the first finite recovered App_WinMain route. main compacts argv into the recovered command-line buffer and calls App_WinMain(GetModuleHandleA(0), 0, g_boot_command_line, 0). App_WinMain still enters Bootstrap_RunRecoveredEarlyStartupPrelude and Bootstrap_RunRecoveredRuntimeAndRenderInit, but command mode r skips Bootstrap_RunRecoveredGameEntry and proceeds through shutdown to exit 0. Router lookup, missing-logical-name diagnostics, mouse-speed setup, compact software-surface destructor dispatch, native SDL DirectDraw-compat COM dispatch, and readable-range guarded strcmp_ were repaired on the reached startup/shutdown surface.",
  "last_green_targets": [
    "clash95_recovered",
    "clash95_bootstrap",
    "clash95_cpp_core",
    "clash95_cpp_regen"
  ],
  "runtime_milestone": "build/bin/clash95_bootstrap r and build/bin/clash95_cpp_regen r both enter recovered early startup, initialize the runtime/render path, skip the game-entry handoff, run shutdown, and exit 0. The new clash95_r_command_shutdown_smoke passes. The default no-arg route is not currently a live-loop milestone in this branch: it exits through UI_StartAnims -> Video_Avi_playIn -> App_RequestQuit with [platform_sdl] Clash: Clash CD not found! before the old no-arg liveness smoke can observe a running process.",
  "blockers": [
    "Recover the default no-arg intro AVI/CD/resource path under SDL; the current route exits early with Clash CD not found via Video_Avi_playIn -> App_RequestQuit.",
    "CSS_Init remains deferred because the retained DirectSound-era device table is not recovered safely enough for x86-64 execution.",
    "The broader playable App_WinMain / PlayGame session milestone is still beyond the current startup route.",
    "sub_492660 and the module-qualified symbol path remain suspect if that path is reached.",
    "The older contained menu-probe commands and CLASH95_* menu controls are no longer valid active bootstrap selectors; do not use them as current verification."
  ],
  "next_actions": [
    "Keep clash95_recovered, clash95_bootstrap, clash95_cpp_core, and clash95_cpp_regen green.",
    "Use tests/verify_r_command_shutdown.sh on both clash95_bootstrap and clash95_cpp_regen as the deterministic finite startup/shutdown verification.",
    "Trace and recover the intro AVI/CD/resource path that currently calls App_RequestQuit on the default no-arg route.",
    "Recover or quarantine the CSS_Init device-table path only when it is evidence-backed and x86-64 safe under the SDL runtime seam.",
    "Continue startup/runtime reductions without reintroducing host-side fake probe modes."
  ],
  "resume_instructions": "On continue: reload AGENTS.md, the regenerate-cpp-executable skill, and the durable sidecars. Treat the current finite milestone as main -> App_WinMain -> Bootstrap_RunRecoveredEarlyStartupPrelude -> Bootstrap_RunRecoveredRuntimeAndRenderInit -> App_Shutdown -> exit 0 for command line r. Validate with cmake --build build --target clash95_recovered clash95_bootstrap clash95_cpp_core clash95_cpp_regen -j, tests/verify_r_command_shutdown.sh build/bin/clash95_bootstrap, tests/verify_r_command_shutdown.sh build/bin/clash95_cpp_regen, JSON checks, and git diff --check. ctest --test-dir build --output-on-failure currently fails because the older no-arg clash95_full_route_smoke exits early through the intro AVI/CD path; do not claim full ctest is green until that default-route blocker is fixed. Remove any generated clash.log after smoke runs so it is not accidentally tracked. Do not rely on old --authentic-menu-probe, --authentic-video-init, --authentic-startup-prelude, --platform-window-only, or CLASH95 menu-probe env controls; those are no longer active bootstrap selectors."
}
