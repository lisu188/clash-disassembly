{
  "phase": "full_app_winmain_route_with_retained_logfile_startup_slice",
  "current_frontier": "clash95_bootstrap and clash95_cpp_regen now use one default executable route: main compacts argv into the recovered command-line buffer and calls App_WinMain(GetModuleHandleA(0), 0, g_boot_command_line, 0). Host-side bootstrap probe switches and menu-probe environment controls are no longer active selectors; old switches are only forwarded command-line text. The retained startup path now restores logEnabled before runtime setup and calls createLogFiles after the still-deferred CSS_Init point. createLogFiles follows the asm-observed side effect by truncating/creating clash.log and battle.log when logEnabled is set, closing both files, and clearing dword_526A20.",
  "last_green_targets": [
    "clash95_recovered",
    "clash95_bootstrap",
    "clash95_cpp_core",
    "clash95_cpp_regen"
  ],
  "runtime_milestone": "Both bootstrap executables enter the recovered App_WinMain path by default and remain in the live full-game loop until an external timeout kills them. The route now includes the retained log-file creation side effect. No finite authentic shutdown milestone exists yet, and the DirectSound-era CSS_Init table remains skipped because it is not safe for the current x86-64 SDL runtime path.",
  "blockers": [
    "The live full-game loop still needs an authentic finite shutdown or deterministic verification milestone instead of relying on forced timeout kills.",
    "CSS_Init remains deferred because the retained DirectSound-era device table is not recovered safely enough for x86-64 execution.",
    "The broader playable App_WinMain / PlayGame session milestone is still beyond the current startup route.",
    "The older contained menu-probe commands and CLASH95_* menu controls are no longer valid active bootstrap selectors; do not use them as current verification."
  ],
  "next_actions": [
    "Keep clash95_recovered, clash95_bootstrap, clash95_cpp_core, and clash95_cpp_regen green on the full App_WinMain route.",
    "Add an authentic finite verification or shutdown milestone for the full live loop.",
    "Recover or quarantine the CSS_Init device-table path only when it is evidence-backed and x86-64 safe under the SDL runtime seam.",
    "Continue startup/runtime reductions without reintroducing host-side fake probe modes."
  ],
  "resume_instructions": "On continue: reload AGENTS.md, the regenerate-cpp-executable skill, and the durable sidecars. Treat the current executable route as main -> App_WinMain -> Bootstrap_RunRecoveredEarlyStartupPrelude -> Bootstrap_RunRecoveredRuntimeAndRenderInit -> Bootstrap_RunRecoveredGameEntry -> Bootstrap_RunMessageLoop. Validate with the build targets clash95_recovered, clash95_bootstrap, clash95_cpp_core, and clash95_cpp_regen, then forced-timeout live smokes for clash95_bootstrap and clash95_cpp_regen. Remove any generated clash.log after smoke runs so it is not accidentally tracked. Do not rely on old --authentic-menu-probe, --authentic-video-init, --authentic-startup-prelude, --platform-window-only, or CLASH95 menu-probe env controls; those are no longer active bootstrap selectors. Next practical frontiers are finite live-loop verification, the skipped CSS_Init table, and the broader playable session milestone."
}
