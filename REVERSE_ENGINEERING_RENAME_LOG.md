# Reverse Engineering Rename Log

## Batch 8 – Unit Stack Wave
| Old Name / Pattern | New Name | Kind | Subsystem | Confidence | Evidence Summary |
|---|---|---|---|---|---|
| sub_40F3C0 | UnitStack_IsIndexOnMap | Function | Unit Lifecycle | High | Walks the 200-column tile occupancy buffer at `gameData + 556374` to locate the stack id before reuse, proving it tests whether the stack id is already on the map. |
| sub_40F440 | UnitSlot_InitFromType | Function | Unit Lifecycle | High | Initializes a 31-byte slot entry with type id, owner, morale, action points, and veterancy from the per-unit tables, so it clearly seeds an individual unit slot. |
| sub_40F4D0 | UnitStack_ResetRecord | Function | Unit Lifecycle | High | Calls `UnitSlot_InitFromType` for the first slot, clears the remaining slots/path buffer, and stores the owner byte, matching a full reset of a stack record. |
| sub_40F7C0 | UnitStack_KillByIndex | Function | Unit Lifecycle | High | Thin wrapper that converts a stack index to its record pointer and forwards to `Unit_Kill`, so the rename documents that it kills stacks by index. |
| sub_40F800 | UnitStack_RemoveFromTile | Function | Unit Lifecycle | High | Validates row/column bounds, retracts army intel, zeros the tile entry, and updates fog-of-war, which is exactly the logic to remove a stack from its occupied tile. |
| sub_40F890 | UnitStack_UnlinkIfEmpty | Function | Unit Lifecycle | High | Checks whether the first slot’s unit type is -1 and, if so, calls `Rules_UnlinkArmyFact`; it only unlinks empty stacks. |
| sub_40F8B0 | UnitStack_GetVisionRadius | Function | Unit Lifecycle | Medium | Iterates every occupied slot, reads `byte_5125AE` for each unit type, and tracks the max value, yielding the stack’s vision range. |
| sub_40F900 | UnitStack_UpdateVision | Function | Unit Lifecycle | High | Uses the owning player id, stack tile, and `UnitStack_GetVisionRadius` to stamp fog-of-war via `sub_40EDE0`, so the rename reflects its vision-update role. |
| sub_40F9F0 | UnitStack_HasReadyUnits | Function | Unit Lifecycle | Medium | Scans up to 10 slots looking for bit0 set in the slot flag byte, returning 1 only if at least one squad member is marked ready — a readiness probe used in `Unit_NewTurn`. |
| sub_40FA20 | UnitStack_ClearReadyFlags | Function | Unit Lifecycle | Medium | Iterates every occupied slot and clears bit0 of the flag byte, which resets the per-turn “ready” state for the entire stack. |
| sub_40FA50 | UnitStack_SetReadyFlags | Function | Unit Lifecycle | Medium | Complement to the above that sets bit0 for each slot once the UI or turn logic re-enables the stack. |
| gameData + 147174 + 725 * index | UNIT_STACK(index) / UnitStackRecord | Recovered Struct | Unit Lifecycle | High | The code constantly adds 147174 + 725*index to reach stack entries (row, column, owner, slot array, path buffer). Exposing this as `UnitStackRecord` aligns with the per-stack macros and clarifies the recovered layout. |

## Deferred / Ambiguous
- Remaining entries of `g_UnitSpriteFolders` (e.g., the sprite folders for Górale, Smoki, etc.) are not emitted in `clash95.c`; resolving those unit names requires dumping the data segment or external resource files.

## Batch 9 – Army Fact Wave
| Old Name / Pattern | New Name | Kind | Subsystem | Confidence | Evidence Summary |
|---|---|---|---|---|---|
| sub_40F6E0 | UnitStack_LinkArmyFact | Function | Unit Lifecycle | High | Wrapper invoked after stack creation and whenever an existing stack tile is re-linked; it passes the stack pointer and tile occupant id into the rules engine (`Rules_CreateArmyFact`) to register the squad in the fact database. |
| sub_4521D0 | Rules_CreateArmyFact | Function | Rules/Meta | High | Builds the "ODDZIAL" fact string, calls `Rules_AssertFact`, stores the returned handle at stack+721, and then calls `Rules_LinkArmyFact`; behavior matches an army-fact creation helper rather than an anonymous subroutine. |

## Batch 10 – Unit Stat Wave
| Old Name / Pattern | New Name | Kind | Subsystem | Confidence | Evidence Summary |
|---|---|---|---|---|---|
| sub_411120 | UnitStats_CalcMeleeAttack | Function | Unit Stats | High | Pulls `byte_51257E` (melee table) plus formation bits and veterancy, then multiplies by morale (`a1[9] / 100`), matching the effective melee attack strength used in combat. |
| sub_4111C0 | UnitStats_CalcRangedAttack | Function | Unit Stats | High | Uses `byte_51257F` (ranged table) with the missile-cover flag at `a1[22]`, adds formation/stance bonus, and scales via morale just like projectile attack code. |
| sub_411240 | UnitStats_GetRangedIconIndex | Function | Unit Stats | Medium | Shares the same inputs as the ranged attack function but omits morale scaling, mirroring `UI_IconIndexFromStats` for melee; used by UI to pick ranged-strength icons. |
| sub_411280 | UnitStats_CalcDamagePerHit | Function | Unit Stats | High | Combines `byte_512581` (damage scalar) with veterancy and morale to compute actual damage inflicted by an attack volley. |
| sub_4112C0 | UnitStats_GetBaseDamage | Function | Unit Stats | High | Returns the base damage scalar plus veterancy without morale scaling, i.e., the raw stat shown in UI. |
| sub_4112F0 | UnitStats_CalcSiegeAttack | Function | Unit Stats | Medium | Reads `byte_512584` (structure damage) and mirrors the melee/ranged calculations, indicating it computes vs-building attack strength. |

## Batch 11 – Unit Naming Wave
| Old Name / Pattern | New Name | Kind | Subsystem | Confidence | Evidence Summary |
|---|---|---|---|---|---|
| off_51256C | g_UnitSpriteFolders | Global | Unit Lifecycle | High | Sprite loader helpers (e.g., `clash95.c:25320-25480`) index this array to build animation paths such as `units_go\peon?.s32`; the base entry is the literal "peon", revealing that the table stores per-unit sprite folder names. |
| *(new constant)* | UNIT_TYPE_PEASANT | Helper | Unit Lifecycle | High | Codifies type id 0 as the Peasant/Chłopi stack, supported by the base sprite folder "peon" and the scripting APIs `IloscChlopow` / `WyprowadzChlopow` that manipulate the same unit. |

## Batch 12 – Unit Morale Wave
| Old Name / Pattern | New Name | Kind | Subsystem | Confidence | Evidence Summary |
|---|---|---|---|---|---|
| sub_4114B0 | Unit_CompactSquad | Function | Unit Lifecycle | High | Iterates through the 10 squad slots, shifts data down whenever it finds `type == -1`, clears the tail slot, and re-links the CLIPS army fact; this matches the compact/cleanup helper invoked after casualties. |
| sub_411560 | UnitSlots_RemoveGaps | Function | Unit Lifecycle | Medium | Helper that scans the first `count` slots and shifts subsequent entries down whenever it encounters an empty slot; combat cleanup code calls it after zeroing hitpoints to remove gaps without touching the full squad. |
| sub_4115E0 | Unit_CheckLowMorale | Function | Unit Lifecycle | High | Copies the unit record, rolls random morale checks per slot, logs via `aUnit_checklowm` strings, shows the morale-break window (`off_512368`), and if deserters appear it compacts the squad and finalizes the army fact—exactly the low-morale check the logs reference. |

## Batch 13 – Turn, Terrain, and Trap State Wave
| Old Name / Pattern | New Name | Kind | Subsystem | Confidence | Evidence Summary |
|---|---|---|---|---|---|
| nextPlayer | Game_AdvanceToNextPlayerTurn | Function | Turn Flow | High | Saves the outgoing player camera, rotates to the next active player, increments `GAME_TURN_COUNTER` when the cycle wraps, runs village/port/queen/building/unit per-turn handlers, and then reinitializes either human UI or AI control for the new player. |
| dword_5202EC | g_CurrentPlayerIndex | Global | Turn Flow | High | The symbol always indexes `PLAYER_DATA`, drives UI ownership checks, is saved/restored around stack-vision stamping, and selects the acting faction for AI and end-turn logic, so it is the current turn player id rather than an anonymous integer. |
| sub_413B10 | Map_InitTerrainMoveTableOffsets | Function | Movement/Map | High | Builds the 1024-entry lookup that converts terrain tile ids into offsets inside `byte_512586`, which is exactly the initialization step for terrain-specific move-cost columns. |
| dword_524568 | g_TerrainMoveTableOffsets | Table | Movement/Map | High | `Map_GetUnitTileMoveCostOrZero` and `UnitStack_GetTileMoveCostOrZero` index this array with the tile terrain id and then add the result to `byte_512586`, proving it is a terrain-to-move-table offset map. |
| sub_413DD0 | Map_GetUnitTileMoveCostOrZero | Function | Movement/Map | High | Rejects occupied, trapped, or blocked tiles, then returns either `byte_512585` road movement cost or a terrain-adjusted `byte_512586` cost for the requested unit template. |
| sub_414150 | UnitStack_GetTileMoveCostOrZero | Function | Movement/Map | High | Copies a stack-derived movement profile with `sub_413920`, applies the same occupancy/trap checks as the unit-template helper, and returns the resulting cost for a concrete stack. |
| gameData + 14 * column + 1400 * row | TILE_TERRAIN_RECORD(row, column) / MapTileRecord | Recovered Struct | Map/Tile | High | Movement code treats word 0 as the terrain id, word 2 as a road/bridge override that switches to road costs, and bridge placement temporarily writes overlay ids like `872` at offset `+4`, exposing a stable 14-byte tile record. |
| gameData + 576374 + column + 100 * row | TILE_TRAP_OWNER_MASK(row, column) / TileTrapOwnerMaskLayer | Recovered Struct | Trap/Map | High | `Trap_New` writes `1 << owner`, `Trap_GetTileOwnerMask` reads and masks the byte, the renderer overlays the trap marker only when the current player bit is set, and `Trap_TriggerAtStackTile` clears the byte after activation. |
| sub_42B3F0 | Trap_CanPlaceAtTile | Function | Trap/Map | High | Validates that the target tile has no trap bit for the acting player, is not occupied, is not blocked by coast/building rules, and is not on one of the disallowed terrain ids before trap placement proceeds. |
| sub_42B680 | Trap_ClearTileOwnerMask | Function | Trap/Map | High | Single-purpose helper that zeros the trap-owner byte at `gameData + 576374 + 100 * row + column`. |
| sub_42B730 | Trap_GetTileOwnerMask | Function | Trap/Map | High | Returns either the raw one-byte trap owner mask for a tile or the bit corresponding to a specific player, matching every placement, rendering, and trigger caller. |
| sub_42B770 | Trap_TriggerAtStackTile | Function | Trap/Combat | High | Reveals nearby trap bits for detector-capable stacks, checks the triggered tile mask under the moving stack, shows the `aWpad_pul` trap message for players with detection, applies `Trap_HurtUnit`, and then clears the trap tile. |

## Deferred / Ambiguous
- `gameData + 147139` vs `gameData + 147143` are now clearly two different player-index slots, but the exact naming split is still deferred: one tracks the active turn owner while the other tracks the player whose camera/UI view should be restored.
- `MapTileRecord` offset `+2` is definitely a secondary overlay word used by terrain-edge rendering, but its exact semantic split between coast edge, decorative overlay, and passability metadata remains ambiguous.
- The `sub_4100B0(...) >= 3` gate inside `Trap_TriggerAtStackTile` almost certainly means trap detection capability, but the underlying slot-state scale returned by `sub_4100B0` is not yet named safely enough to promote.

## Batch 14 – Shared View-State Wave
| Old Name / Pattern | New Name | Kind | Subsystem | Confidence | Evidence Summary |
|---|---|---|---|---|---|
| `gameData + 140000` / `+140004` | `MAP_WIDTH_TILES` / `MAP_HEIGHT_TILES` | Helper | Shared State | High | Map load derives these values by scanning the 14-byte tile grid until `0xFFFF`, while scrolling and rendering clamp viewport coordinates against them as world bounds. |
| `gameData + 140008` / `+140012` | `MAP_VIEW_LEFT` / `MAP_VIEW_TOP` | Helper | Shared State | High | Keyboard scrolling, recentering, and turn handoff all treat these dwords as the current top-left map camera tile. |
| `PLAYER_DATA(player) + 140039` / `+140043` | `PLAYER_CAMERA_LEFT(player)` / `PLAYER_CAMERA_TOP(player)` | Helper | Shared State | High | The end-turn flow stores the outgoing camera here, save/load persists the same values, and the per-player setup routine seeds them from each faction's first building position. |
| `gameData + 147139` / `+147143` | `TURN_OWNER_PLAYER_INDEX` / `VIEWED_PLAYER_INDEX` | Helper | Turn Flow | High | `Game_AdvanceToNextPlayerTurn` writes the acting player into `+147139`, switches `+147143` only for human-controlled turns, and restores the camera from `PLAYER_CAMERA_*` using the viewed-player slot. |
| `gameData + 140000 .. +140012`, `+147139`, `+147143` | `WorldViewState` | Recovered Struct | Shared State | High | These offsets travel together through map-load initialization, save/load persistence, scrolling, and turn rotation, forming a stable shared-state region for world dimensions, camera origin, and active/viewed player ownership. |

## Batch 15 – Spawn and Trap Helper Wave
| Old Name / Pattern | New Name | Kind | Subsystem | Confidence | Evidence Summary |
|---|---|---|---|---|---|
| sub_41F0C0 | Building_FindFreeAdjacentSpawnTile | Function | Building/Garrison | High | The routine logs `Building_FindFreePlaceNear()`, scans the 12 neighboring offsets around a building, rejects occupied tiles, and returns the first tile whose movement helper accepts the building owner's spawn template. |
| sub_42B6A0 | Trap_HurtStack | Function | Trap/Combat | High | The helper logs `Trap_HurtUnit`, iterates the occupied stack slots, applies a randomized damage roll derived from `Unit_CalcEffectivenessB`, removes dead slots, and compacts/finalizes the stack afterward. |
| sub_44C2A0 | Game_InitPlayerViewState | Function | Shared State | High | Seeds each active player's saved camera from the first owned building, picks the first human player as `VIEWED_PLAYER_INDEX`, picks the first active player as both `g_CurrentPlayerIndex` and `TURN_OWNER_PLAYER_INDEX`, and is reused after map setup and scenario load. |

## Batch 16 – Building Transfer and Garrison Wave
| Old Name / Pattern | New Name | Kind | Subsystem | Confidence | Evidence Summary |
|---|---|---|---|---|---|
| sub_412520 | Unit_CreateNearbyUnitGroup | Function | Unit Lifecycle | High | The debug string already names the routine, and the body finds a free neighboring tile, calls `Unit_Create`, copies a supplied squad fragment into the new stack, and links the resulting army fact. |
| sub_41F1A0 | Building_Transfer | Function | Building/Garrison | High | The function logs `Building_Transfer`, temporarily clears the source building footprint from tile occupancy, creates a temporary transport stack (`31` or `32`), copies a percentage of the stored resource or occupants into it, and either moves it near the target building or spawns it on an adjacent tile. |
| sub_43E0C0 | Building_HasFreeAdjacentExitTile | Function | Building/Garrison | High | The helper only scans the building's legal neighbor pattern and returns true when it finds an in-bounds empty tile whose terrain is traversable by the building owner's unit template; it is a pure exit-tile availability probe. |
| sub_43E160 | Building_UnitsLeave | Function | Building/Garrison | High | The debug string names the routine, and the body selects a free adjacent exit tile, creates a new stack there, copies the requested garrison slots out of the building record, updates garrison-change state, and refreshes vision. |
| sub_43E4B0 | Building_CountFreeGarrisonSlots | Function | Building/Garrison | High | The function iterates the 12 building garrison slots (or 10 for smaller buildings) and counts entries whose unit type at offset `+18` is `-1`, so it clearly returns the number of free garrison slots. |
| aUnit_createnea | aUnitCreateNearbyUnitGroup | Helper | Unit Lifecycle | High | String variable rename follows the confirmed `Unit_CreateNearbyUnitGroup` debug label and keeps logging identifiers aligned with the recovered function name. |
| aBuilding_findf | aBuildingFindFreePlaceNear | Helper | Building/Garrison | High | The string literal is the exact log text used by `Building_FindFreeAdjacentSpawnTile`, so the helper name now describes the emitted trace rather than a truncated decompiler token. |
| aBuilding_trans | aBuildingTransfer | Helper | Building/Garrison | High | String helper backing the `Building_Transfer` debug log. |
| aBuilding_units | aBuildingUnitsLeave | Helper | Building/Garrison | High | String helper backing the `Building_UnitsLeave` debug log. |
| gameData + 509674 + 467 * buildingIndex | BuildingRecord | Recovered Struct | Building/Garrison | Medium | Building helpers repeatedly treat this 467-byte region as a stable per-building record with tile/owner header bytes, 12 garrison slots at `+18`, addon ids at `+402`, upgrade/resource state at `+421/+429/+438/+444`, and garrison-change bookkeeping near `+463`. |

## Deferred / Ambiguous
- `BuildingRecord` offset `+4` clearly switches neighbor masks, addon rules, and capture/build handling, but the exact enum behind values `0/1/2` is still not named safely enough.
- `BuildingRecord` offsets `+421` and `+429` behave like a staged castle or fortification upgrade level plus cooldown timer, but the exact feature being upgraded is still UI- and rules-coupled, so those fields remain descriptively unnamed.
- `BuildingRecord` offset `+438` is a stored spendable pool used by transfer, addon purchase, and per-turn accrual logic; it is likely a building-local treasury or stockpile, but the economy resource name remains deferred.

## Batch 17 – Building Production Licence Wave
| Old Name / Pattern | New Name | Kind | Subsystem | Confidence | Evidence Summary |
|---|---|---|---|---|---|
| sub_43E850 | Building_BuyUnitLicence | Function | Building/Garrison | High | The debug string names the routine, and the body spends from the building-local pool at `+438`, then appends a unit-type id into the installed licence/add-on array at `+402`. |
| sub_43E940 | Building_RemoveUnitLicence | Function | Building/Garrison | High | The function logs `Building_RemoveUnitLicence`, stops current production when the selected licence matches, and then removes the target type id from the `+402` licence array. |
| sub_43E9A0 | Building_SetUnitProduction | Function | Building/Garrison | High | The debug string names the routine, and the body stores a selected licence slot at `+414` plus the corresponding production duration at `+415`. |
| sub_43EA10 | Building_StopUnitProduction | Function | Building/Garrison | High | Single-purpose helper named by its debug string; it clears the selected production slot at `+414`. |
| sub_43EA30 | Building_TrainUnit | Function | Building/Garrison | High | The debug string names the function, and it writes per-slot task bits into `BuildingRecord + 390 + slotIndex`, starting a training task for the chosen garrison slot. |
| sub_43EAD0 | Building_RepairUnit | Function | Building/Garrison | High | The debug string names the function, and it writes the complementary repair task bits into the same per-slot task byte used by training. |
| aBuilding_buyun | aBuildingBuyUnitLicence | Helper | Building/Garrison | High | String helper backing the `Building_BuyUnitLicence` debug log. |
| aBuilding_remov | aBuildingRemoveUnitLicence | Helper | Building/Garrison | High | String helper backing the `Building_RemoveUnitLicence` debug log. |
| aBuilding_setun | aBuildingSetUnitProduction | Helper | Building/Garrison | High | String helper backing the `Building_SetUnitProduction` debug log. |
| aBuilding_stopu | aBuildingStopUnitProduction | Helper | Building/Garrison | High | String helper backing the `Building_StopUnitProduction` debug log. |
| aBuilding_train | aBuildingTrainUnit | Helper | Building/Garrison | High | String helper backing the `Building_TrainUnit` debug log. |
| aBuilding_repai | aBuildingRepairUnit | Helper | Building/Garrison | High | String helper backing the `Building_RepairUnit` debug log. |

## Batch 18 – Building Garrison Task Wave
| Old Name / Pattern | New Name | Kind | Subsystem | Confidence | Evidence Summary |
|---|---|---|---|---|---|
| sub_4558D0 | Building_UnitsLeaveReadyGarrisonSlots | Function | Building/Garrison | Medium | Collects up to five occupied garrison slots whose morale/condition byte is `100` and whose task bits are idle, then forwards that slot list into `Building_UnitsLeave` if the building has a legal exit tile. |
| sub_4559D0 | Building_HasTrainableIdleGarrisonUnit | Function | Building/Garrison | Medium | Returns true when the building contains at least one occupied garrison slot with `status & 3 < 2` and no active task bits, which matches the eligibility check later used before starting training. |
| sub_455A50 | Building_HasRepairableIdleGarrisonUnit | Function | Building/Garrison | Medium | Returns true when the building contains at least one occupied garrison slot below 50 condition with no active high task bits, matching the later repair starter. |
| sub_455AD0 | Building_StartTrainingIdleGarrisonUnits | Function | Building/Garrison | Medium | Walks every occupied garrison slot and starts `Building_TrainUnit` on those whose trainability and idle-task predicates pass, so it is an AI or bulk helper for launching training jobs. |
| sub_455B50 | Building_StartRepairIdleGarrisonUnits | Function | Building/Garrison | Medium | Bulk counterpart to the above that starts `Building_RepairUnit` on occupied, damaged, idle garrison slots. |
| sub_455BD0 | Building_UnitsLeaveByUnitType | Function | Building/Garrison | High | Finds the first garrison slot whose unit type id matches `a2`, builds a one-element leave list, and passes it to `Building_UnitsLeave` when an exit tile is available. |

## Batch 19 – Unit Type Metadata Wave
| Old Name / Pattern | New Name | Kind | Subsystem | Confidence | Evidence Summary |
|---|---|---|---|---|---|
| off_512568 | g_UnitTypeMetadataRecords | Global | Unit Types/Stats | High | The base is indexed as `22 * unitType` (88 bytes per type) and serves as more than a name table: field 0 is a localized-name pointer array, field `+4` is the sprite-folder pointer, and bytes `+29..+37` store the per-terrain movement profile merged by stack movement code. |
| dword_51257A | g_UnitTypeFlags | Global | Unit Types/Stats | High | This overlapping dword field lives inside the recovered 88-byte metadata record. Bit0 consistently marks airborne units across render order, movement sounds, death handling, and the “all units flying” predicate; another bit still controls the 6-vs-10 seed at slot `+11`. |
| sub_413920 | UnitStack_BuildMergedTerrainMoveProfile | Function | Unit Types/Stats | High | Iterates every occupied slot, loads the owning unit type's metadata record, and merges bytes `+29..+37` into a temporary profile consumed by world-movement helpers. |
| sub_422BA0 | UnitStack_HasOnlyFlyingUnits | Function | Unit Types/Stats | High | Walks the occupied slots and returns true only when every unit type has `g_UnitTypeFlags bit0` set, matching the airborne-only predicate used by pathing/UI helpers. |
| data segment @ 0x512568 stride 88 | UnitTypeMetadataRecord | Recovered Struct | Unit Types/Stats | High | The decompiler emitted several overlapping globals (`g_UnitTypeMetadataRecords`, `g_UnitSpriteFolders`, `g_UnitTypeFlags`, `byte_512570`+) from one record family. Code evidence proves a stable 88-byte per-type layout with localized names, sprite folders, flags, animation timing, and terrain move costs. |

## Batch 20 – Unit Roster Correlation Wave
| Old Name / Pattern | New Name | Kind | Subsystem | Confidence | Evidence Summary | Sources |
|---|---|---|---|---|---|---|
| *(new constant)* | UNIT_TYPE_RAM | Helper | Unit Types/Stats | High | The asm record xrefs pair `off_512468` (`Taran / Rammbock`) with folder `taran`, and ordinary melee code explicitly excludes type `13`, proving the earlier cannon-at-13 guess was wrong. | c, asm, exe |
| *(new constant)* | UNIT_TYPE_CANNON | Helper | Unit Types/Stats | High | The asm record xrefs pair `off_512444` (`Armata / Cannon`) with folder `armat`, fixing cannon at type `14`. | asm, exe |
| *(new constant)* | UNIT_TYPE_FORESTER | Helper | Unit Types/Stats | High | The asm record xrefs pair `off_512450` (`Leśnik / Forester`) with folder `lesn`, and the type is created directly by three nearby `Unit_Create(0xFu, ...)` call sites. | c, asm, exe |
| *(new constant)* | UNIT_TYPE_GORAL | Helper | Unit Types/Stats | High | The asm record xrefs pair `off_5123E4` (`Góral / Highlander`) with folder `goral`, placing it at type `16` rather than the earlier folder-order-only guess. | asm, exe |
| *(new constant)* | UNIT_TYPE_ELEPHANT | Helper | Unit Types/Stats | High | The asm record xrefs pair `off_5124C8` (`Słoń / Elephant`) with folder `slon`, placing it at type `19`. | asm, exe |
| *(new constant)* | UNIT_TYPE_FLY | Helper | Unit Types/Stats | High | The asm record xrefs pair `off_512534` (`Ważka / Fly / Riesenlibelle`) with folder `wazka`, and movement audio groups type `29` with other looped movers. | c, asm, exe |
| *(new constant)* | UNIT_TYPE_DRAGON | Helper | Unit Types/Stats | High | The asm record xrefs pair `off_512480` (`Smok / Dragon / Drachen`) with folder `smok`, moving the dragon id from the earlier speculative `29` to confirmed `30`. | asm, exe |

## Batch 21 – Unit Move Sound Wave
| Old Name / Pattern | New Name | Kind | Subsystem | Confidence | Evidence Summary | Sources |
|---|---|---|---|---|---|---|
| off_5125B9 | g_UnitMoveSoundStems | Global | Unit Audio / Unit Types | High | Both movement-sound helpers index this overlapping record field by `22 * type` and build `sfx\\ruchy\\<stem>...` paths from it; `clash95.asm` shows the first entry as `b_lekkie\\krokb`. | c, asm |
| byte_5125BD | g_UnitMoveSoundVariantCounts | Table | Unit Audio / Unit Types | High | Step-based movement audio wraps `g_CurrentUnitMoveSoundVariant` modulo this overlapping per-type byte field. | c, asm |
| byte_5125BE | g_UnitMoveSoundBaseVolumes | Table | Unit Audio / Unit Types | High | Both world and battle movement-audio paths pass this overlapping per-type byte into `CSS_PlaySound` as the playback volume. | c, asm |
| dword_543CA8 | g_CurrentUnitMoveSoundHandle | Global | Unit Audio | High | Only the movement-audio start/stop helpers read and write this symbol, always as the active sound handle passed to `CSS_StopSound` / `CSS_SetSoundLoop`. | c |
| dword_543CAC | g_CurrentUnitMoveSoundVariant | Global | Unit Audio | High | The same helpers use this value as the current alternating step index and reset it whenever the moving unit type changes. | c |
| dword_543CB0 | g_CurrentUnitMoveSoundTypeId | Global | Unit Audio | High | The movement-audio helpers cache the currently sounding unit type here to avoid restarting looped sounds or to reset the step variant when the type changes. | c |
| sub_4415A0 | BattleMap_GetMoveSoundSurfaceClass | Function | Unit Audio / Battle Map | Medium | The function maps a battle-map tile id through `byte_517318` and is only used to choose the `p/t/d`-style movement-sound suffix in battle movement audio. | c, asm |
| sub_441F00 | Audio_PlayWorldMapUnitMoveSound | Function | Unit Audio / World Map | High | Called from the world movement animation loop; it builds `sfx\\ruchy\\...` paths, chooses airborne vs terrain-dependent suffixes, and loops siege-like movers. | c, asm |
| sub_442290 | Audio_PlayBattleMapUnitMoveSound | Function | Unit Audio / Battle Map | High | Battle-only counterpart to the world helper; it uses `BattleMap_GetMoveSoundSurfaceClass` instead of `Map_GetTileSurfaceClassOrUnexplored` when selecting footstep suffixes. | c, asm |
| sub_4425B0 | Audio_StopUnitMoveSound | Function | Unit Audio | High | Thin helper that stops the active movement sound and clears the cached active unit type. | c |

## Batch 22 – Unit Action Sound Wave
| Old Name / Pattern | New Name | Kind | Subsystem | Confidence | Evidence Summary | Sources |
|---|---|---|---|---|---|---|
| sub_441A30 | Audio_PlayUnitActivateSound | Function | Unit Audio | High | The helper builds `sfx\\oddzialy\\<unit>\\activ1.wav?`, is called when selecting a new unit on both world and battle maps, and stores the handle later faded by the move-order helper. | c, asm |
| sub_441B00 | Audio_PlayUnitMoveOrderSound | Function | Unit Audio | High | The helper builds `sfx\\oddzialy\\<unit>\\go1.wav?`, fades the last activation sound, and is called immediately before unit movement execution on both world and battle maps. | c, asm |
| sub_441BE0 | Audio_PlayUnitRangedAttackSound | Function | Unit Audio | High | The helper builds `sfx\\oddzialy\\<unit>\\strzal.wav` and is triggered on ranged-attack animation frames. | c, asm |
| sub_441C80 | Audio_PlayUnitHitSound | Function | Unit Audio | High | The helper builds `sfx\\oddzialy\\<unit>\\dostal.wav` and is triggered when the target takes a hit. | c, asm |
| sub_441D20 | Audio_PlayUnitDeathSound | Function | Unit Audio | High | The helper builds `sfx\\oddzialy\\<unit>\\dead.wav` and is called at the start of the battle death animation path. | c, asm |
| sub_441DC0 | Audio_PlayUnitMeleeAttackSound | Function | Unit Audio | High | The helper builds `sfx\\oddzialy\\<unit>\\walka.wav` and is triggered on the designated combat-impact frame during melee attack resolution. | c, asm |

## Batch 23 – Unit Audio Toggle State Wave
| Old Name / Pattern | New Name | Kind | Subsystem | Confidence | Evidence Summary | Sources |
|---|---|---|---|---|---|---|
| dword_5174D4 | g_UnitSoundsEnabled | Global | Unit Audio | High | Every per-unit sound helper gates on this flag, and the toggle helpers plus the settings-apply path only set it to `0` or `1`. | c, asm |
| dword_543CA4 | g_LastUnitActivateSoundHandle | Global | Unit Audio | High | `Audio_PlayUnitActivateSound` stores the returned handle here, and `Audio_PlayUnitMoveOrderSound` immediately fades that same handle out before playing the move-order cue. | c, asm |
| sub_441A10 | Audio_EnableUnitSounds | Function | Unit Audio | High | Single-purpose helper that sets `g_UnitSoundsEnabled = 1`. | c, asm |
| sub_441A20 | Audio_DisableUnitSounds | Function | Unit Audio | High | Single-purpose helper that sets `g_UnitSoundsEnabled = 0`. | c, asm |

## Batch 24 – Special Cargo And Prisoner Wave
| Old Name / Pattern | New Name | Kind | Subsystem | Confidence | Evidence Summary | Sources |
|---|---|---|---|---|---|---|
| *(new constant)* | UNIT_TYPE_GOLD_CARGO | Helper | Unit Types / Special Entries | High | Type `31` resolves to `gold`, is excluded from `UnitStack_HasNormalCombatUnits`, and is checked by dedicated stack helpers rather than ordinary battle code. | c, asm, exe |
| *(new constant)* | UNIT_TYPE_PEASANT_CARGO | Helper | Unit Types / Special Entries | High | Type `32` resolves to `peas`, is excluded from normal-combat checks, and `UnitStack_NormalizePeasantCargo` rebuilds it as stack payload. | c, asm, exe |
| *(new constant)* | UNIT_TYPE_PRISONER_FOOT | Helper | Unit Types / Prisoners | Medium | Type `33` uses `specm`, shares the `Dowódca / Tactician / Soldat` label triplet, and is extracted into the prisoner pipeline on capture. | c, asm, exe |
| *(new constant)* | UNIT_TYPE_PRISONER_MOUNTED | Helper | Unit Types / Prisoners | Medium | Type `34` uses `speck`, shares the same localized-name triplet, and follows the same prisoner-only flow with cavalry-like movement assets. | c, asm, exe |
| sub_412000 | UnitSlots_ExtractSpecialEntries | Function | Unit Lifecycle / Special Entries | High | Copies only types `31..34` into a side buffer while compacting the source slot array, matching the pre-capture special-entry extraction step. | c |
| sub_4120B0 | UnitSlots_AppendEntries | Function | Unit Lifecycle / Special Entries | High | Finds the first empty slot and appends all entries from a temporary 31-byte slot buffer back into the squad. | c |
| sub_412100 | UnitStack_HasNormalCombatUnits | Function | Unit Lifecycle / Special Entries | High | Returns true only if the stack contains at least one slot whose type is not `31..34`, which is exactly the “has ordinary combatants” predicate used by movement, capture, and UI code. | c |
| sub_412170 | UnitStack_HasGoldCargo | Function | Unit Lifecycle / Special Entries | High | Scans squad slots for type `31` only. | c |
| sub_4121A0 | UnitStack_HasPeasantCargo | Function | Unit Lifecycle / Special Entries | High | Scans squad slots for type `32` only. | c |
| sub_4121D0 | UnitStack_NormalizePeasantCargo | Function | Unit Lifecycle / Special Entries | High | Sums all type-`32` slot payloads, removes the old entries, and recreates normalized peasant-cargo slots in capped chunks. | c |
| sub_412300 | UnitStack_CaptureDefeatedStack | Function | Unit Lifecycle / Capture | High | Logs capture, strips prisoner entries into the prisoner queue, flips ownership of captured slots, normalizes peasant cargo, and merges or preserves the defeated stack under the winner. | c |
| sub_412B60 | UnitStack_HasPrisonerUnits | Function | Unit Lifecycle / Prisoners | High | Returns true only when a squad contains type `33` or `34`. | c |
| sub_41A960 | UI_DrawSpecialUnitInfoPane | Function | UI / Unit Info | High | Dedicated info window path for types `31..34`, with alternate icon mapping and the localized-name lookup from the unit-type metadata table. | c |
| sub_44E7A0 | Prisoner_QueueCapturedUnit | Function | Prisoners | High | Debug log string is `Prisoner_AddToInWay`, and the function appends captured type `33/34` entries into the per-player queue later drained by `Prisoner_SetInCastles`. | c, asm |
| sub_44E850 | Building_FindFreePrisonerSlot | Function | Prisoners / Buildings | High | Scans the three 6-byte prison cells at `building + 445` and returns the first free index or `-1` if full. | c |
| sub_44EB70 | BuildingPrisoner_SetAction | Function | Prisoners / Buildings | High | `clash95.asm` exposes the debug string `Building_SetPrisonerAction(0x%08x,%d,%d)`, and the function writes the pending action byte at `building + 445 + 6 * slot + 3`. | c, asm |
| sub_44EBA0 | BuildingPrisoner_GetAction | Function | Prisoners / Buildings | High | Reads back the pending action byte from the same prison-cell field. | c |
| sub_44F1E0 | Building_CreatePrisonerUnit | Function | Prisoners / Buildings | High | Debug log string is `Building_CreateSpecial`, and every recovered caller uses it to insert a type `33/34` prisoner unit into a building garrison slot. | c, asm |
| sub_44F350 | Prisoner_NewTurn | Function | Prisoners | High | Debug log string is `Prisoner_NewTurn`, and the function advances held-prisoner timers, auto-assigns late orders, then dispatches behead/torture/pay behavior for each prison cell. | c |
| sub_44F4E0 | Building_CountPrisoners | Function | Prisoners / Buildings | High | Counts occupied prison cells by scanning the three 6-byte entries starting at `building + 445`. | c |
| sub_43EB80 | Building_CountPrisonerGarrisonEntries | Function | Buildings / Prisoners | High | Counts only type `33/34` entries inside the building garrison array, i.e. the prisoner-class special garrison occupants. | c |
| sub_43EBC0 | Building_CountSpecialGarrisonEntries | Function | Buildings / Special Entries | High | Counts all type `31..34` entries inside the 12-slot building garrison. | c |
| sub_43EC10 | Building_HasPrisonerGarrisonEntries | Function | Buildings / Prisoners | High | Returns true when the building garrison contains at least one type `33` or `34` entry. | c |
| PLAYER_DATA(player) + 141381 + 6 * slot | PlayerPrisonerTransferQueue | Recovered Struct | Prisoners | High | `Prisoner_QueueCapturedUnit` appends 10 fixed-size entries here, and `Prisoner_SetInCastles` drains them into nearby castles using the stored capture coordinates. | c |
| BuildingRecord + 445 + 6 * slot | BuildingPrisonerSlot | Recovered Struct | Prisoners / Buildings | High | Execution, torture, ransom, and prisoner-new-turn helpers all operate on the same three 6-byte cells embedded in the building record. | c |

## Batch 25 – Prisoner Action And Targeting Wave
| Old Name / Pattern | New Name | Kind | Subsystem | Confidence | Evidence Summary | Sources |
|---|---|---|---|---|---|---|
| sub_43F240 | Building_GetTotalValue | Function | Buildings / Economy | High | Sums stored resources, level bits, flag-based flat values, and add-on costs, then feeds both `AI_TickNationPostTurn` and the “richest hidden castle” torture outcome. | c, asm |
| sub_44E880 | BuildingPrisoner_RecalculateRansomValue | Function | Prisoners / Buildings | High | Recomputes prison-cell word `+4` from a nation-strength-derived random value, rounds it to tens, and `Prisoner_Pay` later spends that exact field. | c |
| sub_44EC80 | Building_IsVisibleToPlayer | Function | Buildings / Visibility | High | Tests all four tiles of the building footprint with the player-visibility helper, so it is a building-level visibility predicate rather than a generic boolean. | c |
| sub_44ECF0 | Prisoner_FindRichestHiddenEnemyCastle | Function | Prisoners / Torture | High | Searches enemy-owned castles that are not visible to the interrogating player and selects the one with the highest `Building_GetTotalValue`, matching the torture text about revealing the richest castle. | c |
| sub_44EDB0 | Prisoner_FindAnyHiddenEnemyCastle | Function | Prisoners / Torture | High | Returns the first enemy-owned castle that is hidden from the interrogating player, matching the torture text about revealing one of the king's castles. | c |
| sub_44EE20 | Prisoner_FindAnyHiddenEnemyUnitStack | Function | Prisoners / Torture | High | Returns the first enemy-owned hidden field stack, matching the torture outcome that reveals one enemy unit. | c |
| sub_44EEE0 | Map_RevealTilesInRadius2ForPlayer | Function | Map / Visibility | High | Stamps visibility around a center point in a radius-2 disk and is used immediately after torture-target discovery to reveal the selected target area. | c |
| sub_44EFA0 | Prisoner_Torture | Function | Prisoners | High | The asm log string is `Prisoner_Torture(0x%08x,%d)`, and the function dispatches the five torture outcomes: richest hidden castle, hidden castle, hidden unit, death without intel, or resistance. | c, asm |
| sub_44F260 | Prisoner_Pay | Function | Prisoners | High | The asm log string is `Prisoner_Pay(0x%08x,%d)`, and the function spends the prison cell's ransom value, converts the prisoner into a friendly special garrison unit, and removes the prison entry. | c, asm |

## Batch 26 – Map Loading And gameData State Wave
| Old Name / Pattern | New Name | Kind | Subsystem | Confidence | Evidence Summary | Sources |
|---|---|---|---|---|---|---|
| sub_44AD60 | PlayerRuntimeState_ResetDefaults | Function | gameData / Player Runtime | High | Resets one 1423-byte player block, restores the default ruler name at `+4`, zeros camera/controller fields, and clears the head byte of each embedded 6-byte prisoner queue entry. | c, asm |
| sub_44AE10 | Game_ResetPlayerRuntimeStateByIndex | Function | gameData / Player Runtime | High | Thin wrapper that applies `PlayerRuntimeState_ResetDefaults` to `gameData + 140024 + 1423 * playerIndex`. | c |
| loadMap | Map_LoadFromFile | Function | Map Loading | High | Loads `maps\\<name>.map`, copies three tile layers into the 14-byte tile record, stamps buildings, derives map dimensions, resets mission/view state, and clears unit/building occupancy tables. | c, asm, map |
| loadMultiplayerMaps | Scenario_LoadMultiplayerMapAndSeedPlayers | Function | Map Loading / Multiplayer | High | Formats `multi%d.map`, calls the raw map loader, copies five player runtime records into gameData, then seeds castles/units from fixed per-map starting-coordinate tables. | c, asm, map |
| sub_460360 | Scenario_LoadMissionByIndex | Function | Map Loading / Campaign | High | Jump-table dispatcher keyed by the active mission index register that loads `k_mapa*.map` / `p_mapa*.map` files and applies the mission-specific scripted setup. | c, asm |
| sub_460370 | Scenario_LoadMissionByIndexAndPlay | Function | Map Loading / Campaign | Medium | Preserves view-state bytes, optionally plays the mission-complete interlude, invokes `Scenario_LoadMissionByIndex`, then starts `PlayGame` on the selected mission. | c, asm |
| off_5180C0 | g_MissionStatusTextsByLanguage | Table | Map Loading / Campaign | High | Indexed as `3 * ACTIVE_MISSION_INDEX + language` by `UI_ShowMissionStatusPanel`, so it is the localized mission objective text table. | c |
| off_5181B0 | g_MissionStatusFormatsByLanguage | Table | Map Loading / Campaign | High | Three-entry localized format table used to render `Mission %d` status popups. | c |
| aMaps_1 | aMapsDirectory | Helper | Map Loading | High | Constant prefix copied before every `.map` filename in `Map_LoadFromFile`. | c, asm |
| dword_518938 | g_MultiplayerStartRows | Table | Map Loading / Multiplayer | High | `Scenario_LoadMultiplayerMapAndSeedPlayers` indexes this table alongside the companion column table to position each seeded player/castle pair. | c, asm |
| dword_51893C | g_MultiplayerStartColumns | Table | Map Loading / Multiplayer | High | Companion start-column table paired with `g_MultiplayerStartRows` during multiplayer map seeding. | c, asm |
| gameData + 140024 + 1423 * player | PlayerRuntimeState | Recovered Struct | gameData / Player Runtime | High | Campaign and skirmish code consistently treat this as a per-player runtime block containing enable/name/controller/camera state plus the embedded prisoner transfer queue and queen bytes. | c, asm |
| gameData + 140017 | active_mission_index | Recovered Struct Field | gameData / World Session | High | Mission status UI, mission advancement, and the campaign mission jump table all use this dword as the current scripted mission id, with `-1` meaning free/skirmish map. | c, asm |

## Batch 27 – Unit Slot Runtime And Effective Stat Wave
| Old Name / Pattern | New Name | Kind | Subsystem | Confidence | Evidence Summary | Sources |
|---|---|---|---|---|---|---|
| sub_40FDB0 | UnitSlot_CalcActionPointsFromFatigue | Function | Unit Stats / Runtime Slot | High | Reads the per-type base AP table, then applies the 80/90/100 fatigue thresholds from slot `+10` to derive the runtime action-point budget used at the start of each turn. | c, asm |
| sub_40FE60 | UnitSlot_GetBaseActionPoints | Function | Unit Stats / Runtime Slot | High | Thin helper that returns the unmodified base AP entry from the per-type table for the slot's unit type. | c, asm |
| sub_4118A0 | UnitSlot_ShouldGainFatigueFromLowActionPoints | Function | Unit Stats / Runtime Slot | Medium | Predicate returns true only for slots with AP `<= 3` and no low-morale refusal flag, matching the new-turn path that adds fatigue to overextended units. | c |
| sub_4118C0 | UnitSlot_CanRecoverFatigue | Function | Unit Stats / Runtime Slot | High | Predicate returns true only when the spent-turn flag bit is clear, and the new-turn handler uses it to decide which slots recover fatigue. | c, asm |
| sub_4118D0 | UnitSlot_HasSevereFatigue | Function | Unit Stats / Runtime Slot | High | Predicate checks `fatigue >= 80`, exactly matching the threshold used by the new-turn morale-loss path. | c, asm |
| sub_4127F0 | UnitStack_AdjustFatigueByPredicate | Function | Unit Stats / Runtime Slot | High | Iterates stack slots, filters through a predicate, skips cargo/prisoner entries, adjusts slot `+10`, clamps to `0..100`, and relinks the army fact. | c, asm |
| sub_4128E0 | UnitStack_AdjustMoraleByPredicate | Function | Unit Stats / Runtime Slot | High | Iterates stack slots, filters through a predicate, skips cargo/prisoner entries, adjusts slot `+11`, clears the refusal bit on morale gains, clamps to `0..20`, and relinks the army fact. | c, asm |
| sub_412A30 | UnitStack_SetSpentTurnFlag | Function | Unit Stats / Runtime Slot | High | Sets bit `0x2` in each occupied slot's state-flags byte, and combat/new-turn code uses that bit to suppress fatigue recovery after a spent turn. | c, asm |
| sub_412A60 | UnitStack_ClearSpentTurnFlag | Function | Unit Stats / Runtime Slot | High | Clears bit `0x2` in each occupied slot's state-flags byte during the new-turn refresh pass. | c, asm |
| sub_412AF0 | UnitStack_HasLowMoraleUnit | Function | Unit Stats / Runtime Slot | High | Scans occupied slots for state-flags bit `0x4`, which is set by `Unit_CheckLowMorale` when a unit refuses orders. | c, asm |
| UnitStats_CalcMeleeAttack | UnitStats_CalcEffectiveMeleeAttack | Function | Unit Stats | High | The helper combines stance bits, base melee attack, morale, and current health percent, so `effective` is more accurate than the generic `Calc`. | c, asm |
| UI_IconIndexFromStats | UnitStats_GetMeleeIconIndex | Function | Unit Stats / UI | High | Uses the same melee inputs as the effective melee helper but omits health scaling, matching a UI icon-strength index rather than a generic UI helper. | c, asm |
| UnitStats_CalcRangedAttack | UnitStats_CalcEffectiveRangedAttack | Function | Unit Stats | High | Combines stance bits, base ranged attack, morale, current health percent, and the slot `+22` ranged-state bit, making it the effective ranged-attack helper. | c, asm |
| UnitStats_CalcDamagePerHit | UnitStats_CalcEffectiveDamagePerHit | Function | Unit Stats | High | Uses base damage, morale, and current health percent to produce the effective per-hit damage value. | c, asm |

## Batch 114 – Port Reinforcement Text And SDL Dispatch Wave
| Old Name / Pattern | New Name | Kind | Subsystem | Confidence | Evidence Summary | Sources | Subagent Evidence |
|---|---|---|---|---|---|---|---|
| `g_PortSupplyReadyTexts` | `g_PortReinforcementArrivedTexts` | Table | Port / UI | High | The three localized strings are not generic “supply ready” status text; they are the arrival announcement shown only when `PORT_SUPPLY_READY_FLAG` is set, immediately before the player collects the incoming reinforcement shipment. | c, asm, prior-artifact | yes: Copernicus |
| `DispatchMessageA` hard-coded `Platform_MainWindowProc` dispatch | registered window-proc dispatch with preserved `Platform_MainWindowProc` thunk | sdl_port_fix | SDL / Platform | High | The SDL shim already stores the registered class proc in `RegisterClassA`; dispatch can safely call that proc generically, while keeping the special-case thunk only for `Platform_MainWindowProc` because the recovered handler still uses a decompiler `__thiscall` signature. | c | yes: Copernicus |
| `BuildingRecord.selected_unit_licence_slot_index` | `BuildingRecord.active_production_licence_slot_index` | recovered_struct | Buildings / Production Licences | High | The byte at `+414` is not just the UI-selected licence slot: `Building_ProcessUnitProductionTurn` reads it every turn to resolve the currently produced unit type, `Building_SetUnitProduction` seeds it, and completion/stop paths clear it back to `-1`. | c, asm, prior-artifact | yes: Beauvoir |

## Batch 116 – Battle Shooting And Role Table Wave
| Old Name / Pattern | New Name | Kind | Subsystem | Confidence | Evidence Summary | Sources | Subagent Evidence |
|---|---|---|---|---|---|---|---|
| `sub_436D10` | `UnitBattle_RetreatUnit` | function | Unit Battle / Movement | High | The asm debug string is `cofnij_oddzial`, and the body searches nearby empty traversable tiles, keeps positions from which some enemy remains in range, then builds a path and retreats there. | c, asm | yes: Bacon |
| `sub_437050` | `UnitBattle_IsTileWithinMinRange` | function | Unit Battle / Range | High | Pure Euclidean-distance check from the acting battle slot to the target tile against `g_UnitTypeMinRange`; the shooting-move helper uses it exactly to decide whether the unit must back away before firing. | c, asm | yes: Bacon |
| `sub_4370B0` | `UnitBattle_MoveShootingUnit` | function | Unit Battle / Movement | High | The asm debug string is `ruch_Oddzialem_Strzelajacym`, and the helper either shoots immediately, retreats if the target is inside min range, or moves near the target and then loops `UnitBattle_Shot` / `UnitBattle_ShotWall`. | c, asm | yes: Bacon |
| `sub_43B740` | `UnitBattle_ApproachToSafeDistance` | function | Unit Battle / Movement | High | The asm debug string is `podejdz_na_Bezpieczna_Odleglosc`, the function advances toward a reachable safe-distance tile under AP limits, and then always falls through to `UnitBattle_Defence`. | c, asm | yes: Bacon |
| `sub_4549A0` | `UnitStack_HasNormalCombatUnitsByIndex` | function | Unit Lifecycle / Wrapper | High | Exact index-to-stack wrapper over `UnitStack_HasNormalCombatUnits`. | c, asm | yes: Bacon |
| `sub_4549E0` | `UnitStack_GetSquadCountByIndex` | function | Unit Lifecycle / Wrapper | High | Exact index-to-stack wrapper over `Unit_GetSquadCount`. | c, asm | yes: Bacon |
| `byte_5125B5` | `g_UnitTypeRole` | table | Unit Metadata / Battle Roles | High | The metadata parser loads the literal `role` field into this byte band, battle target-priority and role-matchup helpers index through it, and the healing gate only counts low-health units whose role class is at least `3`. | c, asm, prior-artifact | yes: Bacon |
| UnitStats_CalcSiegeAttack | UnitStats_CalcEffectiveSiegeAttack | Function | Unit Stats | Medium | Mirrors the melee helper but swaps in the alternate siege/building attack table. | c, asm |
| byte_51257E | g_UnitTypeBaseMeleeAttack | Table | Unit Stats | High | Indexed by `88 * unitType` wherever standard melee capability or melee strength is needed. | c, asm |
| byte_51257F | g_UnitTypeBaseRangedAttack | Table | Unit Stats | Medium | Indexed by `88 * unitType` in ranged/projectile attack math and battle-targeting logic. | c, asm |
| byte_512580 | g_UnitTypeBaseActionPoints | Table | Unit Stats | High | Indexed by `88 * unitType` for slot initialization, AP refresh, and battle-map unit seeding. | c, asm |
| byte_512581 | g_UnitTypeBaseDamage | Table | Unit Stats | High | Indexed by `88 * unitType` in the direct damage-per-hit helpers. | c, asm |
| byte_512582 | g_UnitTypeMaxRange | Table | Unit Stats | High | Projectile distance checks compare against this table as the upper range limit. | c, asm |
| byte_512583 | g_UnitTypeMinRange | Table | Unit Stats | Medium | Projectile distance checks compare against this table as the lower dead-zone limit. | c, asm |
| byte_512584 | g_UnitTypeBaseSiegeAttack | Table | Unit Stats | Medium | Alternate attack helper indexes this table for siege/structure contexts. | c, asm |
| gameData + 147174 + 6 + 31 * slot | UnitSlotRecord | Recovered Struct | Unit Stats / Runtime Slot | High | Repeated stable accesses recover a 31-byte per-slot record with type, owner, AP, health, fatigue, morale, stance bits, flags, and auxiliary state. | c, asm |
| UnitStackRecord.unit_slot_fields (old interpretation) | UnitSlotRecord.current_health_percent / fatigue / morale | Recovered Struct Field | Unit Stats / Runtime Slot | High | Cross-checking `UnitSlot_InitFromType`, `Unit_NewTurn`, combat formulas, and UI drawing overturned the earlier `morale_percent`/`veterancy` reading and established the true health/fatigue/morale split. | c, asm |

## Deferred / Ambiguous
- `PlayerRuntimeState.controller_mode` at `+27` is clearly a multiplayer/campaign controller-selection field, but the exact original enum semantics for values `1` and `2` still need a focused pass through UI and AI setup code.
- The earlier prisoner-only interpretation for `specm` and `speck` is superseded: later evidence from `Queen_NewTurn` shows the same ids are also created during the royal birth event. The cross-language label triplet is still unresolved.
- `gold` and `peas` are now mapped as cargo-style special entries (`UnitType31_GoldCargo` and `UnitType32_PeasantCargo`), but the exact relationship between type `32` and ordinary recruitable peasants (type `0`) is still not fully explained by the recovered gameplay rules.
- `g_UnitTypeFlags` bit1 now aligns with a light-unit category through the 6-vs-10 morale seed at slot `+11`, but its broader gameplay consequences are still only partially recovered.
- `UnitSlotRecord.stance_bits` at `+12` is clearly a stance/formation byte, but the exact meaning of the upper subfields remains unresolved.
- `UnitSlotRecord.state_flags` bit `0x8` appears in both special-entry and cargo-related flows, but its full gameplay meaning is still unresolved.
- `BattleMap_GetMoveSoundSurfaceClass` is only medium-confidence. The implementation clearly returns a small sound-surface class used by movement audio, but the original designer-facing labels for values `0/1/2` remain inferred from suffix behavior rather than explicit text.
- `sub_441E60` still appends `gothim.wav`, but the exact gameplay role is not locked down safely enough. It is used in a late attack-resolution path for certain projectile or special-attack unit types, so it remains deferred rather than being mislabeled as a generic kill or victory sound.

## Batch 28 – Building, Port, And Queen Special-Entry Wave
| Old Name / Pattern | New Name | Kind | Subsystem | Confidence | Evidence Summary | Sources |
|---|---|---|---|---|---|---|
| sub_41E1E0 | Building_FinishConstruction | Function | Buildings / Construction | High | Debug log string is `Building_BuildFinished() - %d,%d`; the helper finalizes the build, refreshes nearby map state, and triggers the post-build naming/UI path. | c, asm |
| sub_41E3F0 | Building_ProcessUnitProductionTurn | Function | Buildings / Production | High | Debug log string is `Building_Production(0x%08x) - nowa jednostka`; the function decrements the selected licence timer, spends local resources, and inserts the finished unit into the building garrison. | c, asm |
| sub_43E770 | Building_CanAcceptUnitStack | Function | Buildings / Garrison | High | Called both from movement/UI hover checks and the actual enter-building path; it gates entry by building mode and by whether the stack contains normal units, gold cargo, peasant cargo, or special entries. | c |
| sub_43E820 | Building_HasAddonInGarrison | Function | Buildings / Garrison | High | Scans the 12 add-on/licence bytes at `building + 402` for the requested add-on id, matching every buy/remove add-on caller. | c |
| sub_43EB50 | Building_CountGarrison | Function | Buildings / Garrison | High | Counts occupied 31-byte garrison slots across the 12-slot array starting at `building + 18`. | c |
| sub_43EC40 | Building_CompactGarrison | Function | Buildings / Garrison | High | Shifts occupied 31-byte garrison entries down over empty slots and then calls `Building_OnGarrisonChange`, exactly matching garrison compaction after casualties or transfers. | c |
| sub_43ED20 | Building_CanEquipAddon | Function | Buildings / Add-ons | High | Uses the building footprint class, technology bits, add-on flags, whitelist, and blacklist tables to decide whether an add-on type can be installed. | c, asm |
| sub_43EE50 | Building_GetTaxPressureTier | Function | Buildings / Settlement Economy | High | Converts population bands plus the tax-rate byte into a 0..3 pressure tier by comparing against the 4x4 threshold table later consumed by the population-growth helper. | c |
| sub_43EED0 | Building_UpdatePopulationGrowth | Function | Buildings / Settlement Economy | High | Updates settlement condition from the previous growth delta, derives a new growth percentage from tax pressure, add-ons, tech, and condition, then writes both the new delta and the updated peasant population. | c |
| sub_43F0C0 | Building_CollectGoldIncome | Function | Buildings / Settlement Economy | High | Converts population and tax rate into `gold_income_last_turn`, zeros income during crises, and adds the result into `stored_gold`. | c |
| sub_43F160 | Building_UpdateCrisisState | Function | Buildings / Settlement Economy | Medium | Advances the low 3-bit crisis timer at `building + 435`, applies plague-style population loss while active, and can start a new crisis when the settlement grows large enough. | c |
| sub_44F1E0 / Building_CreatePrisonerUnit | Building_CreateSpecialGarrisonUnit | Function | Buildings / Special Entries | High | The asm/exe log string is `Building_CreateSpecial`; besides prisoner payment and transfer callers, `Queen_NewTurn` also uses it to place the royal birth special entry into a building garrison, proving the older prisoner-specific rename was too narrow. | c, asm, exe |
| sub_44FE70 | Building_ShowPrisonerManagementPanel | Function | Buildings / Prisoners / UI | High | Renders a three-prisoner action panel, nation-strength bars, and queen mood state, then writes the selected behead/torture/pay actions back through `BuildingPrisoner_SetAction`. | c |
| sub_450CE0 | Player_Surrender | Function | Turn Flow / Player State | High | Called directly from the surrender-confirm dialog; destroys all owned buildings and unit stacks, then clears the player's active runtime flag. | c |
| sub_450FD0 | Player_CheckForDefeatAndHandleElimination | Function | Turn Flow / Player State | High | Scans all owned field stacks and buildings; when none remain it deactivates the player, refreshes the UI/music state, and returns the defeat/continue result used by the main turn loop. | c |
| byte_515D00 | g_SettlementTaxPressureThresholds | Table | Buildings / Settlement Economy | High | `Building_GetTaxPressureTier` reads this as a 4x4 threshold grid keyed first by population band and then by tax-rate band. | c |
| dword_517B48 | g_PortSupplySpawnRowOffsets | Table | Port / Reinforcements | High | `Port_GetSupply` walks these offsets in lockstep with the companion column table to find the first free tile around the dock for the spawned reinforcement stack. | c, asm |
| dword_517B4C | g_PortSupplySpawnColumnOffsets | Table | Port / Reinforcements | High | Companion column-offset table paired with `g_PortSupplySpawnRowOffsets` during port reinforcement spawning. | c, asm |
| dword_517BA8 | g_PortSupplyUnitTypePool | Table | Port / Reinforcements | High | `Port_GetSupply` chooses random entries from this 12-element pool when seeding the arriving reinforcement stack. | c, asm |
| off_518D98 | g_PrisonerDeathByExhaustionTexts | Table | Prisoners / UI Text | High | `Prisoner_NewTurn` formats these three localized strings exactly when a held prisoner reaches the terminal exhaustion turn. | c, asm |
| off_519350 | g_QueenSonBirthTexts | Table | Queen / UI Text | High | `Queen_NewTurn` selects this localized triplet only when the birth-event special type is `33`, and the strings explicitly say the queen gave birth to a son. | c, asm, exe |
| off_51935C | g_QueenDaughterBirthTexts | Table | Queen / UI Text | High | Companion localized triplet selected when the birth-event special type is `34`, and the strings explicitly say the queen gave birth to a daughter. | c, asm, exe |
| gameData + 586374 | PortRuntimeState | Recovered Struct | gameData / Port Runtime | High | The contiguous six-dword block at offsets `586374..586394` stores the port footprint, next arrival turn, supply-ready state, shipment size, and shore-refresh flag used together by every port helper. | c, asm |

## Deferred / Ambiguous
- Types `33` and `34` are no longer safe to describe as prisoner-only entries: `Queen_NewTurn` creates the same ids through `Building_CreateSpecialGarrisonUnit`. The conservative documentation names are now `UnitType33_SpecialFootPersonage` and `UnitType34_SpecialMountedPersonage`, but the exact original gameplay label behind `Dowódca / Tactician / Soldat` remains unresolved.

## Batch 29 – Garrison Recovery And Queen Whim Wave
| Old Name / Pattern | New Name | Kind | Subsystem | Confidence | Evidence Summary | Sources | Subagent Evidence |
|---|---|---|---|---|---|---|---|
| sub_41E570 | Building_UpdateGarrisonTrainRepairTimers | Function | Buildings / Garrison | High | Iterates the 12 packed garrison task bytes, decrements repair countdown bits `3..5` until slot health returns to `100`, and decrements train/order countdown bits `0..2` until the stance/order completion path fires. | c, asm | yes: Lorentz |
| sub_41E7B0 | UnitSlot_NeedsMoraleRecovery | Function | Unit Runtime / Morale | High | Reads slot morale at `+11` and compares it against the recovered resting floors `6` or `10` depending on the unit-type flag bit, making it a threshold predicate rather than a timer helper. | c, asm | yes: Lorentz |
| sub_41E7F0 | Building_RecoverGarrisonFatigueAndMorale | Function | Buildings / Garrison | High | Completed-building upkeep pass over 12 garrison slots: first reduces slot fatigue (`+10`) by `50`, then increases morale (`+11`) by `1` only when `UnitSlot_NeedsMoraleRecovery` succeeds, before refreshing the building garrison state. | c, asm | yes: Lorentz |
| AI_FindActionCandidate | Queen_FindEligibleBirthHostBuilding | Function | Queen / Birth | Medium | Only gameplay caller is the childbirth branch in `Queen_NewTurn`; it scans active owned building records and returns the host building used by `Building_CreateSpecialGarrisonUnit` for types `33/34`. | c, asm, map | yes: Pauli, Averroes |
| sub_44FC70 | Building_DrawPrisonerRows | Function | Buildings / Prisoners / UI | High | Reads the three prison cells at `building + 445`, draws the prisoner type icon and action icon for each occupied slot, and renders the ransom/count field, so it is the prisoner-row renderer used by the management panel. | c, asm | yes: Averroes |
| PORT_SHORE_REFRESH_FLAG_OFFSET / PORT_NEEDS_SHORE_REFRESH | PORT_SHORE_VARIANT_FLAG_OFFSET / PORT_SHORE_VARIANT_FLAG | Helper | Port / Runtime State | High | The sixth dword in the port sidecar is read by `Port_BuildShorePieces` to choose which shoreline-piece variant to stamp; it is more specific than a generic “refresh needed” flag. | c, asm | yes: Averroes |
| word_5191F0 + off_5191F2 | g_QueenWhimRecords / QueenWhimRecord | Recovered Struct | Queen / Whim Table | High | `Queen_NewTurn` indexes these overlapped symbols with `7 * whimIndex`, reading a uint16 requirement and one localized pointer triplet; the next table starts exactly after 25 such records. | c, asm | yes: Pauli |
| UNIT_TYPE_PRISONER_FOOT | UNIT_TYPE_SPECIAL_FOOT_PERSONAGE | Helper | Unit Types / Special Entries | High | Type `33` is handled by both prisoner capture code and the queen birth path; retaining a prisoner-only constant name was misleading once `Queen_NewTurn` was corroborated against `specm` and the commander-like localized label. | c, asm, exe | yes: Pauli |
| UNIT_TYPE_PRISONER_MOUNTED | UNIT_TYPE_SPECIAL_MOUNTED_PERSONAGE | Helper | Unit Types / Special Entries | High | Type `34` is the mounted sibling of the same shared special-entry family, created by the queen birth path as well as the prisoner system, so the older prisoner-only constant name was no longer accurate. | c, asm, exe | yes: Pauli |

## Deferred / Ambiguous
- The internal debug string `NewQueenWindow()` was not promoted because the localized prompt text proves the dialog is specifically a marriage offer, not a generic queen-window controller.
- Resolved in Batch 97: `QueenWhimRecord.required_stronghold_funds` is the safer implementation name. The code only proves that whims consume an owned-stronghold treasury pool, not a more specific designer-facing economy label.

## Batch 30 – Marriage Dialog And Prisoner Panel Controls Wave
| Old Name / Pattern | New Name | Kind | Subsystem | Confidence | Evidence Summary | Sources | Subagent Evidence |
|---|---|---|---|---|---|---|---|
| sub_4127A0 | UnitSlot_AdjustFatigueByPredicate | Function | Unit Runtime / Fatigue | High | Single-slot analogue of `UnitStack_AdjustFatigueByPredicate`: applies the caller-supplied delta to slot fatigue at `+10`, skips special-entry types `31..34`, clamps to `0..100`, and gates on the predicate passed in `ebx`. | c, asm | yes: Lorentz |
| sub_412880 | UnitSlot_AdjustMoraleByPredicate | Function | Unit Runtime / Morale | High | Single-slot analogue of `UnitStack_AdjustMoraleByPredicate`: applies the caller-supplied delta to slot morale at `+11`, clears the refusal flag on gains, skips special-entry types, and clamps to `0..20`. | c, asm | yes: Lorentz |
| Building_FindFirstValidAddonSlot | Building_AdjustAllGarrisonMoraleByDelta | Function | Buildings / Garrison | High | The old prototype name was misleading; the wrapper scans every occupied building garrison slot and forwards the caller-supplied morale delta through `UnitSlot_AdjustMoraleByPredicate`, with no add-on search behavior at all. | c, asm | yes: Lorentz |
| sub_43EE10 | Building_CycleAllGarrisonOrdersOnce | Function | Buildings / Garrison | Medium | Scans every occupied building garrison slot and calls `Building_UseGarrisonSlot` once per slot, so it is a bulk order/state cycler rather than a morale or timer helper. | c, asm | yes: Lorentz |
| Queen_ShowProposalDialog | Queen_ShowMarriageProposalDialog | Function | Queen / Marriage | High | The localized prompt explicitly asks whether the ruler wants to marry the princess offered by king Wolfgang's envoy; the weak debug string `NewQueenWindow()` is less semantically accurate than the prompt text and callsite behavior in `Queen_NewTurn`. | c, asm, exe | yes: Pauli, Averroes |
| g_QueenProposalPrompt | g_QueenMarriageProposalTexts | Table | Queen / Marriage | High | Three localized strings describing the envoy-delivered marriage offer, used only by the marriage dialog at `0x446F40`. | c, asm, exe | yes: Pauli |
| g_QueenMoodTexts | g_QueenRelationshipStateTexts | Table | Queen / Relationship State | Medium | The 30-entry localized table covers `No queen`, anger, estrangement, affection, and childbirth intent, so it models broader queen relationship states rather than pure mood text. | c, asm | yes: Pauli |
| sub_44F510 | BuildingPrisonerActionWidget_HasPrisoner | Function | Buildings / Prisoners / UI | High | Converts a prisoner-action widget pointer back into one of the three prison-cell indices and checks whether that slot's prisoner type byte is occupied. | c, asm | yes: Averroes |
| sub_44F580 | BuildingPrisonerActionButton_SelectBehead | Function | Buildings / Prisoners / UI | High | Bound by the widget table to the localized `Behead` labels and only selects that action in the prisoner panel; execution happens later through `BuildingPrisoner_SetAction`. | c, asm, exe | yes: Averroes |
| sub_44F5F0 | BuildingPrisonerActionButton_SelectTorture | Function | Buildings / Prisoners / UI | High | Bound by the widget table to the localized `Torture` labels and acts as the prisoner-panel selector for that action. | c, asm, exe | yes: Averroes |
| sub_44F660 | BuildingPrisonerActionButton_SelectBribery | Function | Buildings / Prisoners / UI | High | Bound by the widget table to the localized `Bribery` labels and selects the ransom/bribery action in the prisoner panel. | c, asm, exe | yes: Averroes |
| sub_44F6D0 | BuildingPrisonerPanel_BackButton | Function | Buildings / Prisoners / UI | Medium | Bound to the localized `Back` button and signals dialog exit by updating `dword_5443F4`, which the prisoner-panel event loop watches. | c, asm, exe | yes: Averroes |

## Deferred / Ambiguous
- `Queen_DrawMoodPanel` is mostly reliable and was left unchanged; `Queen_DrawMoodSummary` would also fit, but the current name is not misleading enough to justify churn.
- `g_QueenRelationshipStateTexts` is a safer name than `g_QueenMoodTexts`, but the exact internal state machine behind each row still mixes queen presence, mood, and childbirth intent.
- `Building_CycleAllGarrisonOrdersOnce` is semantically safe at the implementation level, but the original designer-facing term for the packed order/state byte at slot `+0x0C` is still unresolved.

## Batch 31 – Movement Queue And Building Approach Path Wave
| Old Name / Pattern | New Name | Kind | Subsystem | Confidence | Evidence Summary | Sources | Subagent Evidence |
|---|---|---|---|---|---|---|---|
| sub_410010 | UnitStack_GetMinCurrentActionPoints | Function | Unit Movement / Action Points | High | Scans the ten embedded slot records and returns the minimum live value from slot byte `+8`, which is the shared current action-point budget later compared against path waypoint costs and attack thresholds. | c | yes: Lorentz |
| sub_410130 | UnitStack_SpendActionPointsClamped | Function | Unit Movement / Action Points | High | Iterates occupied slots, clamps the requested spend down to the smallest current action-point value, subtracts that amount from each slot, and relinks the army fact. | c | yes: Lorentz |
| sub_410170 | UnitStack_SpendActionPointsUnchecked | Function | Unit Movement / Action Points | High | Raw per-slot action-point subtraction helper used inside queued-path execution after the step cost has already been validated against the stack minimum. | c | yes: Lorentz |
| sub_4101A0 | UnitStack_SubtractActionPointsFloorZero | Function | Unit Movement / Action Points | Medium | Per-slot subtraction helper that floors each slot's action points at zero instead of clamping the spend amount globally. | c | yes: Lorentz |
| sub_4101E0 | UnitStack_SpendActionPointsByIndexClamped | Function | Unit Movement / Action Points | High | Thin wrapper that applies `UnitStack_SpendActionPointsClamped` to `gameData + 147174 + 725 * stackIndex`. | c | yes: Lorentz |
| sub_410330 | UnitStack_ExecuteQueuedPath | Function | Unit Movement / Path Execution | High | Consumes the queued path at `stack + 316`, validates each waypoint cost against the stack's current action points, advances tile occupancy/vision/traps, and rewrites the remaining waypoint costs after partial movement. | c | yes: Averroes |
| Unit_MoveTrackNear | Building_GenerateApproachTrack | Function | Building / Pathing | High | The embedded string is `Unit_MoveTrackToBuilding(%d,%d)`; the helper temporarily clears the target building footprint, generates a track, trims the tail to stop at the footprint edge, and restores the building tiles. | c, asm, map | yes: Averroes |
| sub_415970 | Building_GenerateNearApproachTrack | Function | Building / Pathing | High | The sibling helper logs `Unit_MoveTrackNearBuilding(%d,%d)`, uses the same temporary footprint clearing, and returns a trimmed approach path that stops one node earlier in the near-building case. | c, asm | yes: Averroes |
| Track_BridgesOn | Pathing_EnableBridgeCrossings | Function | Map / Pathing | High | Pure global mode toggle around path generation; callers wrap only bridge-capable movement cases, and the matching cost readers branch on the same flag to treat bridge overlays as traversable. | c, asm, map | yes: Averroes |
| Track_BridgesOff | Pathing_DisableBridgeCrossings | Function | Map / Pathing | High | Clears the same bridge-crossing mode flag after path generation, restoring normal terrain-cost behavior. | c, asm, map | yes: Averroes |
| sub_415CD0 | QueuedPath_StartsAtTile | Function | Unit Movement / Path Cache | High | Returns false when the queued path count is zero; otherwise compares the first packed waypoint's row and column bytes against the requested tile. | c | yes: Averroes |
| sub_415D00 | QueuedPath_StartsInBuildingFootprint | Function | Building / Path Cache | High | Returns false when the queued path is empty; otherwise checks whether the first packed waypoint already lies on the exact building tile or within its 2x2 footprint. | c | yes: Averroes |
| sub_424370 | Map_GetBridgeCrossingCostOrZero | Function | Map / Pathing | High | Returns cost `5` only for the bridge overlay tile-id range `0x25B..0x262`, and zero otherwise; the terrain-cost readers consult it only while bridge crossing mode is enabled. | c, asm | yes: Averroes |
| Path_GenerateTrack | Temple_GenerateApproachTrack | Function | Temple / Pathing | High | The embedded string is `Unit_MoveTrackToTemple(%d,%d,%d)`; the helper temporarily clears one temple overlay word, runs `Unit_MoveTrack`, then restores the original word. | c, asm, map | yes: Averroes |
| dword_52456C | g_TerrainMoveProfileOffsets | Global | Map / Movement Costs | High | Indexed by terrain tile id to select the correct offset into the byte-based move-cost profiles consumed by world-map and stack-level movement-cost helpers. | c, asm | yes: Averroes |
| dword_52556C | g_PathingAllowBridgeCrossings | Global | Map / Pathing | High | Global bridge-crossing mode bit toggled by the pathing wrappers and consulted by terrain-cost readers when deciding whether bridge overlays should supply a synthetic cost. | c, asm | yes: Averroes |
| gameData + 147174 + 316 | QueuedPathBuffer / queued_path | Recovered Struct | Unit Movement / Path Cache | High | Every planner copies exactly `0x194` bytes into this region, while the queued-path guards and executor treat it as a persistent count-plus-waypoints buffer rather than an anonymous blob. | c, asm | yes: Averroes |
| packed dword at queued_path + 4 | PathWaypoint | Recovered Struct | Unit Movement / Path Cache | High | Low byte is row, next byte is column, and the high word is the cumulative action-point cost; the executor subtracts spent cost from that high word after each partial move. | c, asm | yes: Averroes |

## Deferred / Ambiguous
- `sub_4100B0` clearly derives a stack-wide readiness/order tier from per-slot control bits, but the exact gameplay label behind `(slot + 18) & 3` is still unresolved.
- Resolved in Batch 35: stack state-flags bit `0x8` is the plague marker (`UNIT_SLOT_FLAG_PLAGUE`), so the older cargo-state ambiguity was retired.
- `sub_410260` gates the animated movement path on visibility and reachable waypoints, but the original designer-facing term for that reveal/animation predicate is still unresolved.
- Resolved in Batch 36: `Unit_MoveTrack_1` became `Unit_MoveTrackNearTile` after asm/exe debug-string evidence (`Unit_MoveTrackNear(%d,%d,%d)`) confirmed that the helper intentionally stops adjacent to the target tile.

## Batch 32 – Stack Lifecycle And Combat Utility Wave
| Old Name / Pattern | New Name | Kind | Subsystem | Confidence | Evidence Summary | Sources | Subagent Evidence |
|---|---|---|---|---|---|---|---|
| sub_40FAD0 | Camera_CenterOnUnit | Function | Camera / Turn Flow | High | Centers the strategic-map camera on the stack tile, clamps against map bounds using the world-session width/height fields, and forces a redraw. | c | yes: Pauli |
| sub_40FEC0 | UnitStack_ClearRemainingActionPoints | Function | Unit Lifecycle / Turn State | High | Iterates every occupied slot and zeroes the current action-point byte at `+8`, then relinks the stack fact; used after hide/contact actions that should end the stack's turn immediately. | c | yes: Pauli |
| sub_411810 | UnitStack_ApplyPeasantCargoAttrition | Function | Unit Turn Update / Special Cargo | High | Only damages unit type `32` when slot flag bit `0x8` is set, applies random `15..25` health loss, deletes dead slots, compacts the stack, and refreshes the linked fact. | c | yes: Pauli |
| sub_412BE0 | UnitStack_CalcMilitaryStrength | Function | Combat Evaluation | High | Counts live slots in one stack and feeds the slot block into the shared strength-scoring helper `sub_412B90`. | c | yes: Pauli |
| sub_412C00 | Building_CalcGarrisonStrength | Function | Combat Evaluation / Buildings | High | Computes the building-side factor through `sub_41C410`, counts garrison entries, and scores the garrison with the same shared strength helper used by mobile stacks. | c | yes: Pauli |
| sub_412C30 | Player_CalcMilitaryStrength | Function | Combat Evaluation / Player State | High | Aggregates `Building_CalcGarrisonStrength` over owned buildings and `UnitStack_CalcMilitaryStrength` over owned world-map stacks to produce one per-player military-strength total. | c | yes: Pauli |
| sub_422BE0 | Unit_AddToGroup | Function | Unit Lifecycle / Stack Merge | High | The embedded string is `Unit_AddToGroup(%d,%d,%d)`; the helper optionally approaches the destination stack, spends `4` action points, appends the source slots into the destination stack, and kills the source stack. | c, asm | yes: Pauli |
| sub_422DC0 | Unit_CanMoveSelectionFromGroupToTile | Function | Unit Lifecycle / Stack Split | High | Clones the source stack into a temporary buffer, copies only the selected slot indices, then checks whether the moved subset has at least `4` action points and can either stand on or merge into the destination tile. | c | yes: Pauli |
| sub_423050 | Unit_MoveSelectionFromGroupToTile | Function | Unit Lifecycle / Stack Split | High | The embedded string is `Unit_MoveFromGroup`; the helper moves the selected slots into a new or friendly destination stack, subtracts `4` action points from moved slots, compacts the source stack, and resolves hidden contact/traps at the destination. | c, asm | yes: Pauli |
| sub_444150 | UnitStack_RevealAndEngageHiddenEnemies | Function | Combat / Hidden Contact | Medium | First reveals hidden enemy stacks within vision when the stack's unresolved order tier is at least `3`, then attacks adjacent hidden enemy stacks and clears the acting stack's remaining action points. | c | yes: Pauli |
| unit_stack + 720 | UnitStackRecord.is_hidden_on_world_map | Recovered Struct Field | Unit Lifecycle / Hidden State | High | The hide action sets byte `+720`, merge/split helpers clear it, and `UnitStack_RevealAndEngageHiddenEnemies` clears the same flag on exposed enemy stacks. | c | yes: Pauli |

## Deferred / Ambiguous
- `sub_4100B0` still looks like a stack-wide order/recon tier derived from `(slot + 18) & 3`, but the gameplay label behind that tier is not locked down enough for a factual rename.
- `sub_4129E0` remains deferred because it is a repeated post-combat/post-event slot mutator whose exact designer-facing semantics are still obscured by upstream naming debt.

## Batch 33 – Movement Utility Wrapper Cleanup
| Old Name / Pattern | New Name | Kind | Subsystem | Confidence | Evidence Summary | Sources | Subagent Evidence |
|---|---|---|---|---|---|---|---|
| sub_40FE80 | UnitStack_GetMinBaseActionPoints | Function | Unit Movement / Action Points | High | Scans the embedded slot records and returns the minimum base action-point value derived from `UnitSlot_GetBaseActionPoints`, not the current spent-down value. | c | no |
| sub_414350 | UnitStack_GetMoveCostToTile | Function | Unit Movement / Pathing | High | Thin wrapper that forwards the active stack record to `UnitStack_GetTileMoveCostOrZero` for one destination tile. | c | yes: Averroes |
| sub_414400 | Path_InsertBridgeCornerWaypoints | Function | Unit Movement / Pathing | Medium | Rebuilds a generated waypoint queue, and when two consecutive waypoints are diagonal bridge corners with a legal orthogonal detour, inserts an intermediate packed waypoint with averaged cumulative cost. | c | yes: Averroes |

## Batch 34 – Hidden Orders, Queen State, And Port Footprint Wave
| Old Name / Pattern | New Name | Kind | Subsystem | Confidence | Evidence Summary | Sources | Subagent Evidence |
|---|---|---|---|---|---|---|---|
| sub_443EB0 | UnitStack_TryHide | Function | Unit Hidden State / Orders | High | The helper is the strategic-map hide action: it rejects stacks without enough order tier or with nearby enemies, shows the localized hide-failure string, clears remaining action points on success, sets the hidden byte at `stack + 720`, and logs `ukrycie`. | c, asm, exe | yes: Lorentz |
| sub_4100B0 | UnitStack_GetMaxOrderTier | Function | Unit Hidden State / Orders | Medium | Returns the maximum low-two-bit order tier from slot byte `+12` across occupied slots, with prisoner-bearing stacks forced to tier `3`; that tier gates hiding at `>= 2` and wide reveal behavior at `>= 3`. | c, asm | yes: Lorentz |
| Building_UseGarrisonSlot | UnitSlot_CycleOrderState | Function | Unit Hidden State / Orders | High | The routine only mutates the packed order byte at slot `+12`, cycling the upper two-bit substate and then advancing the lower two-bit tier when the substate wraps. | c, asm | yes: Lorentz |
| sub_4129E0 | UnitStack_CycleAllSlotOrders | Function | Unit Hidden State / Orders | High | Iterates every occupied slot in one stack, applies `UnitSlot_CycleOrderState`, then runs the usual stack refresh notifier; it is a bulk order-cycle helper, not a combat-state mutator. | c, asm | yes: Lorentz |
| UnitStack_RevealAndEngageHiddenEnemies | UnitStack_RevealHiddenEnemiesAndAttackAdjacent | Function | Combat / Hidden Contact | Medium | First reveals hidden enemy stacks inside vision when the acting stack's order tier reaches `3`, then separately auto-attacks adjacent hidden enemy stacks and clears the acting stack's remaining action points. | c, asm | yes: Lorentz |
| Queen_ShowWhimDialog | Queen_ShowWhimDecisionDialog | Function | Queen / Whims | Medium | The dialog displays the whim text, shows the required score versus the current score, and returns a yes/no acceptance decision rather than acting as a passive message window. | c, exe | yes: Pauli |
| Queen_ShowNotification | Queen_ShowMessageDialog | Function | Queen / UI | Medium | The window always draws queen portrait art plus the current whim portrait before presenting the supplied narrative text, so it is a queen-specific message dialog rather than a generic notification primitive. | c, exe | yes: Pauli |
| Queen_DrawMoodPanel | Queen_DrawRelationshipPanel | Function | Queen / UI | Medium | The panel indexes `g_QueenRelationshipStateTexts` by the queen state byte, substitutes `0` for `-1`, and renders the current whim icon only for positive states, so “relationship panel” better fits the displayed content than “mood panel”. | c | yes: Pauli |
| PLAYER_QUEEN_MOOD / PlayerRuntimeState.queen_mood | PLAYER_QUEEN_RELATIONSHIP_STATE / PlayerRuntimeState.queen_relationship_state | Recovered Struct | Queen / Player Runtime State | High | Offset `+1419` does not hold pure mood: the same byte drives no-queen, marriage, departure, relationship, and childbirth branches in `Queen_NewTurn`, and it selects rows in `g_QueenRelationshipStateTexts`. | c | yes: Pauli |
| Port_IsInsideArea | Port_IsInsideFootprint | Function | Port / Footprint | High | Performs an exact `row in [port_row, port_row + 1]` and `column in [port_column, port_column + 1]` bounds check, proving it targets the 2x2 port footprint rather than a broader area. | c, asm | yes: Averroes |
| Port_HasSupplyReady | Port_IsSupplyReady | Function | Port / Runtime State | High | Pure boolean getter for `PortRuntimeState.supply_ready_flag`; the stronger verb matches the actual implementation and calling pattern. | c, asm | yes: Averroes |
| Port_BuildDockingTrack | Port_GenerateApproachTrack | Function | Port / Pathing | Medium | Temporarily clears the 2x2 port footprint, generates a path with `Unit_MoveTrack`, trims path nodes that still lie inside the footprint, and restores the original tiles; it generates an approach path rather than building a persistent track object. | c, asm | yes: Averroes |
| Port_PresentInteger | UI_TrySelectFriendlyStackUnderCursor | Function | UI / Unit Selection | Medium | The map symbol was misleading: the routine converts the current mouse position to a world tile while the frame is active, checks for a friendly loose stack there, rejects the already-selected stack, and writes `g_SelectedUnitIndex`. | c, asm, map | yes: Averroes |
| g_PortSupplySpawnRowOffsets / g_PortSupplySpawnColumnOffsets | PortSpawnOffset[12] | Recovered Struct | Port / Reinforcements | High | `Port_GetSupply` advances the row and column arrays in lockstep for the same candidate tile, and the asm data layout advances by 8 bytes per pair, proving the decompiler split one interleaved `{ row_delta, column_delta }` table into two parallel arrays. | c, asm | yes: Averroes |

## Deferred / Ambiguous
- Resolved in Batch 35: `sub_410100` / `sub_412AC0` became `UnitStack_HasPlagueFlag` / `UnitStack_HasPlague` after the plague-transfer and plague-attrition evidence converged.
- `sub_410260` still depends on the unresolved viewer-exposure naming split around `Map_ClassifyFogOfWarOverlayForPlayer`, so it was not promoted into a factual visibility/reachability rename.
- `Port_GetSupply` clearly spawns a reinforcement stack rather than returning passive data, but the exact public-facing name is still split between the embedded debug string and the higher-level behavior, so it was left unchanged for now.

## Batch 35 – Fog Of War, Plague, And Religious Site Wave
| Old Name / Pattern | New Name | Kind | Subsystem | Confidence | Evidence Summary | Sources | Subagent Evidence |
|---|---|---|---|---|---|---|---|
| sub_40F060 | Map_IsTileVisibleToPlayer | Function | Map / Visibility | High | Directly indexes the per-player revealed-tile bitset at `PlayerRuntimeState + 57` using `13 * row + (column >> 3)` and returns whether the bit for the target tile is set. | c | no |
| sub_40F0C0 | Map_ClassifyFogOfWarOverlayForPlayer | Function | Map / Fog Of War | High | Returns `-1` for directly revealed tiles, `0` for fully black interior tiles, and otherwise classifies neighbor-reveal masks into fog overlay sprite codes later drawn from the dedicated overlay sprite set. | c, asm | yes: Parfit |
| sub_408140 | MapTile_HasOwnUnitStack | Function | Map / Tile Occupancy | High | Used by world-map hover/selection logic as the current-player stack predicate, separate from enemy-visible stack checks. | c | yes: Zeno |
| sub_408200 | MapTile_HasVisibleEnemyUnitStack | Function | Map / Tile Occupancy | High | Matches the enemy-stack branch in the same world-map hover flow and excludes hidden stacks before exposing combat/inspection UI. | c | yes: Zeno |
| sub_4082C0 | MapTile_HasOwnOrVisibleEnemyUnitStack | Function | Map / Tile Occupancy | High | Combined tile-occupancy predicate used by hover, info, and selection code after splitting the own-stack and visible-enemy cases. | c | yes: Zeno |
| sub_4082F0 | MapTile_HasOwnBuilding | Function | Map / Tile Occupancy | High | Building-hover logic branches on this helper before garrison-entry validation, proving it is the current-player building test. | c | yes: Zeno |
| sub_408390 | MapTile_HasEnemyBuilding | Function | Map / Tile Occupancy | High | Complementary occupied-building test used when the selected stack is hovering an enemy building. | c | yes: Zeno |
| sub_408430 | MapTile_HasBuilding | Function | Map / Tile Occupancy | High | General occupied-building predicate shared by building info and movement UI branches. | c | yes: Zeno |
| sub_411AB0 | UnitStack_HasBuilder | Function | Unit Lifecycle / Utility Units | High | Scans the ten embedded slots and returns true only when type id `17` is present; separate digging/building actions gate on this exact helper. | c | no |
| UnitStack_ApplyPeasantCargoAttrition | UnitStack_ApplyPlagueAttritionToPeasantCargo | Function | Unit Turn Update / Plague | High | The helper only damages type-`32` peasant cargo when slot flag bit `0x8` is set, matching plague spread/attrition rather than generic cargo decay. | c | yes: Hilbert |
| sub_410100 | UnitStack_HasPlagueFlag | Function | Unit Turn Update / Plague | High | Duplicate stack predicate that returns true when any occupied slot has state-flag bit `0x8` set. | c | yes: Hilbert |
| sub_412A90 | UnitStack_SetPlagueFlag | Function | Unit Turn Update / Plague | High | Iterates occupied slots and ORs state-flag bit `0x8` into each one when plague spreads to the stack. | c | yes: Hilbert |
| sub_412AC0 | UnitStack_HasPlague | Function | Unit Turn Update / Plague | High | Canonical stack plague predicate wrapped by the higher-level `JestZarazaWOddziale` rules helper and used by building infection transfer. | c | yes: Hilbert |
| sub_43FB10 | MapTile_GetReligiousSiteCategory | Function | Map / Religious Sites | High | Distinguishes shrine, empty shrine, cult place, and empty cult place tile families, and the adjacent text tables plus entry rules agree with those four categories. | c, asm | yes: Zeno |
| sub_443BB0 | MapTile_HasHiddenTreasure | Function | Map / Treasure | High | Checks for the hidden-treasure tile ids and is paired with the localized `Zakopany skarb` strings and `Treasure_DigHere(%d)` action. | c, asm | yes: Zeno |
| sub_443C20 | Treasure_TryDigHere | Function | Map / Treasure | High | Requires a hidden-treasure tile plus a builder-capable stack, logs `Treasure_DigHere(%d)`, consumes the treasure tile, and dispatches the dig outcome. | c, asm | yes: Zeno |
| Move_CanEnterTile | Player_CanEnterReligiousSiteTile | Function | Map / Religious Sites | Medium | The helper allows entry into shrine tiles only when the player religion flag is set and into cult-place tiles only when it is clear, so it is a player-versus-religious-site compatibility gate rather than a generic move validator. | c | yes: Zeno |
| Building_UpdateCrisisState | Building_UpdatePlagueState | Function | Buildings / Plague | High | Only the low three bits at building byte `+435` are updated, the function drains building population while active, and gold income is separately suppressed while those bits are non-zero. | c | yes: Hilbert |
| sub_457DA0 | Building_GetPlagueState | Function | Buildings / Plague | High | Thin accessor that returns `BuildingRecord + 435 & 7`, exactly the plague countdown/state field consumed by UI and turn-update logic. | c | yes: Hilbert |
| sub_457DC0 | UnitStack_HasPlagueByIndex | Function | Unit Turn Update / Plague | High | Index wrapper around `UnitStack_HasPlague` for callers that hold only a stack id. | c | yes: Hilbert |
| dword_5202C4 | g_FogOverlaySpriteSet | Global | Map / Fog Of War | High | The map UI loader allocates and loads this sprite set alongside other world-map sets, and render code indexes it with the fog overlay classifier’s return code before drawing unexplored-edge tiles. | c, asm | yes: Parfit |
| off_511B68 | g_ShrineTexts | Table | Map / Religious Sites | High | `clash95.asm` exposes the localized base text as `Świątynia`, and hover/info logic uses this table when `MapTile_GetReligiousSiteCategory(...) == 1`. | c, asm | yes: Zeno |
| off_511B74 | g_EmptyShrineTexts | Table | Map / Religious Sites | High | Same category family as above, but the asm strings show the empty-site wording (`Pusta świątynia`). | c, asm | yes: Zeno |
| off_511B80 | g_CultPlaceTexts | Table | Map / Religious Sites | High | `clash95.asm` exposes the localized text as `Miejsce kultu`, and the world-map info panel uses it for category `3`. | c, asm | yes: Zeno |
| off_511B8C | g_EmptyCultPlaceTexts | Table | Map / Religious Sites | High | Localized empty-site counterpart for the cult-place family. | c, asm | yes: Zeno |
| off_511BA4 | g_HiddenTreasureTexts | Table | Map / Treasure | High | Localized table headed by `Zakopany skarb`, shown when the hover/info logic detects a hidden-treasure tile. | c, asm | yes: Zeno |
| *(new constants)* categories `1..4` | RELIGIOUS_SITE_CATEGORY_SHRINE / EMPTY_SHRINE / CULT_PLACE / EMPTY_CULT_PLACE | Helper | Map / Religious Sites | High | `MapTile_GetReligiousSiteCategory` is now corroborated by the matching shrine/cult text tables and by `Player_CanEnterReligiousSiteTile`, which only accepts category `1` for one religion flag state and category `3` for the other. | c, asm | yes: Zeno |
| *(new constant)* slot flag bit `0x8` | UNIT_SLOT_FLAG_PLAGUE | Helper | Unit Turn Update / Plague | High | Both plague predicates, plague propagation, and plague attrition target the same slot-flag bit `0x8`, making the plague interpretation stable. | c | yes: Hilbert |
| PlayerRuntimeState + 39 | PlayerRuntimeState.religion_flag | Recovered Struct Field | Player Runtime State / Religion | High | Religious-site entry checks and castle-construction labels branch on this dword to choose shrine-vs-cult / Christian-vs-pagan behavior. | c | yes: Zeno |
| PlayerRuntimeState + 57 | PlayerRuntimeState.revealed_tiles_bitset[1300] | Recovered Struct Field | Player Runtime State / Visibility | High | Visibility stamping and lookup helpers address this exact 1300-byte gap as a 13-bytes-per-row bitset, and fog overlay classification consumes the same data. | c | yes: Parfit |
| BuildingRecord + 435 | BuildingRecord.plague_state | Recovered Struct Field | Buildings / Plague | High | The low three bits at this byte control plague countdown, block income, trigger plague messages, and are seeded when a plagued stack enters the building. | c | yes: Hilbert |

## Deferred / Ambiguous
- `sub_410260` still looks like a “current or reachable queued path step is drawable for the viewed player” predicate, but the shortest non-misleading public name is not locked down enough yet.
- `Map_ClassifyFogOfWarOverlayForPlayer` is stable at the behavior level, but the exact orientation mapping of overlay codes `1..12` remains intentionally undocumented until the sprite sheet is fully cross-labeled.
- `PlayerRuntimeState.religion_flag` is semantically stable as a two-way religion/alignment switch, but the code alone does not justify a stronger lore-facing enum name than “religion flag.”
- Hidden treasure tile ids `752` and `755` are both provably treasure-bearing, but their exact designer-facing distinction is still deferred.

## Batch 36 – Rules Fact And Upgrade Wave
| Old Name / Pattern | New Name | Kind | Subsystem | Confidence | Evidence Summary | Sources | Subagent Evidence |
|---|---|---|---|---|---|---|---|
| Unit_MoveTrack_1 | Unit_MoveTrackNearTile | Function | Movement / Path Planning | High | The original debug string survives in the binary as `Unit_MoveTrackNear(%d,%d,%d)`, and the implementation temporarily clears the destination occupancy, calls the normal track planner, then trims the final step so the path stops adjacent to the target tile instead of entering it. | c, asm, exe | yes: Lorentz |
| sub_41DB20 | Rules_RebuildCastleSiteFacts | Function | Rules / Castle Sites | High | Full-map rebuild pass that scans every tile and re-logs tiles `707` and `711` via the `(zamek_place %d %d)` rules fact emitter. | c | no |
| sub_41F850 | Building_CanStartUpgrade | Function | Buildings / Upgrades | High | Pure predicate that allows an upgrade only when the current stage is below both the hard cap and the building tier cap and no upgrade timer is already running. | c | no |
| sub_41F890 | Building_TryStartUpgrade | Function | Buildings / Upgrades | High | Calls `Building_CanStartUpgrade`, checks the per-stage resource cost table against the building-local stored pool, seeds the upgrade timer, and deducts the cost on success. | c | no |
| sub_43F9B0 | Rules_RebuildTempleFacts | Function | Rules / Religious Sites | High | Full-map rebuild pass that re-logs every non-zero `MapTile_GetReligiousSiteCategory` tile through the `(swiatynia %d %d)` fact logger. | c | no |
| sub_443B60 | Rules_RebuildTreasureFacts | Function | Rules / Treasure | High | Full-map rebuild pass that re-logs every `MapTile_HasHiddenTreasure` tile through the `(skarb %d %d)` fact logger. | c | no |
| sub_455200 | Rules_LogTempleFact | Function | Rules / Religious Sites | High | Emits the exact rules payload `(swiatynia row col)`, making it the temple-fact logger rather than a generic map note helper. | c | no |
| sub_455230 | Rules_LogTreasureFact | Function | Rules / Treasure | High | Emits the exact rules payload `(skarb row col)` for hidden-treasure tiles. | c | no |
| sub_455260 | Rules_RetractTreasureFact | Function | Rules / Treasure | High | Emits the exact rules payload `(kasuj skarb row col)` to retract a previously logged treasure fact after digging. | c | no |
| sub_455290 | Rules_RetractTempleFact | Function | Rules / Religious Sites | High | Emits the exact rules payload `(kasuj swiatynie row col)` after temple-state changes consume or clear the prior fact. | c | no |
| sub_455C90 | Rules_LogCastleSiteFact | Function | Rules / Castle Sites | High | Emits the exact rules payload `(zamek_place row col)`, which is used by the castle-site rebuild scan. | c | no |
| sub_457E00 | Unit_ExecuteQueuedPathAndCheckFinished | Function | Movement / Path Execution | High | Thin wrapper that executes one queued-path step batch and immediately tests whether the embedded `QueuedPathBuffer.waypoint_count` dropped to zero. | c | yes: Lorentz |
| sub_455450 | Building_TryStartUpgradeByIndex | Function | Buildings / Upgrades | High | Thin index wrapper that forwards `UNIT_RECORD(buildingIndex)` into `Building_TryStartUpgrade`, preserving the same upgrade-start side effects for callers that only hold a building id. | c | no |
| sub_455650 | Building_CanStartUpgradeByIndex | Function | Buildings / Upgrades | High | Thin index wrapper that forwards `UNIT_RECORD(buildingIndex)` into `Building_CanStartUpgrade` for id-based UI or rules callers. | c | no |

## Deferred / Ambiguous
- `sub_410260` still looks like a “current or reachable queued path step is drawable for the viewed player” predicate, but the shortest non-misleading public name is not locked down enough yet.
- `sub_41D980` / `sub_41DA50` clearly belong to the castle-site placement cluster, but their mode parameter inverts or narrows the terrain predicate in a way that is still too easy to overstate.
- `sub_41EF10` / `sub_41EF80` / `sub_41F020` still line up with barracks / hospital / smithy-style castle add-on builders by debug-string evidence, but the add-on-bit mapping is not settled cleanly enough for a safe public rename.
- `sub_457E50` is only a wrapper around `Unit_AddToGroup(..., 0)`; it likely wants a group-merge wrapper name, but the narrow behavioral distinction is not yet corroborated well enough.

## Batch 37 – Castle Foundations And Addons Wave
| Old Name / Pattern | New Name | Kind | Subsystem | Confidence | Evidence Summary | Sources | Subagent Evidence |
|---|---|---|---|---|---|---|---|
| sub_410260 | UnitStack_CanReachQueuedPathTileWithFogOverlay | Function | Movement / Fog Of War | Medium | Checks the current tile plus all queued-path waypoints whose cumulative cost does not exceed the stack's current action points, and returns true if any such tile yields a non-zero `Map_ClassifyFogOfWarOverlayForPlayer` result. The implementation is stable, but the name stays implementation-shaped to avoid overstating it as a pure visibility predicate. | c, asm | yes: Lorentz |
| sub_41D980 | MapTile_IsCastleFoundationTile | Function | Map / Castle Sites | High | The raw terrain test accepts tile ids `707..714`, hover/info uses the matching text table headed by `Fundamenty`, and building/trap placement logic treats the same family as a special blocked castle-foundation terrain cluster. | c, asm | yes: Hilbert |
| sub_41DA50 | MapTile_IsCastleFoundationAnchorTile | Function | Map / Castle Sites | High | Narrower castle-foundation subset predicate that accepts only tile ids `707` and `711`; map rebuild code uses this exact subset when caching and logging castle-site anchors. | c, asm | yes: Hilbert |
| sub_41DBA0 | BuildCursor_IsPlacementValid | Function | UI / Build Placement | Medium | Build-preview validator used by the placement cursor. It checks mode-specific range from the acting stack, terrain blockers, occupancy, traps, religious sites, hidden treasure, and, in one mode, excludes castle foundation tiles. | c | yes: Hilbert |
| sub_41EF10 | Building_BuildBarracks | Function | Buildings / Castle Addons | High | The debug string is literally `Building_BuildBarracks()`, the castle UI wrapper uses `budowa\\baraki`, and `Rules_AssertCastleFact` maps bit `0x02` to `koszary` for the same add-on. | c, asm, exe | yes: Zeno |
| sub_41EF80 | Building_BuildHospital | Function | Buildings / Castle Addons | High | The debug string is literally `Building_BuildHospital()`, the host command is `KupSzpital`, and `Rules_AssertCastleFact` maps bit `0x01` to `szpital`. | c, asm | yes: Zeno |
| sub_41F020 | Building_BuildSmiths | Function | Buildings / Castle Addons | High | The original debug string is literally `Building_BuildSmiths()`, the castle UI wrapper uses `budowa\\kuznia`, and `Rules_AssertCastleFact` maps bit `0x10` to `kuznia`, so the binary's own internal wording is the safest recovered symbol. | c, asm | yes: Zeno |
| sub_420CD0 | Castle_BuildSchoolWithAnimation | Function | Castle UI / Addon Construction | High | Calls `Building_BuildSchool`, verifies bit `0x08`, clones the building record, clears the just-built bit on the clone, and plays the `budowa\\szkola` construction resource through the castle-build animation path. | c, asm | yes: Zeno |
| sub_420D50 | Castle_BuildWorkshopWithAnimation | Function | Castle UI / Addon Construction | High | Same castle-build animation wrapper pattern for the workshop add-on, keyed by bit `0x04` and the `budowa\\warsztat` resource. | c, asm | yes: Zeno |
| sub_420DD0 | Castle_BuildBarracksWithAnimation | Function | Castle UI / Addon Construction | High | Same wrapper pattern for the barracks add-on, keyed by bit `0x02` and the `budowa\\baraki` resource. | c, asm | yes: Zeno |
| sub_420E50 | Castle_BuildHospitalWithAnimation | Function | Castle UI / Addon Construction | High | Same wrapper pattern for the hospital add-on, keyed by bit `0x01` and a religion-specific hospital construction resource. | c, asm | yes: Zeno |
| sub_420EF0 | Castle_BuildSmithsWithAnimation | Function | Castle UI / Addon Construction | High | Same wrapper pattern for the smiths add-on, keyed by bit `0x10` and the `budowa\\kuznia` construction resource. | c, asm | yes: Zeno |
| sub_455490 | Building_BuildSchoolByIndex | Function | Buildings / Castle Addons | High | Thin building-index wrapper over `Building_BuildSchool`. | c | yes: Zeno |
| sub_4554B0 | Building_BuildSmithsByIndex | Function | Buildings / Castle Addons | High | Thin building-index wrapper over `Building_BuildSmiths`. | c | yes: Zeno |
| sub_455530 | Building_BuildBarracksByIndex | Function | Buildings / Castle Addons | High | Thin building-index wrapper over `Building_BuildBarracks`. | c | yes: Zeno |
| sub_455550 | Building_BuildHospitalByIndex | Function | Buildings / Castle Addons | High | Thin building-index wrapper over `Building_BuildHospital`. | c | yes: Zeno |
| sub_4555C0 | Building_BuildWorkshopByIndex | Function | Buildings / Castle Addons | High | Thin building-index wrapper over `Building_BuildWorkshop`. | c | yes: Zeno |
| sub_455D50 | Rules_LogCastleSchemeFact | Function | Rules / Castle Sites | High | Emits the exact rules payload `(schemat %d %d %d)` and is called immediately after castle construction logging, making it the castle-scheme fact logger. | c | no |
| sub_455DC0 | Rules_LogCastleUnderConstructionFact | Function | Rules / Castle Sites | High | Emits the exact rules payload `(zamek w budowie %d)` when a new castle site is claimed for construction. | c, asm | no |
| sub_455DF0 | Rules_LogCastleBuiltFactAndScheme | Function | Rules / Castle Sites | High | Emits `(zbudowano zamek %d)` and then immediately logs a scheme fact through `Rules_LogCastleSchemeFact`, so the paired behavior is stable. | c | no |
| sub_455E20 | Rules_LogAssignedCastleFact | Function | Rules / Castle Sites | High | Emits the exact rules payload `(wyznaczony zamek %d %d)`. | c | no |
| sub_455E50 | Rules_LogAssignedPlayerFact | Function | Rules / Castle Sites | High | Emits the exact rules payload `(wyznaczony gracz %d %d)`. | c | no |
| sub_455E80 | Building_SelectedAddonMatchesTypeByIndex | Function | Buildings / Production Licences | High | Reads `BuildingRecord.selected_addon_slot_index` and compares the indexed `addon_type_ids` entry against the requested type id. | c | no |
| sub_455F20 | Building_HasGarrisonUnitTypeByIndex | Function | Buildings / Garrison | High | Scans the 12 embedded 31-byte garrison entries in `BuildingRecord` and returns true when any slot holds the requested unit type id. | c | no |
| sub_455FF0 | Map_RebuildCastleSiteAnchorCache | Function | Map / Castle Sites | Medium | Rebuilds the global castle-site anchor coordinate arrays by scanning the full map for `MapTile_IsCastleFoundationAnchorTile`; the exact consumer cluster is rules/AI-facing, but the cache behavior itself is explicit. | c, asm | yes: Hilbert |
| sub_457E50 | Script_UnitAddToGroup | Function | Script / Unit Grouping | High | Pure script-layer wrapper around `Unit_AddToGroup(a1, a2, 0, ...)`, sitting inside the script dispatch cluster and adding no distinct gameplay logic beyond the hardcoded mode `0`. | c, asm | yes: Lorentz |
| off_511B98 | g_CastleFoundationTexts | Table | Map / Castle Sites | High | Hover/info code selects this table when `MapTile_IsCastleFoundationTile` is true, and the asm data headed by `Fundamenty` confirms it is the castle-foundation text family. | c, asm | yes: Hilbert |
| dword_544570 | g_CastleSiteAnchorRows | Global | Map / Castle Sites | Medium | Filled exclusively by `Map_RebuildCastleSiteAnchorCache` with the row component of each `707` / `711` anchor tile. | c, asm | yes: Hilbert |
| dword_544574 | g_CastleSiteAnchorColumns | Global | Map / Castle Sites | Medium | Filled exclusively by `Map_RebuildCastleSiteAnchorCache` with the column component of each `707` / `711` anchor tile. | c, asm | yes: Hilbert |
| BuildingRecord + 416 / building_flags | BuildingRecord.castle_addon_flags | Recovered Struct Field | Buildings / Castle Addons | High | Castle add-on builders and `Rules_AssertCastleFact` converge on the exact bit mapping: `0x01` hospital, `0x02` barracks, `0x04` workshop, `0x08` school, `0x10` smiths. | c, asm | yes: Zeno |

## Deferred / Ambiguous
- `MapTile_IsCastleFoundationTile` is stable for the `707..714` family and `MapTile_IsCastleFoundationAnchorTile` is stable for `707` / `711`, but the exact semantic split among the non-anchor variants remains unresolved.
- `sub_455D90` logs `(zdobyty %d %d %d)`, but this pass did not pin down whether the fact is castle-specific, faction-specific, or a broader conquest record.

## Batch 37 – Trap Facts And Castle Fact Handles
| Old Name / Pattern | New Name | Kind | Subsystem | Confidence | Evidence Summary | Sources | Subagent Evidence |
|---|---|---|---|---|---|---|---|
| sub_4551A0 | Rules_LogTrapFact | Function | Rules / Traps | High | Emits the exact rules payload `(pulapka row col)` and is called when map-load reconstruction finds armed trap tiles. | c, asm | no |
| sub_4551D0 | Rules_RetractTrapFact | Function | Rules / Traps | High | Emits the exact rules payload `(kasuj pulapke row col)` immediately after a trap fires and the owner mask is cleared from the tile. | c, asm | no |
| BuildingRecord + 463 | BuildingRecord.castle_fact_id | Recovered Struct Field | Buildings / Castle Facts | High | `Rules_AssertCastleFact` stores its returned fact handle at `+463`, `Rules_RetractCastleFact` retracts and zeroes the same dword, and `Building_OnGarrisonChange` plus `Unit_CaptureBuilding` update its `moc` and `gracz` attributes. | c, exe | yes: Bacon |

## Deferred / Ambiguous
- `sub_455150` updates the `gracz` attribute on `BuildingRecord.castle_fact_id`, but this pass stopped short of renaming it until the non-castle call envelope is ruled out more rigorously.

## Batch 40 – Building Rules Host Stat Wrappers
| Old Name / Pattern | New Name | Kind | Subsystem | Confidence | Evidence Summary | Sources | Subagent Evidence |
|---|---|---|---|---|---|---|---|
| sub_455470 | Building_GetTaxPressureByIndex | Function | Buildings / Rules Host Stats | High | The `Podatek` rules host stub at `loc_4568F6` dispatches directly to this getter, which returns `BuildingRecord + 436 & 0x3F`, the same tax-pressure value consumed by `Building_GetTaxPressureTier`. | c, asm | no |
| sub_4554D0 | Building_GetWallStrengthByIndex | Function | Buildings / Rules Host Stats | High | The `SilaMurow` rules host stub at `loc_456A10` dispatches directly to this getter, which returns byte `+421`, the per-building wall-upgrade strength/stage field. | c, asm | no |
| sub_4554F0 | Building_GetMoneyByIndex | Function | Buildings / Rules Host Stats | High | The `Pieniadze` rules host stub at `loc_4569DA` dispatches directly to this getter, which returns the building-local stored money pool at `+438`. | c, asm | no |
| sub_455510 | Building_GetCastleStrengthByIndex | Function | Buildings / Rules Host Stats | High | The `SilaZamku` rules host stub at `loc_456A46` dispatches directly to this wrapper around `Building_GetTotalValue`, exposing the building's aggregate strength/value to the rules layer. | c, asm | no |
| sub_455580 | Building_GetTechLevelByIndex | Function | Buildings / Rules Host Stats | High | The `PoziomTech` rules host stub at `loc_456BBB` dispatches directly to this getter, which returns the low three bits at `+444`. | c, asm | no |
| sub_4555A0 | Building_GetTypeByIndex | Function | Buildings / Rules Host Stats | High | The `TypBudowli` rules host stub at `loc_456BF1` dispatches directly to this getter, which returns the raw building type byte at offset `+4`. | c, asm | no |
| sub_4555E0 | Building_GetSatisfactionByIndex | Function | Buildings / Rules Host Stats | High | The `Zadowolenie` rules host stub at `loc_456C93` dispatches directly to this getter, which returns byte `+434`, the clamped satisfaction value used by population growth. | c, asm | no |
| sub_455600 | Building_GetPeasantCountByIndex | Function | Buildings / Rules Host Stats | High | The `IloscChlopow` rules host stub at `loc_456CFF` dispatches directly to this getter, which returns the low 12 bits at `+430`, the same population value updated by `Building_UpdatePopulationGrowth`. | c, asm | no |
| sub_455670 | Building_GetGarrisonCountByIndex | Function | Buildings / Rules Host Stats | High | The `IloscOddzialow` rules host stub at `loc_456E7F` dispatches directly to this wrapper around `Building_CountGarrison`. | c, asm | no |
| sub_455690 | Building_IsGarrisonFullByIndex | Function | Buildings / Rules Host Stats | High | The `MaxIloscOddzialow` rules host stub at `loc_456F87` dispatches directly to this predicate, which returns true exactly when the garrison count reached the 12-slot limit. | c, asm | no |
| BuildingRecord + 421 | BuildingRecord.wall_strength | Recovered Struct Field | Buildings / Walls | High | The `SilaMurow` rules host getter returns byte `+421`, and wall-upgrade logic reads and advances the same byte as the staged wall-strength field. | c, asm | no |
| BuildingRecord + 430 | BuildingRecord.peasant_count | Recovered Struct Field | Buildings / Population | High | The `IloscChlopow` rules host getter returns the low 12 bits at `+430`, and `Building_UpdatePopulationGrowth` mutates the same value as the building's peasant population. | c, asm | no |
| BuildingRecord + 434 | BuildingRecord.satisfaction | Recovered Struct Field | Buildings / Population | High | The `Zadowolenie` rules host getter returns byte `+434`, and `Building_UpdatePopulationGrowth` clamps it between `0` and `100` as a settlement satisfaction/happiness meter. | c, asm | no |
| BuildingRecord + 438 / stored_resource_pool | BuildingRecord.stored_money | Recovered Struct Field | Buildings / Economy | High | The `Pieniadze` rules host getter returns dword `+438`, while add-on purchase and income logic spend and replenish the same pool as money. | c, asm | no |
| BuildingRecord + 444 / level_bits | BuildingRecord.tech_level_bits | Recovered Struct Field | Buildings / Technology | High | The `PoziomTech` rules host getter returns the low three bits at `+444`, and upgrade/add-on requirement checks treat those bits as the building's tech level cap. | c, asm | no |

## Batch 41 – Building Production And Licence Wrappers
| Old Name / Pattern | New Name | Kind | Subsystem | Confidence | Evidence Summary | Sources | Subagent Evidence |
|---|---|---|---|---|---|---|---|
| sub_455620 | Building_HasProductionByIndex | Function | Buildings / Production Wrappers | High | The `IsProduction` rules host stub at `loc_456CC9` dispatches directly to this predicate, which returns true when the selected production/licence slot byte at `+414` is not `-1`. | c, asm | no |
| sub_4556C0 | Building_RepairUnitByIndex | Function | Buildings / Production Wrappers | High | Thin building-id wrapper that forwards directly into `Building_RepairUnit`. | c | no |
| sub_4556E0 | Building_TrainUnitByIndex | Function | Buildings / Production Wrappers | High | Thin building-id wrapper that forwards directly into `Building_TrainUnit`. | c | no |
| sub_455700 | Building_SetUnitProductionByIndex | Function | Buildings / Production Wrappers | High | Thin building-id wrapper that forwards directly into `Building_SetUnitProduction`. | c | no |
| sub_455720 | Building_RemoveUnitLicenceByIndex | Function | Buildings / Production Wrappers | High | Thin building-id wrapper that forwards directly into `Building_RemoveUnitLicence`. | c | no |
| sub_4557C0 | Building_HasUnitLicenceByIndex | Function | Buildings / Licence Wrappers | High | The `IsLicence` rules host stub at `loc_45692C` dispatches directly to this wrapper around `Building_HasUnitLicence`, exposing whether the castle currently owns the requested production licence. | c, asm | no |
| sub_4557E0 | Building_BuyUnitLicenceByIndex | Function | Buildings / Licence Wrappers | High | The `BuyLicence` rules host stub at `loc_456B0D` dispatches directly to this building-id wrapper over the licence-purchase path. | c, asm | no |
| sub_455800 | Building_CanBuyUnitLicenceByIndex | Function | Buildings / Licence Wrappers | High | The `CanBuyLicence` rules host stub at `loc_456D8F` dispatches directly to this predicate, which forwards into `Building_IsUnitLicenceEligible` for the requested licence type. | c, asm | no |

## Batch 38 – Castle Lifecycle Logging Wave
| Old Name / Pattern | New Name | Kind | Subsystem | Confidence | Evidence Summary | Sources | Subagent Evidence |
|---|---|---|---|---|---|---|---|
| sub_41D930 | Building_LogBuiltCastleFacts | Function | Buildings / Castle Lifecycle | High | Pure wrapper that derives the building id from the record's footprint tile and forwards it twice into `Rules_LogCastleBuiltFactAndScheme`; scenario/setup paths call it after `Building_New` to seed rules facts for pre-existing castles. | c | no |
| sub_41E050 | Building_Stop | Function | Buildings / Castle Lifecycle | High | Embedded debug string is `Building_Stop(0x%08x)`, and the implementation ejects all occupied garrison slots, clears the active-building state word, and retracts the castle fact. | c, asm | no |
| sub_41F900 | Unit_CaptureBuilding | Function | Unit Lifecycle / Building Capture | High | Embedded debug string is `Unit_CaptureBuilding(%d,%d)`, and the implementation transfers building ownership to the acting stack's player, updates building tech tier, logs a capture fact, moves the unit inside, handles special prisoner slots, and refreshes nearby tiles. | c, asm | no |
| sub_455CF0 | Rules_LogNewCastleFact | Function | Rules / Castle Lifecycle | High | Literal rules logger for the payload `(powstal-nowy-zamek %d %d)`, called from `Building_FinishConstruction` after `Rules_LogCastleBuiltFactAndScheme`. | c, asm | no |
| sub_455D20 | Rules_LogCastleDestroyedFact | Function | Rules / Castle Lifecycle | High | Literal rules logger for `(zginal-zamek %d %d)`, reached from `Rules_RetractCastleFact` when a castle/building record is removed. | c, asm | no |
| sub_455D90 | Rules_LogBuildingCapturedFact | Function | Rules / Building Capture | Medium | Literal rules logger for `(zdobyty %d %d %d)` called only from `Unit_CaptureBuilding`; the three arguments are the capturing player id, building id, and current turn number. The capture path is generic building capture, not castle-only capture. | c, asm | no |

## Batch 39 – Castle Transfer And Addon Field Wave
| Old Name / Pattern | New Name | Kind | Subsystem | Confidence | Evidence Summary | Sources | Subagent Evidence |
|---|---|---|---|---|---|---|---|
| sub_4554B0 | Building_BuildSmithsByIndex | Function | Buildings / Castle Addons | High | Thin id-based wrapper that forwards directly into `Building_BuildSmiths`, preserving the same add-on build side effects for callers that only hold a building id. | c | no |
| sub_455530 | Building_BuildBarracksByIndex | Function | Buildings / Castle Addons | High | Thin id-based wrapper that forwards directly into `Building_BuildBarracks`. | c | no |
| sub_455550 | Building_BuildHospitalByIndex | Function | Buildings / Castle Addons | High | Thin id-based wrapper that forwards directly into `Building_BuildHospital`. | c | no |
| sub_455CC0 | Rules_LogBuildingTransferFact | Function | Rules / Building Transfer | Medium | Literal rules logger for `(budowanie transfer 0 0 %d %d %d)`, reached from `Building_Transfer` after army-fact relinking in the no-human-controller branch. The name stays implementation-shaped to avoid over-claiming the designer-facing semantics of the three payload numbers. | c, asm | no |
| sub_455D50 | Rules_LogCastleSchemeFact | Function | Rules / Castle Lifecycle | High | Literal rules logger for `(schemat %d %d %d)` with a randomized scheme value `1..3`; `Rules_LogCastleBuiltFactAndScheme` delegates to it immediately after logging castle construction. | c, asm | no |
| sub_455DC0 | Rules_LogCastleUnderConstructionFact | Function | Rules / Castle Lifecycle | High | Literal rules logger for `(zamek w budowie %d)`, emitted immediately after a castle site is claimed for construction. | c, asm | no |
| sub_455DF0 | Rules_LogCastleBuiltFactAndScheme | Function | Rules / Castle Lifecycle | High | Logs `(zbudowano zamek %d)` and then immediately logs a scheme fact through `Rules_LogCastleSchemeFact`, so the paired behavior is explicit. | c, asm | no |
| sub_455E20 | Rules_LogAssignedCastleFact | Function | Rules / Castle Lifecycle | High | Literal rules logger for `(wyznaczony zamek %d %d)`. | c, asm | no |
| sub_455E50 | Rules_LogAssignedPlayerFact | Function | Rules / Castle Lifecycle | High | Literal rules logger for `(wyznaczony gracz %d %d)`. | c, asm | no |
| sub_455FF0 | Map_RebuildCastleSiteAnchorCache | Function | Map / Castle Sites | Medium | Rebuilds the global castle-site anchor coordinate arrays by scanning the map for `MapTile_IsCastleFoundationAnchorTile`; consumers are rules/AI-facing, but the cache behavior itself is explicit. | c, asm | no |
| dword_544570 | g_CastleSiteAnchorRows | Global | Map / Castle Sites | Medium | Filled exclusively by `Map_RebuildCastleSiteAnchorCache` with the row component of each castle-foundation anchor tile. | c, asm | no |
| dword_544574 | g_CastleSiteAnchorColumns | Global | Map / Castle Sites | Medium | Filled exclusively by `Map_RebuildCastleSiteAnchorCache` with the column component of each castle-foundation anchor tile. | c, asm | no |
| BuildingRecord + 416 / building_flags | BuildingRecord.castle_addon_flags | Recovered Struct Field | Buildings / Castle Addons | High | Castle add-on builders and `Rules_AssertCastleFact` converge on the exact bit mapping: `0x01` hospital, `0x02` barracks, `0x04` workshop, `0x08` school, `0x10` smiths. | c, asm | no |

## Deferred / Ambiguous
- No additional medium/high-confidence candidates remained in the castle transfer and castle-fact neighborhood after this batch. The next unresolved local target is the exact gameplay meaning of the `sub_455F60` distance/cache predicate and its paired `dword_544578` / `dword_54457C` arrays.

## Batch 39 – Castle Foundations And Addon Flags Wave
| Old Name / Pattern | New Name | Kind | Subsystem | Confidence | Evidence Summary | Sources | Subagent Evidence |
|---|---|---|---|---|---|---|---|
| sub_41D980 | MapTile_IsCastleFoundationTile | Function | Map / Castle Sites | High | The predicate accepts tile ids `707..714`, the hover/info path selects the `Fundamenty` text table only when it passes, and castle placement logic treats the same family as special blocked terrain. | c, asm, map | no |
| sub_41DA50 | MapTile_IsCastleFoundationAnchorTile | Function | Map / Castle Sites | High | Narrower subset predicate that accepts only tile ids `707` and `711`; the anchor-cache rebuild records exactly this subset as castle-site anchors. | c | no |
| sub_41DBA0 | BuildCursor_IsPlacementValid | Function | UI / Building Placement | Medium | Build-preview validation enforces cursor radius, blocked tiles, occupant checks, trap/religious-site/treasure exclusions, and extra castle-foundation restrictions before construction is allowed. | c | no |
| sub_41EF10 | Building_BuildBarracks | Function | Buildings / Castle Addons | High | The debug string is `Building_BuildBarracks() - %d,%d`, and the helper spends resources, sets add-on bit `0x02`, and arms the construction-in-progress flag. | c, asm | no |
| sub_41EF80 | Building_BuildHospital | Function | Buildings / Castle Addons | High | The debug string is `Building_BuildHospital() - %d,%d`, and the helper spends resources, sets add-on bit `0x01`, and arms the construction-in-progress flag. | c, asm | no |
| sub_41F020 | Building_BuildSmiths | Function | Buildings / Castle Addons | High | The debug string is `Building_BuildSmiths() - %d,%d`, and the helper spends resources, sets add-on bit `0x10`, and arms the construction-in-progress flag. | c, asm | no |
| sub_4554B0 | Building_BuildSmithsByIndex | Function | Buildings / Castle Addons | High | Thin building-id wrapper that forwards directly into `Building_BuildSmiths`. | c | no |
| sub_455530 | Building_BuildBarracksByIndex | Function | Buildings / Castle Addons | High | Thin building-id wrapper that forwards directly into `Building_BuildBarracks`. | c | no |
| sub_455550 | Building_BuildHospitalByIndex | Function | Buildings / Castle Addons | High | Thin building-id wrapper that forwards directly into `Building_BuildHospital`. | c | no |
| sub_455CC0 | Rules_LogBuildingTransferFact | Function | Rules / Buildings | High | Emits the exact rules payload `(budowanie transfer 0 0 %d %d %d)` from the building-transfer path after a transfer stack is linked into the rules system. | c, asm | no |
| sub_455D50 | Rules_LogCastleSchemeFact | Function | Rules / Castle Lifecycle | High | Emits the exact rules payload `(schemat %d %d %d)` after `Rng_RandRange(1, 3)`, and the castle-built logger calls it immediately after the built fact. | c, asm | no |
| sub_455DC0 | Rules_LogCastleUnderConstructionFact | Function | Rules / Castle Lifecycle | High | Emits the exact rules payload `(zamek w budowie %d)` directly after `Rules_AssertCastleFact` during `Building_New`. | c, asm | no |
| sub_455DF0 | Rules_LogCastleBuiltFactAndScheme | Function | Rules / Castle Lifecycle | High | Emits `(zbudowano zamek %d)` and then immediately calls `Rules_LogCastleSchemeFact`, making the paired behavior explicit. | c, asm | no |
| sub_455E20 | Rules_LogAssignedCastleFact | Function | Rules / Castle Lifecycle | High | Emits the exact rules payload `(wyznaczony zamek %d %d)`. | c, asm | no |
| sub_455E50 | Rules_LogAssignedPlayerFact | Function | Rules / Castle Lifecycle | High | Emits the exact rules payload `(wyznaczony gracz %d %d)`. | c, asm | no |
| sub_455FF0 | Map_RebuildCastleSiteAnchorCache | Function | Map / Castle Sites | Medium | Rebuilds the cached row/column arrays by scanning the full map and recording every `MapTile_IsCastleFoundationAnchorTile` tile. | c | no |
| off_511B98 | g_CastleFoundationTexts | Table | Map / Castle Sites | High | The hover/info path selects this multilingual table only for castle-foundation terrain, and the asm/map data label it with `Fundamenty`. | c, asm, map | no |
| dword_544570 | g_CastleSiteAnchorRows | Global | Map / Castle Sites | Medium | Filled only by `Map_RebuildCastleSiteAnchorCache` with the row component of each cached castle-site anchor tile. | c | no |
| dword_544574 | g_CastleSiteAnchorColumns | Global | Map / Castle Sites | Medium | Filled only by `Map_RebuildCastleSiteAnchorCache` with the column component of each cached castle-site anchor tile. | c | no |
| BuildingRecord + 416 / building_flags | BuildingRecord.castle_addon_flags | Recovered Struct Field | Buildings / Castle Addons | High | The castle add-on builders and `Rules_AssertCastleFact` converge on the exact bit mapping: `0x01` hospital, `0x02` barracks, `0x04` workshop, `0x08` school, `0x10` smiths. | c, asm | no |

## Deferred / Ambiguous
- `sub_410260` still looks like a “current or reachable queued path step is drawable for the viewed player” predicate, but the shortest non-misleading public name is not locked down enough yet.
- Non-anchor castle-foundation tile ids `708, 709, 710, 712, 713, 714` still behave as foundation variants, but their exact piece/orientation mapping remains deferred.
- `sub_457E50` is only a wrapper around `Unit_AddToGroup(..., 0)`; it likely wants a group-merge wrapper name, but the narrow behavioral distinction is not yet corroborated well enough.

## Batch 42 – Special Personage And Tax State Corrections
| Old Name / Pattern | New Name | Kind | Subsystem | Confidence | Evidence Summary | Sources | Subagent Evidence |
|---|---|---|---|---|---|---|---|
| UnitStack_HasPrisonerUnits | UnitStack_HasSpecialPersonageUnits | Function | Unit Lifecycle / Special Entries | High | The predicate only checks for unit types `33/34`, and `Queen_NewTurn` creates the same ids through the special-garrison helper, so `prisoner` overstates one acquisition path instead of the underlying unit family. | c, asm, exe | yes: Averroes |
| Building_CountPrisonerGarrisonEntries | Building_CountSpecialPersonageGarrisonEntries | Function | Buildings / Special Entries | High | Counts only types `33/34` in the building garrison array. Those ids are now corroborated as the broader special-personage family reused by both prisoner and queen-birth flows. | c, exe | yes: Averroes |
| Building_HasPrisonerGarrisonEntries | Building_HasSpecialPersonageGarrisonEntries | Function | Buildings / Special Entries | High | Boolean sibling of the same `33/34` count helper; the old prisoner wording was too narrow for the recovered unit-family semantics. | c, exe | yes: Averroes |
| Building_CreateSpecialGarrisonUnit | Building_CreateSpecialPersonageGarrisonUnit | Function | Buildings / Special Entries | High | Called from prisoner release and from `Queen_NewTurn` childbirth, always seeding one of the type `33/34` special-personage units into a building garrison. | c, asm, exe | yes: Averroes |
| sub_43EAC0 | Building_ClearGarrisonTrainingTimer | Function | Buildings / Garrison Orders | High | Clears bits `0..2` in `BuildingRecord + 390 + slot`, the same low timer bits written by `Building_TrainUnit`; call sites use it to cancel or reset training state. | c | no |
| sub_43EB40 | Building_ClearGarrisonRepairTimer | Function | Buildings / Garrison Orders | High | Clears bits `3..5` in `BuildingRecord + 390 + slot`, matching the repair timer bits written by `Building_RepairUnit`; call sites use it to cancel or reset repair state. | c | no |
| Building_GetTaxPressureTier | Building_GetTaxBurdenTier | Function | Buildings / Settlement Economy | High | The helper derives a 0..3 burden tier from population-band-specific thresholds and the raw `Podatek` value at `+436`; the tier is then consumed by population-growth logic rather than serving as the raw tax field itself. | c, asm | no |
| Building_GetTaxPressureByIndex | Building_GetTaxRateByIndex | Function | Buildings / Rules Host Stats | High | The `Podatek` rules host getter returns the raw low six bits at `BuildingRecord + 436`, which are adjusted by `ZmienPodatek` and multiplied directly into `Building_CollectGoldIncome`, proving it is the tax rate/value rather than a derived pressure tier. | c, asm | no |
| sub_455740 | Building_AdjustTaxRateByIndex | Function | Buildings / Rules Host Stats | High | The `ZmienPodatek` host path adds a float delta to the same low-six-bit `Podatek` field at `+436` while preserving the packed upper bits, so this is the tax-rate mutator for a building id. | c, asm | no |
| PLAYER_QUEEN_WHIM / PlayerRuntimeState + 1420 | PLAYER_QUEEN_PORTRAIT_INDEX / PlayerRuntimeState.queen_portrait_index | Global / Recovered Struct Field | Queen / Player Runtime State | High | The byte is set to `Rng_RandRange(0, 8)` during the marriage-proposal path and then used as the sprite index in the proposal, whim, message, and relationship-panel UI; it is not the whim/request id. | c, asm, exe | yes: Averroes |
| PLAYER_QUEEN_NEXT_REVIEW / PlayerRuntimeState + 1421 | PLAYER_QUEEN_NEXT_RELATIONSHIP_CHECK_TURN / PlayerRuntimeState.queen_next_relationship_check_turn | Global / Recovered Struct Field | Queen / Player Runtime State | High | The word is repeatedly set to `current_turn + Rng_RandRange(5, 8)` after queen events and only gates the next relationship review/check; the old `review` wording was serviceable but underspecified. | c | yes: Averroes |
| byte_515D00 | g_SettlementTaxBurdenThresholds | Table | Buildings / Settlement Economy | High | `Building_GetTaxBurdenTier` reads this 4x4 threshold grid by population band to convert the raw tax rate into a derived burden tier. | c | no |
| BuildingRecord + 436 | BuildingRecord.tax_rate | Recovered Struct Field | Buildings / Settlement Economy | High | The low six bits are returned by `Podatek`, adjusted by `ZmienPodatek`, and multiplied into `Building_CollectGoldIncome`; the burden tier is derived later through `Building_GetTaxBurdenTier`, so the field itself is the raw tax rate. | c, asm | no |

## Deferred / Ambiguous
- Types `33` and `34` are securely a broader special-personage family, but the exact original designer-facing label behind `Dowódca / Tactician / Soldat` still is not corroborated well enough for a narrower public rename.
- `BuildingRecord + 436` clearly stores the raw tax rate in its low six bits, but the preserved upper two bits still carry unrelated packed state and remain unnamed.

## Batch 43 – Map And Pathing Unknown-Function Wave
| Old Name / Pattern | New Name | Kind | Subsystem | Confidence | Evidence Summary | Sources | Subagent Evidence |
|---|---|---|---|---|---|---|---|
| sub_40ED70 | Map_RevealAllTilesForPlayer | Function | Map / Fog Of War | High | Walks the full per-player reveal bitset, sets every tile-visible bit, and then refreshes the display, which is the map-reveal-all helper rather than a generic draw/update call. | c | yes: James |
| sub_40EDE0 | Map_RevealTileWithPropagation | Function | Map / Fog Of War | Medium | Reveals one tile if hidden, redraws it, and recursively fills bridging gaps when tiles two steps away are already visible, so the safest name is a reveal helper with propagation rather than a plain visibility predicate. | c | yes: James |
| sub_414390 | UnitStack_GetMoveCostToTileIgnoringOccupancy | Function | Unit Movement | Medium | Temporarily clears the destination tile occupancy, calls the standard tile-move-cost helper, then restores the old occupant, proving that it queries move cost while bypassing current occupancy. | c | yes: James |
| sub_423BB0 | MapTile_HasNorthRoadConnection | Function | Map / Roads | Medium | Normalizes the road overlay one row above the target tile and tests for the north-connected road-family ids. | c | yes: James |
| sub_423C50 | MapTile_HasSouthRoadConnection | Function | Map / Roads | Medium | South-neighbor sibling of the same road-connection predicate family. | c | yes: James |
| sub_423CF0 | MapTile_HasWestRoadConnection | Function | Map / Roads | Medium | West-neighbor road-connection predicate, with an extra special case for adjacent 2x2 building footprints occupying the western side. | c | yes: James |
| sub_423E10 | MapTile_HasEastRoadConnection | Function | Map / Roads | Medium | East-neighbor sibling of the same road-connection predicate family. | c | yes: James |
| sub_423E90 | Map_RebuildRoadOverlayAtTile | Function | Map / Roads | High | Recomputes the four-direction road connection mask from the cardinal predicates and rewrites the overlay tile through the `dword_5141A0` lookup table. | c | yes: James |
| sub_423FC0 | Map_NormalizeRoadOverlayTileId | Function | Map / Roads | High | Canonicalizes road/overlay tile ids before directional connection testing, collapsing multiple overlay variants into the base road-family ids. | c | yes: James |
| sub_4530A0 | Rules_PortCollectSupply | Function | Rules / Port | High | Pure rules-host wrapper over `Port_GetSupply`; the registration cluster exposes the port-supply collection command semantics directly. | c | yes: James |
| sub_453110 | Rules_QueuePathToTile | Function | Rules / Pathing | High | Rules-host wrapper that builds an exact path to a target tile and stores the resulting queued path into the stack's persistent path buffer. | c | yes: James |
| sub_4532A0 | Rules_QueuePathNearTile | Function | Rules / Pathing | High | Rules-host wrapper over `Unit_MoveTrackNearTile`, queuing a path that stops adjacent to the target tile. | c | yes: James |
| sub_453440 | Rules_QueuePathNearCastle | Function | Rules / Pathing | High | Rules-host wrapper over `Building_GenerateNearApproachTrack`, queuing a path that stops adjacent to the castle/building footprint. | c | yes: James |
| sub_453600 | Rules_QueuePathToPort | Function | Rules / Port Pathing | High | Rules-host wrapper over `Port_GenerateApproachTrack`, copying the resulting approach track into the stack's queued path buffer. | c | yes: James |

## Deferred / Ambiguous
- `sub_424020` and `sub_424120` are definitely road/bridge overlay predicates inside the road-building cluster, but this pass still did not lock down their exact designer-facing distinction safely enough for a public rename.
- `sub_40D890` clearly redraws some tile-level world-map representation after reveals and ownership changes, but the code still does not prove whether the narrowest honest name is a main-map tile refresh, a minimap tile refresh, or a combined redraw helper.

## Batch 44 – Building Economy Dialog And Hospital Rules Wave
| Old Name / Pattern | New Name | Kind | Subsystem | Confidence | Evidence Summary | Sources | Subagent Evidence |
|---|---|---|---|---|---|---|---|
| sub_42A890 | BuildingTransferDialog_DrawPeasantTransferAmount | Function | UI / Building Economy Dialog | High | Pure draw helper for the top transfer number box; it only redraws the peasant-transfer value region in the building economy dialog cluster. | c | yes: Darwin |
| sub_42A910 | BuildingTransferDialog_DrawGoldTransferAmount | Function | UI / Building Economy Dialog | High | Pure draw helper for the bottom transfer number box; it only redraws the gold-transfer value region in the same dialog cluster. | c | yes: Darwin |
| sub_42A9B0 | BuildingEconomyDialog_CommitTransfers | Function | UI / Building Economy Dialog | High | Commits the two pending transfer amounts through `Building_Transfer`, using the currently selected target-building entry and then redrawing the confirm strips. | c | yes: Darwin |
| sub_42AB80 | BuildingEconomyDialog_DecreaseTaxRate | Function | UI / Building Economy Dialog | High | Animates the low six bits of `BuildingRecord + 436` downward while preserving the upper packed bits, matching the building tax-rate field. | c | yes: Darwin |
| sub_42AC80 | BuildingEconomyDialog_IncreaseTaxRate | Function | UI / Building Economy Dialog | High | Upward sibling of the same tax-rate animation helper operating on the low six bits at `BuildingRecord + 436`. | c | yes: Darwin |
| sub_42AD70 | BuildingTransferDialog_DecreasePeasantTransferAmount | Function | UI / Building Economy Dialog | High | Decrements and animates the pending peasant-transfer amount, clamping the value at zero. | c | yes: Darwin |
| sub_42AE30 | BuildingTransferDialog_IncreasePeasantTransferAmount | Function | UI / Building Economy Dialog | High | Increments and animates the pending peasant-transfer amount, clamping it against the building's stored peasant population at `+430`. | c | yes: Darwin |
| sub_42AF10 | BuildingTransferDialog_DecreaseGoldTransferAmount | Function | UI / Building Economy Dialog | High | Decrements and animates the pending gold-transfer amount, clamping it at zero. | c | yes: Darwin |
| sub_42AFD0 | BuildingTransferDialog_IncreaseGoldTransferAmount | Function | UI / Building Economy Dialog | High | Increments and animates the pending gold-transfer amount, clamping it against the building's stored money at `+438`. | c | yes: Darwin |
| sub_42B0A0 | BuildingEconomyDialog_Run | Function | UI / Building Economy Dialog | High | Seeds the selected building and sprite set, zeroes pending transfers, draws the modal building-economy screen, and spins until the shared exit signal changes. | c | yes: Darwin |
| sub_456070 | Building_CalcGarrisonFactStrength | Function | Buildings / Rules Facts | High | `Building_OnGarrisonChange` uses it exclusively to refresh the asserted castle fact's `moc` value, and the helper sums effective per-slot combat contributions from the garrison. | c | yes: Darwin |
| sub_4577F0 | UnitStack_HasUnitsNeedingHealing | Function | Unit Lifecycle / Healing | High | The `MaJednostkiDoLeczenia` rules host path dispatches here, and the helper scans stack slots for units at or below 50 health in healable combat classes. | c | yes: Darwin |
| sub_457860 | UnitStack_FindPathToNearestHospitalCastle | Function | Unit Movement / Healing | High | Scans owned buildings for castle records with the hospital add-on bit, generates approach tracks, and keeps the cheapest one in the queued-path buffer. | c | yes: Darwin |
| sub_457A10 | UnitStack_MoveToBuildingAndCheckArrival | Function | Unit Movement / Building Approach | Medium | Ensures a queued approach path exists toward the target building, executes it, and returns whether the stack finished the movement. The current implementation is generic even though one caller uses it for healing. | c | yes: Darwin |
| dword_531CE4 | g_BuildingEconomyDialogSpriteSet | Global | UI / Building Economy Dialog | High | Loaded once by `BuildingEconomyDialog_Run` and then reused by the dialog's title/icon drawing helpers until the modal loop ends. | c | yes: Darwin |
| dword_531CE8 | g_BuildingEconomyDialogExitSignal | Global | UI / Building Economy Dialog | High | Written by the dialog callback helper `sub_42A990` and polled by `BuildingEconomyDialog_Run` to terminate the modal loop. | c | yes: Darwin |
| dword_531CEC | g_BuildingEconomyDialogPendingPeasantTransfer | Global | UI / Building Economy Dialog | High | Mutated only by the peasant-transfer adjustment buttons and consumed by `BuildingEconomyDialog_CommitTransfers`. | c | yes: Darwin |
| dword_531CF0 | g_BuildingEconomyDialogPendingGoldTransfer | Global | UI / Building Economy Dialog | High | Mutated only by the gold-transfer adjustment buttons and consumed by `BuildingEconomyDialog_CommitTransfers`. | c | yes: Darwin |
| dword_531CF4 | g_BuildingEconomyDialogBuilding | Global | UI / Building Economy Dialog | High | Shared pointer to the building currently being edited in the economy dialog; tax and transfer handlers all dereference it. | c | yes: Darwin |
| dword_53235C | g_BuildingTransferTargetListIndex | Global | UI / Building Transfer Target List | High | Shared scrolling-selection index into the transfer target list, mutated by keyboard and mouse navigation helpers. | c | yes: Darwin |
| word_532360 | g_BuildingTransferTargetIds | Global | UI / Building Transfer Target List | High | Shared int16 target-building id list used by both transfer dialogs and the target-list renderer; `-2` and `-1` act as sentinels. | c | yes: Darwin |
| g_BuildingEconomyDialogSpriteSet + g_BuildingEconomyDialogExitSignal + g_BuildingEconomyDialogPendingPeasantTransfer + g_BuildingEconomyDialogPendingGoldTransfer + g_BuildingEconomyDialogBuilding | BuildingEconomyDialogState | Recovered Struct | UI / Building Economy Dialog | High | These five globals are seeded, mutated, and consumed together by the modal building-economy dialog and its button callbacks, forming one coherent transient UI-state region. | c | yes: Darwin |
| g_BuildingTransferTargetListIndex + g_BuildingTransferTargetIds | BuildingTransferTargetList | Recovered Struct | UI / Building Transfer Target List | High | Transfer-target list initialization, rendering, navigation, and commit handlers all operate on this index-plus-array pair as one shared UI-state region. | c | yes: Darwin |

## Deferred / Ambiguous
- `sub_42A760` clearly draws the building economy dialog's local settlement-status icon, but the narrowest honest public name is still ambiguous between population-growth, plague-state, and broader settlement-status semantics.

## Batch 45 – Battle Control Helper Wave
| Old Name / Pattern | New Name | Kind | Subsystem | Confidence | Evidence Summary | Sources | Subagent Evidence |
|---|---|---|---|---|---|---|---|
| sub_429E30 | UnitBattle_Defence | Function | Unit Battle / Orders | High | The debug string is `UnitBattle_Defence(%d)`, the helper requires at least 5 action points, zeroes the acting unit's remaining AP, and sets the defending-state bit. | c, asm, exe | yes: Pauli |
| sub_42D290 | UnitBattle_RefreshSelectedUnitUI | Function | Unit Battle / UI | Medium | Clears the transient battle-mode flag, reinitializes the small battle UI state object, and redraws the current selected unit's panel through `sub_431DE0(g_SelectedUnitIndex)`. | c | yes: Pauli |
| sub_42D3A0 | UnitBattle_SelectNextControllableUnit | Function | Unit Battle / UI | Medium | Cycles from `g_SelectedUnitIndex` to the next current-player battle unit, recenters/redraws the view when needed, refreshes the selected-unit UI, and plays the activation sound. | c | yes: Pauli |

## Batch 46 – Battle Shot Action Wave
| Old Name / Pattern | New Name | Kind | Subsystem | Confidence | Evidence Summary | Sources | Subagent Evidence |
|---|---|---|---|---|---|---|---|
| sub_429740 | UnitBattle_Shot | Function | Unit Battle / Ranged Combat | High | The exact `UnitBattle_Shot(%d,%d)` string survives in asm/exe, and all call sites treat it as the standard ranged attack action against another unit. | asm, exe, c | yes: Pauli |
| sub_429BD0 | UnitBattle_ShotWall | Function | Unit Battle / Siege Combat | High | The exact `UnitBattle_ShotWall(%d,%d)` string survives in asm/exe, and all call sites use it for ranged attacks against wall hit points rather than unit targets. | asm, exe, c | yes: Pauli |

## Batch 47 – Battle View And Shot Math Wave
| Old Name / Pattern | New Name | Kind | Subsystem | Confidence | Evidence Summary | Sources | Subagent Evidence |
|---|---|---|---|---|---|---|---|
| sub_426E20 | UnitBattle_CenterViewOnUnit | Function | Unit Battle / Camera | High | Recomputes the tactical viewport origin as `unit_tile - 3`, clamps it to the 7x7 battle window, and is called before battle redraws and attack animations. | c | yes: Pauli |
| sub_426EF0 | UnitBattle_CountAdjacentEnemies | Function | Unit Battle / Positioning | High | Counts occupied adjacent battle tiles whose owner differs from the acting unit's owner. | c | yes: Pauli |
| sub_426F90 | UnitBattle_GetTargetCrowdingScale | Function | Unit Battle / Damage Math | Medium | Maps adjacent-enemy count to the discrete scales `256, 230, 204, 179, 128`, and melee/ranged damage helpers use it only as a crowding multiplier. | c | yes: Pauli |
| sub_4295D0 | UnitBattle_CalcShotTargetHealthAfterHit | Function | Unit Battle / Ranged Combat | High | Pure shot-damage math helper whose return value is written directly into the target's health byte after applying range and crowding scaling. | c | yes: Pauli |

## Batch 48 – World Attention, Queen Castle, And Army Fact Wave
| Old Name / Pattern | New Name | Kind | Subsystem | Confidence | Evidence Summary | Sources | Subagent Evidence |
|---|---|---|---|---|---|---|---|
| sub_40FA80 | Facing_DirectionFromDelta8 | Function | Movement / Orientation | High | Pure 8-way sign decoder from `(deltaRow, deltaColumn)` to facing ids `0..7`; the result is written into stack facing bytes during world movement, pathing, and battle-orientation helpers. | c | no |
| sub_411F60 | UnitStack_CanExecuteQueuedPathNow | Function | Unit Movement / Path Execution | High | Reads the tail waypoint cost from the queued path buffer at `stack + 316` and returns true only when the stack's current minimum AP can pay that immediate queued step; all callers use it as the gate before auto-executing queued movement. | c | no |
| sub_418D90 | UI_StartWorldMapUnitAttentionFlash | Function | UI / World Map Unit Attention | High | Called after morale-break notifications, temple gifts, peasant/special-unit spawns, and port reinforcements; it stores the target unit index plus the current tick for the world-map renderer's timed pulse. | c, asm | no |
| sub_418DA0 | UI_UpdateWorldMapUnitAttentionFlash | Function | UI / World Map Unit Attention | High | Frame-update helper that redraws the flashed unit tile each tick and clears the state after the pulse duration expires. | c, asm | no |
| sub_418E20 | UI_IsWorldMapUnitAttentionFlashActive | Function | UI / World Map Unit Attention | High | Single-purpose predicate over the flash-state unit index, used by the morale-break info loop to wait until the pulse completes. | c, asm | no |
| dword_5139F4 | g_WorldMapAttentionFlashUnitIndex | Global | UI / World Map Unit Attention | High | Read only by the world-map unit renderer/update helpers as the currently pulsed unit id; `-1` means inactive. | c, asm | no |
| dword_52699C | g_WorldMapAttentionFlashStartTick | Global | UI / World Map Unit Attention | High | Paired with `g_WorldMapAttentionFlashUnitIndex`; the update helper subtracts it from `Time_Now()` to derive pulse age and timeout. | c, asm | no |
| Unit_FindById | Building_FindRandomOwnedCompletedCastle | Function | Queen / Retaliation | High | Only the queen-retaliation branches call it. The helper filters owner-matching class-2 buildings with build timer `+16 == 0`, and the corresponding localized event texts explicitly say the queen steals gold from, poisons, or burns “the castle of %s”. | c, exe | no |
| sub_442680 | Audio_PlayArtifactSound | Function | Audio / Rewards | High | Builds `sfx\\artefakt\\%d.wav` and plays it only when unit sounds are enabled; callers use it for temple/treasure-style rewards and port supply collection. | c, exe | no |
| sub_454E70 | UnitStack_CalcArmyFactStrength | Function | Rules / Army Facts | High | Sums the stronger of the two per-slot effectiveness calculations across the live squad and is only consumed by army-fact `moc` synchronization and stack-strength comparisons. | c | no |
| sub_454ED0 | Rules_EnsureArmyFactForStack | Function | Rules / Army Facts | High | Creates an army fact when a live stack lacks one, retracts it when the stack becomes invalid/empty, and otherwise leaves the existing fact alone. | c | no |
| sub_455070 | Rules_SyncArmyFactStrength | Function | Rules / Army Facts | High | After `Rules_EnsureArmyFactForStack` succeeds, it reads the current army fact `moc`, recomputes `UnitStack_CalcArmyFactStrength`, and updates the fact only when the value changed. | c | no |
| g_WorldMapAttentionFlashUnitIndex + g_WorldMapAttentionFlashStartTick | WorldMapUnitAttentionFlashState | Recovered Struct | UI / World Map Unit Attention | High | These two globals are seeded and consumed together by the timed unit-pulse helpers and the world-map unit renderer, forming one coherent transient UI-state region. | c, asm | no |

## Deferred / Ambiguous
- `off_5191BC`, `off_5191C8`, and `off_5191D4` are now strongly indicated to be the three localized queen-retaliation message tables (gold theft, poisoned well, castle fire), but the decompiler still hides their exact array boundaries, so they remain deferred rather than promoted with guessed lengths.
- `Building_FindRandomOwnedCompletedCastle` is high-confidence for the queen-retaliation callers, but the wider global enum behind `BuildingRecord + 4 == 2` still is not named broadly enough to generalize that castle label to every class-2-building path.

## Batch 48 – Unit Sprite Cache And Animation Wave
| Old Name / Pattern | New Name | Kind | Subsystem | Confidence | Evidence Summary | Sources | Subagent Evidence |
|---|---|---|---|---|---|---|---|
| sub_40FA80 | Facing_DirectionFromDelta8 | Function | Unit Movement / Facing | High | Pure 8-way sign-map from row/column deltas to the facing ids later stored in stack runtime state and reused by movement/attack animation selection. | c | no |
| sub_411B30 | Map_RedrawUnitSpriteFootprintByIndex | Function | Map / Unit Rendering | High | Redraws the unit's anchor tile and any extra occupied/covered tiles implied by `g_UnitSpriteVerticalOffsets`, facing, and special-case type `30`, so it is the world-map sprite-footprint invalidator by stack index. | c | no |
| sub_411CB0 | Map_RedrawUnitNeighborhoodByIndex | Function | Map / Unit Rendering | High | Redraws the anchor tile plus all eight neighbors when `Unit_GetClassIndex` is non-zero, matching the broader neighborhood invalidation path used while classed units move. | c | no |
| sub_411D70 | Map_UpdateIdleAnimatedUnits | Function | Map / Unit Rendering | High | Scans all 500 world stacks, advances their low-three-bit animation frame when `g_UnitAnimationFlags bit0` and `g_UnitAnimationFrameIntervals` permit it, then redraws the affected footprint. | c | no |
| sub_411E20 | UnitStack_GetWorldSpriteVerticalOffsetByIndex | Function | Map / Unit Rendering | High | Thin helper that indexes `g_UnitSpriteVerticalOffsets` with the selected stack's unit type and returns the world-sprite anchor offset. | c | no |
| sub_411F60 | UnitStack_CanExecuteQueuedPathNow | Function | Unit Movement / Queued Path | High | Reads the queued path's last waypoint cost and returns true only when the stack's current minimum action points can pay it, which is exactly the immediate-execution predicate used by click handling. | c | no |
| sub_412F00 | UnitSpriteCache_Reset | Function | Unit Rendering / Cache | High | Initializes the 100-entry 15-byte cache region by stamping each entry's type-id slot to `-1`. | c | no |
| Unit_SetFlag | UnitSpriteCache_LoadMoveSheet | Function | Unit Rendering / Cache | High | The old name was misleading: the function performs LRU eviction in the 15-byte cache, builds a `units_go\\...` path, loads the movement sprite sheet, and stores the cache handle plus timestamp. | c | no |
| sub_413080 | UnitSpriteCache_GetOrLoadMoveSheet | Function | Unit Rendering / Cache | High | Looks up a movement-sheet cache entry by unit type, owner color, facing, and frame; on miss it falls back to `UnitSpriteCache_LoadMoveSheet`. | c | no |
| sub_413120 | UnitSpriteCache_Clear | Function | Unit Rendering / Cache | High | The helper's own debug string is `UnitsCache_Clear`, and it frees every live cached movement-sheet handle before resetting the type-id sentinel. | c | no |
| sub_413180 | UnitSpriteCache_CountEntries | Function | Unit Rendering / Cache | High | Counts live cache entries by scanning the 100-entry 15-byte cache for non-`-1` type ids. | c | no |
| sub_4131B0 | UnitSprite_BuildMoveSheetPath | Function | Unit Rendering / Asset Paths | High | Concatenates `units_go\\`, the per-type sprite folder, the owner digit, and `.s32`, producing the cached world/battle movement sheet path. | c | no |
| sub_413350 | UnitSprite_BuildInfoGraySheetPath | Function | UI / Unit Info Assets | Medium | Builds `units_i\\<folder>g_i.s32`, the gray-sheet variant loaded into building/garrison unit-list slots. | c | no |
| sub_413430 | UnitSprite_BuildInfoColorSheetPath | Function | UI / Unit Info Assets | High | Builds `units_i\\<folder>c_i.s32`, the colored unit-info sprite sheet later paired with the color palette loader. | c | no |
| sub_413510 | UnitSprite_BuildInfoColorPalettePath | Function | UI / Unit Info Assets | High | Builds `units_i\\<folder>c_i.pal`, and the only callers immediately copy palette entries `224..255` from the loaded file into the active UI palette. | c | no |
| sub_4135F0 | UnitSprite_BuildMeleeAttackSheetPath | Function | Unit Battle / Asset Paths | High | Builds `units_at\\<folder><owner>_a.s32`, and the melee-attack animation path loads it right before stepping through `g_UnitMeleeAttackFrameCounts`. | c | no |
| sub_4136D0 | UnitSprite_BuildRangedAttackSheetPath | Function | Unit Battle / Asset Paths | High | Builds `units_at\\<folder><owner>_s.s32`, and the shot-animation path loads it right before stepping through `g_UnitRangedAttackFrameCounts`. | c | no |
| sub_418A90 | Map_RedrawVisibleTile | Function | Map / Rendering | High | Validates that the target tile is inside the current 9x7 viewport, redraws the tile through `sub_416850`, and refreshes the exact 64x64 screen rectangle, so it is a visible-tile redraw helper rather than a generic effect call. | c | no |
| byte_512572 | g_UnitAnimationFrameIntervals | Table | Unit Rendering / Animation | High | World idle animation, battle idle animation, melee attack loops, and ranged attack loops all gate frame advancement on this per-type byte. | c | no |
| byte_512573 | g_UnitAnimationFlags | Table | Unit Rendering / Animation | High | Bit0 is the recovered “idle animation present” flag checked before world-map and battle idle frame updates advance. | c | no |
| byte_512577 | g_UnitSpriteVerticalOffsets | Table | Unit Rendering / Sprite Layout | High | Rendering subtracts this byte from the screen Y anchor, and redraw helpers use non-zero values to decide whether adjacent tiles must be invalidated for overhanging sprites. | c | no |
| byte_512578 | g_UnitMeleeAttackFrameCounts | Table | Unit Battle / Animation | High | The melee attack animation loop copies this per-type byte into its frame-count limit before drawing `_a.s32` attack sheets. | c | no |
| byte_512579 | g_UnitRangedAttackFrameCounts | Table | Unit Battle / Animation | High | The shot-animation loop copies this per-type byte into its frame-count limit before drawing `_s.s32` attack sheets. | c | no |
| byte_5125B7 | g_UnitMeleeAttackSoundFrames | Table | Unit Battle / Animation | High | The melee attack loop compares the current frame counter to this per-type byte to decide when to play the melee-impact sound. | c | no |
| byte_5125B8 | g_UnitRangedAttackSoundFrames | Table | Unit Battle / Animation | High | Both ranged-attack animation branches compare the current frame counter to this per-type byte to decide when to play the ranged shot sound. | c | no |
| dword_523F88 | g_UnitMoveSpriteCacheTypeIds | Global | Unit Rendering / Cache | High | Entry offset `+0` in the 15-byte cache stores the low-16-bit unit type id and uses `-1` as the free-entry sentinel. | c | no |
| byte_523F8C | g_UnitMoveSpriteCacheOwnerIndices | Global | Unit Rendering / Cache | High | Entry offset `+4` stores the owner-color index that becomes the trailing digit in `units_go\\...<owner>.s32`. | c | no |
| byte_523F8D | g_UnitMoveSpriteCacheFacingDirections | Global | Unit Rendering / Cache | High | Entry offset `+5` stores the facing direction used when converting `(frame + 8 * facing)` into a loaded sprite index. | c | no |
| byte_523F8E | g_UnitMoveSpriteCacheFrameIndices | Global | Unit Rendering / Cache | High | Entry offset `+6` stores the low-three-bit frame index matched by `UnitSpriteCache_GetOrLoadMoveSheet`. | c | no |
| dword_523F8F | g_UnitMoveSpriteCacheHandles | Global | Unit Rendering / Cache | High | Entry offset `+7` stores the loaded movement-sheet handle returned to renderers on cache hits. | c | no |
| dword_523F93 | g_UnitMoveSpriteCacheLastUsedTicks | Global | Unit Rendering / Cache | High | Entry offset `+11` stores the timestamp refreshed on hits and consulted by the LRU eviction path on misses. | c | no |
| data segment @ 0x523F88 stride 15 | UnitMoveSpriteCacheEntry | Recovered Struct | Unit Rendering / Cache | High | `UnitSpriteCache_LoadMoveSheet`, `UnitSpriteCache_GetOrLoadMoveSheet`, `UnitSpriteCache_Clear`, and `UnitSpriteCache_CountEntries` all treat `0x523F88` as a 100-entry stride-15 cache record family keyed by type/owner/facing/frame. | c | no |

## Deferred / Ambiguous
- `sub_411E60` clearly probes whether a stack with at least 3 AP can generate and afford at least one adjacent move track, but the narrowest honest public name still needs another caller-context pass.
- `sub_418D90/sub_418DA0`, `sub_418E30/sub_418E50`, and `sub_418EC0/sub_418EE0` are all temporary highlight/attention animation families, but this pass still did not pin down the exact gameplay naming split between spawn flash, reward blink, and tile ping.
- `UnitSprite_BuildInfoGraySheetPath` is behaviorally secure, but the `g_i` suffix may stand for “gray” or a slightly different art-pipeline term; the current rename is still marked medium confidence for that reason.

## Batch 48 – Battle Attack Helper Wave
| Old Name / Pattern | New Name | Kind | Subsystem | Confidence | Evidence Summary | Sources | Subagent Evidence |
|---|---|---|---|---|---|---|---|
| sub_426FC0 | UnitBattle_CalcMeleeExchangeOutcome | Function | Unit Battle / Damage Math | Medium | Computes one melee exchange round between attacker and defender slot records, returns the attacker's health loss, and writes both post-exchange health totals through out-parameters; callers use it both for live melee resolution and AI outcome simulation. | c | no |
| sub_4272A0 | UnitBattle_PlayAttackAnimation | Function | Unit Battle / Animation | High | The routine logs `AttackAnim(%d,%d)`, performs the attacker lunge, impact frames, hit shake, death-animation handoff, and return motion for the melee action path. | c, asm, map | no |
| sub_427FA0 | UnitBattle_PlayDeathAnimation | Function | Unit Battle / Animation | High | The routine logs `DeathAnim(%d)`, plays the ground or flying death sequence, updates corpse/air-death overlays, and clears the unit occupancy once the animation completes. | c, asm, map | no |
| sub_4283D0 | UnitBattle_GetCorpseSpriteIndex | Function | Unit Battle / Animation | Medium | Returns `byte_5125B6[88 * type] + ((facing + 4) % 8)`, and callers store that value into the battle corpse overlay bytes only when a unit dies, matching corpse-sprite selection. | c | no |
| sub_428400 | UnitBattle_Attack | Function | Unit Battle / Melee Combat | High | The preserved debug string is `UnitBattle_Attack(%d,%d,%d)`, and the body validates adjacency or a short approach path, spends 5 AP, applies the melee exchange, runs the attack animation, and removes any killed units. | c, asm, exe | no |
| sub_4287E0 | UnitBattle_IsTileInRangedAttackRange | Function | Unit Battle / Ranged Combat | High | Computes the Euclidean distance from the acting battle unit to an arbitrary target tile and compares it against the per-type min/max range tables; both AI and UI legality checks rely on it. | c | no |
| sub_428850 | UnitBattle_IsUnitInRangedAttackRange | Function | Unit Battle / Ranged Combat | High | Thin wrapper over `UnitBattle_IsTileInRangedAttackRange` that feeds the target unit's current battle tile. | c | no |
| sub_428880 | UnitBattle_PlayShotAnimation | Function | Unit Battle / Ranged Animation | High | The routine logs `_ShotAnim(%d,%d,%d,%d,%d,%d)`, loads projectile sprites, advances ranged attack frames, animates the shot toward a tile/unit/wall target, and then hands off to hit effects. | c, asm, map | no |
| sub_4298E0 | UnitBattle_AttackWall | Function | Unit Battle / Siege Combat | High | The preserved debug string is `UnitBattle_AttackWall(%d,%d,%d)`, and the body path-checks approach to the wall section, spends 5 AP, plays the attack animation, applies siege damage to wall HP, and refreshes wall visuals/sounds. | c, asm, exe | no |

## Deferred / Ambiguous
- `UnitBattle_CalcMeleeExchangeOutcome`'s fifth parameter is clearly a variant flag that scales one side of the exchange, but the exact designer-facing meaning (charge, initiative, first-strike bias, etc.) is still unresolved.

## Batch 49 – Save Slot And Notification Wave
| Old Name / Pattern | New Name | Kind | Subsystem | Confidence | Evidence Summary | Sources | Subagent Evidence |
|---|---|---|---|---|---|---|---|
| sub_4443C0 | SaveSlot_FormatDataFilePath | Function | Save / Load | High | Formats `save\\%d.dat`, and every caller uses it for the main save payload/header file. | c | no |
| sub_4443D0 | SaveSlot_FormatFactsFilePath | Function | Save / Load / Rules | High | Formats `save\\%d.fac`; the paired writer/reader helpers are corroborated by asm error strings `save-facts` and `load-facts`, proving this companion file stores serialized rules facts. | c, asm | no |
| sub_444490 | SaveSlot_LoadGame | Function | Save / Load | High | Opens the `.dat` file, restores the header and `gameData`, re-links army and castle facts, clears transient caches, and then loads the companion `.fac` rules-facts file. | c, asm | no |
| sub_4446E0 | SaveSlot_LoadLabelOrPlaceholder | Function | Save / Load UI | High | Reads the first 16-byte label stored in the `.dat` header for a slot, or falls back to the placeholder string `---` when no save file exists. | c, asm | no |
| sub_444750 | SaveSlot_HasData | Function | Save / Load UI | High | Returns true only when the slot's `.dat` file opens successfully. | c | no |
| sub_444780 | SaveSlotMenu_DrawSlotRow | Function | Save / Load UI | High | Draws one save/load slot row, using the live edit buffer for the currently selected save slot or the stored slot label / placeholder otherwise. | c | no |
| sub_444920 | SaveSlotMenu_CancelSelection | Function | Save / Load UI | High | Closes the modal slot menu with `selected_slot = -1` and the exit flag set. | c | no |
| sub_444950 | SaveSlotMenu_ConfirmSelection | Function | Save / Load UI | High | In save mode it writes the selected slot; in load mode it first checks that the slot has data, then records the chosen slot and exits the modal loop. | c | no |
| sub_444D50 | SaveSlotMenu_Run | Function | Save / Load UI | High | Runs the full save/load slot modal, including slot-row rendering, save-name text editing, selection changes, and confirm/cancel handling. | c | no |
| sub_4458E0 | Building_ShowPlagueOutbreakNotifications | Function | Buildings / Notifications | High | Scans the current player's buildings for plague state `5`, formats the localized outbreak message with the building name, and shows a notification window for each affected building. | c, asm | no |
| sub_4459A0 | Player_ShowTechLevelAdvanceNotification | Function | Player Runtime State / Notifications | High | Compares the player's current tech-level byte against the last reported tech-level byte, shows the localized advancement message once, and then synchronizes the reported value. | c, asm | no |
| PlayerRuntimeState + 47 | PlayerRuntimeState.tech_level | Recovered Struct Field | Player Runtime State / Technology | High | New castles inherit this low-three-bit player tech tier into `BuildingRecord.tech_level_bits`, and the technology-notification path reads it as the current progression level. | c | no |
| PlayerRuntimeState + 48 | PlayerRuntimeState.last_reported_tech_level | Recovered Struct Field | Player Runtime State / Technology | High | `Player_ShowTechLevelAdvanceNotification` only fires when this byte differs from `tech_level`, then copies the current tier into it. | c | no |
| PlayerRuntimeState + 49 | PlayerRuntimeState.battle_action_taken_flag | Recovered Struct Field | Player Runtime State / Battle Flow | High | Battle movement, melee attack, and wall-attack helpers set this dword when the player performs a tactical action, and the tactical turn loop clears it at round boundaries. | c | no |
| PlayerRuntimeState + 53 | PlayerRuntimeState.consecutive_idle_battle_turns | Recovered Struct Field | Player Runtime State / Battle Flow | High | The tactical turn loop increments this dword only when `battle_action_taken_flag` stays clear and invokes `GodAnger` once it reaches two rounds. | c | no |

## Deferred / Ambiguous
- `off_517EA0` and `off_517EAC` are overlapping notification-text blocks rather than clean standalone tables, so I kept the global names deferred even though the first triplets are now behaviorally identified as plague-outbreak and tech-level-advance texts.

## Batch 48 – Army Host And Strategic Targeting Wave
| Old Name / Pattern | New Name | Kind | Subsystem | Confidence | Evidence Summary | Sources | Subagent Evidence |
|---|---|---|---|---|---|---|---|
| sub_4561B0 | Rules_RegisterBuildingHostFunctions | Function | Rules / Host Registration | High | Registers only building, castle, wall-upgrade, licence, garrison-training, tax, and castle-stat host callbacks into the rules engine, so it is the building-side host registration entrypoint. | c, asm | no |
| sub_4570E3 | Rules_RegisterArmyHostFunctions | Function | Rules / Host Registration | High | Registers only army-facing host callbacks such as enter-castle, join/regroup, healing, plague, detach, march, and best-target queries, making it the army-side rules host registration entrypoint. | c, asm | no |
| sub_4579B0 | UnitStack_ExecuteHealingPathAndCheckArrival | Function | Unit Movement / Healing | High | The only gameplay caller is the `IdzDoLeczenia` host stub; the helper executes the already-queued path and returns success exactly when the destination stack slot becomes empty, i.e. the healing movement finished. | c, asm | no |
| sub_457BA0 | UnitStack_QueuePathNearBuilding | Function | Unit Movement / Building Approach | High | The `ZnajdzDrogeWPoblizeZamku` rules host stub dispatches here, and the body simply builds a near-approach track with `Building_GenerateNearApproachTrack`, copies it into the stack queue, and frees the temporary buffer. | c, asm | no |
| sub_457C00 | Building_GetMaxEnemyStrengthUnderWalls | Function | Buildings / Threat Assessment | High | The rules host label is `MocPrzeciwnikaPodMurami2`; the helper scans the ring around the building footprint and returns the maximum `sub_454E70` strength among adjacent enemy field stacks. | c, asm | no |
| sub_457E60 | UnitStack_DetachWeakUnitsToAdjacentTile | Function | Unit Lifecycle / Split | High | The rules host/debug labels are `OdlaczJednostkiSlabe`; the helper collects slots whose health byte `+9` is `<= 10`, finds a traversable adjacent tile, and moves exactly that slot selection into a new neighboring stack. | c, asm | no |
| sub_457FE0 | UnitStack_DetachUnitTypeToAdjacentTile | Function | Unit Lifecycle / Split | High | The rules host label is `OdlaczJednostkiTypu`; the helper selects all slots whose type id matches the requested unit type, probes for a free adjacent tile, and moves only that typed subset into a new neighboring stack. | c, asm | no |
| sub_458160 | UnitStack_HasUnitType | Function | Unit Lifecycle / Type Queries | High | The rules host label is `ArmiaMaJednostkiTypu`, and the helper returns true as soon as any occupied slot matches the requested unit type id. | c, asm | no |
| sub_4581C0 | UnitStack_HasOnlyUnitType | Function | Unit Lifecycle / Type Queries | High | The rules host label is `ArmiaTylkoJednostkiTypu`; it returns true only when every occupied slot matches the requested type id until the first empty slot. | c, asm | no |
| sub_458220 | UnitStack_GetHealthPercentAggregate | Function | Unit Lifecycle / Health Queries | High | The rules host label is `StanOsobArmii`; the sign of the mode parameter selects minimum, average, or maximum over slot byte `+9`, which is the recovered per-slot health/strength byte. | c, asm | no |
| sub_4582B0 | UnitStack_RegroupWithOtherStackByHealth | Function | Unit Lifecycle / Regroup | High | The debug string `PrzegrupujArmie2(%d,%d)` and host label `PrzegrupujArmie` match the body: it brings two stacks adjacent if needed, pools both slot arrays, sorts by slot-health byte `+9`, and repartitions them back into two stacks under the 10-slot cap. | c, asm, exe | no |
| sub_4589C0 | UnitStack_RegroupWithBuildingGarrisonByHealth | Function | Unit Lifecycle / Regroup | High | The debug string `PrzegrupujZamek2(%d,%d)` and host label `PrzegrupujZamek` match the implementation: it approaches the building if needed, pools stack slots with the 12-slot building garrison, sorts by slot-health byte `+9`, and repartitions them back into stack plus garrison. | c, asm, exe | no |
| sub_459230 | AI_CalcStrategicPriorityScore | Function | AI / Strategic Targeting | High | Opens the `strategprior` data file, looks up the float weight for the requested target class, and divides it by the recovered tile-distance scale before returning the candidate score. | c, asm, exe | no |
| sub_4592E0 | AI_EvaluateStrategicTargetAtTile | Function | AI / Strategic Targeting | High | Classifies one tile as building, unit, hidden treasure, or religious site, filters it by requested class/owner, scores it through `AI_CalcStrategicPriorityScore`, and updates the current best result triplet. | c, asm | no |
| sub_459570 | AI_FindBestStrategicTargetNearTile | Function | AI / Strategic Targeting | High | The `Znajdz_Najlepszy` host stub dispatches here; the body scans concentric rings around an origin tile, reuses `AI_EvaluateStrategicTargetAtTile` for each candidate, and returns the best encoded strategic target result. | c, asm | no |

## Deferred / Ambiguous
- `UnitStack_RegroupWithOtherStackByHealth` and `UnitStack_RegroupWithBuildingGarrisonByHealth` are behaviorally secure, but the original designer-facing intent behind the `>= 8` versus `<= 10` split thresholds is still only implementation-level; the code proves health-based repartitioning, not whether the buckets were meant as frontline/reserve, healthy/wounded, or another UI term.
- `AI_FindBestStrategicTargetNearTile` clearly encodes target classes as `1=building`, `2=unit stack`, `3=hidden treasure`, and `4=religious site`, but the result is still returned as a packed integer instead of a named recovered enum or record.

## Batch 50 – Castle Management Screen And Tooltip Wave
| Old Name / Pattern | New Name | Kind | Subsystem | Confidence | Evidence Summary | Sources | Subagent Evidence |
|---|---|---|---|---|---|---|---|
| sub_41E0E0 | Building_AssignUniqueGeneratedName | Function | Buildings / Naming | High | Picks one of 48 preset names from `off_513A84`, rejects duplicates by case-insensitive comparison against every building name at `BuildingRecord + 5`, and finalizes the accepted choice through `sub_40C450`, so it assigns a unique generated building name. | c | yes: Peirce |
| sub_4202C0 | Building_ShowConstructionProgressDialog | Function | Buildings / UI | High | Opens a modal construction dialog, draws the localized building type/name/rest-turn text for the record at `a1`, and lets the player either wait or invoke `Building_Stop`, matching a construction-progress confirmation screen. | c | yes: Peirce |
| sub_420870 | Castle_RebuildMissingAddonFlags | Function | Castle / Add-ons UI | High | Recomputes the five buildable-add-on booleans directly from `BuildingRecord.castle_addon_flags` and disables the add-on picker when no missing add-ons remain. | c | yes: Peirce |
| sub_420A90 | Castle_PlayAddonConstructionReveal | Function | Castle / Add-ons UI | High | Renders the castle scene against a temporary building-record copy with one add-on bit cleared, finds the changed sprite region, then crossfades the completed add-on into the live castle screen over 200 ms. | c | yes: Peirce |
| sub_420FB0 | Castle_ConfirmDestroyCurrentBuilding | Function | Castle / UI | High | Shows the localized destroy-building yes/no prompt, destroys `g_SelectedBuildingRecord` on acceptance, and sets the castle-screen exit flags. | c | yes: Peirce |
| sub_421010 | Castle_DrawStatusPanel | Function | Castle / UI | High | Draws the owner crest, religion overlay, wall-strength/status icon, building name, stat labels, and plague marker in the castle-screen status strip. | c | yes: Peirce |
| sub_421240 | Castle_RenderCompositeSceneLayers | Function | Castle / Scene Rendering | High | Builds `zamek_1\\...` asset names from the selected building's add-on bits and footprint class, renders the appropriate static scene layers, and then overlays the icon strip and ambient animations into the target surface. | c, exe | yes: Peirce |
| Audio_PlaySfx | Castle_DrawAmbientAnimationLayer | Function | Castle / Ambient Rendering | High | The old name was misleading: the function switches over ambient layer ids `1..10`, checks castle/add-on state bits, and draws the current animated overlay frame from `g_CastleAmbientSpriteSet`. | c | yes: Peirce |
| sub_421C20 | Castle_DrawAllAmbientAnimationLayers | Function | Castle / Ambient Rendering | High | Tight wrapper that redraws every ambient layer by iterating `1..10` through `Castle_DrawAmbientAnimationLayer`. | c | yes: Peirce |
| sub_421C40 | Castle_UpdateAmbientAnimationLayers | Function | Castle / Ambient Rendering | High | Advances the per-layer castle ambient timers and frame counters on fixed or randomized cadences, redraws only the affected layer, and restores the prior render device afterward. | c | yes: Peirce |
| sub_422020 | Castle_RebuildSceneBuffers | Function | Castle / Scene Rendering | High | Rebuilds the castle background/resource buffers, redraws the status panel and ambient overlays, and refreshes the off-screen castle surface used by the render hook. | c | yes: Peirce |
| Debug_LogRenderHookChange | Castle_OpenManagementScreen | Function | Castle / UI | High | The old name was misleading: it installs `Castle_RebuildSceneBuffers` as the render hook, seeds `g_SelectedBuildingRecord`, loads the castle sprite sets, runs the castle-management loop, shows hover tooltips, and restores the prior render state on exit. | c | yes: Peirce |
| sub_422880 | Tooltip_CaptureBackdrop | Function | UI / Tooltips | High | Captures the screen rectangle under a tooltip into a saved surface and widens the left/right bounds from the rendered text width; cross-call review shows the same helper is reused in castle, strategic, and battle screens. | c | yes: Peirce |
| sub_422960 | Tooltip_ReleaseBackdropSurface | Function | UI / Tooltips | High | Releases the saved tooltip backdrop surface and clears the handle; world-map and battle teardown both call it, confirming this is generic tooltip state rather than castle-only state. | c | no |
| sub_4229A0 | Tooltip_ShowText | Function | UI / Tooltips | High | Restores the captured backdrop, draws tooltip text over it, presents, and caches the last shown tooltip string; callers span castle, strategic, and battle UI. | c | yes: Peirce |
| sub_422AC0 | Tooltip_RestoreBackdrop | Function | UI / Tooltips | High | Restores the captured backdrop without drawing new text, matching the hover-tooltip clear path when the cursor leaves a hotspot. | c | yes: Peirce |
| sub_422B50 | Tooltip_RestoreIfTextMatches | Function | UI / Tooltips | Medium | Compares the candidate hover-text string against the currently cached tooltip text and only restores the saved backdrop when they match, preventing unrelated hover exits from clearing a newer tooltip. | c | no |
| sub_422B70 | Tooltip_SetResourceHandle | Function | UI / Tooltips | High | Single-purpose setter for the shared tooltip draw/resource handle that `Tooltip_ShowText` consumes before drawing the next tooltip. | c | no |
| g_CastleUnitRecord | g_SelectedBuildingRecord | Global | Castle / UI State | High | `Castle_OpenManagementScreen` sets it to `gameData + 509674 + 467 * buildingIndex`, add-on reveal temporarily swaps it to a preview copy, and every castle UI helper dereferences it as the active building record. | c | yes: Peirce |
| dword_526A68 | g_CastleScreenSurface | Global | Castle / UI State | High | Allocated as a 640x480 off-screen surface on castle-screen entry, rendered into by the castle scene helpers, and freed on exit. | c | yes: Peirce |
| dword_526E74 | g_CastleSceneIconSpriteSet | Global | Castle / UI State | High | Loaded from `zamek_1\\z_iko.s32` and then used for the castle icon strip plus the top-banner animation that precedes the add-on picker. | c, exe | yes: Peirce |
| dword_526E78 | g_CastleAmbientSpriteSet | Global | Castle / UI State | High | All ten ambient layer draw paths source their frames from this shared sprite-set handle. | c | yes: Peirce |
| dword_526E7C | g_CastleStatusSpriteSet | Global | Castle / UI State | High | The status panel uses this sprite set for owner crests, religion overlays, wall-strength icons, and the plague icon. | c | yes: Peirce |
| dword_526E80 | g_CastleScreenExitRequested | Global | Castle / UI State | High | Cleared on castle-screen entry, polled by the modal loop, and set by the close/destroy callbacks, so it is the shared exit-request flag. | c | no |
| dword_526E84 | g_CastleDestroyConfirmed | Global | Castle / UI State | High | Set only by `Castle_ConfirmDestroyCurrentBuilding` and checked once the castle screen exits to continue the destroy flow. | c | no |
| dword_526EF4 | g_TooltipBackdropSurface | Global | UI / Tooltips | High | Shared saved-surface handle containing the pixels captured beneath the current tooltip. | c | no |
| dword_526EF8 | g_TooltipTop | Global | UI / Tooltips | High | Top screen coordinate of the captured tooltip rectangle. | c | no |
| dword_526EFC | g_TooltipLeft | Global | UI / Tooltips | High | Left screen coordinate of the captured tooltip rectangle after width expansion. | c | no |
| dword_526F00 | g_TooltipBottom | Global | UI / Tooltips | High | Bottom screen coordinate of the captured tooltip rectangle. | c | no |
| dword_526F04 | g_TooltipRight | Global | UI / Tooltips | High | Right screen coordinate of the captured tooltip rectangle after width expansion. | c | no |
| dword_526F08 | g_TooltipResourceHandle | Global | UI / Tooltips | High | `Tooltip_CaptureBackdrop` stores the active draw/resource handle here, `Tooltip_ShowText` restores it before drawing text, and `Tooltip_SetResourceHandle` acts as a setter for the same state. | c | no |
| unk_526F10 | g_TooltipTextBuffer | Global | UI / Tooltips | High | `Tooltip_ShowText` copies the last displayed tooltip string into this shared buffer, and `Tooltip_RestoreIfTextMatches` compares against it before clearing the hover text. | c | no |
| BuildingRecord + 5 | BuildingRecord.display_name | Recovered Struct Field | Buildings / Naming | High | `Building_AssignUniqueGeneratedName` writes generated stronghold names into this in-record string, and construction/notification UI later prints the same buffer as the building name. | c | yes: Peirce |
| BuildingRecord + 420 | BuildingRecord.construction_lock_flags | Recovered Struct Field | Castle / Build State | High | Every castle add-on builder checks bit 0, sets it when construction begins, and completion/toggle paths clear or consult the same byte before enabling new castle build actions. | c | yes: Peirce |
| g_SelectedBuildingRecord + g_CastleScreenSurface + g_CastleSceneIconSpriteSet + g_CastleAmbientSpriteSet + g_CastleStatusSpriteSet + g_CastleScreenExitRequested + g_CastleDestroyConfirmed | CastleManagementScreenState | Recovered Struct | Castle / UI State | High | The castle-screen runner seeds, updates, and tears down these globals together as one coherent modal-screen state region. | c | no |
| data segment @ 0x526EF4 size 28 | TooltipBackdropState | Recovered Struct | UI / Tooltips | High | `Tooltip_CaptureBackdrop`, `Tooltip_ShowText`, `Tooltip_RestoreBackdrop`, `Tooltip_ReleaseBackdropSurface`, and `Tooltip_SetResourceHandle` all operate on one tooltip state block holding the saved surface, capture rect, resource handle, and one still-ambiguous auxiliary dword. | c | no |

## Deferred / Ambiguous
- `sub_420910` clearly rebuilds the missing-add-on flags, plays the top-banner icon animation, and then dispatches through the castle action menu callback table, but the narrowest honest public name still needs one more pass over the input/command side of that menu family.
- `sub_421740` is a stable low-level sprite blit helper for castle ambient overlays, but this pass stopped short of renaming it until its exact ownership outside the castle layer renderer is ruled out.
- `dword_526E70` is loaded from the owning player's runtime block when the castle screen opens, but this batch did not prove whether it is music/voice/theme state or another castle-session attribute.
- `dword_526F0C` is captured alongside `TooltipBackdropState`, but this batch did not prove whether it is an anchor id, max-width bound, or another auxiliary tooltip draw parameter.

## Batch 51 – Decision Dialog State Wave
| Old Name / Pattern | New Name | Kind | Subsystem | Confidence | Evidence Summary | Sources | Subagent Evidence |
|---|---|---|---|---|---|---|---|
| sub_446430 | DecisionDialog_ConfirmIfAllowed | Function | UI / Decision Dialogs | High | Shared accept callback for yes-no and queen decision windows; it only terminates with an accepting result when the shared `g_DecisionDialogConfirmDisabled` flag is clear. | c | no |
| sub_446460 | DecisionDialog_Cancel | Function | UI / Decision Dialogs | High | Shared cancel callback that unconditionally terminates the active decision dialog with result `0`. | c | no |
| dword_543D54 | g_DecisionDialogExitSignal | Global | UI / Decision Dialogs | High | The queen whim, queen marriage proposal, generic yes-no dialog, and the modal text-entry runner all poll this dword as the shared loop-exit signal. | c | no |
| dword_543D58 | g_DecisionDialogResult | Global | UI / Decision Dialogs | High | Accept/cancel callbacks write `1/0` here, and the yes-no plus queen decision dialogs all return this same shared result once the modal loop exits. | c | no |
| dword_543D5C | g_DecisionDialogConfirmDisabled | Global | UI / Decision Dialogs | High | `Queen_ShowWhimDecisionDialog` sets it when the required score exceeds the current score, and `DecisionDialog_ConfirmIfAllowed` refuses to accept while it is nonzero. | c | no |
| unk_517ED0 | g_QueenWhimDecisionButtonWidgetsTemplate | Global Template | Queen / UI | High | `Queen_ShowWhimDecisionDialog` clones this widget template block before patching positions and sprite-set pointers for the whim yes/no buttons. | c | no |
| unk_517F70 | g_YesNoDialogButtonWidgetsTemplate | Global Template | UI / Decision Dialogs | High | `YesNoWindow` clones this template block verbatim before patching dialog-local positions and sprite-set pointers for the standard yes/no buttons. | c | no |
| unk_518020 | g_QueenMarriageProposalButtonWidgetsTemplate | Global Template | Queen / UI | High | `Queen_ShowMarriageProposalDialog` clones this template block before placing the proposal yes/no buttons, distinct from the whim and generic yes-no widget templates. | c | no |
| g_DecisionDialogExitSignal + g_DecisionDialogResult + g_DecisionDialogConfirmDisabled | DecisionDialogState | Recovered Struct | UI / Decision Dialogs | High | The shared accept/cancel callbacks and all three modal decision windows treat these three globals as one coherent decision-dialog state region. | c | no |

## Deferred / Ambiguous
- `sub_446CB0` is a compact modal info-window runner adjacent to the decision-dialog family, but this pass stopped short of renaming it until its full caller set is reviewed outside the queen UI cluster.

## Batch 52 – Compileability Bootstrap Wave
| Old Name / Pattern | New Name | Kind | Subsystem | Confidence | Evidence Summary | Sources | Subagent Evidence |
|---|---|---|---|---|---|---|---|
| missing local `windows.h` include target | local `windows.h` compatibility shim | Compile Fix | Build / Platform Layer | High | The first compiler probe stopped immediately at `#include <windows.h>`. A conservative local shim restored the Win32 scalar types, structs, and handle aliases actually referenced by `clash95.c`, letting the parser reach real source issues. | c | no |
| missing local `defs.h` include target | local `defs.h` compatibility shim | Compile Fix | Build / Decompiler Support | High | `clash95.c` depends on Hex-Rays helper aliases/macros such as `_BYTE`, `_WORD`, `_DWORD`, `qmemcpy`, `LOBYTE`, and lvalue word/byte accessors. Adding a local shim moved compilation past the decompiler support layer. | c | no |
| live Hex-Rays calling annotations (`__usercall`, `__userpurge`, `__spoils<...>`, `@<...>`) | stripped from live code | Compile Fix | Build / Parser Recovery | High | The file contained thousands of syntactically invalid decompiler-only calling/register annotations. Removing them was source-level behavior-neutral within this recovered whole-program codebase and exposed the next real blocker families. | c | no |
| overloaded `PlayAvi` declaration/definition for `IDirectDrawSurface2` | `PlayAviSurface2` | Helper | Build / AVI Helpers | High | C cannot overload on pointer type alone. The `IDirectDrawSurface2` variant is behaviorally distinct but signature-equivalent otherwise, so a suffix split is the smallest safe compile-preserving repair. | c | no |
| overloaded `PlayAviStretch` declaration/definition for `IDirectDrawSurface2` | `PlayAviStretchSurface2` | Helper | Build / AVI Helpers | High | Same overload conflict as `PlayAvi`; the renamed variant preserves the decompiler's separate surface-v2 implementation while making the translation unit legal C. | c | no |
| missing weak data-label targets `loc_46BC9D` … `loc_46CE33` | placeholder `_UNKNOWN` declarations | Compile Fix | Build / Data Initializers | Medium | Static initializer tables reference these labels directly, but the recovered data declaration block did not emit matching placeholder symbols. Adding conservative `_UNKNOWN` declarations moved compilation past that concrete initializer failure without inventing semantics. | c | no |

## Deferred / Ambiguous
- The next dominant compile blocker is the duplicate autogenerated weak-data label family (`a_`, `a__0`, `a__1`, and siblings). Those collisions are mechanically recoverable, but this pass stopped short of mass-renaming them until a consistent normalization strategy is chosen.
- Initializer tables still reference many undeclared weak targets (`unk_504D43`, `unk_51D9A0`, `byte_51DE60`, `nullsub_15`, `nullsub_23`, `nullsub_24`, `nullsub_29`, `nullsub_30`, `nullsub_32`, `_no_support_loaded`, and others). They are likely recoverable as missing placeholder declarations, but this batch only advanced the smallest already-proven subset.
- `bad_exception::~bad_exception` remains a live C++ residue in a static initializer. It likely wants the same flattening treatment already used elsewhere (`*_dtor`), but that rename was deferred until the surrounding exception-runtime family is reviewed together.

## Batch 53 – Weak Label Split And Arity Recovery Wave
| Old Name / Pattern | New Name | Kind | Subsystem | Confidence | Evidence Summary | Sources | Subagent Evidence |
|---|---|---|---|---|---|---|---|
| `a_p` (`"_p"`) | `aUnitSpritePaletteSuffix` | Global / String | Units / Sprite Paths | High | The only live callsite appends it between `g_UnitSpriteFolders[unitType]` and `.s32`, so this weak string is the unit-sprite palette suffix rather than the deffunction `?p` token. | c | no |
| `a_` (`"."`) | `aDot_MinimumResourcePrefix` | Global / String | Resource Loading | Medium | Bound only to the `data\\minimum.res` registration call in the resource bootstrap, so the duplicate dot string can be separated from the unrelated lexer `?` token family. | c | no |
| `a__0` (`"."`) | `aDot_NormalResourcePrefix` | Global / String | Resource Loading | Medium | Bound only to the `data\\normal.res` registration call in the same resource bootstrap cluster. | c | no |
| `a__1` (`"."`) | `aDot_MaximumResourcePrefix` | Global / String | Resource Loading | Medium | Bound only to the `data\\maximum.res` registration call in the same resource bootstrap cluster. | c | no |
| `a_` (`"?"`) | `aQuestion_LexerSingleFieldVarPrefix` | Global / String | Rules / Lexer | High | The lexer concatenates or interns it only when parsing single-field variable tokens, proving it is the lexical `?` prefix rather than the definstances marker or resource dot. | c | no |
| `a__0` (`"$?"`) | `aQuestion_LexerMultiFieldVarPrefix` | Global / String | Rules / Lexer | High | The lexer uses it only for multifield variable parsing and token interning. | c | no |
| `a__1` (`"?"`) | `aQuestion_PrintSingleFieldVarPrefix` | Global / String | Rules / Pretty Printer | High | The pretty-printer writes it only for token kinds `13/15`, i.e. the single-field variable print path. | c | no |
| `a__2` (`"$?"`) | `aQuestion_PrintMultiFieldVarPrefix` | Global / String | Rules / Pretty Printer | High | The pretty-printer writes it only for token kinds `14/16`, i.e. the multifield variable print path. | c | no |
| `a_p` (`"?p"`) | `aQuestionP_DeffunctionArgumentPrefix` | Global / String | Rules / Deffunctions | High | The deffunction pretty-printer prepends it to each numbered argument, matching the CLIPS-style `?p` argument naming convention. | c | no |
| `a_` (`"@"`) | `aDefinstancesConstructMarker` | Global / String | Rules / Definstances | High | `sub_4CE360` passes it as the construct marker to `sub_49C440`; neighboring constructs pass `* + $ ^ & ! # ~`, so `@` is the definstances marker in the same family. | c | no |
| `a__5` (`":== ?*"`) | `aDefglobalWatchPrefix` | Global / String | Rules / Defglobals | High | The watch/trace path prints it before a defglobal name and then appends `* ==> ... <== ...`, proving it is the defglobal watch prefix rather than a generic `".\n"` suffix. | c | no |
| `a__9` (`"?*"`) | `aDefglobalVariablePrefix` | Global / String | Rules / Defglobals | High | The defglobal pretty-printer writes it directly before the global name in `(defglobal ...)` output. | c | no |
| `a__3` (`"$?"`) | `aAccessorSetterMultifieldParamPrefix` | Global / String | Objects / Message Handlers | Medium | The default accessor/message-handler generator chooses this branch when a slot is multifield and uses it in the generated `put-...` parameter list. | c | no |
| `a__4` (`"?"`) | `aAccessorSetterSinglefieldParamPrefix` | Global / String | Objects / Message Handlers | Medium | Paired with the previous entry for single-field setter parameter generation. | c | no |
| `a__11` (`"(?]"`) | `aPrettyPrintUnknownExpressionToken` | Global / String | Rules / Pretty Printer | Medium | It is only selected by the default branch of the expression pretty-printer switch, so it is a fallback token for unclassified expression nodes. | c | no |
| duplicated weak `".\n"` suffix family | `aDotNewline_Agenda`, `aDotNewline_RuleRuntime`, `aDotNewline_FileError`, `aDotNewline_InstanceError`, `aDotNewline_ExpressionError`, `aDotNewline_ConstructError`, `aDotNewline_ParserError` | Global / String | Rules / UI Text | High | Each duplicate `".\n"` string now binds to a separate verified caller cluster: agenda/salience errors, rule runtime/breakpoint output, file-open errors, instance errors, expression errors, construct/import conflicts, and parser-expression errors respectively. | c | no |
| `unk_504D43` address-only gap | local zero-byte definition for `unk_504D43` | Compile Fix | Build / Data Initializers | High | `clash95.asm` names `unk_504D43` as a single zero byte and `off_50D0A0` points directly at it, so materializing the byte in C is the smallest faithful initializer repair. | c, asm | no |
| `bad_exception::~bad_exception` in `off_510F44` | `bad_exception_dtor` | Compile Fix | Build / Exception Runtime | High | The C translation unit cannot keep a destructor spelling in a function-pointer initializer; flattening it to a plain C-safe destructor symbol matches the surrounding `*_dtor` recovery pattern. | c, asm | no |
| narrow prototypes for `DD_Pump`, `Render_Begin`, `Debug_Log`, `sub_42C130` | variadic-compatible declarations/definitions | Compile Fix | Build / Signature Recovery | High | Direct callsites and nearby asm usage show extra pushed arguments beyond the recovered fixed arity, but the current bodies tolerate/ignore those extras. Widening them is safer than inventing new fixed signatures prematurely. | c, asm | no |
| malformed `TILE_INDEX(v17; ... v18)` site in `sub_407D20` | reconstructed `TILE_INDEX(v17, v18)` with recovered second coordinate | Compile Fix | Tile / Cursor-to-Tile Mapping | High | The neighboring terrain-tooltip path computes the missing `dword_544D00`-based coordinate cleanly. Reusing that formula restored the broken mouse-to-tile index expression with matching surrounding bounds logic. | c | no |
| `PlayGame(..., extra_char, a5)` outlier calls | normalized to the established 5-argument `PlayGame` shape | Compile Fix | Turn / Mission Flow | Medium | Two callers used six arguments, but the function body only consumes five and the other callsites already match the 5-argument form. The stray extra char was dropped as the smallest safe normalization. | c | no |

## Deferred / Ambiguous
- `PlayGame` still warrants a dedicated signature review against `Scenario_LoadMissionByIndexAndPlay` and the recursive mission-chain path. The 5-argument normalization is compile-safe, but the original calling convention may have encoded an additional transient state byte that still needs confirmation.
- `sub_420F70` currently contains a live `.fn_init()` token, and several later clusters still stop at `#error "call analysis failed"` sentinels. Those are now the dominant non-namespace compile blockers.
- `BuildBuilding`, `Trap_New`, and `Port_GetSupply` have moved into view with additional missing format strings, support tables, and shifted helper signatures; this wave stopped once the frontier moved from weak-label cleanup into those subsystem-specific repairs.

## Batch 54 – Runtime Compatibility And ExcString Normalization Wave
| Old Name / Pattern | New Name | Kind | Subsystem | Confidence | Evidence Summary | Sources | Subagent Evidence |
|---|---|---|---|---|---|---|---|
| missing `BYTE4` / `BYTE5` / `BYTE6` / `BYTE7` lvalue macros | byte-accessor compatibility macros in `defs.h` | Compile Fix | Build / Decompiler Support | High | The late AVI/runtime frontier exposed surviving Hex-Rays byte-write artifacts such as `BYTE6(a20) = ...`. Extending the existing `BYTEn` macro family is behavior-neutral and restores those assignments as legal C lvalues. | c | no |
| missing `NT_TIB`, `TEB`, `NtCurrentTeb()`, and `MEMORYSTATUS` shims | conservative Win32 runtime compatibility layer in `windows.h` | Compile Fix | Build / Platform Layer | High | The fresh compiler frontier stopped repeatedly on `NtCurrentTeb()->NtTib.ExceptionList` and `LPMEMORYSTATUS`. The source only needs a minimal exception-list field and memory-status typedefs to parse the recovered runtime wrappers. | c | no |
| bulk `__ExcString::__ExcString()` residues | `ExcString_Ctor()` / `ExcString_CtorFromPtr()` compatibility helpers | Compile Fix | Build / Exception Runtime | Medium | These constructor-style residues are decompiler artifacts from the late runtime/error-wrapper layer. Rebinding them to explicit no-op helpers is the smallest parser-safe normalization that preserves control flow while keeping the unresolved exception-text semantics clearly isolated. | c | no |

## Deferred / Ambiguous
- The new compile frontier is now concentrated in the AVI/runtime subsystem. The remaining blockers there are not gameplay-semantic names but compatibility artifacts: `ICSendMessage`, `SHIDWORD`, `IDirectDraw` vtable typing, one `#error "46983B: call analysis failed"`, and an inline `__asm { fstp ... }` block.
- `__ExcString::operator char *` still survives as a live C++-style residue around `sub_46A0A0`; this wave only normalized the constructor form.
- The rules/CLIPS loader cluster around `sub_47C850` surfaced a new set of undeclared text labels (`aFile`, `aIsNotABinaryCo`, `aBinaryLoadCann`, and siblings). They appear recoverable as straightforward extern declarations, but this pass stopped once the runtime compatibility layer became the primary blocker.

## Batch 55 – Loader And Path Helper Compile Fixes
| Old Name / Pattern | New Name | Kind | Subsystem | Confidence | Evidence Summary | Sources | Subagent Evidence |
|---|---|---|---|---|---|---|---|
| missing `ICSendMessage` declaration | local Win32 import shim | Compile Fix | Build / AVI Runtime | High | `clash95.map` exports `ICSendMessage`, and the late AVI wrappers only need a declaration to type-check their thunk calls. Adding the import shim is behavior-neutral and removes an avoidable parser stop. | c, asm, map | no |
| `__ExcString::operator char *` residue in `sub_46A0A0` | `ExcString_AsCharPtr` compatibility helper name | Compile Fix | Build / Exception Runtime | Medium | The constructor-form residues were already normalized to `ExcString_Ctor*`. The surviving cast-to-operator form is the same late-runtime artifact family, so rebinding it to an explicit helper name is the smallest consistent parser repair. | c, asm | no |
| `sub_4721C0(const char **, ...)` | `sub_4721C0(char **, ...)` | Compile Fix | Loader / Path Editing | High | The body temporarily inserts a NUL terminator into the caller-owned buffer and then restores the byte, so the `const` qualifier is provably wrong. The asm also treats the input as mutable storage. | c, asm | no |
| `sub_472230(const char **, ...)` | `sub_472230(char **, ...)` | Compile Fix | Loader / Path Editing | High | The helper allocates and rewrites a mutable path buffer, including direct byte stores through the incoming pointer. Keeping a `const char **` type caused false read-only write failures that are contradicted by the implementation. | c, asm | no |

## Deferred / Ambiguous
- The remaining hard blocker in the late runtime layer is `sub_4697E0` (`#error "46983B: call analysis failed"`). The asm body is available and looks recoverable, but it is large enough that this pass deferred it rather than landing a low-confidence stub.
- `sub_47C850` still needs one more straightforward string-extern sweep (`aConstructsBeca`, `aBload`) after the compiler moves past the current loader-text frontier.
- `sub_47E910` contains a live MSVC inline-assembly `fstp` residue. That site likely wants either a direct C temporary assignment or a helper shim once the surrounding floating-point logic is reviewed from asm.

## Batch 56 – Queen Flow And Build Loop Compile Fixes
| Old Name / Pattern | New Name | Kind | Subsystem | Confidence | Evidence Summary | Sources | Subagent Evidence |
|---|---|---|---|---|---|---|---|
| `#error "4127A3: call analysis failed"` | restored `sub_4127A0(int slot, int delta, BOOL (*predicate)(int))` | Compile Fix | Unit / Fatigue | High | `clash95.asm` at `0x4127A0` shows a tiny helper that gates on the predicate, skips special-personage cargo types, adjusts slot byte `+10`, and clamps to `[0,100]`. Re-emitting that helper directly was safer than widening unrelated callers. | c, asm | no |
| raw `.fn_init()` token residues | valid helper identifiers (`sub_41FF80`, `sub_49A0E0`, `sub_4B6DD0`, `sub_4BDD40`, `sub_4D88F0`) | Compile Fix | Build / Runtime Glue | High | The map already exposed distinct `.fn_init` addresses. Rebinding the surviving invalid tokens to the file’s existing valid placeholder symbols removed parser-only artifacts without inventing new semantics. | c, map | no |
| `BuildBuilding` extra helper arguments | trimmed `sub_40ADF0(v8)`, `sub_407D20(v9)`, `sub_4620F0(aZakl_pul, 1, v20, v8, 3u)` | Compile Fix | Building / Placement UI | High | The `BuildBuilding` asm shows these helpers are reached without the extra pushed callback/resource arguments the decompiler invented. Removing the extras matched the actual call setup and unblocked the placement loop. | c, asm | yes |
| decompiler-invented `Building_FindRandomOwnedCompletedCastle(...)` calls in `Queen_NewTurn` | `Unit_FindById(g_CurrentPlayerIndex)` | Compile Fix | Queen Systems | High | The queen-escape switch in `clash95.asm` loads `dword_5202EC` and calls `Unit_FindById` in cases 1-3 before mutating the returned record. The decompiled castle-finder calls were a clear illusion. | c, asm | no |
| `Rules_CreateArmyFact(..., a5, a3)` 6-arg outlier | normalized to 5-arg `Rules_CreateArmyFact((__int16 *)a1, tileOccupant, (int)a1, a4, a5)` | Compile Fix | Rules / Army Facts | High | The established declaration and nearby callers all use the 5-argument form; this site was carrying a stale floating-point spill from the decompiler. | c | no |
| missing loader/report strings `aConstructsBeca`, `aBload` | late-loader extern declarations | Compile Fix | Rules / BLOAD Text | High | The labels are present in `clash95.asm` and only needed to type-check the already recovered loader/reporting path in `sub_47C850`. | c, asm, map | no |

## Deferred / Ambiguous
- The isolated AVI/runtime block starting at `sub_4697E0` remains the dominant compile blocker. The asm body is available but large; this wave intentionally avoided a low-confidence stub.
- `sub_47E910` still contains MSVC inline floating-point assembly that needs a direct C temporary-based rewrite.
- The late runtime and loader fixes in this batch were compile-first only; they did not change the earlier gameplay rename conclusions.

## Batch 57 – Runtime Quarantine And Builtin Rename Wave
| Old Name / Pattern | New Name | Kind | Subsystem | Confidence | Evidence Summary | Sources | Subagent Evidence |
|---|---|---|---|---|---|---|---|
| `sub_4A2AB0` | `Rules_RegisterStringBuiltins` | Function | Rules / String Builtins | High | The function registers `str-cat`, `sym-cat`, `str-length`, `str-compare`, `upcase`, `lowcase`, `sub-string`, `str-index`, `eval`, and `build` through the host-function table, so it is the string-builtin registration entrypoint rather than a generic helper. | c | no |
| `sub_4A2BC0` | `Rules_StrCatBuiltin` | Function | Rules / String Builtins | High | The body is a thin wrapper that invokes the shared concatenation core with mode `3`, matching the registered `str-cat` behavior. | c | no |
| `sub_4A2C00` | `Rules_SymCatBuiltin` | Function | Rules / String Builtins | High | The body is a thin wrapper that invokes the shared concatenation core with mode `2`, matching the registered `sym-cat` behavior. | c | no |
| `sub_4A2C40` | `Rules_ConcatBuiltinCore` | Function | Rules / String Builtins | High | The common implementation evaluates arguments, stringifies atoms and numeric values, concatenates them into a transient buffer, and returns either a string or symbol result based on the wrapper-selected mode. | c | no |
| `sub_4CCAE0` | `Defglobals_ResetCallback` | Function | Rules / Defglobals | High | The defglobal construct registration passes this helper as its reset callback, and the body emits the literal `ResetDefglobals();` command string. The asm corroborates the pushed reset text before the `sub_476301` call. | c, asm | no |
| `sub_4D977B` | `Fpu_ClearExceptions` | Function | Runtime / FPU | High | The recovered body is just `fnclex`, so the helper's only purpose is clearing pending x87 exception state. | c, asm | no |
| hard late-runtime decompiler sentinels in `sub_4697E0`, `sub_4849EE`, `sub_4842DF`, and `sub_48469F` | quarantined compile-time stubs in `compat/decomp_runtime_stubs.c` | Compile Fix | Build / Runtime Quarantine | Medium | These bodies were isolated parser stops in the late AVI/exception-runtime tail rather than central gameplay logic. Moving them behind explicit stub boundaries let the recovered translation unit compile to an object while keeping the unrecovered behavior visibly deferred. | c, asm | no |
| MSVC inline floating-point residue in `sub_47E910` | explicit C temporaries for profiling/report calculations | Compile Fix | Runtime / Rules Profiling | Medium | The function already computed start/end timestamps, rule counts, and mean fact/instance/activation statistics around the inline `fstp` residue. Rewriting that block with direct C temporaries preserved the surrounding data flow and removed the last parser-only FPU artifact in the function. | c | no |
| raw `MEMORY[0x51ACC4]` absolute accesses | `dword_51ACC4` | Global | Rules / Runtime State | High | Every surviving use is a plain mutable integer-like global in the rule-engine runtime loop. Naming the absolute location as a normal global removed the last raw-address lvalue blocker in that cluster without changing semantics. | c | no |
| under-specified `Parser_ParseExpression(int a1, int a2)` prototype | old-style no-prototype declaration/definition | Compile Fix | Build / Parser Recovery | Medium | The recovered callsites disagree on arity because Hex-Rays spilled a stale temporary into one late parser call. Relaxing the declaration to an old-style K&R form preserved the proven two-argument body and let the translation unit compile without widening the parser subsystem on weak evidence. | c | no |
| missing segmented-pointer compatibility (`MK_FP`, `__ES__`, `__DS__`) | local far-pointer compatibility shim | Compile Fix | Build / Runtime Compatibility | Medium | The late runtime tail still contains 16-bit-flavored `MK_FP` and selector references. Adding conservative local shims was enough to type-check the recovered flat build while keeping the missing real segmented semantics explicit. | c | no |
| incomplete local Win32 runtime declarations (`CONTEXT`, `EXCEPTION_POINTERS`, `CPINFO`, `STARTUPINFOA`, `PROCESS_INFORMATION`) | normalized minimal compatibility declarations in `windows.h` | Compile Fix | Build / Platform Compatibility | High | The recovered runtime and loader wrappers only require a narrow subset of these Win32 types. Filling in the minimal fields used by the code and removing duplicate declarations cleared repeated parser/type conflicts without inventing higher-level behavior. | c | no |

## Deferred / Ambiguous
- `compat/decomp_runtime_stubs.c` is a compile-preserving quarantine layer, not a completed reconstruction. `sub_4697E0`, `sub_4849EE`, `sub_4842DF`, and `sub_48469F` still need asm-backed recovery before the runtime tail can be considered behaviorally faithful.
- The current `MK_FP` shim intentionally models flat-address compileability, not real segmented-memory behavior. Any subsystem that depends on genuine selector semantics should be rechecked against asm before claiming high-confidence runtime fidelity.
- The `sub_47E910` profiling rewrite is compile-safe and locally coherent, but it should still be revisited against asm if the runtime-statistics path becomes a priority.

## Batch 58 – Battle Control And Port Host Rename Wave
| Old Name / Pattern | New Name | Kind | Subsystem | Confidence | Evidence Summary | Sources | Subagent Evidence |
|---|---|---|---|---|---|---|---|
| `sub_425A00` | `UnitBattle_MoveTrack` | Function | Unit Battle / Movement | High | The asm and debug string both expose `UnitBattle_MoveTrack(%d,%d,%d)`, and the body is the main battle pathfinder from the active unit's tile to a requested destination with terrain-cost and diagonal-blocking checks. | c, asm | no |
| `sub_4262D0` | `UnitBattle_MoveTrackNear` | Function | Unit Battle / Movement | High | The debug string is `UnitBattle_MoveTrackNear(%d,%d,%d)`, and the implementation temporarily clears the occupied destination tile, runs the main track builder, then restores the tile state to get an approach path to an adjacent attack position. | c, asm | no |
| `sub_4264D0` | `UnitBattle_MoveTrackNearWall` | Function | Unit Battle / Movement | High | The asm-backed debug string is `UnitBattle_MoveTrackNearWall(%d,%d,%d)`, and the body compares left/right near-wall approach tracks before returning the cheaper one. | c, asm | no |
| `sub_426650` | `UnitBattle_MoveTrackForce` | Function | Unit Battle / Movement | High | The debug string is `UnitBattle_MoveTrackForce(%d,%d,%d)`, and the helper flips the global occupancy-ignore flag before forcing a track through an otherwise blocked tile. | c, asm | no |
| `sub_4266E0` | `UnitBattle_Move` | Function | Unit Battle / Movement | High | The asm-backed debug string is `UnitBattle_Move(%d)`, and the function consumes the precomputed track in slot `+23`, animates step-by-step movement, updates facing and tile occupancy, and deducts action points. | c, asm | no |
| `sub_4272A0` | `UnitBattle_PlayAttackAnimation` | Function | Unit Battle / Combat | High | The debug string is `AttackAnim(%d,%d)`, and the implementation performs the attack lunge, hit/death animation, recoil, and sound timing for attacker and optional defender. | c, asm | no |
| `sub_427FA0` | `UnitBattle_PlayDeathAnimation` | Function | Unit Battle / Combat | High | The debug string is `DeathAnim(%d)`, and the function animates unit death, clears the battlefield occupancy slot, and finalizes corpse/disappearance handling. | c, asm | no |
| `sub_428400` | `UnitBattle_Attack` | Function | Unit Battle / Combat | High | The debug string is `UnitBattle_Attack(%d,%d,%d)`, and the body checks melee eligibility, optionally walks into range, spends 5 action points, computes combat results, and dispatches the attack animation. | c, asm | no |
| `sub_4287E0` | `UnitBattle_IsTileWithinRange` | Function | Unit Battle / Targeting | High | The function computes Euclidean tile distance from the attacker's current coordinates and compares it directly against `g_UnitTypeMaxRange` and `g_UnitTypeMinRange`. | c | no |
| `sub_428850` | `UnitBattle_IsUnitWithinRange` | Function | Unit Battle / Targeting | High | This is a thin wrapper around `UnitBattle_IsTileWithinRange` that substitutes the target unit's current coordinates. | c | no |
| `sub_42E160` | `UnitBattle_UpdateActionTooltip` | Function | Unit Battle / UI | High | The body maps mouse rectangles over the battle sidebar to the localized `off_514DC8` label table and redraws the tooltip only when the hovered action row changes. | c | no |
| `sub_42E3C0` | `UnitBattle_RunTurnLoop` | Function | Unit Battle / Turn Flow | High | It selects the initial controllable unit, restores the saved battle viewport, shows the localized `Your turn` text from `off_514E2C`, and then runs the main battle input/render loop until the turn-exit flag changes. | c | no |
| `sub_430B20` | `UnitBattle_RedrawTile` | Function | Unit Battle / Rendering | High | The helper redraws exactly one visible 64x64 battle tile, clips it against the current 7x7 viewport, and presents the updated rectangle. | c | no |
| `sub_430C20` | `UnitBattle_RedrawVisibleGrid` | Function | Unit Battle / Rendering | High | The function redraws the entire visible 7x7 battle viewport and refreshes the screen margins around the active clipped region. | c | no |
| `sub_430F80` | `UnitBattle_DrawSelectedUnitPanel` | Function | Unit Battle / UI | High | The body renders the selected or explicit unit's battle-side panel: sprite, localized name, quantity/strength, action points, attack rows, morale, fatigue, and related iconography. | c | no |
| `sub_431DE0` | `UnitBattle_RedrawUnitFootprint` | Function | Unit Battle / Rendering | Medium | The helper redraws the base tile plus the extra occupied tiles implied by the current unit type and facing, so it is the oriented-footprint invalidation routine rather than a generic tile redraw. | c, asm | no |
| `sub_431FB0` | `UnitBattle_RedrawUnitNeighborhood` | Function | Unit Battle / Rendering | Medium | The function redraws the unit's current tile and, for large/overhanging units, the whole neighboring footprint neighborhood used by movement and death animation cleanup. | c, asm | no |
| `loc_4527DB` | `Rules_HostCollectPortSupply` | Function / Host Wrapper | Port / Rules Bridge | High | The rules registration binds this wrapper to script name `pobierz_oddzial_z_portu`, and the underlying implementation just calls `Port_GetSupply()` then returns success. | c, asm | yes |
| `loc_452807` | `Rules_HostPortHasSupplyReady` | Function / Host Wrapper | Port / Rules Bridge | Medium | The rules registration binds this wrapper to script name `pelny_port` with boolean return semantics. The wrapper target is still a tiny collapsed thunk, so the host-facing name is clearer than the unresolved implementation body. | c, asm | yes |

## Deferred / Ambiguous
- `UnitBattle_RedrawUnitFootprint` and `UnitBattle_RedrawUnitNeighborhood` are accurate at the redraw/invalidations level, but the exact original designer language for the large-unit footprint flag still needs one more pass through the battle sprite-layout tables.
- `loc_4529BF` still looks like the next safe port-host wrapper rename, but its side-effect of writing the move-track buffer means `CanPathToPort` versus `QueuePathToPort` is not yet settled enough to freeze.

## Batch 59 – Tactical Combat Setup And Visual-Class Recovery Wave
| Old Name / Pattern | New Name | Kind | Subsystem | Confidence | Evidence Summary | Sources | Subagent Evidence |
|---|---|---|---|---|---|---|---|
| `sub_40D6D0` | `Map_RefreshOverviewRegion` | Function | Map / Overview | Medium | The function clamps a rectangle and repaints it by repeatedly calling the single-tile overview helper, so it is the minimap/overview region refresh routine rather than a generic map redraw. | c | yes |
| `sub_40D890` | `Map_UpdateOverviewTile` | Function | Map / Overview | Medium | The body repaints one overview tile from terrain, occupancy, owner color, and visibility state before blitting the updated minimap pixel region. | c | yes |
| `sub_411E20` | `Unit_GetClassIndex` | Function | Units / Visual Class | High | `clash95.map` already labels the symbol at `0x411E20`, and the body is a pure `byte_512577[88 * unitType]` lookup for the stack leader's type. | c, map | yes |
| `sub_41C100` | `UnitSlots_CalcCombatStrengthScore` | Function | Battle Resolution | Medium | Inside battle-result calculation this helper contributes the main positive combat-strength scalar and is reused by stack/garrison strength estimation, but some contributing metadata bytes remain only partially decoded. | c | yes |
| `sub_41C300` | `UnitSlots_CalcDefenseScore` | Function | Battle Resolution | Medium | This is the paired defensive scalar inside `CalculateBattleResult`, returning the side's resistance term used against the opposing combat-strength score. | c | yes |
| `sub_41C360` | `Building_ApplySiegeDamageToWallSections` | Function | Buildings / Siege Combat | High | The helper only mutates the seven wall-section bytes at `building + 422..428`, zeroing or decrementing them based on a siege-damage budget. | c | yes |
| `sub_41C410` | `Building_CalcWallDefenseBonus` | Function | Buildings / Siege Combat | High | The function scans the same seven wall-section bytes, tracks the weakest segments, and converts that into the fortification-defense bonus used by battle-result resolution. | c | yes |
| `sub_41C8B0` | `UI_PromptLeadTroopsPersonally` | Function | Battle / UI | High | The body renders both forces, shows the localized prompt `Do you want to lead the troops on your own?`, and its return value gates entry into the tactical-battle path from both unit-vs-unit and unit-vs-building combat. | c | yes |
| `sub_42E9E0` | `Battle_RunTacticalCombat` | Function | Battle / Turn Flow | High | The function allocates and seeds the battle runtime block, loads the battle map and sprite sets, alternates human/AI turns, calls `HandleBattleResults`, and returns the tactical battle result code. | c | yes |
| `dword_532048` runtime region | `BattleRuntimeState` | Recovered Struct | Battle / Shared State | High | The tactical battle entrypoint, turn loop, redraw helpers, and result-pack path all treat `dword_532048` as one coherent runtime block with viewport, side ids, battle-unit entries, occupant grid, and per-player selection state. | c | yes |
| `UnitTypeMetadataRecord[15].sprite_vertical_offset` | `UnitTypeMetadataRecord[15].visual_class_index` | Recovered Struct | Units / Metadata | Medium | New evidence from `Unit_GetClassIndex` shows `byte_512577` is a reusable per-type class lookup, not safely just a raw Y offset. Rendering still subtracts it from sprite origin and footprint redraws still depend on it, so the conservative recovered name is a visual class. | c, map | yes |

## Deferred / Ambiguous
- `UI_PromptLeadTroopsPersonally` is clearly the manual-battle prompt, but the accept/cancel polarity still needs one direct callback pass before the exact return-value semantics should be documented as fact.
- `UnitSlots_CalcCombatStrengthScore` and `UnitSlots_CalcDefenseScore` are correctly placed in battle resolution, but the exact semantics of the contributing unit-type metadata bytes `+22/+24/+25/+26` are still not ready for stronger field names.
- `visual_class_index` is intentionally conservative. The value clearly affects draw height and footprint invalidation, but it may still encode a broader visual-class enum rather than one single geometric property.

## Batch 60 – Building Garrison Dialog Consistency And State Recovery Wave
| Old Name / Pattern | New Name | Kind | Subsystem | Confidence | Evidence Summary | Sources | Subagent Evidence |
|---|---|---|---|---|---|---|---|
| stale mixed garrison-dialog body references after prior symbol recovery | normalized all live uses to the recovered `BuildingGarrisonDialog_*` function family and dialog-state globals | Compile Fix | Building / Garrison UI | High | The file already carried renamed prototypes and global definitions for the garrison dialog cluster, but fresh GCC compilation still failed because the bodies were calling old `sub_*` / `dword_*` names. Normalizing the live uses restored clean object builds without changing control flow. | c | yes |
| `sub_432910` | `BuildingGarrisonDialog_CountSelectedSlots` | Function | Building / Garrison UI | High | The helper scans the 12-slot selection array and returns the number of selected garrison slots. | c, asm | yes |
| `sub_432940` | `BuildingGarrisonDialog_DrawSlotGrid` | Function | Building / Garrison UI | High | The function redraws the 12-slot barracks grid, including empty-slot art, selection marks, repair/training icons, and pending-exit countdown overlays. | c, asm | yes |
| `sub_432D30` | `BuildingGarrisonDialog_ReloadSlotSprite` | Function | Building / Garrison UI | High | The helper frees and reloads the sprite set for one displayed garrison slot from the building's embedded slot record. | c, asm | yes |
| `sub_432DC0` | `BuildingGarrisonDialog_RebuildSlotSprites` | Function | Building / Garrison UI | High | It reloads all 12 slot sprites and then redraws the whole slot grid. | c, asm | yes |
| `sub_432DF0` | `BuildingGarrisonDialog_TickExitCountdown` | Function | Building / Garrison UI | High | The timed updater advances the leave-barracks countdown and, when it expires, dispatches `Building_UnitsLeave` for the selected slots. | c, asm | yes |
| `sub_432ED0` | `BuildingGarrisonDialog_DrawSelectedUnitPanel` | Function | Building / Garrison UI | High | The body renders the selected garrison unit's detail panel, including localized name, sprite, morale/fatigue/status icons, and special-personage handling. | c, asm | yes |
| `sub_4336C0` | `BuildingGarrisonDialog_ReloadSelectedUnitDetailSprites` | Function | Building / Garrison UI | Medium | It rebuilds the selected-unit detail sprite sheet and then refreshes both the detail panel and the corresponding slot sprite. The dialog role is clear, but the exact original authoring term for the sprite family is still slightly inferential. | c, asm | yes |
| `sub_4337E0` | `BuildingGarrisonDialog_AnimateSelectedUnit` | Function | Building / Garrison UI | High | The helper advances the selected-unit animation frame on a 10-tick cadence and redraws the detail panel. | c, asm | yes |
| `sub_433820` | `BuildingGarrisonDialog_HitTestSlotGrid` | Function | Building / Garrison UI | High | It converts the mouse position into a 0..11 garrison-slot index or `-1` when the cursor is outside the slot grid. | c, asm | yes |
| `sub_4338C0` | `BuildingGarrisonDialog_RequestClose` | Function | Building / Garrison UI | High | The body is wired to the localized `Back` action and only sets the dialog exit signal. | c, asm | yes |
| `sub_4338E0` | `BuildingGarrisonDialog_ShowProductionDialog` | Function | Building / Garrison UI | High | The callback opens the production dialog and then restores the garrison screen assets and slot/detail panels. | c, asm | yes |
| `sub_433A40` | `BuildingGarrisonDialog_ToggleRepairSelectedUnits` | Function | Building / Garrison UI | High | It toggles repair state for each selected garrison slot, clearing existing repair timers or starting repairs when the castle supports them. | c, asm | yes |
| `sub_433AC0` | `BuildingGarrisonDialog_ToggleTrainingSelectedUnits` | Function | Building / Garrison UI | High | It toggles training state for each selected garrison slot, clearing training timers or starting training when the school add-on is available. | c, asm | yes |
| `sub_433B40` | `BuildingGarrisonDialog_BeginSelectedUnitsExit` | Function | Building / Garrison UI | High | It validates the selected slots and adjacent exit availability, then arms the countdown that releases the chosen units from the barracks. | c, asm | yes |
| `sub_433BF0` | `BuildingGarrisonDialog_TickAnimations` | Function | Building / Garrison UI | High | The per-frame callback switches to the dialog resource handle, ticks the selected-unit animation and exit countdown helpers, then restores the prior handle. | c, asm | yes |
| `dword_514FC0` | `BuildingGarrisonDialogActions` | Table | Building / Garrison UI | High | The table is the dialog action-box descriptor block used by the `Back`, `Production`, `Cure`, `Training`, and `Leave barracks` callbacks. | c, asm | yes |
| `dword_53214C` | `g_BuildingGarrisonDialogUseChrTheme` | Global | Building / Garrison UI | High | The flag only selects the `castle_chr` versus `castle_pog` asset/theme branch while rebuilding the dialog. | c, asm | yes |
| `dword_532150` | `g_BuildingGarrisonDialogActiveBuilding` | Global | Building / Garrison UI | High | Every slot-grid, detail-panel, production, repair, training, and leave-barracks helper dereferences this pointer as the currently displayed building record. | c, asm | yes |
| `dword_532158` | `g_BuildingGarrisonDialogSelectedSlots` | Global | Building / Garrison UI | High | The 12-dword array is the shared slot-selection state used by the count, draw, repair, training, and leave helpers. | c, asm | yes |
| `dword_532188` | `g_BuildingGarrisonDialogSelectedSlotIndex` | Global | Building / Garrison UI | High | The selected-unit detail panel and sprite reloaders only use this dword as the currently highlighted garrison slot index. | c, asm | yes |
| `dword_53218C` | `g_BuildingGarrisonDialogSelectedUnitSpriteSet` | Global | Building / Garrison UI | High | The selected-unit panel animates sprites exclusively from this sprite-set handle. | c, asm | yes |
| `dword_532190` | `g_BuildingGarrisonDialogSelectedUnitAnimFrame` | Global | Building / Garrison UI | High | The selected-unit animation helper increments this frame modulo 8 before redrawing the panel. | c, asm | yes |
| `dword_532194` | `g_BuildingGarrisonDialogSlotSpriteSets` | Global | Building / Garrison UI | High | The slot-grid drawer and slot-sprite reloader treat this as the 12-entry array of per-slot sprite-set handles. | c, asm | yes |
| `dword_5321F4` | `g_BuildingGarrisonDialogPendingExitCountdown` | Global | Building / Garrison UI | High | The leave-barracks flow stores the active countdown here, and the slot-grid renderer overlays countdown sprites from the same value. | c, asm | yes |
| `dword_53214C..dword_5321F4` dialog-state region | `BuildingGarrisonDialogState` | Recovered Struct | Building / Garrison UI | High | The recovered globals form one coherent modal-dialog state block: theme flag, active building, 12 selection flags, selected slot, selected/detail sprite state, per-slot sprite handles, and the pending leave countdown. | c, asm | yes |
| local `ICSendMessage` redeclaration in `clash95.c` | header-owned `windows.h` declaration only | Compile Fix | Build / Win32 Surface | High | The live `windows.h` prototype already carries the full 5-argument import signature. Removing the duplicate local declaration makes the import single-source without changing calls or object output. | c | yes |

## Deferred / Ambiguous
- `BuildingGarrisonDialog_ReloadSelectedUnitDetailSprites` is still the weakest name in the batch. The detail-sprite reload role is clear, but the exact original authoring label for that sprite family is not yet cross-checked against asset names.
- The hidden runner recovered by Aristotle as `BuildingGarrisonDialog_Run` is not yet exposed as a textual symbol in `clash95.c`, so this batch left that entry for a future write once the decompiled label is surfaced cleanly.
- The garrison dialog still contains several undefined-temporary scars (`v6`, `v7`, `v8`, `v12`, `v13`) that compile today only because the broader TU is still being built under permissive decompiler-style assumptions. They are not semantic blockers, but they remain cleanup candidates if a stricter compiler mode becomes a goal.

## Batch 60 – Garrison Dialog And Agenda Loop Rename Wave
| Old Name / Pattern | New Name | Kind | Subsystem | Confidence | Evidence Summary | Sources | Subagent Evidence |
|---|---|---|---|---|---|---|---|
| `sub_47E910` | `Rules_RunAgendaLoop` | Function | Rules / Agenda | High | The body is the core rule-firing loop: it emits `FIRE` trace lines, honors breakpoints and rule-firing limits, updates watch/profiling counters, and is called by both the `run` host wrapper and gameplay-side rule execution paths. Darwin's asm/map pass also places it between other public agenda/rule symbols. | c, asm, map | yes |
| `sub_432910` | `BuildingGarrisonDialog_CountSelectedSlots` | Function | Building / Garrison Dialog | High | The helper just counts the 12 per-slot selection flags that drive repair, training, and exit actions. | c, asm | yes |
| `sub_432940` | `BuildingGarrisonDialog_DrawSlotGrid` | Function | Building / Garrison Dialog | High | The body draws all 12 garrison slots, overlays repair/training/exit markers, and highlights currently selected slots. | c, asm | yes |
| `sub_432D30` | `BuildingGarrisonDialog_ReloadSlotSprite` | Function | Building / Garrison Dialog | High | This helper frees and reloads the per-slot sprite-set for one garrison slot, then stores it in the dialog state array. | c, asm | yes |
| `sub_432DC0` | `BuildingGarrisonDialog_RebuildSlotSprites` | Function | Building / Garrison Dialog | High | It reloads all 12 slot sprite-sets and immediately redraws the slot grid. | c, asm | yes |
| `sub_432DF0` | `BuildingGarrisonDialog_TickExitCountdown` | Function | Building / Garrison Dialog | High | The timer-driven helper advances slot animation frames, counts down the staged `Leave barracks` exit sequence, and dispatches `Building_UnitsLeave` when the countdown completes. | c, asm | yes |
| `sub_432ED0` | `BuildingGarrisonDialog_DrawSelectedUnitPanel` | Function | Building / Garrison Dialog | High | The function renders the right-hand detail pane for the currently selected garrison slot, including the special-personage portrait branch for unit types `33/34` and the normal unit stats/status rows. | c, asm | yes |
| `sub_4336C0` | `BuildingGarrisonDialog_ReloadSelectedUnitDetailSprites` | Function | Building / Garrison Dialog | Medium | It rebuilds the selected-unit sprite-set used by the detail pane, refreshes palette state, redraws the panel, and then reloads the corresponding slot sprite. | c, asm | yes |
| `sub_4337E0` | `BuildingGarrisonDialog_AnimateSelectedUnit` | Function | Building / Garrison Dialog | High | This is the timer-gated frame-stepper for the selected-unit detail sprite animation. | c, asm | yes |
| `sub_433820` | `BuildingGarrisonDialog_HitTestSlotGrid` | Function | Building / Garrison Dialog | High | The helper converts the current mouse position into one of the 12 garrison slot indices or `-1`. | c, asm | yes |
| `sub_4338C0` | `BuildingGarrisonDialog_RequestClose` | Function | Building / Garrison Dialog | High | Wired to the `Back` action and only flips the dialog-exit state. | c, asm | yes |
| `sub_4338E0` | `BuildingGarrisonDialog_ShowProductionDialog` | Function | Building / Garrison Dialog | High | Wired to the `Production` action; it opens the production dialog and then restores the garrison screen art and actions. | c, asm | yes |
| `sub_433A40` | `BuildingGarrisonDialog_ToggleRepairSelectedUnits` | Function | Building / Garrison Dialog | High | The callback toggles repair state for all selected slots, clearing existing repair timers or starting repair as needed. | c, asm | yes |
| `sub_433AC0` | `BuildingGarrisonDialog_ToggleTrainingSelectedUnits` | Function | Building / Garrison Dialog | High | The callback toggles training state for all selected slots, clearing training timers or starting training as needed. | c, asm | yes |
| `sub_433B40` | `BuildingGarrisonDialog_BeginSelectedUnitsExit` | Function | Building / Garrison Dialog | High | The callback validates adjacent exit space, gathers the selected slots, starts the close/countdown flow, and stages `Building_UnitsLeave`. | c, asm | yes |
| `sub_433BF0` | `BuildingGarrisonDialog_TickAnimations` | Function | Building / Garrison Dialog | High | The frame tick temporarily switches the resource handle, advances the selected-unit animation, advances the pending exit countdown, and restores the prior handle. | c, asm | yes |
| `dword_514FC0` | `BuildingGarrisonDialogActions` | Table | Building / Garrison Dialog | High | Aristotle's asm pass tied this table to the `Back`, `Production`, `Cure`, `Training`, and `Leave barracks` action descriptors that drive the dialog. | c, asm | yes |
| `dword_53214C` | `g_BuildingGarrisonDialogUseChrTheme` | Global | Building / Garrison Dialog | Medium | The flag only selects between the `castle_chr_*.s32` and `castle_pog_*.s32` art/resource families when the dialog is restored after production. | c, asm | yes |
| `dword_532150` | `g_BuildingGarrisonDialogActiveBuilding` | Global | Building / Garrison Dialog | High | Every slot-grid, detail-panel, repair, training, and exit helper indexes the same active building record through this global. | c, asm | yes |
| `dword_532158` | `g_BuildingGarrisonDialogSelectedSlots` | Global | Building / Garrison Dialog | High | This 12-entry flag array is the shared selection mask used by the slot counter, slot-grid renderer, repair/training toggles, and exit staging. | c, asm | yes |
| `dword_532188` | `g_BuildingGarrisonDialogSelectedSlotIndex` | Global | Building / Garrison Dialog | High | The selected slot index gates whether the right-hand detail panel and selected-unit sprite-set are active. | c, asm | yes |
| `dword_53218C` | `g_BuildingGarrisonDialogSelectedUnitSpriteSet` | Global | Building / Garrison Dialog | High | This global owns the sprite-set used by the selected-unit detail pane and is freed/reloaded as the selection changes. | c, asm | yes |
| `dword_532190` | `g_BuildingGarrisonDialogSelectedUnitAnimFrame` | Global | Building / Garrison Dialog | High | The selected-unit animation tick advances this frame index modulo 8 and the detail pane renders directly from it. | c, asm | yes |
| `dword_532194` | `g_BuildingGarrisonDialogSlotSpriteSets` | Global | Building / Garrison Dialog | High | This array stores the 12 per-slot sprite-set handles used by the left-hand garrison grid. | c, asm | yes |
| `dword_5321F4` | `g_BuildingGarrisonDialogPendingExitCountdown` | Global | Building / Garrison Dialog | High | The `Leave barracks` flow increments this countdown and only calls `Building_UnitsLeave` when it reaches the terminal staged frame. | c, asm | yes |
| garrison dialog state region `dword_53214C .. dword_5321F4` | `BuildingGarrisonDialogState` | Recovered Struct | Building / Garrison Dialog | High | The localized garrison helpers treat the theme flag, active building pointer, selected-slot mask, selected-slot index, selected-unit sprite/frame state, per-slot sprite-set array, and pending-exit countdown as one coherent dialog-state block. | c, asm | yes |

## Deferred / Ambiguous
- `g_BuildingGarrisonDialogUseChrTheme` is intentionally conservative. The flag clearly selects the `chr` versus `pog` asset family, but this pass still did not prove the original designer-facing meaning behind that split.
- `BuildingGarrisonDialog_ReloadSelectedUnitDetailSprites` is the safest medium-confidence name in the family. Its sprite/palette refresh role is clear, but the exact original separation between “detail sprites”, “panel sprites”, and palette setup helpers is still somewhat decompiler-shaped.

## Batch 60 – Helper Surface Cleanup Wave
| Old Name / Pattern | New Name | Kind | Subsystem | Confidence | Evidence Summary | Sources | Subagent Evidence |
|---|---|---|---|---|---|---|---|
| duplicate `MK_FP` / `__ES__` / `__DS__` compatibility declarations in `defs.h` | unified root-header compatibility authority | Compile Fix | Build / Header Surface | High | The active translation units only include the root headers, but `defs.h` still carried duplicated segment-shim declarations and two conflicting `MK_FP` macros. Collapsing them into one authority removed drift without widening any runtime semantics. | c | yes |
| missing helper macros `SBYTE1`, `SLODWORD`, `__PAIR64__`, and `__CFSHL__` | restored root-header Hex-Rays helper surface | Compile Fix | Build / Decompiler Helpers | High | `nm -u` on the object build showed these names surviving as unresolved symbols even though every use was a decompiler helper pattern, not a real external function. Restoring them in `defs.h` eliminated helper-only unresolved references while preserving the current C data flow. | c | yes |
| untyped `ICSendMessage()` declaration in `windows.h` | normalized 5-argument `ICSendMessage(DWORD,DWORD,DWORD,DWORD,DWORD)` prototype | Compile Fix | Build / Win32 Compatibility | High | The live code already declares and calls `ICSendMessage` with five DWORD-like arguments, so matching the header to the recovered call surface removed a stale declaration mismatch without inventing new behavior. | c | yes |

## Deferred / Ambiguous
- `JUMPOUT` still survives as a real unresolved branch escape in the object output. It was intentionally not stubbed in this wave because those sites are behavior-bearing control-flow fractures, not inert helper macros.
- The restored `__PAIR64__` and `__CFSHL__` helpers are compile-faithful Hex-Rays shims, but a future behavior-fidelity pass should still verify edge-case signedness against asm in the few arithmetic-heavy runtime clusters that remain.

## Batch 61 – Building Garrison Dialog Rename Wave
| Old Name / Pattern | New Name | Kind | Subsystem | Confidence | Evidence Summary | Sources | Subagent Evidence |
|---|---|---|---|---|---|---|---|
| `sub_432910` | `BuildingGarrisonDialog_CountSelectedSlots` | Function | Buildings / Garrison Dialog | High | The helper scans the 12-slot selection array and returns the number of checked slots; it is used by the slot-grid renderer to decide whether selection toggles are still allowed. | c, asm | yes |
| `sub_432940` | `BuildingGarrisonDialog_DrawSlotGrid` | Function | Buildings / Garrison Dialog | High | The body redraws the 12-slot barracks grid, overlays repair/training badges, selection markers, and pending-exit countdown frames, and optionally redraws only one slot. | c, asm | yes |
| `sub_432D30` | `BuildingGarrisonDialog_ReloadSlotSprite` | Function | Buildings / Garrison Dialog | High | The function frees and reloads the sprite set for one garrison slot based on the current unit type in the active building. | c, asm | yes |
| `sub_432DC0` | `BuildingGarrisonDialog_RebuildSlotSprites` | Function | Buildings / Garrison Dialog | High | This is a thin loop over all 12 slots that reloads their sprite sets and then redraws the slot grid. | c, asm | yes |
| `sub_432DF0` | `BuildingGarrisonDialog_TickExitCountdown` | Function | Buildings / Garrison Dialog | High | The timer tick advances the leave-barracks countdown, gathers the selected slots when it expires, and dispatches `Building_UnitsLeave`. | c, asm | yes |
| `sub_432ED0` | `BuildingGarrisonDialog_DrawSelectedUnitPanel` | Function | Buildings / Garrison Dialog | High | The function renders the large detail panel for the currently selected garrison unit, including name, stats, morale/status icons, and the animated unit sprite. | c, asm | yes |
| `sub_4336C0` | `BuildingGarrisonDialog_ReloadSelectedUnitDetailSprites` | Function | Buildings / Garrison Dialog | Medium | The body frees/reloads the selected-unit sprite set, updates the detail panel art resources, redraws the detail panel, and refreshes the corresponding slot sprite. The detail-sprite role is clear even though the exact asset-layer wording remains slightly inferred. | c, asm | yes |
| `sub_4337E0` | `BuildingGarrisonDialog_AnimateSelectedUnit` | Function | Buildings / Garrison Dialog | High | The helper advances the selected-unit animation frame on a time gate and redraws the detail panel. | c, asm | yes |
| `sub_433820` | `BuildingGarrisonDialog_HitTestSlotGrid` | Function | Buildings / Garrison Dialog | High | The function converts cursor coordinates into one of the 12 slot-grid indices or `-1` if the cursor lies outside the slot rectangles. | c, asm | yes |
| `sub_4338C0` | `BuildingGarrisonDialog_RequestClose` | Function | Buildings / Garrison Dialog | High | The action-table callback is bound to the localized `Back` button and only signals dialog exit. | c, asm | yes |
| `sub_4338E0` | `BuildingGarrisonDialog_ShowProductionDialog` | Function | Buildings / Garrison Dialog | High | The callback is bound to `Production`, opens the production dialog, then rebuilds and redraws the garrison screen after returning. | c, asm | yes |
| `sub_433A40` | `BuildingGarrisonDialog_ToggleRepairSelectedUnits` | Function | Buildings / Garrison Dialog | High | The callback toggles repair state for all selected slots by either clearing the repair timer or invoking `Building_RepairUnit`. | c, asm | yes |
| `sub_433AC0` | `BuildingGarrisonDialog_ToggleTrainingSelectedUnits` | Function | Buildings / Garrison Dialog | High | The callback toggles training state for all selected slots by either clearing the training timer or invoking `Building_TrainUnit`. | c, asm | yes |
| `sub_433B40` | `BuildingGarrisonDialog_BeginSelectedUnitsExit` | Function | Buildings / Garrison Dialog | High | The callback gathers selected slots, validates an adjacent exit tile, arms the leave-barracks countdown, and switches the modal button state to `close`. | c, asm | yes |
| `sub_433BF0` | `BuildingGarrisonDialog_TickAnimations` | Function | Buildings / Garrison Dialog | High | The per-frame tick temporarily switches the dialog resource handle, then advances the selected-unit animation and pending-exit countdown together. | c, asm | yes |
| `dword_514FC0` | `BuildingGarrisonDialogActions` | Table | Buildings / Garrison Dialog | High | The table is passed into the dialog action-box builder and the asm-backed action labels show it owns the `Back`, `Production`, `Cure`, `Training`, and `Leave barracks` callbacks. | c, asm | yes |
| `dword_53214C` | `g_BuildingGarrisonDialogUseChrTheme` | Global | Buildings / Garrison Dialog | High | This flag chooses between the `castle_chr` and `castle_pog` resource families when the dialog rebuilds its screen art after production changes. | c, asm | yes |
| `dword_532150` | `g_BuildingGarrisonDialogActiveBuilding` | Global | Buildings / Garrison Dialog | High | Every slot, timer, and callback dereferences this pointer-sized global as the currently open building record. | c, asm | yes |
| `dword_532158` | `g_BuildingGarrisonDialogSelectedSlots` | Global | Buildings / Garrison Dialog | High | The 12-entry array is the shared selection bitmap for barracks slots. | c, asm | yes |
| `dword_532188` | `g_BuildingGarrisonDialogSelectedSlotIndex` | Global | Buildings / Garrison Dialog | High | The detail-panel renderer and detail-sprite loader both treat this dword as the currently focused slot index. | c, asm | yes |
| `dword_53218C` | `g_BuildingGarrisonDialogSelectedUnitSpriteSet` | Global | Buildings / Garrison Dialog | High | The detail-panel renderer and reload helper use this as the sprite-set handle for the currently focused unit. | c, asm | yes |
| `dword_532190` | `g_BuildingGarrisonDialogSelectedUnitAnimFrame` | Global | Buildings / Garrison Dialog | High | The selected-unit animation tick increments this frame counter modulo eight before redrawing the detail panel. | c, asm | yes |
| `dword_532194` | `g_BuildingGarrisonDialogSlotSpriteSets` | Global | Buildings / Garrison Dialog | High | The slot-grid renderer indexes this contiguous array of sprite-set handles for the 12 garrison slots. | c, asm | yes |
| `dword_5321F4` | `g_BuildingGarrisonDialogPendingExitCountdown` | Global | Buildings / Garrison Dialog | High | The leave-barracks flow sets this countdown, the slot grid displays it, and the timer helper consumes it to trigger `Building_UnitsLeave`. | c, asm | yes |
| contiguous state around `dword_53214C..dword_5321F4` | `BuildingGarrisonDialogState` | Recovered Struct | Buildings / Garrison Dialog | High | The dialog callbacks, renderers, and timer helpers all share one stable state region covering theme choice, active building, selection state, selected-unit animation state, slot sprite sets, and the pending-exit countdown. | c, asm | yes |

## Deferred / Ambiguous
- The dialog runner entrypoint that Aristotle traced as `loc_433C20` still does not surface as a clean top-level function symbol in the current decompiled C. The subsystem wave therefore stops at the helpers and shared state that are directly recoverable in-source.
- `dword_532154`, `dword_5321C0`, and `dword_5321C4` are clearly adjacent garrison-dialog resource/animation state, but this batch left them unnamed because their exact roles were not yet strong enough to freeze beyond the already renamed core state.

## Batch 62 – MiniMap Semantics Tightening Wave
| Old Name / Pattern | New Name | Kind | Subsystem | Confidence | Evidence Summary | Sources | Subagent Evidence |
|---|---|---|---|---|---|---|---|
| `sub_40D330` | `MiniMap_CreateSurface` | Function | Map / MiniMap | High | The function computes minimap pixel scale from world dimensions, derives the on-screen rect, allocates the backing surface, stamps the frame art, and seeds the terrain-color lookup used by the tile drawer. | c | yes |
| `sub_40D560` | `MiniMap_BlitDirtyRectAndDrawViewportBox` | Function | Map / MiniMap | High | The helper copies a dirty rectangle from the minimap surface to the main render target, then draws the current world viewport box over the minimap using `gameData + 140008/+140012`. | c | yes |
| `Map_RefreshOverviewRegion` | `MiniMap_RedrawTileRect` | Function | Map / MiniMap | High | New subsystem evidence shows this routine is specifically the minimap rect refresh path: it clamps a tile rectangle, redraws each minimap cell in the region, then blits the corresponding minimap dirty rect. The earlier `Overview` name was accurate but too generic. | c | yes |
| `sub_40D850` | `MiniMap_RedrawAllTiles` | Function | Map / MiniMap | High | The body iterates every world tile and redraws the whole minimap surface before jumping back into the shared post-redraw surface path. | c | yes |
| `Map_UpdateOverviewTile` | `MiniMap_DrawTileCell` | Function | Map / MiniMap | High | The function chooses the minimap cell color from terrain, unit owner, building owner, and fog-of-war state, then paints the corresponding minimap cell. The prior `OverviewTile` name understated that this is the per-cell minimap renderer. | c | yes |
| `sub_40DC10` | `MiniMap_UpdateViewportFromCursor` | Function | Map / MiniMap | High | It converts cursor position inside the minimap into new world viewport origin coordinates, clamps them to map bounds, and triggers a world-map redraw. | c | yes |
| `sub_40DD60` | `MiniMap_IsCursorInside` | Function | Map / MiniMap | High | The helper only checks whether the current cursor lies inside the live minimap screen rectangle while the minimap is enabled. | c | yes |
| minimap state cluster (`word_523344/346/348/34A`, `dword_52334C`, `byte_523F54`, `dword_523F50`, `dword_511FF0`) | `MiniMapState` | Recovered Struct | Map / MiniMap | High | These globals form one coherent minimap runtime block: on-screen rect, backing surface, pixels-per-tile scale, terrain-color lookup, and draw-layer mask. The create/draw/blit helpers share them as one subsystem, not isolated scalars. | c | yes |

## Deferred / Ambiguous
- `sub_40D800` is clearly part of the minimap hit/intersection path, but the currently recovered predicate only exposes part of the boundary logic, so a stronger public name is still deferred.
- `MiniMap_RedrawAllTiles` still ends in a `JUMPOUT(0x40D410)` control-flow scar. The minimap semantics are high-confidence, but the exact post-redraw structural split with `MiniMap_CreateSurface` still needs asm if we later want to remove the `JUMPOUT`.

## Batch 60 – Garrison Dialog And Helper Surface Wave
| Old Name / Pattern | New Name | Kind | Subsystem | Confidence | Evidence Summary | Sources | Subagent Evidence |
|---|---|---|---|---|---|---|---|
| unresolved active helper macros (`SBYTE1`, `SLODWORD`, `__PAIR64__`, `__CFSHL__`) and duplicate selector shims | normalized Hex-Rays helper surface in `defs.h` | Compile Fix | Build / Compatibility | High | Rebuilding `/tmp/clash95.o` after the helper/header cleanup removed the leaked helper undefineds, shrinking the unresolved import surface from 532 to 527 and leaving only real external calls plus `JUMPOUT`. The active root header is now the authoritative helper surface instead of the stale compat copy. | c | yes |
| old-style `ICSendMessage()` declaration | full 5-argument `ICSendMessage(DWORD,DWORD,DWORD,DWORD,DWORD)` prototype | Compile Fix | Build / Platform Compatibility | High | The recovered callsites all pass five dword-like arguments, and the new prototype matches the already recovered local declaration in `clash95.c` without widening behavior. | c | yes |
| `sub_432910` | `BuildingGarrisonDialog_CountSelectedSlots` | Function | Buildings / Garrison Dialog | High | The body is a pure 12-slot scan over the dialog selection array and is only used to cap multiselect behavior in the slot grid renderer. | c, asm | yes |
| `sub_432940` | `BuildingGarrisonDialog_DrawSlotGrid` | Function | Buildings / Garrison Dialog | High | The function draws the 12 garrison slots, overlays selection/repair/training/exit-countdown markers, and is the common redraw path after every garrison-dialog action. | c, asm | yes |
| `sub_432D30` | `BuildingGarrisonDialog_ReloadSlotSprite` | Function | Buildings / Garrison Dialog | High | It frees/reloads exactly one slot sprite-set based on the current unit in the active building garrison, then stores the handle in the per-slot sprite array. | c, asm | yes |
| `sub_432DC0` | `BuildingGarrisonDialog_RebuildSlotSprites` | Function | Buildings / Garrison Dialog | High | This helper iterates all 12 slots through `BuildingGarrisonDialog_ReloadSlotSprite` and then redraws the full slot grid. | c, asm | yes |
| `sub_432DF0` | `BuildingGarrisonDialog_TickExitCountdown` | Function | Buildings / Garrison Dialog | High | The function advances the leave-barracks countdown, animates per-slot frames, finalizes `Building_UnitsLeave`, clears selection, and redraws the slot grid. | c, asm | yes |
| `sub_432ED0` | `BuildingGarrisonDialog_DrawSelectedUnitPanel` | Function | Buildings / Garrison Dialog | High | The body renders the selected slot's localized name, hero/non-hero portrait framing, stat labels, fatigue/morale/order indicators, and idle sprite animation in the right-hand detail panel. | c, asm | yes |
| `sub_4336C0` | `BuildingGarrisonDialog_ReloadSelectedUnitDetailSprites` | Function | Buildings / Garrison Dialog | Medium | It reloads the selected-unit detail sprite set, refreshes palette data, redraws the selected-unit panel, and refreshes the selected slot sprite. The role is clear, though the exact original art-pipeline terminology remains uncertain. | c, asm | yes |
| `sub_4337E0` | `BuildingGarrisonDialog_AnimateSelectedUnit` | Function | Buildings / Garrison Dialog | High | The helper advances the selected-unit animation frame on a timer and redraws the selected-unit panel. | c, asm | yes |
| `sub_433820` | `BuildingGarrisonDialog_HitTestSlotGrid` | Function | Buildings / Garrison Dialog | High | It converts mouse position into a 0..11 slot index only when the cursor lands inside the 6x2 slot grid bounds. | c, asm | yes |
| `sub_4338C0` | `BuildingGarrisonDialog_RequestClose` | Function | Buildings / Garrison Dialog | High | The button callback only delegates to the generic action-box closer and stores the exit signal, matching the localized `Back/Koniec` action row in the dialog table. | c, asm | yes |
| `sub_4338E0` | `BuildingGarrisonDialog_ShowProductionDialog` | Function | Buildings / Garrison Dialog | High | The action row is localized as `Production`, and the body temporarily opens the production screen before restoring the garrison dialog artwork and action table. | c, asm | yes |
| `sub_433A40` | `BuildingGarrisonDialog_ToggleRepairSelectedUnits` | Function | Buildings / Garrison Dialog | High | The callback iterates selected slots and toggles each unit between repair-in-progress and `Building_RepairUnit`, gated by the building's repair addon bit. | c, asm | yes |
| `sub_433AC0` | `BuildingGarrisonDialog_ToggleTrainingSelectedUnits` | Function | Buildings / Garrison Dialog | High | The callback iterates selected slots and toggles each unit's training timer versus `Building_TrainUnit`, gated by the building's school/training addon bit. | c, asm | yes |
| `sub_433B40` | `BuildingGarrisonDialog_BeginSelectedUnitsExit` | Function | Buildings / Garrison Dialog | High | It validates that at least one slot is selected and that the building has a free adjacent exit tile, then starts the leave-barracks countdown and close notice. | c, asm | yes |
| `sub_433BF0` | `BuildingGarrisonDialog_TickAnimations` | Function | Buildings / Garrison Dialog | High | The per-frame dialog tick simply swaps the temporary resource handle, advances the selected-unit animation, advances the exit countdown, and restores the handle. | c, asm | yes |
| `dword_514FC0` | `BuildingGarrisonDialogActions` | Table | Buildings / Garrison Dialog | High | The data table encodes the five action-box entries for `Back`, `Production`, `Cure`, `Training`, and `Leave barracks`, each pointing at the matching recovered callback. | c, asm | yes |
| `dword_53214C` | `g_BuildingGarrisonDialogUseChrTheme` | Global | Buildings / Garrison Dialog | High | The runner and production-return path branch on this flag to pick `castle.chr\\dw_12...` versus `castle.pog\\dw_12...` assets, so it is a theme/culture selector rather than a generic state bit. | c, asm | yes |
| `dword_532150` | `g_BuildingGarrisonDialogActiveBuilding` | Global | Buildings / Garrison Dialog | High | All dialog callbacks dereference this pointer as the currently displayed building record for garrison slots, addon flags, and leave/repair/train actions. | c, asm | yes |
| `dword_532158` | `g_BuildingGarrisonDialogSelectedSlots` | Global | Buildings / Garrison Dialog | High | This 12-entry array is the shared selection bitset for the slot grid, action callbacks, and exit countdown path. | c, asm | yes |
| `dword_532188` | `g_BuildingGarrisonDialogSelectedSlotIndex` | Global | Buildings / Garrison Dialog | High | The selected-unit detail panel and sprite reload path both use this dword as the currently focused slot index, with `-1` meaning no selection. | c, asm | yes |
| `dword_53218C` | `g_BuildingGarrisonDialogSelectedUnitSpriteSet` | Global | Buildings / Garrison Dialog | High | The selected-unit panel draws animated frames from this handle, and the detail-sprite reload helper frees/rebuilds it when selection changes. | c, asm | yes |
| `dword_532190` | `g_BuildingGarrisonDialogSelectedUnitAnimFrame` | Global | Buildings / Garrison Dialog | High | The selected-unit animator advances this scalar modulo 8 before redrawing the detail panel. | c, asm | yes |
| `dword_532194` | `g_BuildingGarrisonDialogSlotSpriteSets` | Global | Buildings / Garrison Dialog | High | The slot-grid renderer indexes this array per slot, and the sprite reload helpers refill it from the active building's garrison entries. | c, asm | yes |
| `dword_5321F4` | `g_BuildingGarrisonDialogPendingExitCountdown` | Global | Buildings / Garrison Dialog | High | The slot grid overlays the countdown sprites from this value and the timed leave helper decrements/clears it around `Building_UnitsLeave`. | c, asm | yes |

## Deferred / Ambiguous
- `loc_433C20` is still best described as `BuildingGarrisonDialog_Run`, but the current `clash95.c` output does not expose it as a standalone function symbol to rename safely. The asm evidence is strong; the live C rename should wait until that label/function boundary is reconstructed cleanly.
- `BuildingGarrisonDialog_ReloadSelectedUnitDetailSprites` is medium confidence because the behavior is clear but the exact original art-pipeline wording could still have been narrower than `DetailSprites`.
- `g_BuildingGarrisonDialogUseChrTheme` is intentionally concrete about the observed `castle.chr` branch. If later asset research proves the flag is a broader religion/culture enum, the global should be widened then rather than guessed now.

## Batch 63 – Pathing Helper And Movement Profile Wave
| Old Name / Pattern | New Name | Kind | Subsystem | Confidence | Evidence Summary | Sources | Subagent Evidence |
|---|---|---|---|---|---|---|---|
| `sub_413F50` | `UnitStack_GetTileMoveCostFromMergedProfileOrZero` | Function | Movement / Pathing | High | The helper is called only from `Unit_MoveTrack` after `UnitStack_BuildMergedTerrainMoveProfile`, consumes the premerged byte slice instead of rebuilding it, and returns the same blocked-or-cost result shape as `UnitStack_GetTileMoveCostOrZero`. | c, asm | no |
| `sub_415D80` | `Math_SinDegreesQ16` | Helper | Math / Animation | High | The function normalizes signed degree inputs into `0..359`, indexes the 360-entry trig table at `dword_513434`, and callers scale the result with `>> 16`, which matches a fixed-point sine helper rather than a gameplay-specific unknown. | c | no |
| `sub_415E40` | `Math_CeilSqrt` | Helper | Math / Distance | High | The Newton-style integer routine is fed almost exclusively with `dx*dx + dy*dy` distance-squared terms and returns the ceiling of the square root, which is then compared against reveal, range, and proximity radii. | c | no |
| `UnitTypeMetadataRecord + 29..37` and stack-local profile buffers | `MergedTerrainMoveProfile` | Recovered Struct | Movement / Pathing | High | `UnitStack_BuildMergedTerrainMoveProfile` merges metadata bytes `+29..+37` from every occupied slot into a temporary buffer, and `UnitStack_GetTileMoveCostFromMergedProfileOrZero` consumes that exact slice as one pathing record covering road and terrain movement costs. | c | no |

## Deferred / Ambiguous
- `Building_GenerateApproachTrack` and `Building_GenerateNearApproachTrack` match the asm debug strings `Unit_MoveTrackToBuilding` and `Unit_MoveTrackNearBuilding`, but this batch left the existing names intact because they were already descriptive and not compile-blocking.

## Batch 64 – MiniMap Action And Player UI State Wave
| Old Name / Pattern | New Name | Kind | Subsystem | Confidence | Evidence Summary | Sources | Subagent Evidence |
|---|---|---|---|---|---|---|---|
| `sub_40D430` | `MiniMap_DestroySurface` | Function | Map / MiniMap | High | The body is a thin destructor for the minimap backing surface: it checks `g_MiniMapSurface` and forwards to the surface vtable destroy slot when present. The declaration was already recovered; this wave aligned the live definition and callsites with it. | c, asm | no |
| `sub_40DD30` | `MiniMap_ToggleVisibility` | Function | Map / MiniMap | High | The helper only XORs the per-player dword at `PlayerRuntimeState + 23` and triggers a redraw, matching the map-screen hotkey/menu behavior for toggling the minimap on and off. | c, asm | no |
| `sub_40DDE0` | `MiniMap_ShowAllLayers` | Function | Map / MiniMap | High | The asm action table binds this callback to `Wszystko / All / Alles`, and the implementation sets the minimap-visible flag plus draw mask `7`, enabling terrain, units, and buildings together. | c, asm | no |
| `sub_40DE20` | `MiniMap_ShowUnitsOnly` | Function | Map / MiniMap | High | The asm action table binds this callback to `Jednostki / Units / Einheiten`, and the implementation sets the minimap-visible flag plus draw mask `2`, which is the unit-only overlay path used by `MiniMap_DrawTileCell`. | c, asm | no |
| `sub_40DE60` | `MiniMap_ShowBuildingsOnly` | Function | Map / MiniMap | High | The asm action table binds this callback to `Budynki / Buildings / Gebäude`, and the implementation sets the minimap-visible flag plus draw mask `4`, matching the building-owner overlay branch in `MiniMap_DrawTileCell`. | c, asm | no |
| `sub_40DEA0` | `MiniMap_Hide` | Function | Map / MiniMap | High | The asm action table binds this callback to `Nic / None / Keine`, and the body only clears the same per-player minimap-visible flag before triggering a redraw. | c, asm | no |
| `PlayerRuntimeState + 23` | `PlayerRuntimeState.minimap_visible_flag` | Recovered Struct Field | Map / MiniMap | High | The minimap show/hide/toggle actions mutate this dword, and the minimap blit, hit-test, and viewport-drag helpers all early-out on the same viewed-player field. Scenario setup also seeds player 0 with the flag enabled. | c, asm | no |
| `PLAYER_MINIMAP_VISIBLE_OFFSET / PLAYER_MINIMAP_VISIBLE` | `PLAYER_MINIMAP_VISIBLE_OFFSET / PLAYER_MINIMAP_VISIBLE` | Helper | Map / MiniMap | High | Promoting the recovered `PlayerRuntimeState + 23` field to a shared macro removes repeated raw `+140047` arithmetic and ties the minimap UI code back to the central game-state model without changing behavior. | c | no |

## Deferred / Ambiguous
- `sub_40D800` remains deferred. The asm confirms it gates part of the minimap screen-space intersection path, but the current C still only exposes a partial rectangle predicate, so freezing a narrower public name would overstate what is proven.

## Batch 64 – World Map Unit Render Wave
| Old Name / Pattern | New Name | Kind | Subsystem | Confidence | Evidence Summary | Sources | Subagent Evidence |
|---|---|---|---|---|---|---|---|
| `sub_415F20` | `WorldMap_DrawUnitStackWithOverlays` | Function | World Map / Rendering | High | The helper is called from the world-map tile renderer for the main tile and neighboring overhang tiles, selects the unit sprite, applies the attention-flash alpha pulse, and draws the squad-count, low-morale, hidden, and debug-turn overlays for that stack. | c, asm | no |

## Batch 63 – Battle Wall And Strategic Host Sync Wave
| Old Name / Pattern | New Name | Kind | Subsystem | Confidence | Evidence Summary | Sources | Subagent Evidence |
|---|---|---|---|---|---|---|---|
| `sub_4283D0` | `UnitBattle_GetCorpseSpriteIndex` | Function | Unit Battle / Animation | Medium | The helper returns `byte_5125B6[88 * type] + ((facing + 4) % 8)`, and callers only store that value into the battle corpse overlay bytes after a unit dies. This batch syncs the previously logged recovery into live `clash95.c`. | c | yes |
| `sub_428880` | `UnitBattle_PlayShotAnimation` | Function | Unit Battle / Ranged Animation | High | The preserved `_ShotAnim(%d,%d,%d,%d,%d,%d)` trace, projectile sprite loading, and shared unit/wall hit handoff already established the role; this batch applies that high-confidence name in-source. | c, asm, map | yes |
| `sub_4298E0` | `UnitBattle_AttackWall` | Function | Unit Battle / Siege Combat | High | The asm/exe debug string `UnitBattle_AttackWall(%d,%d,%d)` plus the body's near-wall pathing, 5-AP spend, siege-damage application, and wall redraw/audio path prove this is the melee wall-attack helper. | c, asm, exe | yes |
| `sub_452390` | `Rules_RegisterStrategicActionHostFunctions` | Function | Rules / Host Registration | Medium | The body registers only strategic-map action/query host callbacks: temple and treasure interactions, port supply, multiple path predicates, march/attack actions, ford checks, road/trap building, and `unit_canmove`. That makes it the strategic action/query registration entrypoint rather than a generic helper. | c, asm | yes |
| `BattleRuntimeState + 3134` | `BattleRuntimeState.wall_section_hitpoints[20][20]` | Recovered Struct Field | Unit Battle / Siege Combat | High | `UnitBattle_AttackWall`, `UnitBattle_ShotWall`, and the siege AI loops all index `battle_state + 3134 + 20 * column + row` as a mutable wall-durability byte plane that drops to zero when a section collapses. | c | yes |

## Deferred / Ambiguous
- `Rules_RegisterStrategicActionHostFunctions` is medium confidence because the registration cluster clearly covers strategic-map action and reachability hosts, but the original internal subsystem label may have been narrower than “StrategicAction”.
- `UnitBattle_GetCorpseSpriteIndex` remains medium confidence. The corpse-overlay role is solid, but the exact original art-pipeline wording behind the sprite-index table is still slightly inferential.

## Batch 65 – World Map HUD And Menu Semantics Wave
| Old Name / Pattern | New Name | Kind | Subsystem | Confidence | Evidence Summary | Sources | Subagent Evidence |
|---|---|---|---|---|---|---|---|
| `sub_40AD40` | `WorldMap_RenderHook` | Function | World Map / Rendering | Medium | `PlayGame` installs this helper as `g_RenderHook`, and the body redraws the main-map chrome, refreshes the shared resource surface, and captures the tooltip backdrop for the strategic HUD. | c, asm | no |
| `sub_40ADF0` | `WorldMap_RedrawFrame` | Function | World Map / Rendering | High | The body pumps DirectDraw, redraws the strategic terrain/unit/building layers plus overlays, and is the common redraw path used by turn flow, movement, and multiple UI handlers. | c, asm | no |
| `sub_40AED0` | `WorldMap_LoadResources` | Function | World Map / Resource Loading | High | Loads the `frame`, `marks`, `step`, `fog`, `flag`, `whirl`, and `turakomp` sprite sets, allocates the 640x480 world-map surface, loads the `mainmap` palette/data, and initializes the shared menu sprite set. | c, asm | no |
| `sub_40B020` | `WorldMap_UnloadResources` | Function | World Map / Resource Loading | High | Frees the same world-map sprite/surface handles, empties cached samples, and releases the shared menu sprite set on strategic-map teardown. | c, asm | no |
| `sub_40B0A0` | `WorldMap_RunHumanTurnLoop` | Function | World Map / Turn Flow | High | This is the human-controlled strategic turn loop: it reads input, redraws the world map, supports minimap dragging and map menus, handles unit hotkeys, and exits on end-turn/quit conditions. | c, asm | no |
| `sub_40B640` | `WorldMap_Initialize` | Function | World Map / Initialization | Medium | Startup wrapper that loads the world-map resource family, rebuilds terrain move offsets and runtime caches, then finalizes strategic-map setup before control returns to `PlayGame`. | c, asm | no |
| `sub_40DD30` | `MiniMap_ToggleVisibility` | Function | Map / MiniMap | High | Single-purpose helper that flips the viewed player's minimap-visibility dword and triggers a redraw. | c | no |
| `sub_40DDE0` | `MiniMap_ShowAllLayers` | Function | Map / MiniMap | High | Enables the minimap, sets draw mask `7`, and redraws; the mask bits already map to terrain, units, and buildings in `MiniMap_DrawTileCell`. | c | no |
| `sub_40DE20` | `MiniMap_ShowUnitsOnly` | Function | Map / MiniMap | High | Enables the minimap, sets draw mask `2`, and redraws, leaving only the unit-owner overlay active. | c | no |
| `sub_40DE60` | `MiniMap_ShowBuildingsOnly` | Function | Map / MiniMap | High | Enables the minimap, sets draw mask `4`, and redraws, leaving only the building-owner overlay active. | c | no |
| `sub_40DEA0` | `MiniMap_Hide` | Function | Map / MiniMap | High | Clears the same per-player minimap-visibility dword and triggers a redraw. | c | no |
| `sub_40DED0` | `UI_MenuHitTestEntry` | Function | UI / Menus | High | Scans the vertically stacked menu-entry rows, computes centered text bounds from the loaded sprite/font metrics, and returns the hovered entry index or `-1`. | c | no |
| `sub_40DFF0` | `UI_RunMenu` | Function | UI / Menus | High | Builds a temporary menu surface, drives hover/selection updates, supports nested submenus and tooltip text, and returns the selected callback/result. | c | no |
| `sub_40ED30` | `UI_MenuEntry_Enable` | Function | UI / Menus | High | Sets the per-entry availability flag at `+26`; callers use it when actions become selectable. | c | no |
| `sub_40ED50` | `UI_MenuEntry_Disable` | Function | UI / Menus | High | Clears the same per-entry availability flag; callers use it to gray out unavailable actions such as closed gates or missing upgrades. | c | no |
| `PlayerRuntimeState + 23` | `PlayerRuntimeState.minimap_visible_flag` | Recovered Struct Field | gameData / Player Runtime | High | The minimap show/hide/toggle helpers mutate this dword, and minimap blits, hit-tests, and viewport dragging all early-out on the same field for the viewed player. | c | no |

## Deferred / Ambiguous
- `sub_40AE80` still looks like a redraw gate for non-human strategic turns, but this batch left it unnamed because the exact designer-facing distinction between “AI redraw”, “animation tick”, and “redraw suppression” is not yet pinned down.
- `sub_40D800` is clearly another minimap boundary predicate, but the currently recovered body still exposes only part of the intended hit-test semantics.

## Batch 68 – Port Runtime State And Path Query Wave
| Old Name / Pattern | New Name | Kind | Subsystem | Confidence | Evidence Summary | Sources | Subagent Evidence |
|---|---|---|---|---|---|---|---|
| `UNIT_RECORD_SIZE` / `UNIT_TABLE_OFFSET` / `UNIT_RECORD` | `BUILDING_RECORD_SIZE` / `BUILDING_TABLE_OFFSET` / `BUILDING_RECORD` | Helper | gameData / Buildings | High | The 467-byte table at `gameData + 509674` is already the recovered `BuildingRecord` slab. Exposing a matching macro family removes a misleading unit-table fiction while preserving `UNIT_RECORD` as a compatibility alias for untouched callsites. | c, asm, map | yes: Plato |
| `dword_517B48` / `dword_517B4C` split spawn arrays | `PortSpawnOffset` / `g_PortSupplySpawnRingOffsets` | Recovered Struct | Port / Spawn Ring | High | `Port_GetSupply` advances the two symbols in lockstep as 12 row/column pairs around the 2x2 port footprint, and the asm-side data layout corroborates one interleaved 24-dword table rather than two unrelated arrays. | c, asm | yes: Kepler, Plato |
| `g_PortArrivalTexts[6]` | `g_PortSupplyReadyTexts[3]` | Table | Port / UI | High | The first three strings are the “supply arrived” family, while `off_517BE4` / `g_PortEmptyTexts` is a distinct empty-port table. The old six-entry symbol was a decompiler fusion of adjacent globals. | c, asm, exe | yes: Kepler |
| `UI_DrawPanelWithSprite` | `UI_DrawPortStatusPanel` | Function | Port / UI | High | The helper only loads `port.s32`, titles the window `Port/Hafen`, selects the sprite from `PORT_SUPPLY_READY_FLAG`, and displays the ready or empty port text families. | c, asm, exe | yes: Kepler |
| `sub_453E60` | `Rules_GetPathDistanceToObject` | Function | Rules / Pathing | Medium | The host-registration string is `odleglosc_od_obiektu`, and the body special-cases temple, treasure, port, and generic near-tile routing before returning a large failure sentinel when no path is found. | c, asm | yes: Kepler |

## Deferred / Ambiguous
- `sub_4084A0` is now clearly a world-map hover/click dispatcher, but this batch deferred the live rename until one more pass resolves whether the original role was framed around a hovered tile, a cursor event, or the whole map-panel interaction loop.

## Batch 69 – Battle Viewport And Exchange Wave
| Old Name / Pattern | New Name | Kind | Subsystem | Confidence | Evidence Summary | Sources | Subagent Evidence |
|---|---|---|---|---|---|---|---|
| `sub_411D70` | `Map_UpdateIdleAnimatedUnits` | Function | World Map / Units | High | The helper scans all 500 world stacks, advances the idle animation frame when the per-type animation timer elapses, redraws the affected stack footprint, then updates the world-map attention-flash overlays. | c, asm | yes: Sartre |
| `sub_411E60` | `Unit_AttemptNeighborMove` | Function | World Map / Movement | High | The prototype had already been recovered, and the live definition now matches its behavior: it checks whether the selected stack has at least 3 AP and whether any of the eight adjacent tiles yields an affordable `Unit_MoveTrack`, freeing the temporary path before returning. | c, asm | yes: Sartre |
| `sub_426FC0` | `UnitBattle_CalcMeleeExchange` | Function | Battle / Combat | High | The helper reads both tactical slots, seeds both out-parameters from the current health bytes, applies the crowding-scaled combat effectiveness math for both sides, optionally scales one side for the alternate exchange mode, and clamps the resulting health losses to `0..100`. | c, asm | yes: Sartre |
| `sub_42C0F0` | `UnitBattle_IsTileInViewport` | Function | Battle / Viewport | High | Pure 7x7 viewport predicate over `BattleRuntimeState.view_left_tile` and `view_top_tile`; tactical render and attack-animation helpers use it only to test whether a battle tile is currently visible. | c, asm | yes: Sartre |
| `BattleRuntimeState + 852 + 31 * slotIndex` | `BattleUnitEntry` | Recovered Struct | Battle / Runtime State | High | The tactical battle block stores 22 fixed 31-byte entries at this offset. Combat, viewport, and animation helpers all agree on the slot header: unit type at `+0`, owner at `+2`, facing at `+3`, grid coordinates at `+4/+6`, and runtime AP/health at `+8/+9`. | c | yes: Sartre |

## Deferred / Ambiguous
- `sub_423760` remains deferred. It is clearly a modal selection dialog over a 10-slot stack subset, but this batch left the exact “inspect” vs “split” vs “move selected members” naming for the next pass with `sub_423420` and `sub_423AC0`.

## Batch 70 – Building Garrison Capacity And Licence Wave
| Old Name / Pattern | New Name | Kind | Subsystem | Confidence | Evidence Summary | Sources | Subagent Evidence |
|---|---|---|---|---|---|---|---|
| `Building_GetCapacity` | `Building_CountFreeGarrisonSlots` | Function | Buildings / Garrison | High | The live body counts only empty `-1` entries in the embedded garrison array and caps the scan at 10 slots for building type `0` and 12 otherwise. `Building_UnitGetInto` compares incoming squad size against this free-slot count, not a total-capacity constant. | c, asm, map | yes: Banach |
| `Building_HasAddonInGarrison` | `Building_HasAddonLicence` | Function | Buildings / Addons | High | The helper scans bytes `+402..+413` in `BuildingRecord`, which are the recovered addon/licence ids rather than the real garrison entries at `+18..+389`. | c, asm, map | yes: Banach |
| `Building_CountSpecialGarrisonEntries` | `Building_CountNonCombatGarrisonEntries` | Function | Buildings / Garrison | High | The loop counts `SPECIAL_FOOT_PERSONAGE`, `SPECIAL_MOUNTED_PERSONAGE`, `GOLD_CARGO`, and `PEASANT_CARGO`, so the old “special” label was too narrow once the exact type set became explicit. | c | yes: Banach |

## Deferred / Ambiguous
- Building type ids `0/1/2` still remain behaviorally distinct but not safely lore-named. This batch left the numeric type mapping neutral even though the capacity and cargo-acceptance rules are now clearer.

## Batch 71 – World Map Interaction Dispatch Wave
| Old Name / Pattern | New Name | Kind | Subsystem | Confidence | Evidence Summary | Sources | Subagent Evidence |
|---|---|---|---|---|---|---|---|
| `sub_4084A0` | `WorldMap_HandleTileHoverAndClick` | Function | World Map / Interaction | Medium | Called from the main strategic-map input loop after redraw/minimap updates and before hotkey handling. The body drives cursor hover feedback, stack inspection, building entry/attack, port supply collection, shrine and treasure interactions, and queued-path execution for the currently hovered tile. | c | no |
| `sub_423760` | `UnitStack_ShowSelectionDialog` | Function | World Map / Unit Selection | Medium | Modal helper that snapshots the current selection globals, clears a 10-slot mask, runs the stack-subset toggle UI loop, and ultimately feeds the selected member mask into `Unit_MoveSelectionFromGroupToTile`. | c, asm | yes: Sartre |

## Deferred / Ambiguous
- `sub_423420` and `sub_423AC0` still need their own names before the full stack-split interaction family reads cleanly end-to-end.

## Batch 72 – Unit Footprint Redraw Wave
| Old Name / Pattern | New Name | Kind | Subsystem | Confidence | Evidence Summary | Sources | Subagent Evidence |
|---|---|---|---|---|---|---|---|
| `sub_411B30` | `Map_RedrawUnitFootprintByIndex` | Function | World Map / Rendering | High | Redraws the unit's tile and any extra tiles implied by its current footprint/overhang class and facing, using the type-dependent footprint class table and the current stack coordinates. | c | yes: Sartre |
| `sub_411CB0` | `Map_RedrawUnitNeighborhoodByIndex` | Function | World Map / Rendering | High | Redraws the unit's tile plus the full surrounding 8-neighbor ring when the stack's footprint class requires a broader redraw envelope. | c | yes: Sartre |

## Batch 73 – SDL Platform Shim Wave
| Old Name / Pattern | New Name | Kind | Subsystem | Confidence | Evidence Summary | Sources | Subagent Evidence |
|---|---|---|---|---|---|---|---|
| `#include <windows.h>` in `clash95.c` | `#include "platform_sdl.h"` | SDL Port Fix | Platform / SDL | High | The main translation unit only needed the project-local compatibility surface. Rebinding it to a dedicated SDL-target shim makes the final platform target explicit without changing gameplay logic. | c | yes: Nash |
| project-local compatibility headers `windows.h` / `compat/windows.h` | wrappers around `platform_sdl.h` | SDL Port Fix | Platform / SDL | High | Both include paths now forward to one shared SDL-target header instead of carrying duplicated Win32 placeholder declarations. | c | no |
| stable Win32-style opaque handles (`HWND`, `HDC`, `HBITMAP`, `HPALETTE`, `HCURSOR`, `HICON`) | SDL-style opaque handles (`SDL_Window*`, `SDL_Surface*`, `SDL_Palette*`, `SDL_Cursor*`) | SDL Port Fix | Platform / SDL | High | The recovered code uses these as abstract window/surface/palette/cursor handles. Mapping them to SDL-style types is the thinnest safe port seam while the DirectDraw-shaped adapters remain in place for later backend replacement. | c, exe | yes: Nash |

## Batch 74 – Unit Stack Selection And Battle Pathing Wave
| Old Name / Pattern | New Name | Kind | Subsystem | Confidence | Evidence Summary | Sources | Subagent Evidence |
|---|---|---|---|---|---|---|---|
| `Render_LoadResourceSprite` | `UnitStackSelection_SyncForCurrentSelection` | Function | World Map / Unit Selection | High | The old name was only a stray prototype. Every live caller opens, closes, or refreshes the split/selection strip based on `g_SelectedUnitIndex`, squad count, and the currently bound stack. | c, asm | yes: Plato |
| `sub_423420` | `UnitStackSelection_RedrawPanel` | Function | World Map / Unit Selection | High | Rebinds the active stack, rebuilds move highlights for marked members, draws the 10-slot strip, and overlays selected-slot markers. | c, asm | yes: Sartre, Plato |
| `sub_423860` | `UnitStackSelection_HandleInput` | Function | World Map / Unit Selection | High | Handles slot-strip clicks, unit info popups, slot toggling, and marked-member moves into adjacent tiles. | c, asm | yes: Sartre, Plato |
| `sub_423AC0` | `UnitStackSelection_HasSelectedSlots` | Function | World Map / Unit Selection | High | Pure scan over the 10-dword selection mask; returns true only when at least one slot is marked. | c | yes: Sartre, Plato |
| `sub_423B00` | `UnitStackSelection_BeginForSelectedStack` | Function | World Map / Unit Selection | High | Activates the strip for `g_SelectedUnitIndex`, clears the mask, and redraws the panel. | c | yes: Plato |
| `sub_423B40` | `UnitStackSelection_End` | Function | World Map / Unit Selection | High | Clears tile highlights, disables the strip, and resets the active stack index to `-1`. | c | yes: Plato |
| `sub_423B70` | `UnitStackSelection_ClearMask` | Function | World Map / Unit Selection | High | Zeroes the 10-entry slot-selection mask used by the split panel. | c | yes: Plato |
| `sub_423B90` | `UnitStackSelection_RefreshForSelectedStack` | Function | World Map / Unit Selection | High | Rebinds the strip to the current selected stack and redraws it after ownership or selection changes. | c | yes: Plato |
| `sub_425850` | `UnitBattle_InitPathingTables` | Function | Battle / Pathing | High | Precomputes the tactical tile-cost grid from battle-map tiles and seeds the airborne-type cost table from `g_UnitTypeFlags bit0`. | c | yes: Sartre |
| `sub_425970` | `UnitBattle_GetTileMoveCostOrZero` | Function | Battle / Pathing | High | Rejects occupied or blocked tiles, returns fixed tactical move cost `5` for airborne types, and otherwise reads the precomputed tile-cost grid. | c | yes: Sartre |
| `sub_461B30` | `Win_ProcessMessagesAndBlitFrame` | Function | Platform / Window Loop | High | Blits the frame, pumps the Win32 message queue, dispatches messages, waits when inactive, and exits through the mode-switch path on quit. | c, asm, map, exe | yes: Meitner, Nash |
| `dword_514194 / dword_526994 / dword_526F78 / dword_526FA0 / dword_527C24` | `UnitStackSelectionState` | Recovered Struct | World Map / Unit Selection | High | These globals move in lockstep across strip setup, redraw, modal dialog, and selected-member movement, proving one shared selection-panel state cluster. | c, asm | yes: Sartre, Plato |
| `BattleRuntimeState + 816` | `BattleRuntimeState.default_tile_move_cost` | Recovered Struct Field | Battle / Pathing | High | `UnitBattle_InitPathingTables` copies this byte into the tactical ground-cost scratch grid for traversable tiles. | c | yes: Sartre |
| `byte_531890 / byte_531C8F / byte_531C90 / dword_531CB8` | `BattlePathingTables` | Recovered Struct | Battle / Pathing | High | Tactical movement callers share one cost grid, one overlapping airborne-type cost table, and one ignore-occupancy flag; the decompiler just split the symbols awkwardly. | c | yes: Sartre, Plato |

## Batch 75 – Top Menu And Player Info Sprite Wave
| Old Name / Pattern | New Name | Kind | Subsystem | Confidence | Evidence Summary | Sources | Subagent Evidence |
|---|---|---|---|---|---|---|---|
| `sub_40E8B0` | `WorldMap_HandleTopMenuBar` | Function | World Map / UI Menu | High | Called only from the human strategic-turn loop, logs `MENU: Draw` / `MENU: Hide`, draws the `menu.s32` strip, hit-tests the top bar, and dispatches `UI_RunMenu`, returning false only when the main loop should break. | c, asm, exe | yes: Plato |
| `sub_40ECF0` | `WorldMapTopMenu_LoadSpriteSet` | Function | World Map / UI Menu | High | Thin sprite-set loader for the dedicated `menu.s32` resource stored in `dword_523F5C`. | c, asm, exe | yes: Plato |
| `sub_40ED20` | `WorldMapTopMenu_FreeSpriteSet` | Function | World Map / UI Menu | High | Matching teardown helper that frees the same top-menu sprite-set handle. | c | yes: Plato |
| `sub_423370` | `UI_SetCurrentPlayer` | Function | UI / Player Context | High | The asm already labels the function this way, world-map turn flow calls it under that name, and the selection panel temporarily switches to the stack owner and restores the original player through the same body. | c, asm | yes: Sartre |
| `sub_4233E0` | `UI_FreeCurrentPlayerInfoSpriteSet` | Function | UI / Player Context | High | Solely frees and nulls the `info%d.s32` sprite-set handle used by `UI_SetCurrentPlayer`, and `WorldMap_UnloadResources` calls it during strategic-map teardown. | c, asm | yes: Sartre |

## Deferred / Ambiguous
- `WorldMap_HandleTopMenuBar` is high confidence on role, but the exact original internal verb could still have been `Process` or `Run` rather than `Handle`.
- The top-menu descriptor family around `word_512348..word_51234E` and `off_512350` is real, but this batch did not freeze a complete struct layout for the menu-entry records.

## Batch 76 – Input Backend Seam And Menu Loader Repair Wave
| Old Name / Pattern | New Name | Kind | Subsystem | Confidence | Evidence Summary | Sources | Subagent Evidence |
|---|---|---|---|---|---|---|---|
| `sub_47BD10` | `InputBackend_Initialize` | Function | Platform / Input Backend | High | Initializes the DirectInput-backed backend slab, creates keyboard/mouse/third devices, configures cooperative levels/data formats, and marks the acquired-device flags used by later focus/poll helpers. | c | yes: Nash |
| `sub_47BF30` | `InputBackend_Acquire` | Function | Platform / Input Backend | High | Reacquires each live backend device on focus regain and other platform reentry paths. | c | yes: Nash |
| `sub_47BF80` | `InputBackend_Unacquire` | Function | Platform / Input Backend | High | Releases each live backend device on focus loss and related platform suspension paths. | c | yes: Nash |
| `Time_Sleep` | `InputBackend_PollState` | Function | Platform / Input Backend | High | The body polls backend devices and fans keyboard/mouse state out into the globals consumed by gameplay; it does not sleep. | c | yes: Nash |
| `DLXSpriteSet_Load(result, a2)` in `WorldMapTopMenu_LoadSpriteSet` | `DLXSpriteSet_Load(result, "menu.s32")` | Compile Fix | World Map / UI Menu | High | Asm corroborates that the helper always loads the dedicated top-menu sprite resource `menu.s32`; the decompiled argument register was wrong and the string pointer was not materialized in C. | c, asm, exe | yes: Plato |

## Batch 77 – CMake Build Surface Wave
| Old Name / Pattern | New Name | Kind | Subsystem | Confidence | Evidence Summary | Sources | Subagent Evidence |
|---|---|---|---|---|---|---|---|
| missing project build system | root `CMakeLists.txt` with `clash95_recovered` static library target | Compile Fix | Build / CMake | High | There was no build system in the repo. The current verified compileable state is a GNU89 object build of `clash95.c` plus `compat/decomp_runtime_stubs.c`; representing that as a static library target preserves the real milestone without inventing a full executable/link harness. | c | yes: Nash |

## Batch 78 – Player Info Sprite Semantics Wave
| Old Name / Pattern | New Name | Kind | Subsystem | Confidence | Evidence Summary | Sources | Subagent Evidence |
|---|---|---|---|---|---|---|---|
| `UI_SetCurrentPlayer` | `UI_LoadCurrentPlayerInfoSpriteSet` | Function | UI / Player Context | High | The body only frees the existing `info%d.s32` sprite handle, formats the next `info%d.s32` filename, loads that sprite set, and stores the new handle. It does not mutate gameplay player state. | c, asm | yes: Meitner |
## Batch 79 – Platform Activation State Wave
| Old Name / Pattern | New Name | Kind | Subsystem | Confidence | Evidence Summary | Sources | Subagent Evidence |
|---|---|---|---|---|---|---|---|
| `sub_4753E0` | `Render_RestoreLostSurfaceIfNeeded` | Function | Platform / Rendering | High | The helper dereferences a DirectDraw-surface-style pointer at offset `+164`, calls vtable slot `+96` (`IsLost` in the DirectDraw surface layout), and only then calls slot `+108` (`Restore`). `Win_WndProc` uses it on both `WM_PAINT` and `WM_ACTIVATEAPP` before redraw/present paths. | c, asm, map | no |
| `dword_5199D4` | `g_AppIsActive` | Global | Platform / Window Loop | High | Initialized to `1`, assigned from `WM_ACTIVATEAPP` `wParam`, checked by the message-pump fast path, and used to decide whether the inactive app should block in `WaitMessage`. | c, asm | no |
| `dword_545190` | `g_SoundPausedForInactiveApp` | Global | Platform / Audio / Activation | High | Set when the deactivate branch pauses audio, cleared when the activate branch resumes audio, and consulted to avoid duplicate pause/resume calls. | c, asm | no |
| `dword_5452D8` | `g_ShouldPresentOnReactivate` | Global | Platform / Rendering / Activation | Medium | Captures the pre-deactivation `dword_544D10` present-enabled state and gates the one-shot `Render_Present` call on activation; the deeper display-mode owner remains unnamed, but the latch role itself is stable. | c, asm | no |

## Batch 80 – Platform Seam Reconciliation And Selection Mask Repair Wave
| Old Name / Pattern | New Name | Kind | Subsystem | Confidence | Evidence Summary | Sources | Subagent Evidence |
|---|---|---|---|---|---|---|---|
| `Input_MousePresent` | `Platform_IsWindowsNt4` | Function | Platform / OS Detection | High | The helper only checks `GetVersion()` for a clear high bit plus major version `4`, which matches the literal NT4 branch and has nothing to do with mouse presence. The prior name was a decompiler illusion. | c, asm, map | no |
| `Input_MouseAcquire` | `Platform_IsWindows9x` | Function | Platform / OS Detection | High | The helper only checks `GetVersion()` for a set high bit plus major version `4`, which matches the non-NT Windows 9x family branch rather than any input acquire operation. | c, asm, map | no |
| `Win_WndProc` | `Platform_MainWindowProc` | Function | Platform / Window Loop | High | Handles `WM_DESTROY`, `WM_PAINT`, and `WM_ACTIVATEAPP`, falls through to `DefWindowProcA` for everything else, and is the concrete platform window-message handler. Neutralizing the prefix keeps the SDL migration seam platform-focused without pretending the body is already SDL-native. | c, asm, map | yes: Planck |
| `Win_CreateMainWindow` | `Platform_CreateMainWindow` | Function | Platform / Window Loop | High | Registers the `"Clash"` class, creates the 640x480 main window, shows/updates it, and immediately initializes the input backend. This is platform bootstrap, not generic game init. | c, asm, map | yes: Planck |
| `Win_ProcessMessagesAndBlitFrame` | `Platform_PumpMessagesAndBlitFrame` | Function | Platform / Window Loop | High | Blits the current frame, pumps and dispatches the platform message queue, idles with `WaitMessage` when inactive, and exits through the render mode-switch path on quit. | c, asm, map, exe | yes: Planck |
| `sub_461C80` | `Video_CanContinuePlayback` | Function | Platform / Video Playback | High | The helper pumps the render/message loop three times and returns true only while no key is pressed and the display surface is neither flipping nor lost. It is used exclusively as the AVI playback continuation callback in `Win_EndModeChange`. | c | no |
| `InputBackend_CreateFirstJoystickDeviceCallback` | `InputBackend_StoreEnumeratedDevice` | Function | Platform / Input Backend | Medium | The callback is only proven to capture the first enumerated auxiliary DirectInput device into a temporary global for later creation during backend init. The older joystick-specific name overstated the evidence. | c, asm | yes: Planck |
| `UnitStack_ShowSelectionDialog: memset_(0, 0)` | `UnitStackSelection_ClearMask((void *)dword_526F78)` | Compile Fix | World Map / Unit Selection | High | Asm shows the dialog runner zeroes the 10-dword `dword_526F78` selection mask before redraw. The decompiled zero-pointer `memset_` was a register-loss artifact, not real behavior. | c, asm | yes: Kuhn |
| `UnitStackSelection_BeginForSelectedStack: memset_(a1, 0)` | `UnitStackSelection_ClearMask((void *)a1)` | Compile Fix | World Map / Unit Selection | High | Asm proves the begin helper clears the 10-entry selection mask supplied in `eax` before redrawing the strip. Routing through the recovered helper removes the last decompiler fiction in this cluster. | c, asm | yes: Kuhn |
| `dword_545198 input slab` | `InputBackendState` | Recovered Struct | Platform / Input Backend | High | The reset, release, initialize, acquire, unacquire, and poll helpers all agree on one 320-byte input slab holding DirectInput interfaces, mouse deltas/buttons, joystick axes/buttons, keyboard state, and per-device ready flags. | c, asm | yes: Planck |

## Batch 81 – SDL Runtime Build Surface Wave
| Old Name / Pattern | New Name | Kind | Subsystem | Confidence | Evidence Summary | Sources | Subagent Evidence |
|---|---|---|---|---|---|---|---|
| `sub_47BC40` | `InputBackend_ResetState` | Function | Platform / Input Backend | High | The helper zeroes the DirectInput/device pointers, clears the cached mouse/joystick deltas, and drops all three device-ready flags in the unified input slab. | c, asm | no |
| `sub_47BCA0` | `InputBackend_ReleaseDevices` | Function | Platform / Input Backend | High | Releases the keyboard, mouse, joystick, and root DirectInput interfaces from the unified backend state block and nulls the stored handles afterward. | c, asm | no |
| header-only platform shim declarations | `platform_sdl_runtime.c` compiled runtime seam | sdl_port_fix | Platform / SDL Runtime | High | The new translation unit implements the currently required window/message/surface helpers (`RegisterClassA`, `CreateWindowExA`, `DispatchMessageA`, `GetMessageA`, `PeekMessageA`, and peers) against SDL-shaped local structs, and CMake now builds it into `clash95_recovered`. That moves the port target from declarations-only compatibility toward an actual compiled SDL-side platform layer without pretending the gameplay loop is fully ported yet. | c | no |
| split globals `dword_5451A8`, `dword_5451AC`, `byte_5451C0`, `byte_5451C8`, `byte_5451CC` | `InputBackendState.mouse_delta_x`, `mouse_delta_y`, `mouse_button_primary`, `mouse_button_secondary`, `keyboard_state` | recovered_struct | Platform / Input Backend | High | The address arithmetic lines up exactly with offsets inside `g_InputBackendState`, and the polled consumers (`Input_IsKeyPressed`, `Input_PollEventsUntil`, cursor-update logic) only need the alias view, not distinct globals. Rebinding them through the recovered struct removes one more decompiler illusion while preserving current behavior. | c, asm | no |
| `unk_51CA18`, `unk_51CA28`, `unk_51CA38` | `g_InputBackendMouseDeviceGuid`, `g_InputBackendKeyboardDeviceGuid`, `g_InputBackendJoystickInterfaceIid` | Global | Platform / Input Backend | Medium | The first two values are passed to the DirectInput `CreateDevice` path for the mouse and keyboard slots, while the third is used in the post-enumeration interface-upgrade path for the auxiliary device slot. The exact COM type of the third GUID is still only partially evidenced, so the IID wording stays conservative. | c, asm | no |
| `dword_4E80F0`, `dword_4E9110`, `dword_4E93F0` | `g_InputBackendMouseDataFormat`, `g_InputBackendKeyboardDataFormat`, `g_InputBackendJoystickDataFormat` | Table | Platform / Input Backend | High | These three 5-dword tables are passed directly into the DirectInput `SetDataFormat`-style vtable slot for the mouse, keyboard, and auxiliary device paths, respectively. | c, asm | no |
| `dword_54DD20` | `g_InputBackendTempJoystickDevice` | Global | Platform / Input Backend | High | The enumeration callback stores the first discovered auxiliary device here, backend init upgrades/configures it, then releases and clears the temporary handle. | c, asm | no |

## Batch 83 – Timing, Tile Highlight, And Castle Production Recovery Wave
| Old Name / Pattern | New Name | Kind | Subsystem | Confidence | Evidence Summary | Sources | Subagent Evidence |
|---|---|---|---|---|---|---|---|
| `sub_4359B0` | `CastleProduction_RebuildAvailableUnitList` | Function | Castle / Production | High | The helper scans unit types `0..39`, filters them through the building addon/licence gates, and rebuilds the selectable unit-id list used by the castle production panel. | c, asm | yes: Kant |
| `sub_435BC0` | `Castle_ShowUnitProductionPanel` | Function | Castle / Production | High | The body initializes the castle production panel state, seeds the licence-slot animation state, loads panel assets, and runs the buy/remove/set/stop production modal loop. | c, asm | yes: Kant |
| `Time_Now` returning `nullsub_2(v5)` | performance-counter / frequency quotient return | compile_fix | Platform / Timing | High | `clash95.asm` shows `Time_Now` returning the `__I8D` result in `eax` and ending immediately before the collapsed `nullsub_2` padding. The decompiled tail-call into `nullsub_2` was a pure control-flow illusion. | c, asm, map | no |
| `Pathing_EnableBridgeCrossings` storing undefined `v4` | store literal `1` in `g_PathingAllowBridgeCrossings` | compile_fix | Map / Pathing | High | `Track_BridgesOn` in asm logs the mode change, loads `edx = 1`, and writes that literal to the bridge-tracking global. | c, asm, map | yes: Hooke |
| `UI_ClearTileHighlight: memset_(this, -1)` | `memset(dword_5269D8, 0xFF, 0x40)` | compile_fix | UI / Tile Highlight | High | The asm body sets `eax = &dword_5269D8`, `edx = -1`, `ebx = 0x40`, then calls `memset_`, proving the live target is the paired highlight arrays, not the incoming `this` pointer. | c, asm | yes: Hooke |
| `BuildingGarrisonDialog_ToggleRepairSelectedUnits: memset_(v4, 0)` | clear `g_BuildingGarrisonDialogSelectedSlots[12]` with `memset` | compile_fix | Building / Garrison | High | After the fixed 12-slot repair/training loop, the only coherent object to clear is the selection-mask array; the decompiler-lost pointer was called out by the building/garrison pass. | c | yes: Kant |
| `BuildingGarrisonDialog_ToggleTrainingSelectedUnits: memset_(v4, 0)` | clear `g_BuildingGarrisonDialogSelectedSlots[12]` with `memset` | compile_fix | Building / Garrison | High | Same artifact as the repair toggle path: once the 12-slot loop ends, the code resets the slot-selection mask before redrawing the grid. | c | yes: Kant |
| `Castle_ShowUnitProductionPanel: memset_(0, 0)` | clear `dword_532304[12]` slot sprite-set handle array with `memset`; keep `dword_5322D4[12]` as the separately seeded animation-frame array | compile_fix | Castle / Production | High | Asm at `0x435BC0` zeroes `0x532304` for 12 slot handles and only then seeds random `0..7` frame values into the separate `0x5322D4` array. The earlier `dword_5322D0[12]` interpretation was a decompiler-fused false grouping. | c, asm | yes: Kant |

## Batch 87 - Castle Preview Semantics And SDL Bitmap/DC Seam Wave
| Old Name / Pattern | New Name | Kind | Subsystem | Confidence | Evidence Summary | Sources | Subagent Evidence |
|---|---|---|---|---|---|---|---|
| `sub_4347A0` | `CastleProduction_RedrawSelectedUnitPanel` | Function | Castle / Production | High | The helper rebuilds the selected portrait sprite, redraws the five-entry available-unit strip, and repaints the selected-unit stats/details block for the castle production panel. Asm at `0x4347A0` shows it frees the previous preview handle, builds the sprite path into a stack buffer through `sub_413430`, allocates a `0x1010` sprite set, and loads that asset path directly through `DLXSpriteSet_Load`. | c, asm | yes: Kant, Dirac |
| `dword_532218 / dword_532220 / dword_532224 / dword_5322C8 / dword_5322D0 / dword_5322D4 / dword_532304 / dword_532334 / dword_532338 / dword_53233C` | `CastleProductionPanelState` | recovered_struct | Castle / Production | High | The opener, selected-preview redraw path, grid/strip click handlers, and animation tick helper all agree on one shared modal state region: active building, selected available-unit index, available-unit list, selected licence slot, single preview handle, per-slot frame array, per-slot sprite handles, and the add/remove animation latches. | c, asm, prior-artifact | yes: Kant |
| missing Win32 bitmap/DC import cluster (`CreateCompatibleDC`, `DeleteDC`, `DeleteObject`, `GetObjectA`, `SelectObject`, `StretchBlt`, `StretchDIBits`, `LoadImageA`) | SDL-target surface ownership stubs in `platform_sdl_runtime.c` | sdl_port_fix | Platform / SDL Runtime | High | The current compile/link surface only needs conservative width/height/resource-name ownership for bitmap/DC paths, not a fully faithful renderer. Implementing those imports behind the SDL seam removes them from the first unresolved link wave while keeping platform ownership local. | c | yes: Dirac, Kierkegaard |

## Batch 88 - CRT Wrapper Link-Surface Reduction Wave
| Old Name / Pattern | New Name | Kind | Subsystem | Confidence | Evidence Summary | Sources | Subagent Evidence |
|---|---|---|---|---|---|---|---|
| missing `vsprintf_`, `fclose_`, `fwrite_`, `fread_`, `strncmp_`, `tolower_` imports | conservative wrappers in `compat/decomp_runtime_stubs.c` | link_fix | Runtime / CRT Quarantine | High | These calls are low-risk CRT aliases compared with the still-unsafe `_wcpp_*`, allocator, process/thread, and parser helpers. Owning them in the quarantine translation unit removes them from the first unresolved link wave without spreading speculative runtime signatures through gameplay code. | c, prior-artifact | yes: Kierkegaard |
| implicit `GetTickCount` / `GetVersion` / `Sleep` declarations in `clash95.c` | explicit recovered prototypes | compile_fix | Platform / SDL Runtime | High | The SDL-target runtime seam now owns these imports directly, so the recovered translation unit no longer needs GNU89 implicit-declaration fallback for timing/version helpers. | c | no |
| missing SDL-target timing import | `GetTickCount` runtime seam in `platform_sdl_runtime.c` | sdl_port_fix | Platform / SDL Runtime | High | Implemented as a thin alias over the already recovered `timeGetTime()` host/SDL-side millisecond seam, removing one more trivial Win32 dependency from the link surface. | c | yes: Kierkegaard |
| missing SDL-target sleep import | `Sleep` runtime seam in `platform_sdl_runtime.c` | sdl_port_fix | Platform / SDL Runtime | High | Implemented with `usleep()` inside the SDL-target compatibility layer so the recovered code owns its basic timing delay primitive locally rather than through Win32 headers/imports. | c | yes: Kierkegaard |
| missing SDL-target timing/version imports | `GetVersion` runtime seam in `platform_sdl_runtime.c` | sdl_port_fix | Platform / SDL Runtime | Medium | The placeholder returns an NT-style major version `4` so the recovered OS-family probes follow a stable non-9x path until startup/runtime reconstruction is deeper. This is an explicit compile/runtime seam choice, not a claim about the original host environment. | c | yes: Kierkegaard |
| missing SDL-target drive-type import | `GetDriveTypeA` runtime seam in `platform_sdl_runtime.c` | sdl_port_fix | Platform / SDL Runtime | Medium | Added as a conservative fixed-drive placeholder so the import surface keeps shrinking under CMake/link probes while CD-path semantics remain explicitly deferred. | c | yes: Kierkegaard |

## Batch 89 - Battle Prompt And Garrison Asset Load Wave
| Old Name / Pattern | New Name | Kind | Subsystem | Confidence | Evidence Summary | Sources | Subagent Evidence |
|---|---|---|---|---|---|---|---|
| `sub_42D560` | `UnitBattle_HandleRetreatAction` | Function | Battle / UI | High | The helper is only reachable from the selected-action path for the retreat button, validates the current battle context, and dispatches the retreat-specific state changes before returning to the battle turn loop. | c, asm | yes: Boyle |
| `sub_42D670` | `UnitBattle_HandlePrepareDefenceAction` | Function | Battle / UI | High | The body is gated by the defend/preparation action button, toggles the selected unit into the defensive readiness state, and reuses the same selected-action redraw flow as the other action-button handlers. | c, asm | yes: Boyle |
| `sub_42D730` | `UnitBattle_ShowPlayerMessageBanner` | Function | Battle / UI | High | Shared callers pass modal battle text such as the current-player prompt and god-anger messaging while the function formats the banner with player portrait/name state and waits for the brief UI display cycle. | c, asm | yes: Boyle |
| `sub_42DAE0` | `UnitBattle_ShowCurrentPlayerPromptDialog` | Function | Battle / UI | High | Builds the current-player prompt text, enters the short modal loop around the same banner/dialog state family, and is used specifically at player-turn handoff in tactical combat. | c, asm | yes: Boyle |
| `sub_42DEC0` | `UnitBattle_AnimateSelectedUnitPanel` | Function | Battle / UI | Medium | The function only redraws and advances the selected-unit side panel animation state; it is adjacent to turn/prompt flow but does not own those transitions itself. | c | yes: Boyle |
| `sub_431940` | `UnitBattle_ShowWallInfoPopup` | Function | Battle / UI | High | Loads `battle\\mur_info.s32`, selects the localized wall-description strings, and shows the hover/info popup for wall sections during siege battles. | c, asm, exe | yes: Boyle |
| `BuildingGarrisonDialog_ReloadSlotSprite: DLXSpriteSet_Load((_DWORD *)result, v5)` | `BuildingGarrisonDialog_ReloadSlotSprite: DLXSpriteSet_Load((_DWORD *)result, &v10)` | compile_fix | Building / Garrison | High | Asm at `0x432d30` shows the helper building the slot asset path into the current stack frame and passing `esp` to `DLXSpriteSet_Load`; `v5` was a false register-split artifact. | c, asm | yes: Kant |
| `BuildingGarrisonDialog_ReloadSelectedUnitDetailSprites: DLXSpriteSet_Load(v7, a2)` | `BuildingGarrisonDialog_ReloadSelectedUnitDetailSprites: DLXSpriteSet_Load(v7, v15)` | compile_fix | Building / Garrison | High | Asm at `0x4336c0` shows the selected-unit detail loader building the sprite path into its stack buffer and loading from that buffer, not from the incoming `a2` argument. | c, asm | yes: Kant |

## Batch 90 - Castle Preview Helper Naming Wave
| Old Name / Pattern | New Name | Kind | Subsystem | Confidence | Evidence Summary | Sources | Subagent Evidence |
|---|---|---|---|---|---|---|---|
| `sub_434110` | `CastleProduction_DrawSelectedUnitPortrait` | Function | Castle / Production | High | The body reads the currently loaded preview sprite set from `dword_5322D0`, measures the current frame bounds, draws the preview sprite at the portrait position, and optionally presents. It does not rebuild panel state or reload assets. | c, asm | yes: Kant |
| `sub_434760` | `CastleProduction_AnimateSelectedUnitPortrait` | Function | Castle / Production | High | The helper only checks the `Time_Now` gate, advances the selected preview frame `dword_5322CC = (dword_5322CC + 1) & 7`, and tail-calls `CastleProduction_DrawSelectedUnitPortrait`. | c, asm | yes: Kant |

## Batch 91 - CRT Startup And TLS Naming Wave
| Old Name / Pattern | New Name | Kind | Subsystem | Confidence | Evidence Summary | Sources | Subagent Evidence |
|---|---|---|---|---|---|---|---|
| `sub_472558` | `CRT_ExitProcessWithFinalizers` | Function | Runtime / Startup | High | The helper invokes the runtime exit/finalizer callback path and then tail-calls the lower-level process-exit routine. Both observed callers use it as the last step out of startup/runtime control flow. | c, asm, map | yes: Dirac |
| `sub_4855AF` | `CRT_InitializeRuntimeBeforeWinMain` | Function | Runtime / Startup | High | Gets the module handle, initializes runtime tables and stack limits, calls the runtime init hooks, and prepares the process before the broader startup flow continues. | c, asm, map | yes: Dirac |
| `sub_4865AA` | `CRT_GetOrCreateThreadDataPreserveLastError` | Function | Runtime / TLS | High | Saves `GetLastError`, returns existing TLS thread data or reallocates/creates it, then restores the saved last-error value before returning. | c, asm | yes: Dirac |
| `sub_486628` | `CRT_AllocateTlsIndex` | Function | Runtime / TLS | High | Owns the `TlsAlloc` loop and the legacy Win32 version guard that skips low TLS indices, returning whether a valid TLS slot was acquired. | c, asm | yes: Dirac |
| `sub_48667D` | `CRT_CreateAndAttachThreadData` | Function | Runtime / TLS | High | Builds a new thread-data block, links it into the runtime list, and stores it in the active TLS slot. | c, asm | yes: Dirac |
| `sub_4866CC` | `CRT_DetachThreadDataAndMaybeCloseHandle` | Function | Runtime / TLS | High | Removes the current TLS thread-data block, clears the TLS slot, and optionally closes the saved OS handle carried in the thread data. | c, asm | yes: Dirac |
| `sub_486721` | `CRT_DestroyTlsIndexAndThreadData` | Function | Runtime / TLS | High | Calls the detach helper with close-on-detach enabled, frees the TLS index, and resets `dwTlsIndex` to `-1`. | c, asm | yes: Dirac |
| `sub_48674D` | `CRT_InitializeThreadAndFileHandleHooks` | Function | Runtime / TLS / File Handles | High | Populates the runtime hook tables for thread-data locking and file-handle ownership, initializes the shared critical-section slab, and binds the active TLS getter/destroyer callbacks. | c, asm | yes: Dirac |
| `sub_486869` | `CRT_ShutdownThreadAndFileHandleHooks` | Function | Runtime / TLS / File Handles | High | Walks the hook-managed critical-section slabs, frees thread-data state, and tears down the runtime hook ownership installed by `CRT_InitializeThreadAndFileHandleHooks`. | c, asm | yes: Dirac |

## Batch 84 – Battle Action Semantics Wave
| Old Name / Pattern | New Name | Kind | Subsystem | Confidence | Evidence Summary | Sources | Subagent Evidence |
|---|---|---|---|---|---|---|---|
| `sub_42D2C0` | `UnitBattle_RefreshSelectedActionButtons` | Function | Battle / UI | High | Rebuilds the selected-unit action-button state from the current battle unit’s ranged/melee capabilities, forces the one valid mode when the unit is not dual-capable, and redraws the `dword_514B78` descriptor block. | c, asm | yes: Boyle |
| `sub_42D4E0` | `UnitBattle_ToggleSelectedShootingMode` | Function | Battle / UI | High | Only available for units that have both melee and nonzero range, flips the shooting-mode latch, and updates the selected action-button state accordingly. | c, asm | yes: Boyle |
| `sub_42D5B0` | `UnitBattle_ToggleSelectedChargeMode` | Function | Battle / UI | High | Flips the selected unit’s charge-mode latch, timestamps the toggle, redraws the unit neighborhood, and cancels incompatible shooting mode when charge is enabled. | c, asm | yes: Boyle |
| `sub_42F7C0` | `UnitBattle_GetFootprintClass` | Function | Battle / Footprint | High | Direct per-unit-type lookup into `byte_512577`, and all live callers use the result only for multi-tile footprint / redraw invalidation logic rather than generic battle class semantics. | c | yes: Boyle |

## Batch 85 – Pathing Move-Cost Register Recovery Wave
| Old Name / Pattern | New Name | Kind | Subsystem | Confidence | Evidence Summary | Sources | Subagent Evidence |
|---|---|---|---|---|---|---|---|
| `Map_GetUnitTileMoveCostOrZero` final bridge-enabled profile lookup using undefined `v12` | reuse the live `TILE_TERRAIN_RECORD` pointer (`v11`) | compile_fix | Map / Pathing | High | The helper already loads the terrain-record pointer once; the bridge-enabled fallback indexes the same terrain id. The stray `v12` was a decompiler register-loss artifact identified in the tile/pathing review. | c, asm | yes: Hooke |
| `UnitStack_GetTileMoveCostOrZero` owner/trap/profile checks using undefined `v8` and `v14` | reuse `a1` owner byte and the live `TILE_TERRAIN_RECORD` pointer (`v13`) | compile_fix | Map / Pathing | High | The stack helper already has the stack pointer and the terrain-record pointer in scope; the undefined `v8` / `v14` temporaries were register-split artifacts, not real inputs. Restoring those live values removes the last known bogus locals in this move-cost path. | c, asm | yes: Hooke |

## Batch 86 – Runtime Shim And Castle/CRT Naming Wave
| Old Name / Pattern | New Name | Kind | Subsystem | Confidence | Evidence Summary | Sources | Subagent Evidence |
|---|---|---|---|---|---|---|---|
| `sub_435A00` | `CastleProduction_HandleLicenceGridClick` | Function | Castle / Production | High | Hit-tests the 12-slot licence grid, updates the selected licence slot, reconciles the selected unit against the available-unit list, and redraws the detail panel. | c, asm | yes: Kant |
| `sub_435AC0` | `CastleProduction_HandleAvailableUnitStripClick` | Function | Castle / Production | High | Maps clicks in the five-entry visible unit strip back to the current available-unit index and redraws the unit detail panel. | c, asm | yes: Kant |
| `sub_435B90` | `CastleProduction_TickAnimations` | Function | Castle / Production | High | Thin panel tick helper that advances the castle-production animation state and restores the render target afterward. | c, asm | yes: Kant |
| `sub_4E7AAE` | `CRT_SpawnveLaunchProcess` | Function | Runtime / Process | High | Wraps the `CreateProcessA` / `WaitForSingleObject` / `GetExitCodeProcess` spawn path used by the CRT-style `spawnve_` flow. | c, asm, map | yes: Dirac |
| `sub_4E7CA5` | `CRT_SetEnvironmentVariableCompat` | Function | Runtime / Environment | High | Converts wide strings through `wcstombs_` when needed and dispatches to `SetEnvironmentVariableW/A`, matching the mixed ANSI/Unicode environment setter compatibility path. | c, asm | yes: Dirac |
| `sub_4E7DE8` | `CRT_GetOsHandleFromFd` | Function | Runtime / File Descriptors | High | Validates an fd index against the runtime table and returns the backing OS handle, setting errno on failure. | c, asm, map | yes: Dirac |
| `UnitStack_GetMoveCostToTileIgnoringOccupancy` restoring the saved occupant through undefined `v9` | restore the saved occupant through the computed tile-slot pointer (`v6`) | compile_fix | Map / Pathing | High | The helper temporarily clears one concrete occupant slot, calls the ordinary move-cost helper, then restores the same slot. The previous restore address was a pure register-loss artifact. | c, asm | yes: Hooke |
| missing `nfree_` runtime alias | conservative free wrapper in `compat/decomp_runtime_stubs.c` | compile_fix | Runtime / Memory | High | The decompiled code consistently calls `nfree_` as a one-argument free-like helper; a guarded alias removes one low-risk runtime linker blocker without touching gameplay logic. | c | yes: Kierkegaard |
| missing `strcmp_` runtime alias | null-tolerant string compare wrapper in `compat/decomp_runtime_stubs.c` | compile_fix | Runtime / Strings | High | Call sites use `strcmp_` as a thin string-compare import, but the recovered C still contains null-bearing sites; a guarded wrapper preserves compile/link progress without asserting stronger reconstruction than the source supports. | c | yes: Kierkegaard |
| missing `sprintf_` runtime alias | variadic formatting wrapper in `compat/decomp_runtime_stubs.c` | compile_fix | Runtime / Strings | High | The recovered code uses `sprintf_` as a standard formatting helper across UI, logging, and asset-path generation. | c | yes: Kierkegaard |
| missing `toupper_` runtime alias | narrow-character case-fold wrapper in `compat/decomp_runtime_stubs.c` | compile_fix | Runtime / Character Tables | High | The surviving call sites treat `toupper_` as a single-character uppercase helper in ASCII/multibyte runtime code. | c | yes: Kierkegaard |
| missing `towupper_` runtime alias | wide-character case-fold wrapper in `compat/decomp_runtime_stubs.c` | compile_fix | Runtime / Character Tables | High | The wide-character environment/runtime helpers use `towupper_` as a direct uppercase transform for `_WORD` data. | c | yes: Kierkegaard |
| missing `GetModuleHandleA` import | SDL-target platform seam in `platform_sdl_runtime.c` | sdl_port_fix | Platform / SDL Runtime | High | The current recovered runtime only needs a stable non-null module handle token so image/resource callers stay owned by the platform seam instead of Win32 imports. | c | yes: Kierkegaard |
| missing `OutputDebugStringA` import | SDL-target platform seam in `platform_sdl_runtime.c` | sdl_port_fix | Platform / SDL Runtime | High | The bitmap/resource error paths only need debug-string ownership inside the platform seam; forwarding to stderr keeps that behavior local and non-Win32. | c | yes: Kierkegaard |
| `UI_DrawPortStatusPanel: DLXSpriteSet_Load(v3, a1)` | `DLXSpriteSet_Load(v3, "port.s32")` | compile_fix | Port / UI | High | The port-status panel allocates a dedicated sprite set, and asm corroborates the hardcoded `port.s32` resource rather than a caller-provided filename. | c, asm, exe | yes: Popper |

## Batch 92 - Castle Garrison State And Tile Highlight Recovery Wave
| Old Name / Pattern | New Name | Kind | Subsystem | Confidence | Evidence Summary | Sources | Subagent Evidence |
|---|---|---|---|---|---|---|---|
| `dword_532144` | `g_BuildingGarrisonDialogUiSpriteSet` | global | Building / Garrison | High | The sprite-sheet handle is only used for the fixed garrison-dialog chrome and per-slot/status overlays in the selected-unit panel and slot-grid redraw paths, never as the mutable render/backbuffer resource handle. | c, asm | yes: Kant |
| `dword_532154` | `g_BuildingGarrisonDialogResourceHandle` | global | Building / Garrison | High | The symbol owns the mutable 256-entry palette/resource block that `sub_401CE0`, `sub_401AF0`, `sub_460CB0`, and `Render_LoadResourceSprite_v4` mutate before presenting the dialog panels. | c, asm | yes: Kant |
| `BuildingGarrisonDialog_ReloadSelectedUnitDetailSprites` | `BuildingGarrisonDialog_RebuildSelectedUnitPanelAssets` | function | Building / Garrison | High | The helper frees/reloads the selected-unit sprite set, rebuilds the 256-entry dialog resource block from the stack-built asset path, redraws the selected-unit panel, and refreshes the selected slot sprite. It does more than just load one detail sprite. | c, asm | yes: Kant |
| `dword_532220` | `g_CastleProductionSelectedAvailableUnitIndex` | global | Castle / Production | High | The value is a single scalar index at address `0x532220`; arrow/grid handlers mutate it, and the selected portrait/details paths index `dword_532224` through it. | c, asm, prior-artifact | yes: Kant |
| `dword_5322D0` | `g_CastleProductionSelectedUnitSpriteSet` | global | Castle / Production | High | `CastleProduction_DrawSelectedUnitPortrait`, `CastleProduction_RedrawSelectedUnitPanel`, and panel teardown all treat `0x5322D0` as one preview sprite-set handle, not as a 12-entry array. | c, asm, prior-artifact | yes: Kant |
| `sub_435580` | `UI_GetGridIndexFromMouse` | function | Castle / Production UI | High | The body converts the mouse position into a `4 x 3` licence-grid cell index and returns `-1` outside the active grid; callers use it exactly as the castle licence-grid hit test. | c, asm | no |
| `dword_5269D8` / `dword_5269DC` | `g_TileHighlightSlots[8]` / `TileHighlightSlotTable` | recovered_struct | UI / Tile Highlight | High | The highlight helpers walk the region in 8-byte steps, compare/store paired coordinates, and clear exactly `0x40` bytes, proving an 8-entry `{tile_x, tile_y}` table rather than two unrelated arrays. | c, asm | yes: Hooke |
| `UI_DrawTileHighlightOverlay` width-only centering and undefined register artifact | center highlight sprite on both X and Y from the 64x64 tile rect while looking up `g_TileHighlightSlots` directly | compile_fix | UI / Tile Highlight | High | Asm at `0x419000` computes both centered axes from sprite width/height before drawing the overlay for sprite `40`; the previous C only centered X and carried a false undefined local. | c, asm | yes: Hooke |
| `Building_CreateSpecialPersonageGarrisonUnit` undefined `v6` / `v9` slot math | reconstruct the normal 12-slot garrison scan, fallback leave-mask copy, `UnitSlot_InitFromType`, and `slot_flags |= 3` write | compile_fix | Building / Garrison / Special Personages | High | Asm at `0x44F1E0` shows the helper scanning `building + 18 + 31 * slot`, calling `Building_UnitsLeave` with the fallback mask at `0x518D5C` when full, then initializing the chosen slot and setting the resident-special flag bits at `building + 30 + 31 * slot`. | c, asm | yes: Kant |

## Batch 93 - Battle Interaction, Castle Production Actions, And Footprint-Class Wave
| Old Name / Pattern | New Name | Kind | Subsystem | Confidence | Evidence Summary | Sources | Subagent Evidence |
|---|---|---|---|---|---|---|---|
| `sub_412B20` | `UnitStackSelection_BuildSelectedSlotIndexList` | function | Unit Selection | High | Scans the 10-entry unit-selection mask, emits selected slot indices into an output array, and terminates the list with `-1` for the stack-selection move/split UI. | c, asm | yes: Boyle |
| `sub_412B90` | `UnitSlots_CalcCombatStrengthScoreWithSpecialPersonageCheck` | function | Unit Strength / Special Personages | High | Scans the slot list for unit types `33` or `34`, then forwards that presence flag into `UnitSlots_CalcCombatStrengthScore`; both stack and building garrison strength helpers use it. | c, asm | yes: Boyle |
| `sub_42CB50` | `UnitBattle_HandleBattlefieldInteraction` | function | Battle / Input | High | Maps the cursor to battle tiles and dispatches hovered-unit info, friendly reselection, movement, melee, ranged, and wall-interaction behavior from one central battlefield click handler. | c, asm, map | yes: Dirac |
| `sub_435280` | `CastleProduction_DrawProductionStatus` | function | Castle / Production | High | Draws the castle production status text block, including the localized “turns until production” variants and the separate “production stopped” text when the garrison is full. | c, asm | no |
| `sub_435640` | `CastleProduction_SelectPreviousAvailableUnit` | function | Castle / Production | High | Decrements the selected available-unit index and redraws the selected-unit panel when the left strip-arrow action fires. | c, asm | no |
| `sub_435680` | `CastleProduction_SelectNextAvailableUnit` | function | Castle / Production | High | Advances the selected available-unit index when another available unit exists and redraws the selected-unit panel. | c, asm | no |
| `sub_4356C0` | `CastleProduction_HandleBuyLicenceAction` | function | Castle / Production | High | The castle action table labels this button “Buy licence”, and the body calls `Building_BuyAddon` for the selected unit before reloading the licence slot sprite and rebuilding the available-unit list. | c, asm | no |
| `sub_435770` | `CastleProduction_HandleRemoveLicenceAction` | function | Castle / Production | High | The castle action table labels this button “Remove licence”, and the body only runs when a licence slot is selected, staging that slot for removal and playing the close action sound. | c, asm | no |
| `sub_4357E0` | `CastleProduction_HandleProduceAction` | function | Castle / Production | High | The action table labels this button “Produce”, and the body calls `Building_SetUnitProduction` before redrawing the licence grid and production status. | c, asm | no |
| `sub_435830` | `CastleProduction_HandleStopProductionAction` | function | Castle / Production | High | The action table labels this button “Stop”, and the body calls `Building_StopUnitProduction` before redrawing the licence grid and status block. | c, asm | no |
| `sub_435860` | `CastleProduction_HandleInfoAction` | function | Castle / Production | High | The action table labels this button “Info”, and the body builds the selected unit asset path, shows the info panel overlay, then restores the castle production panel surfaces. | c, asm | no |
| `missing GetDeviceCaps / GetPixel / SetPixel / SetRect / EqualRect / IntersectRect / IsRectEmpty imports` | `SDL-target utility wrappers in platform_sdl_runtime.c` | sdl_port_fix | Platform / SDL Runtime | High | The rendering/resource callsites only require conservative device-cap, key-pixel, and rect operations. Owning them inside the existing SDL seam removes another low-risk Win32 helper cluster without touching gameplay logic. | c | yes: Kierkegaard |
| `UnitTypeMetadataRecord[15].visual_class_index` | `UnitTypeMetadataRecord[15].footprint_class_index` | recovered_struct | Unit Metadata / Rendering | High | `byte_512577` is now directly corroborated by `UnitBattle_GetFootprintClass` and `Unit_GetClassIndex` as a reusable per-type footprint/overhang lookup used by both world and battle redraw invalidation, not just a raw visual offset. | c, asm, prior-artifact | yes: Boyle |

## Batch 94 - Battle Dialog And Building Transfer UI State Wave
| Old Name / Pattern | New Name | Kind | Subsystem | Confidence | Evidence Summary | Sources | Subagent Evidence |
|---|---|---|---|---|---|---|---|
| `sub_42A990` | `BuildingEconomyDialog_SetExitSignal` | function | Building / Economy Dialog | High | Thin modal callback helper that routes through `sub_419E60`, writes the shared economy-dialog exit latch, and is only consumed by `BuildingEconomyDialog_Run`. | c, prior-artifact | no |
| `sub_42D6F0` | `UnitBattle_RequestActionLoopExit` | function | Battle / Action Loop | High | Sets the shared battle action-loop exit flag, clears the active bit on the bound action widget, and redraws that widget; the tactical turn loop exits when this latch becomes nonzero. | c, asm | yes: Boyle |
| `sub_42DAB0` | `UnitBattleDialog_SelectAffirmativeResponse` | function | Battle / Prompt Dialog | High | Callback writes `1` into the prompt-dialog result latch after the common UI helper runs; asm explicitly loads `edx = 1` before the store. | c, asm | yes: Boyle |
| `sub_42DAD0` | `UnitBattleDialog_SelectNegativeResponse` | function | Battle / Prompt Dialog | High | Twin prompt callback writes `0` into the same dialog-result latch after the common UI helper runs; asm explicitly zeroes `edx` before the store. | c, asm | yes: Boyle |
| `sub_42E6F0` | `UnitBattle_TemporarilyClearGateBlocker` | function | Battle / Fortification State | Medium | Saves a nonzero fortification-grid byte into the shared saved-byte slot and clears the live byte while pathfinding runs, then only executes in the guarded defender/attacker setup path. | c, asm | yes: Boyle |
| `sub_42E770` | `UnitBattle_RestoreGateBlocker` | function | Battle / Fortification State | Medium | Direct inverse of `UnitBattle_TemporarilyClearGateBlocker`, restoring the saved fortification-grid byte back into the same tile. | c, asm | yes: Boyle |
| `sub_42E7C0` | `UnitBattle_OverrideControllerOrderBits` | function | Battle / Unit Orders | Medium | Snapshots the low two bits of each battle slot’s order-state byte and forces those bits to `3` for one or both specified controllers before the battle loop runs. | c, asm | yes: Boyle |
| `sub_42E860` | `UnitBattle_RestoreControllerOrderBits` | function | Battle / Unit Orders | Medium | Restores the previously saved low two order-state bits for all 22 battle slots after the temporary controller override phase ends. | c | yes: Boyle |
| `sub_435620` | `CastleProduction_SetExitSignal` | function | Castle / Production | High | Thin modal callback helper that writes the shared castle-production exit latch used by `Castle_ShowUnitProductionPanel` to terminate its loop. | c | no |
| `sub_436610` | `BuildingTransferTargetList_SetDrawOrigin` | function | Building / Transfer Target List | High | Stores the shared X/Y origin consumed by both the target-list renderer and click handler. | c | no |
| `sub_436620` | `BuildingTransferTargetList_Draw` | function | Building / Transfer Target List | High | Lazily loads the transfer-target sprite set, renders the five visible rows, and highlights the current selection using the shared list index and draw origin. | c | no |
| `sub_4368F0` | `BuildingTransferTargetList_SelectPrevious` | function | Building / Transfer Target List | High | Scrolls the selected transfer target upward when possible and redraws the visible five-row list. | c | no |
| `sub_436930` | `BuildingTransferTargetList_SelectNext` | function | Building / Transfer Target List | High | Scrolls the selected transfer target downward while a non-sentinel entry remains and redraws the visible five-row list. | c | no |
| `sub_436970` | `BuildingTransferTargetList_Rebuild` | function | Building / Transfer Target List | High | Rebuilds the target-building list from eligible same-player buildings, injects sentinel `-2` / `-1` entries, and resets the selected index to zero. | c, prior-artifact | no |
| `sub_436A30` | `BuildingTransferTargetList_HandleClick` | function | Building / Transfer Target List | High | Maps mouse position back into the five visible target rows, updates the shared selected-index latch, and redraws the list when the clicked row is valid. | c | no |
| `sub_436AF0` | `BuildingTransferTargetList_FreeSpriteSet` | function | Building / Transfer Target List | High | Releases the lazily cached target-list sprite set and clears the shared handle. | c | no |
| `dword_532068` | `g_UnitBattleActionLoopExitRequested` | global | Battle / Action Loop | High | Cleared when the tactical loop starts, set by `UnitBattle_RequestActionLoopExit`, and polled by the active battle UI loop to terminate processing. | c, asm | yes: Boyle |
| `dword_532080` | `g_UnitBattlePromptDialogResult` | global | Battle / Prompt Dialog | High | Shared prompt-dialog result latch written by the affirmative/negative callbacks and returned by `UnitBattle_ShowCurrentPlayerPromptDialog`. | c, asm | yes: Boyle |
| `dword_532210` | `g_CastleProductionExitSignal` | global | Castle / Production | High | Cleared on panel open, written by `CastleProduction_SetExitSignal`, and polled by `Castle_ShowUnitProductionPanel` until the modal loop exits. | c | no |
| `dword_532428` | `g_BuildingTransferTargetListSpriteSet` | global | Building / Transfer Target List | High | Lazily loaded icon sprite-set handle owned by the transfer-target list renderer and released by the matching teardown helper. | c | no |
| `dword_53242C` | `g_BuildingTransferTargetListDrawX` | global | Building / Transfer Target List | High | Shared left-edge coordinate for the transfer-target list, written once per dialog open and reused by draw/click helpers. | c | no |
| `dword_532430` | `g_BuildingTransferTargetListDrawY` | global | Building / Transfer Target List | High | Shared top-edge coordinate for the transfer-target list, written once per dialog open and reused by draw/click helpers. | c | no |
| `g_BuildingTransferTargetListSpriteSet / g_BuildingTransferTargetListDrawX / g_BuildingTransferTargetListDrawY` | `BuildingTransferTargetListViewState` | recovered_struct | Building / Transfer Target List | High | The dialog seeds one cached sprite set plus a shared draw origin, and the renderer/click handler both depend on that trio as one coherent transient selector-view state. | c, prior-artifact | no |

## Batch 95 - Battle Idle Animation And Slot Flag Consistency Wave
| Old Name / Pattern | New Name | Kind | Subsystem | Confidence | Evidence Summary | Sources | Subagent Evidence |
|---|---|---|---|---|---|---|---|
| `sub_431CC0` | `UnitBattle_UpdateIdleAnimatedUnits` | function | Battle / Animation | High | Iterates all 22 tactical battle slots, advances looping idle frames from `byte_512572/byte_512573`, redraws the affected footprints, triggers the same refresh for crowded units, and keeps the selected-unit charge redraw active while charge mode is enabled. | c | no |
| `UnitSlotRecord.state_flags` (older notes missing bit `0x1`) | `UnitSlotRecord.state_flags = ready / spent-turn / refusal / plague flag byte` | recovered_struct_field | Unit Stats / Runtime Slot | High | `UnitStack_HasReadyUnits`, `UnitStack_SetReadyFlags`, and `UnitStack_ClearReadyFlags` prove bit `0x1` is the ready flag; `UnitStack_SetSpentTurnFlag`, `UnitStack_ClearSpentTurnFlag`, and `UnitSlot_CanRecoverFatigue` prove bit `0x2`; `Unit_CheckLowMorale` and `UnitStack_HasLowMoraleUnit` prove bit `0x4`; the already renamed plague helpers prove bit `0x8`. | c, prior-artifact | no |
| `BattleUnitEntry` header-only layout | `BattleUnitEntry.stance_bits / anim_frame_and_effect_bits / last_animation_tick / battle_state_bits` | recovered_struct_field | Battle / Runtime State | High | `UnitBattle_DrawSelectedUnitPanel` and the combat helpers consume byte `+12` as the tactical stance/order byte, `UnitBattle_UpdateIdleAnimatedUnits` advances the low three bits of byte `+17` and rewrites dword `+18` as the animation time gate, and `UnitBattle_Defence` plus `UnitStats_CalcEffectiveRangedAttack` use the low bit at byte `+22` as a prepared-defence state. | c | no |

## Batch 95 - Port Reinforcement Semantics And Metadata Correction Wave
| Old Name / Pattern | New Name | Kind | Subsystem | Confidence | Evidence Summary | Sources | Subagent Evidence |
|---|---|---|---|---|---|---|---|
| `Port_GetSupply` | `Port_CollectReinforcementShipment` | function | Port / Reinforcements | High | The helper scans the shoreline spawn ring, creates the arriving stack from the fixed port reinforcement pool, links/syncs the army fact, flashes the new unit, clears the ready flag, and reschedules the next arrival; it is a shipment-collection path, not a generic getter. | c, asm | yes: Hubble |
| `PortRuntimeState.shoreline_variant_flag` | `PortRuntimeState.supply_arrival_visual_flag` | recovered_struct | Port / Runtime State | High | The field is cleared by `Port_NewTurn`, raised during reinforcement collection, and only steers which shoreline visual variant `Port_BuildShorePieces` renders, so it is a visual arrival latch rather than the logical ready flag. | c, prior-artifact | yes: Hubble |
| `missing j_Mem_Alloc runtime thunk` | `asm-backed stub forwarding to recovered Mem_Alloc in compat/decomp_runtime_stubs.c` | link_fix | Runtime / Memory | High | `clash95.map` binds `j_Mem_Alloc` to `0x4730FB`, `clash95.asm` shows that address as a collapsed 5-byte thunk, and the jump target is the recovered `Mem_Alloc` body at `0x461C00`, making a direct forwarding stub the lowest-risk linker repair. | c, asm, map | yes: Hypatia |
| `UnitTypeMetadataRecord[15].footprint_class_index` | `UnitTypeMetadataRecord[15].sprite_vertical_offset_px` | recovered_struct | Unit Metadata / Rendering | High | World-map and battle draw paths subtract the raw byte directly from the sprite Y anchor, redraw helpers only test it for non-zero overhang behavior, and only Pegasus/Dragon carry non-zero metadata bytes (`18`, `28`), which fits pixel lift rather than a reusable class id. | c, asm, exe, prior-artifact | yes: Goodall |
| `Unit_GetClassIndex` | `Unit_GetSpriteVerticalOffsetPx` | function | Unit Metadata / Rendering | High | The helper is a thin lookup of metadata byte `+15`, and all live callers either subtract the returned value from the world-map draw anchor or use non-zero as the redraw-overhang test. | c, prior-artifact | yes: Goodall |
| `UnitBattle_GetFootprintClass` | `UnitBattle_GetSpriteVerticalOffsetPx` | function | Battle / Rendering | High | The battle-side twin is the same thin lookup of metadata byte `+15`; callers use it to shift the selected sprite vertically and to test whether adjacent battle tiles must be redrawn for overhanging sprites. | c, prior-artifact | yes: Goodall |
| `UnitTypeMetadataRecord[30..37].terrain_move_costs[8]` | `UnitTypeMetadataRecord[30..37].world_surface_move_costs[8]` | recovered_struct | Unit Metadata / Movement | High | `Map_InitTerrainMoveTableOffsets` first normalizes raw terrain ids into one of eight lanes, then world movement readers index the unit metadata slice through that lane table. The bytes are therefore an eight-lane surface profile rather than raw terrain-id slots. | c, asm, prior-artifact | yes: Goodall |
| `sub_4127A0` definition drift | `UnitSlot_AdjustFatigueByPredicate` | link_fix | Unit Runtime / Fatigue | High | The recovered prototype and live callers already used `UnitSlot_AdjustFatigueByPredicate`, but the implementation body was still stranded under `sub_4127A0`; rebinding the definition removes one avoidable unresolved symbol without changing semantics. | c, prior-artifact | yes: Boole |

## Batch 96 - Port Shoreline Variant And Prisoner Owner Tracking Wave
| Old Name / Pattern | New Name | Kind | Subsystem | Confidence | Evidence Summary | Sources | Subagent Evidence |
|---|---|---|---|---|---|---|---|
| `Port_BuildShorePieces` | `Port_UpdateShorelineVariantTiles` | function | Port / Shoreline Visuals | High | The helper only walks the 2x2 port footprint and rewrites the secondary tile ids between the `720/721/726/727` and `718/719/722/724` shoreline variants. It does no spawning, queueing, or rules work. | c, asm, prior-artifact | yes: Bernoulli |
| `PortRuntimeState.supply_arrival_visual_flag` | `PortRuntimeState.shoreline_variant_visual_flag` | recovered_struct | Port / Runtime State | High | The port sidecar field is a pure shoreline-tile visual latch: `Port_NewTurn` clears and conditionally re-raises it, and the shoreline updater consumes it when swapping the 2x2 shoreline tile family. | c, asm, prior-artifact | yes: Bernoulli |
| `PlayerPrisonerTransferQueue.variant_or_payload_byte` | `PlayerPrisonerTransferQueue.captured_owner_player_index` | recovered_struct | Prisoners / Special Personages | High | `Unit_Capture` queues the captured special slot's owner byte, and `Prisoner_SetInCastles` later copies that same byte into the building prison slot, proving the queue byte is an owner/player id rather than a generic payload. | c, prior-artifact | no |
| `BuildingPrisonerSlot.variant_or_payload_byte` | `BuildingPrisonerSlot.captured_owner_player_index` | recovered_struct | Prisoners / Special Personages | High | `Prisoner_SetInCastles` copies queue byte `+1` into the prison slot unchanged, and `BuildingPrisoner_RecalculateRansomValue` passes slot byte `+1` into `AI_TickNationPostTurn`, proving it is a captured-owner id used by ransom logic. | c, prior-artifact | no |
| `missing _set_errno_ runtime setter` | `conservative errno setter wrapper in compat/decomp_runtime_stubs.c` | link_fix | Runtime / CRT | High | `clash95.asm` shows `__set_EINVAL_` loading `9` and tail-calling `__set_errno_`, while the recovered C uses `_set_errno_` as a plain setter/clearer path. A direct `errno = value; return value;` wrapper matches that setter role without overclaiming the deeper NT/DOS mapping helpers. | c, asm | no |

## Batch 97 - Building Sprite Cache And Temple Reward Wave
| Old Name / Pattern | New Name | Kind | Subsystem | Confidence | Evidence Summary | Sources | Subagent Evidence |
|---|---|---|---|---|---|---|---|
| `sub_43F600` | `BuildingSpriteCache_Reset` | function | Buildings / Rendering Cache | High | Clears the 50-entry, 13-byte-per-entry building sprite cache by marking each primary key byte as `-1`; world-map init calls it before building rendering begins. | c | yes: Lovelace |
| `sub_43F630` | `BuildingSpriteCache_LoadEntry` | function | Buildings / Rendering Cache | High | Selects an eviction slot, stores five one-byte visual keys plus a timestamp, and loads the sprite set from `buildin1.s32`, `buildin2.s32`, or `buildin3.s32` into the cache entry. | c, asm | yes: Lovelace |
| `sub_43F880` | `BuildingSpriteCache_GetOrLoadEntry` | function | Buildings / Rendering Cache | High | Scans the 50 cached entries for a five-byte building visual-key match, refreshes the LRU tick on hits, and otherwise falls back to `BuildingSpriteCache_LoadEntry`. | c | yes: Lovelace |
| `sub_43F920` | `BuildingSpriteCache_Clear` | function | Buildings / Rendering Cache | High | Frees every occupied cached building sprite-set handle and marks each entry free during world-map resource teardown. | c | yes: Lovelace |
| `sub_43F980` | `BuildingSpriteCache_CountEntries` | function | Buildings / Rendering Cache | High | Counts the live 13-byte building sprite-cache entries whose primary key byte is not `-1`. | c | yes: Lovelace |
| `byte_5438E8 / byte_5438E9 / byte_5438EA / byte_5438EB / byte_5438EC / dword_5438ED / dword_5438F1` | `BuildingSpriteCacheEntry` | recovered_struct | Buildings / Rendering Cache | High | The world-map building renderer keys a 50-entry cache on five one-byte visual parameters, stores a sprite-set handle at offset `+5`, stores an LRU timestamp at `+9`, and frees or reloads those same entries through the reset/clear/load helpers. | c, prior-artifact | yes: Lovelace |
| `sub_43FC60` | `Temple_SpawnGiftUnitGroup` | function | Temple / Rewards | High | Builds temporary unit slots from mission-specific or random temple reward tables and spawns the resulting ordinary unit group next to the religious site before flashing attention on the new stack. | c | yes: Lovelace |
| `sub_43FDE0` | `Temple_SpawnGiftGoldCargoStack` | function | Temple / Rewards | High | Initializes type `31` cargo slots, writes the quantity/health byte with `50`, `100`, `200`, or `300` payload-scaled values, and spawns the resulting gold cargo stack near the temple or treasure site. | c | yes: Lovelace |
| `sub_43FEF0` | `Temple_ShowOutcomePopup` | function | Temple / Rewards UI | High | Loads `temple.s32`, draws the fixed religious-site outcome frame plus an optional reward icon, prints localized reward text, and waits for dismissal before returning to the world map. | c | yes: Lovelace |
| `sub_455150` | `Rules_SyncCastleFactOwner` | function | Rules / Castle Facts | High | The helper is only reached from `Unit_CaptureBuilding` and updates the asserted castle fact's `gracz` field from `BuildingRecord.owner_player_index` through `BuildingRecord.castle_fact_id`. | c, asm, prior-artifact | yes: Lovelace |

## Batch 97 - Rules Path Host Wrapper Wave
| Old Name / Pattern | New Name | Kind | Subsystem | Confidence | Evidence Summary | Sources | Subagent Evidence |
|---|---|---|---|---|---|---|---|
| `sub_453770` | `Rules_QueuePathToCastle` | function | Rules / Pathing | High | Reuses or rebuilds a queued building-approach track for the castle occupying the target tile, temporarily enables bridge crossings for builders, and stores the resulting queued path on success. The host registration table binds the wrapper to `jest_droga_do_zamku`. | c, asm | yes: Raman |
| `sub_453C90` | `Rules_QueuePathIntoArmyRange` | function | Rules / Pathing | High | Reuses or rebuilds a `Unit_MoveTrackNearTile` path, rejects it when the acting stack lacks the cached path AP cost, and stores the queued path on success. The wrapper logs `oddzial_w_zasiegu_armii2(%d,%d,%d)` and is registered as `oddzial_w_zasiegu_armii`. | c, asm | yes: Raman |
| `sub_453FE0` | `Rules_IsTempleWithinArmyRange` | function | Rules / Temple Pathing | High | Lazily seeds the temple approach track when the cached target tile drifts, then returns true only when the acting stack's current AP can pay the cached temple-track cost. The host registration name is `swiatynia_w_zasiegu_armii`. | c, asm | yes: Raman |
| `sub_454330` | `Rules_MarchToTemple` | function | Rules / Temple Pathing | High | Reuses or rebuilds the queued temple approach track, executes the queued move, and emits the `PA` feedback only when the stack failed to advance after the march attempt. The host registration name is `maszeruj_do_swiatyni`. | c, asm | yes: Raman |
| `sub_454590` | `Rules_MarchNearTile` | function | Rules / Pathing | High | Reuses or rebuilds a queued `Unit_MoveTrackNearTile` path, executes the queued march, and emits the same `PA` failure feedback when no progress occurs. The host registration name and debug string family are both `maszeruj_blisko`. | c, asm | yes: Raman |

## Batch 97 - Runtime Finalizer And Bridge Overlay Wave
| Old Name / Pattern | New Name | Kind | Subsystem | Confidence | Evidence Summary | Sources | Subagent Evidence |
|---|---|---|---|---|---|---|---|
| `sub_4530D0` | `Rules_UnitStackHasNormalCombatUnits` | function | Rules / Stack Classification | High | Returns true only when a stack contains at least one slot whose type is neither gold cargo nor peasant cargo. Port supply pickup and special-personage predicates use it as the ordinary-combat-unit gate. | c, asm | yes: Agent C |
| `dword_51420C` | `g_BridgeApproachRoadOverlayTileIds` | table | Map / Bridge Approach | High | `sub_424020` scans 48 overlay ids from this table against `MapTileRecord.overlay_tile_id`, branches on the matched index band, and then confirms the neighboring terrain is in the bridge range `603..610`. | c, asm | yes: Raman |
| `sub_473ED5` | `CRT_RegisterFinalizableObject` | function | Runtime / CRT Finalizers | High | `clash95.asm` shows a 5-byte thunk that immediately forwards into `sub_48703D`; live callers use it to register CRT-owned objects in the shared finalizer list rather than to allocate or clear memory. | c, asm, map | yes: Ohm |
| `sub_487002` | `CRT_RunRegisteredFinalizers` | function | Runtime / CRT Finalizers | High | Pops the shared finalizer list under the CRT lock and calls each entry's vtable finalizer during shutdown. | c, asm | yes: Ohm |
| `off_51A568` | `g_CrtThreadDataAccessor` | global | Runtime / CRT TLS | High | Initialized to `CRT_GetBootstrapThreadData`, later swapped to `CRT_GetOrCreateThreadDataPreserveLastError`, and every caller treats it as the active thread-data accessor function pointer. | c, asm | yes: Ohm |
| `dword_51A648` | `g_CrtFinalizerListHead` | global | Runtime / CRT Finalizers | High | Shared head pointer written by `CRT_RegisterFinalizableObject` and consumed by `CRT_RunRegisteredFinalizers`. | c, asm | yes: Ohm |
| `sub_485374` | `CRT_GetBootstrapThreadData` | function | Runtime / CRT TLS | High | Initial thread-data accessor installed into `g_CrtThreadDataAccessor` before the fuller per-thread allocator accessor takes over. | c, asm | yes: Ohm |

## Batch 97 - Queen Treasury And Departure Wave
| Old Name / Pattern | New Name | Kind | Subsystem | Confidence | Evidence Summary | Sources | Subagent Evidence |
|---|---|---|---|---|---|---|---|
| `AI_CalcFrontlineScore` | `Player_CalcAvailableStrongholdFunds` | function | Queen / Economy | High | Sums `BuildingRecord.stored_money` across owned class-1/class-2 completed strongholds. The whim gate compares available treasury, not frontline pressure. | c, asm | yes: Agent B |
| `AI_ApplyFrontlineScore` | `Player_SpendStrongholdFundsEvenly` | function | Queen / Economy | High | Evenly subtracts the requested whim cost from owned strongholds with non-zero `stored_money` until the spend is exhausted. | c, asm | yes: Agent B |
| `QueenWhimRecord.required_frontline_score` | `QueenWhimRecord.required_stronghold_funds` | recovered_struct_field | Queen / Whim Table | High | The same uint16 is compared against `Player_CalcAvailableStrongholdFunds` and then deducted from owned stronghold treasuries by `Player_SpendStrongholdFundsEvenly` on acceptance. | c, asm, prior-artifact | yes: Agent B |
| `g_QueenNewTurnText0` | `g_QueenDepartureTexts` | table | Queen / Event Text | High | Selected only for the plain departure branch. The matching exe string says the queen decided to leave the ruler. | c, asm, exe | yes: Agent B |
| `g_QueenNewTurnText1` | `g_QueenCastleTreasuryTheftTexts` | table | Queen / Event Text | High | Used only after the chosen stronghold's `stored_money` is zeroed, and the localized string says she took the castle's jewellery and gold. | c, asm, exe | yes: Agent B |
| `g_QueenNewTurnText2` | `g_QueenCastleWellPoisoningTexts` | table | Queen / Event Text | High | Used only after the target stronghold's plague bits are forced to `5`, and the localized string says she poisoned the well there. | c, asm, exe | yes: Agent B |
| `g_QueenNewTurnText3` | `g_QueenCastleArsonTexts` | table | Queen / Event Text | High | Used only after the target stronghold is destroyed, and the localized string says she ordered the castle set on fire. | c, asm, exe | yes: Agent B |
| `g_QueenMsgBuf` | `g_QueenDepartureEventMessageBuffer` | global | Queen / Event Text | High | Only xref family is the queen departure/retaliation switch, where the buffer is built and then passed into `Queen_ShowMessageDialog`. | c, asm | yes: Agent B |
| `unk_544408` | `g_QueenBirthMessageBuffer` | global | Queen / Event Text | High | Only xref family is the queen childbirth path, where the son/daughter birth text is formatted and passed into `UI_ShowInfoWindow`. | c, asm | yes: Agent B |
| `BuildingRecord +16` | `BuildingRecord.construction_turns_remaining` | recovered_struct_field | Buildings / Construction | High | `0` marks completed buildings, positive values count staged construction down, and `-1` marks destroyed or nonexistent records across build, destruction, and queen retaliation support paths. | c, prior-artifact | yes: Agent B |

## Batch 97 - Battle Runtime AP And Target Scoring Wave
| Old Name / Pattern | New Name | Kind | Subsystem | Confidence | Evidence Summary | Sources | Subagent Evidence |
|---|---|---|---|---|---|---|---|
| `sub_432770` | `Battle_RestoreSavedActionPointsBeforeResultCopy` | function | Battle / Result Copy | High | `HandleBattleResults` calls this first, and the helper walks all 22 tactical entries restoring byte `+8` from the saved AP buffer before survivors are copied back into world/building slots. | c, asm | yes: Agent A |
| `sub_42C840` | `UnitBattle_UpdateViewportFromInputAndGetHoveredSlot` | function | Battle / Viewport Input | High | Updates `BattleRuntimeState.view_left_tile` / `view_top_tile` from keyboard scroll and lost-surface mouse-drag input, then returns the hovered occupant id from `BattleRuntimeState.tile_occupant_ids`. | c, asm | yes: Agent A |
| `sub_437630` | `UnitBattle_EstimateDamageScoreAgainstUnit` | function | Battle / AI Target Scoring | High | Temporarily simulates range-closing and repeated ranged/melee exchanges against one target, restores the original AP/coords/health state, and returns a score proportional to projected defender HP loss for AI target ranking. | c | yes: Agent A |
| `byte_532120` | `g_BattleSavedActionPointsBySlot` | global | Battle / Result Copy | High | `Battle_PlaceUnit` stores each source slot's action points here by tactical-entry index, and `Battle_RestoreSavedActionPointsBeforeResultCopy` restores those bytes before result packing. | c, asm | yes: Agent A |

## Batch 97 - Runtime Stub Containment Wave
| Old Name / Pattern | New Name | Kind | Subsystem | Confidence | Evidence Summary | Sources | Subagent Evidence |
|---|---|---|---|---|---|---|---|
| `missing CRT_RegisterFinalizableObject thunk at link time` | `asm-backed wrapper in compat/decomp_runtime_stubs.c` | link_fix | Runtime / CRT Finalizers | High | `sub_473ED5` is an exact thunk to `sub_48703D` in `clash95.asm`, so a direct wrapper removes the live unresolved runtime entry point without changing gameplay code. | c, asm, prior-artifact | yes: Agent G, Ohm |
| `missing CRT_GetBootstrapThreadData accessor at link time` | `asm-backed wrapper in compat/decomp_runtime_stubs.c` | link_fix | Runtime / CRT TLS | High | `sub_485374` is a one-instruction bootstrap accessor that returns `lpTlsValue`; mirroring it in the quarantined compat layer removes the live unresolved initial thread-data accessor symbol. | c, asm, prior-artifact | yes: Agent G, Ohm |

## Batch 98 - Runtime TLS Error And Filesystem Wrapper Wave
| Old Name / Pattern | New Name | Kind | Subsystem | Confidence | Evidence Summary | Sources | Subagent Evidence |
|---|---|---|---|---|---|---|---|
| `missing GetLastError import at link time` | `compat last-error store in compat/decomp_runtime_stubs.c` | link_fix | Runtime / Error Handling | High | `CRT_GetOrCreateThreadDataPreserveLastError` saves and restores Win32 last-error state around `TlsGetValue`, and later process/file code also queries it directly, so a quarantined host-side last-error store is the narrowest faithful containment. | c, prior-artifact | yes: Agent G |
| `missing SetLastError import at link time` | `compat last-error setter in compat/decomp_runtime_stubs.c` | link_fix | Runtime / Error Handling | High | The same TLS bootstrap path restores the saved last-error value after allocating thread data, which fixes the role as a plain last-error setter rather than gameplay logic. | c, prior-artifact | yes: Agent G |
| `missing _set_errno_nt_ helper at link time` | `approximate NT-error to errno mapper in compat/decomp_runtime_stubs.c` | link_fix | Runtime / Error Handling | Medium | Agent F corroborated that the helper is the Win32 failure mapper used after `FindFirstFileA`, `CreateDirectoryA`, `RemoveDirectoryA`, `GetFileAttributesA`, and process wait/exit failures. The current wrapper intentionally keeps a conservative fallback mapping while deeper CRT recovery remains pending. | c, asm, prior-artifact | yes: Agent F |
| `missing _set_errno_dos_ helper at link time` | `approximate DOS-error to errno mapper in compat/decomp_runtime_stubs.c` | link_fix | Runtime / Error Handling | Medium | Agent F confirmed that this companion helper is called with explicit DOS-style error codes such as `2` and `5`; the current containment keeps that role but does not yet claim the exact original CRT mapping table. | c, asm, prior-artifact | yes: Agent F |
| `missing TlsAlloc import at link time` | `compat TLS slot allocator in compat/decomp_runtime_stubs.c` | link_fix | Runtime / TLS | High | `CRT_AllocateTlsIndex` only needs a stable process-local TLS index provider so later thread-data helpers can attach `lpTlsValue`; a small quarantined slot table is sufficient until real thread startup is rebuilt. | c, asm, prior-artifact | yes: Agent G |
| `missing TlsFree import at link time` | `compat TLS slot releaser in compat/decomp_runtime_stubs.c` | link_fix | Runtime / TLS | High | `CRT_DestroyTlsIndexAndThreadData` only clears the one recovered runtime TLS slot, so a slot-table releaser matches the observed usage. | c, prior-artifact | yes: Agent G |
| `missing TlsGetValue import at link time` | `compat TLS slot getter in compat/decomp_runtime_stubs.c` | link_fix | Runtime / TLS | High | `CRT_GetOrCreateThreadDataPreserveLastError` and `CRT_DetachThreadDataAndMaybeCloseHandle` both treat this as the plain per-slot pointer fetch, with preserved last-error behavior around the call. | c, prior-artifact | yes: Agent G |
| `missing TlsSetValue import at link time` | `compat TLS slot setter in compat/decomp_runtime_stubs.c` | link_fix | Runtime / TLS | High | Thread-data creation, detach, and bootstrap initialization all use `TlsSetValue` as the plain slot assignment step once the runtime record is ready. | c, prior-artifact | yes: Agent G |
| `missing DeleteFileA import at link time` | `POSIX-backed DeleteFileA wrapper in compat/decomp_runtime_stubs.c` | link_fix | Runtime / Filesystem | High | Agent F confirmed `sub_489D18` is the direct `DeleteFileA` wrapper with immediate NT-error mapping on failure; `unlink` is the narrowest host equivalent for the current link surface. | c, asm, prior-artifact | yes: Agent F |
| `missing CreateDirectoryA import at link time` | `POSIX-backed CreateDirectoryA wrapper in compat/decomp_runtime_stubs.c` | link_fix | Runtime / Filesystem | High | Agent F confirmed `sub_489FBA` is the `mkdir`-style wrapper that returns `0` on success and maps the OS failure path through `_set_errno_nt_`. | c, asm, prior-artifact | yes: Agent F |
| `missing RemoveDirectoryA import at link time` | `POSIX-backed RemoveDirectoryA wrapper in compat/decomp_runtime_stubs.c` | link_fix | Runtime / Filesystem | High | `sub_489FD7` is the matching `rmdir` wrapper with the same NT-error failure path, so a direct host wrapper is safe containment. | c, asm, prior-artifact | yes: Agent F |
| `missing GetFileAttributesA import at link time` | `POSIX stat/access-backed GetFileAttributesA wrapper in compat/decomp_runtime_stubs.c` | link_fix | Runtime / Filesystem | High | Agent F confirmed the main caller is a file-access preflight that only needs the invalid-file sentinel and readonly-directory bits, not a richer platform abstraction. | c, asm, prior-artifact | yes: Agent F |
| `missing ExitProcess import at link time` | `host exit wrapper in compat/decomp_runtime_stubs.c` | link_fix | Runtime / Process | High | `CRT_ExitProcessWithFinalizers` is already recovered as the runtime shutdown helper that finishes by dispatching into `ExitProcess`, so a direct host `exit()` bridge is the narrowest faithful containment. | c, asm, prior-artifact | yes: Agent F |

## Batch 98 - Process, Critical Section, And SDL Message Seam Wave
| Old Name / Pattern | New Name | Kind | Subsystem | Confidence | Evidence Summary | Sources | Subagent Evidence |
|---|---|---|---|---|---|---|---|
| `missing GetCurrentProcessId import at link time` | `host pid-backed GetCurrentProcessId wrapper in compat/decomp_runtime_stubs.c` | link_fix | Runtime / Process | High | Agent G flagged this as one of the remaining thin helper wrappers. Current callsites only need a stable process identifier during CRT/process-launch setup. | c, prior-artifact | yes: Agent G |
| `missing GetCurrentThreadId import at link time` | `single-thread pid-backed GetCurrentThreadId wrapper in compat/decomp_runtime_stubs.c` | link_fix | Runtime / TLS | High | The current recovered host still runs the decompiled core on one thread, so a pid-backed placeholder is the narrowest safe containment until actual thread creation/resume recovery lands. | c, prior-artifact | yes: Agent G |
| `missing InitializeCriticalSection import at link time` | `local critical-section bootstrap wrapper in compat/decomp_runtime_stubs.c` | link_fix | Runtime / Synchronization | High | Agent G identified the critical-section family as the last thin synchronization wrapper band before the behavior-sensitive event/thread frontier. The current wrapper only seeds the recovered structure fields and avoids claiming full scheduler semantics. | c, prior-artifact | yes: Agent G |
| `missing EnterCriticalSection import at link time` | `local critical-section enter wrapper in compat/decomp_runtime_stubs.c` | link_fix | Runtime / Synchronization | High | Runtime/audio/CRT code only needs the structure-level enter bookkeeping to keep the current single-thread build surface moving; full blocking semantics remain deferred with the event/thread layer. | c, prior-artifact | yes: Agent G |
| `missing LeaveCriticalSection import at link time` | `local critical-section leave wrapper in compat/decomp_runtime_stubs.c` | link_fix | Runtime / Synchronization | High | Complements the same single-thread bookkeeping path and intentionally stops short of claiming a fully reconstructed Win32 lock implementation. | c, prior-artifact | yes: Agent G |
| `missing DeleteCriticalSection import at link time` | `local critical-section teardown wrapper in compat/decomp_runtime_stubs.c` | link_fix | Runtime / Synchronization | High | The observed callers only tear the structure down during runtime cleanup, so zeroing the recovered struct is the narrowest containment. | c, prior-artifact | yes: Agent G |
| `missing CloseHandle import at link time` | `non-owning CloseHandle compatibility wrapper in compat/decomp_runtime_stubs.c` | link_fix | Runtime / Process | Medium | The current host still lacks real event/process/thread handle ownership reconstruction, so the wrapper intentionally acts as a conservative non-owning success path for the remaining runtime scaffolding. | c, prior-artifact | yes: Agent G |
| `missing ResumeThread import at link time` | `single-thread ResumeThread compatibility wrapper in compat/decomp_runtime_stubs.c` | link_fix | Runtime / Threading | Medium | The build still lacks real thread creation/resume reconstruction; the current wrapper only preserves the non-crashing control-flow surface for single-thread containment. | c, prior-artifact | yes: Agent G |
| `GetMessageA returns 0 on empty queue` | `non-quit empty-queue GetMessageA behavior in platform_sdl_runtime.c` | sdl_port_fix | SDL / Message Pump | High | Agent H traced the inactive-app pump loop and showed that the old seam treated an empty queue like quit. Returning a zeroed message but non-zero status on the empty non-quit path matches the surrounding `PeekMessageA` gate more closely. | c, prior-artifact | yes: Agent H |
| `WaitMessage immediately returns on idle queue` | `idle-sleep WaitMessage shim in platform_sdl_runtime.c` | sdl_port_fix | SDL / Message Pump | High | Agent H showed the current seam skipped the intended inactive-app wait semantics entirely. A short host sleep keeps the SDL seam conservative without reviving Win32. | c, prior-artifact | yes: Agent H |
| `ClientToScreen ignores window origin` | `window-origin ClientToScreen shim in platform_sdl_runtime.c` | sdl_port_fix | SDL / Coordinates | High | Agent H traced the DirectDraw-facing clipper path and showed the existing seam already tracks `SDL_Window.x/y`; applying that offset is the narrowest fix. | c, prior-artifact | yes: Agent H |

## Batch 99 - Builder Path Queue And Port Reinforcement Wave
| Old Name / Pattern | New Name | Kind | Subsystem | Confidence | Evidence Summary | Sources | Subagent Evidence |
|---|---|---|---|---|---|---|---|
| `sub_424EC0` | `UnitStack_MoveOneTileInDirection` | function | Strategic / Pathing | High | Converts a direction id through `Map_NeighborDX` / `Map_NeighborDY`, builds a one-step `Unit_MoveTrack`, copies it into the queued-path buffer, executes it immediately, and redraws the stack. | c, asm | yes: Agent C+D |
| `sub_454A20` | `Rules_IsQueuedPathTargetBridgeCrossing` | function | Rules / Bridge Roadbuilding | High | Requires a queued path, inspects the final queued node, and returns true only when `Map_GetBridgeCrossingCostOrZero` is non-zero there. The host registration string is `jest_brod`. | c, asm | yes: Agent C+D |
| `sub_454AE0` | `Rules_BuildRoadOrStepTowardQueuedPath` | function | Rules / Roadbuilding | High | Registered as `buduj_droge`; follows the queued path's last-step direction, prefers stepping on owned non-bridge tiles, otherwise tries `Road_Build` with direction fallbacks, and clears the queued path afterward. | c, asm | yes: Agent C+D |
| `sub_454D20` | `Rules_BuildTrapNearTile` | function | Rules / Trap Placement | High | Registered as `buduj_pulapke`; builds a `Unit_MoveTrackNearTile`, executes it, then creates the trap only if the acting stack finishes within one tile of the target coordinates. | c, asm | yes: Agent C+D |
| `dword_517BA8` | `g_PortSupplyUnitTypePool` | table | Port / Reinforcements | High | `Port_CollectReinforcementShipment` rolls `Rng_RandRange(0, 11)` for each arriving slot and passes the indexed table entry straight into `UnitSlot_InitFromType`; asm decodes the fixed roster `{0,1,2,3,4,5,7,9,10,15,16,17}`. | c, asm, prior-artifact | yes: Agent C+D |

## Batch 99 - SDL Seam Declaration Ownership Wave
| Old Name / Pattern | New Name | Kind | Subsystem | Confidence | Evidence Summary | Sources | Subagent Evidence |
|---|---|---|---|---|---|---|---|
| `clash95.c-local window/message/DC wrapper declarations` | `platform_sdl.h-owned SDL seam wrapper declarations` | sdl_port_fix | SDL / Header Ownership | High | The implementations already live in `platform_sdl_runtime.c`, but `clash95.c` still redeclared the same Win32-shaped wrapper surface locally. Moving the prototypes into `platform_sdl.h` keeps the compatibility boundary owned by the SDL seam. | c, prior-artifact | yes: Agent H |
| `PeekMessageA empty false-return leaves MSG contents stale` | `zeroed empty false-return path in platform_sdl_runtime.c` | sdl_port_fix | SDL / Message Pump | High | The only live caller reads `Msg.hwnd` immediately after a false `PeekMessageA` return in the inactive-app loop, so clearing `MSG` on the empty non-quit path removes the remaining idle-path UB without reintroducing Win32 behavior. | c, prior-artifact | yes: Agent H |

## Batch 100 - Local JUMPOUT Scar Recovery Wave
| Old Name / Pattern | New Name | Kind | Subsystem | Confidence | Evidence Summary | Sources | Subagent Evidence |
|---|---|---|---|---|---|---|---|
| `JUMPOUT(0x4065BD) in sub_4065D0` | `direct shared-epilogue return of the computed minimum run length` | compile_fix | Render / Sprite Decode | High | Agent G traced the asm to a shared epilogue at `loc_4065BD` that only moves the local `var_1C` into `eax` and returns. The recovered C now returns the same computed local directly instead of calling the unresolved `JUMPOUT` shim. | c, asm | yes: Agent G |
| `JUMPOUT(0x4065BD) in sub_406650` | `direct shared-epilogue return of the computed row count` | compile_fix | Render / Sprite Decode | High | The same shared epilogue services `sub_406650`; asm shows no hidden side effects beyond returning the local counter in `eax`, so the C body now returns that value directly. | c, asm | yes: Agent G |
| `JUMPOUT(0x4E619D) in sub_4E60F5` | `recovered ownership-flag left-shift loop after environment-table compaction` | compile_fix | Runtime / Environment Table | Medium | Agent G tied the target to an in-function byte-copy loop inside `sub_4E60F5`, not to an external helper. The asm bytes at `0x4E6199` decode to copying `flags[i + 1]` down into `flags[i]` while the compacted environment table closes the removed slot. | c, asm | yes: Agent G |

## Deferred / Ambiguous
- Keep the asm-backed `Unit_FindById(g_CurrentPlayerIndex)` call symbol in the queen retaliation cases. The behavior acts like a random owned completed-castle selector there, but the call target itself is already constrained by the assembly.
- `sub_48703D` is now boxed in as the internal list-link worker behind `CRT_RegisterFinalizableObject`, but the exact original CRT helper label is still not strong enough to promote.
- `sub_4620F0` still looks like a generic illustrated event/interlude presenter; the queen-specific tokens were not enough to lock a broader semantic rename safely.

## Batch 101 - Unit Stat Band Correction Wave
| Old Name / Pattern | New Name | Kind | Subsystem | Confidence | Evidence Summary | Sources | Subagent Evidence |
|---|---|---|---|---|---|---|---|
| `UnitStats_CalcEffectiveRangedAttack` | `UnitStats_CalcEffectiveDefensePower` | function | Unit Stats | High | `UnitSlots_CalcDefenseScore`, `UnitBattle_CalcMeleeExchange`, `Trap_HurtStack`, and `UnitBattle_CalcShotTargetHealthAfterHit` all consume this helper or its base table as the defending side's resistance scalar, while the battle UI presents the same family under `Defence power`. | c, asm | no |
| `UnitStats_GetRangedIconIndex` | `UnitStats_GetDefenseIconIndex` | function | Unit Stats / UI | High | The selected-unit battle and garrison panels draw this unconditional numeric/icon row under the localized `Defence power` label, not under any ranged-only row. | c, asm | no |
| `UnitStats_CalcEffectiveDamagePerHit` | `UnitStats_CalcEffectiveShotPower` | function | Unit Stats | High | Agent A boxed this family in as metadata byte `+25`; the local UI/combat pass confirmed it is the `Shot power` row and the outgoing projectile-strength term used by shot-damage projection. | c, asm | yes: Agent A |
| `UnitStats_GetBaseDamage` | `UnitStats_GetBaseShotPower` | function | Unit Stats / UI | High | The non-scaled companion is the exact helper displayed under `Shot power` in the selected-unit panels whenever metadata byte `+25` is non-zero. | c, asm | yes: Agent A |
| `g_UnitTypeBaseRangedAttack` | `g_UnitTypeBaseDefensePower` | table | Unit Stats | High | The table is indexed by the target unit type in `UnitBattle_CalcShotTargetHealthAfterHit`, summed by `UnitSlots_CalcDefenseScore`, and scaled by the prepared-defence bit, which identifies it as defence power rather than outgoing ranged attack. | c, asm | no |
| `g_UnitTypeBaseDamage` | `g_UnitTypeBaseShotPower` | table | Unit Stats | High | The table feeds `UnitStats_CalcEffectiveShotPower` / `UnitStats_GetBaseShotPower`, matches the localized `Shot power` row, and supplies the outgoing projectile-strength term before target defence is applied. | c, asm | yes: Agent A |
| `UnitTypeMetadataRecord +22..+28` | `UnitTypeMetadataRecord.base_melee_attack / base_defense_power / base_action_points / base_shot_power / attack_range_max / attack_range_min / base_siege_attack` | recovered_struct | Unit Metadata | High | Agent A resolved the surrounding stat band at `+22/+24/+25/+26`; the local corroboration pass completed `+23` as defence power from battle-resolution/UI callsites and `+27` as the min-range floor from `UnitBattle_IsTileWithinRange`, yielding one contiguous per-type combat-stat band. | c, asm, prior-artifact | yes: Agent A |

## Batch 102 - Wall Attack Stat Recovery Wave
| Old Name / Pattern | New Name | Kind | Subsystem | Confidence | Evidence Summary | Sources | Subagent Evidence |
|---|---|---|---|---|---|---|---|
| `UnitStats_CalcEffectiveSiegeAttack` | `UnitStats_CalcEffectiveWallAttack` | function | Unit Stats / Fortification Assault | High | The helper has only two confirmed callers: the ram's building-assault branch inside `UnitSlots_CalcCombatStrengthScore`, and `UnitBattle_AttackWall`, where its result is subtracted directly from wall-section health. No unit-vs-unit or generic projectile caller uses this family. | c, asm | no |
| `g_UnitTypeBaseSiegeAttack` | `g_UnitTypeBaseWallAttack` | table | Unit Stats / Fortification Assault | High | The underlying table is read only by `UnitStats_CalcEffectiveWallAttack`, which in turn is only used for wall assaults and fortification-strength scoring. | c, asm | no |
| `UnitTypeMetadataRecord +28 / base_siege_attack` | `UnitTypeMetadataRecord.base_wall_attack` | recovered_struct | Unit Metadata | High | Metadata byte `+28` is the same table-backed scalar consumed by `UnitStats_CalcEffectiveWallAttack`; the direct tactical wall-attack path and the ram's building-assault scoring branch both agree on the narrower wall-assault role. | c, asm, prior-artifact | no |

## Batch 103 - Unit Production Metadata Recovery Wave
| Old Name / Pattern | New Name | Kind | Subsystem | Confidence | Evidence Summary | Sources | Subagent Evidence |
|---|---|---|---|---|---|---|---|
| `byte_5125AF` | `g_UnitTypeProductionTime` | table | Unit Metadata / Building Production | High | `Building_SetUnitProduction` seeds `BuildingRecord +415` from this metadata byte, `Building_ProcessUnitProductionTurn` counts that byte down each turn, and the asm-backed building-info string labels the same record field as `production time`. | c, asm | yes: Agent E |
| `byte_5125B0` | `g_UnitTypeProductionCost` | table | Unit Metadata / Building Production | High | `Building_ProcessUnitProductionTurn` compares this metadata byte against `BuildingRecord.stored_money`, subtracts it on completion, and the adjacent asm-backed building-info label names the same field `production cost`. | c, asm, prior-artifact | yes: Agent E |
| `UnitTypeMetadataRecord +71` | `UnitTypeMetadataRecord.production_time` | recovered_struct | Unit Metadata / Building Production | High | `0x5125AF - 0x512568 = 71`, and both production helpers index the 88-byte per-type metadata record by the selected unit type before reading the countdown value copied into `BuildingRecord +415`. | c, asm, prior-artifact | yes: Agent E |
| `UnitTypeMetadataRecord +72` | `UnitTypeMetadataRecord.production_cost` | recovered_struct | Unit Metadata / Building Production | High | `0x5125B0 - 0x512568 = 72`, and the same 88-byte indexing path reads the money-cost byte that gates and funds completed unit production. | c, asm, prior-artifact | yes: Agent E |

## Batch 104 - Unit Production Licence Metadata Recovery Wave
| Old Name / Pattern | New Name | Kind | Subsystem | Confidence | Evidence Summary | Sources | Subagent Evidence |
|---|---|---|---|---|---|---|---|
| `Building_AddonCostLUT` | `g_UnitTypeProductionLicenceCost` | table | Unit Metadata / Building Production | High | `Building_BuyUnitLicence` debits this word-indexed table before appending a unit type into the building's 12-slot licence roster, and `Building_GetTotalValue` adds the same word for each installed licence, proving it is the per-type production-licence purchase cost rather than a generic building-only add-on fee. | c, asm | yes: Agent E |
| `Building_ReqLevelByModeA` | `g_UnitTypeProductionRequiredTechLevelMode2` | table | Unit Metadata / Building Production | High | `Building_CanEquipAddon` selects this byte when `building[4] == 2`, compares it against `BuildingRecord.tech_level_bits & 7`, and `CastleProduction_RebuildAvailableUnitList` uses the same gate to decide which unit types appear in the licensable roster. | c, asm | yes: Agent E |
| `Building_ReqLevelByModeB` | `g_UnitTypeProductionRequiredTechLevelOtherModes` | table | Unit Metadata / Building Production | High | The same eligibility helper switches to this byte when the production-building mode is non-zero but not `2`, so it is the alternate per-type tech-level threshold family for the same licence gate. | c, asm | yes: Agent E |
| `UnitTypeMetadataRecord +73/+74` | `UnitTypeMetadataRecord.production_licence_cost` | recovered_struct | Unit Metadata / Building Production | High | `0x5125B1 - 0x512568 = 73`, the table is 16-bit and 88-stride indexed by unit type, and both the purchase helper and building-value scorer consume the same word as the installed licence's price contribution. | c, asm, prior-artifact | yes: Agent E |
| `UnitTypeMetadataRecord +75` | `UnitTypeMetadataRecord.production_required_tech_level_mode_2` | recovered_struct | Unit Metadata / Building Production | High | `0x5125B3 - 0x512568 = 75`, and `Building_CanEquipAddon` reads that byte only when `BuildingRecord +4 == 2` before comparing it against the building's tech-level bits. | c, asm, prior-artifact | yes: Agent E |
| `UnitTypeMetadataRecord +76` | `UnitTypeMetadataRecord.production_required_tech_level_other_modes` | recovered_struct | Unit Metadata / Building Production | High | `0x5125B4 - 0x512568 = 76`, and the same gate reads that byte when the production-building mode is the alternate non-zero family, proving a second per-type tech threshold immediately adjacent to the recovered production fields. | c, asm, prior-artifact | yes: Agent E |

## Batch 105 - World Surface Movement Lane Recovery Wave
| Old Name / Pattern | New Name | Kind | Subsystem | Confidence | Evidence Summary | Sources | Subagent Evidence |
|---|---|---|---|---|---|---|---|
| `byte_512585` | `g_UnitTypeRoadMoveCost` | table | Unit Metadata / World Movement | High | World movement reads this byte directly whenever the tile has a road or bridge overlay, bypassing the normalized surface-lane table entirely. | c, asm, prior-artifact | yes: Agent C |
| `byte_512586` | `g_UnitTypePlainClassMoveCostA` | table | Unit Metadata / World Movement | Medium | This is lane `0` of the normalized eight-byte world-surface profile. The terrain-class pass proves it is one of two distinct plain/open-ground lanes, but not the exact designer-facing terrain label beyond that conservative class. | c, asm, exe | yes: Agent A+F, Agent D |
| `unk_512587` | `g_UnitTypeForestMoveCost` | table | Unit Metadata / World Movement | High | Raw terrain ids `45..160` normalize to this byte, the canonical terrain-class table maps that whole band to class `185`, and the world tooltip labels class `185` as `Forest`. | c, asm, exe | yes: Agent A+F, Agent D |
| `unk_512588` | `g_UnitTypeDesertMoveCost` | table | Unit Metadata / World Movement | Medium | This normalized lane is the desert-class band in every directly classified terrain range, but the high tail of raw ids still needs tighter tileset corroboration, so the name stays conservative. | c, asm, exe | yes: Agent A+F, Agent D |
| `unk_512589` | `g_UnitTypeSwampMoveCost` | table | Unit Metadata / World Movement | High | Raw terrain id `7` alone normalizes to this byte, the canonical terrain-class table maps that id to class `39`, and the world tooltip labels class `39` as `Swamp`. | c, asm, exe | yes: Agent A+F, Agent D |
| `unk_51258A` | `g_UnitTypePlainClassMoveCostB` | table | Unit Metadata / World Movement | Medium | This is the second normalized plain/open-ground lane. Theme-0 terrain classification maps its raw-id band to `Plain`, but the exact split between the two plain-class lanes is still unresolved. | c, asm, exe | yes: Agent A+F, Agent D |
| `unk_51258B` | `g_UnitTypeWaterSurfaceMoveCost` | table | Unit Metadata / World Movement | Medium | Most raw ids in this lane classify as `Water`, but the same normalized band also absorbs a small hills/desert outlier range and the bridge-terrain band, so `water_surface` is the narrowest safe placeholder. | c, asm, exe | yes: Agent A+F, Agent D |
| `unk_51258C` | `g_UnitTypeHillsMoveCost` | table | Unit Metadata / World Movement | High | Raw terrain ids `161..185` normalize to this byte, the canonical terrain-class table maps that band to class `204`, and the world tooltip labels class `204` as `Hills`. | c, asm, exe | yes: Agent A+F, Agent D |
| `unk_51258D` | `g_UnitTypeMountainsMoveCost` | table | Unit Metadata / World Movement | High | Raw terrain ids `186..221` plus `746..750` normalize to this byte, the canonical terrain-class table maps both bands to class `202`, and the world tooltip labels class `202` as `Mountains`. | c, asm, exe | yes: Agent A+F, Agent D |

## Batch 106 - Special Personage Garrison And Licence Sync Wave
| Old Name / Pattern | New Name | Kind | Subsystem | Confidence | Evidence Summary | Sources | Subagent Evidence |
|---|---|---|---|---|---|---|---|
| `Building_HasAddonLicence` | `Building_HasUnitLicence` | function | Buildings / Production Licences | High | The helper scans building bytes `+402..+413` for a requested unit type id, and the castle production panel plus rules-host wrappers use it strictly as the production-licence ownership predicate. | c, prior-artifact | yes: Agent B, Agent F+G |
| `Building_CanEquipAddon` | `Building_IsUnitLicenceEligible` | function | Buildings / Production Licences | High | The helper selects the per-unit production-tech threshold tables, applies the building-mode split plus smith/workshop whitelist gates, and is reused by both the buy-licence flow and the castle available-unit roster rebuild, so it is the unit-licence eligibility gate rather than a generic add-on installer. | c, asm, prior-artifact | yes: Agent B, Agent F+G |
| `Building_SelectedAddonMatchesTypeByIndex` | `Building_SelectedUnitLicenceMatchesTypeByIndex` | function | Buildings / Production Licences | High | Reads the currently selected licence slot byte at `BuildingRecord +414` and compares the indexed type id from `+402..+413` against the requested unit type, matching the castle panel's selected-licence semantics. | c, prior-artifact | yes: Agent E |
| `BuildingRecord.addon_type_ids[12]` | `BuildingRecord.unit_licence_type_ids[12]` | recovered_struct | Buildings / Production Licences | High | `Building_BuyUnitLicence`, `Building_RemoveUnitLicence`, the selected-licence predicate, and the castle panel all treat bytes `+402..+413` as the installed per-unit production-licence roster rather than the real castle add-on state. | c, prior-artifact | yes: Agent D, Agent F+G |
| `BuildingRecord.selected_addon_slot_index` | `BuildingRecord.selected_unit_licence_slot_index` | recovered_struct | Buildings / Production Licences | High | Byte `+414` is set by `Building_SetUnitProduction`, cleared by `Building_StopUnitProduction`, and read by the selected-licence predicate, so it tracks the selected production-licence slot rather than a generic add-on index. | c, prior-artifact | yes: Agent D, Agent F+G |

## Batch 107 - SDL Seam Ownership And Local JUMPOUT Repair Wave
| Old Name / Pattern | New Name | Kind | Subsystem | Confidence | Evidence Summary | Sources | Subagent Evidence |
|---|---|---|---|---|---|---|---|
| `clash95.c`-local declarations for `GetTickCount` / `GetVersion` / `GetModuleHandleA` / `GetDriveTypeA` / `OutputDebugStringA` | `platform_sdl.h`-owned SDL seam declarations | sdl_port_fix | SDL / Header Ownership | High | All five wrappers are already implemented in `platform_sdl_runtime.c`, but `clash95.c` was still redeclaring them locally. Moving the declarations into `platform_sdl.h` keeps the Win32-shaped compatibility surface owned by the SDL seam instead of the recovered gameplay translation unit. | c, prior-artifact | yes: Agent C+H |
| `JUMPOUT(0x47103D) in sub_471B40` | `direct call into sub_471030` | compile_fix | Runtime / Stream State | High | The asm shows `sub_471B40` only bounds-checks `a1` and tail-jumps into `sub_471030`'s in-function block at `loc_47103D`, which simply clears the record field at `+96` and returns. Replacing the scar with `sub_471030(a1)` preserves the exact guarded behavior without an out-of-bounds control-flow escape. | c, asm | yes: Agent F+G |

## Batch 108 - Unit Metadata Front Edge And Hash Probe Repair Wave
| Old Name / Pattern | New Name | Kind | Subsystem | Confidence | Evidence Summary | Sources | Subagent Evidence |
|---|---|---|---|---|---|---|---|
| `g_UnitSpriteFolders` | `g_UnitTypeResourceKeys` | global | Unit Types / Asset Metadata | High | Metadata field `+4` is reused as the shared asset stem for `units_go`, `units_i`, `units_at`, and `sfx\\oddzialy` path construction, so the older sprite-folder label was too narrow. | c, asm, prior-artifact | yes: Agent A, Agent D |
| `UnitTypeMetadataRecord.localized_name_ptrs` | `UnitTypeMetadataRecord.localized_name_table` | recovered_struct | Unit Types / Metadata | High | Field `+0` is always loaded as one pointer and then indexed by language in UI and battle-log helpers; the asm table rows show the final special-personage pair sharing one localized-name table pointer rather than embedding multiple unrelated pointer slots. | c, asm, prior-artifact | yes: Agent A, Agent D |
| `UnitTypeMetadataRecord.sprite_folder_name` | `UnitTypeMetadataRecord.resource_key` | recovered_struct | Unit Types / Metadata | High | Field `+4` is not limited to sprite folders: it is also reused by unit-info sheets and unit sound paths, so the narrower folder-only name was misleading. | c, asm, prior-artifact | yes: Agent A, Agent D |
| `undefined temporaries in sub_478950` | `asm-backed keyed-entry probe loop` | compile_fix | Runtime / Hash Table | High | The asm around `0x478970..0x4789AD` shows a stable probe loop: hash the string, walk backward through slots, stop on empty state, and return the matched entry pointer on `strncmp_ == 0`. | c, asm | yes: Agent G |
| `void sub_4789C0 + JUMPOUT(0x478765)` | `slot-index return path` | compile_fix | Runtime / Hash Table | High | `sub_4789C0` does not tail-call an external helper: the target `loc_478765` is only the shared epilogue `mov eax, ecx; retn`, so returning the probed vacant slot index directly preserves behavior and removes the remaining local scar. | c, asm | yes: Agent G |
| `undefined lookup-result temporaries in sub_4789F0` | `propagated keyed-entry lookup result` | compile_fix | Runtime / Hash Table | High | The asm moves `sub_478950`'s return value into `ecx`, preserves it across the temporary stack-string cleanup, and then reuses that same entry pointer for the type/value checks and `sub_478770` constructor inputs. | c, asm | no |
| `undefined temporaries in sub_478A60 after sub_4789C0` | `asm-backed keyed-entry insert path` | compile_fix | Runtime / Hash Table | High | Once `sub_4789C0` returns the vacant slot index, the asm at `0x478A94..0x478AFC` cleanly resolves the rest of the insert path: clear key byte `13`, copy the key, write the state/type fields, mark both dirty flags, increment the count, and return the slot index. | c, asm | yes: Agent G |

## Batch 109 - Castle Building Host Helper Rename Wave
| Old Name / Pattern | New Name | Kind | Subsystem | Confidence | Evidence Summary | Sources | Subagent Evidence |
|---|---|---|---|---|---|---|---|
| `sub_455830` | `Rules_BuildCastle` | function | Buildings / Rules Host | High | The helper is only a thin wrapper over `Building_New(a1, a2, a3, a4, 0)`, and the rules-host registration exports it under `BudujZamek`, so the behavior is specifically the castle-build host action. | c, asm | yes: Agent A, Agent E, Agent F+G |
| `sub_455850` | `Building_FindUnitLicenceSlotIndexOrZero` | function | Buildings / Production Licences | High | The helper linearly scans the recovered `BuildingRecord.unit_licence_type_ids[12]` bytes for a requested unit type and returns the matched slot index; both the asm and the `LicencjaIndex` host registration confirm the licence-slot lookup role. The `OrZero` suffix is required because the function also returns `0` on miss, colliding with a real slot-0 hit. | c, asm, prior-artifact | yes: Agent A, Agent E, Agent F+G |
| `sub_455EC0` | `Player_GetInternedNameByIndex` | function | Player Runtime / Rules Host | High | Although the host surface still calls it `NazwaZamku`, the implementation allocates a 12-byte buffer, copies the per-player string at `gameData + 140028 + 1423 * playerIndex`, interns it, frees the temporary buffer, and returns the interned pointer. The same base is initialized and drawn as the player-name block elsewhere, disproving a literal castle-name accessor. | c, asm, prior-artifact | yes: Agent A, Agent B, Agent E, Agent F+G |
| `sub_455F60` | `Map_IsCastleSiteDistanceMinimal` | function | Map / Castle Sites | Medium | The helper computes the candidate distance to a reference point, then rejects it if any cached castle-site anchor is strictly closer. `Map_RebuildCastleSiteAnchorCache` rebuilds the paired cache immediately adjacent to the arrays this helper walks, and the host registration `CzyMinimalny` confirms a conservative minimal-distance predicate. | c, asm | yes: Agent A, Agent D, Agent F+G |

## Batch 110 - Castle Licence Selector Clarification Wave
| Old Name / Pattern | New Name | Kind | Subsystem | Confidence | Evidence Summary | Sources | Subagent Evidence |
|---|---|---|---|---|---|---|---|
| `sub_455890` | `Building_FindFirstNonPeasantNonBuilderLicenceSlotOrZero` | function | Buildings / Production Licences | High | The helper scans the recovered `unit_licence_type_ids[12]` roster, skips entries `<= 0`, skips unit type `17`, and returns the first remaining slot index or `0`. New-building initialization seeds slot `0` to unit type `0` and all later slots to `-1`, proving the `> 0` filter explicitly skips the built-in peasant licence rather than empty slots, while the `!= 17` arm skips builder licences. | c, asm, prior-artifact | yes: Agent A, Agent C+H, Agent E |

## Batch 111 - Army Fact Owner Sync Rename Wave
| Old Name / Pattern | New Name | Kind | Subsystem | Confidence | Evidence Summary | Sources | Subagent Evidence |
|---|---|---|---|---|---|---|---|
| `sub_4550F0` | `Rules_SyncArmyFactOwner` | function | Rules / Army Facts | High | The helper sits directly beside `Rules_SyncArmyFactStrength` and `Rules_SyncCastleFactOwner`, first calls `Rules_EnsureArmyFactForStack`, then writes the stack owner's player id into the `"gracz"` slot of the existing army fact through `sub_480160`. Capture paths call it exactly when stack ownership changes without necessarily rebuilding the whole fact, making the owner-sync role precise. | c, asm, map | yes: Agent E, Agent F+G |

## Batch 112 - Terrain Surface Classifier Wave
| Old Name / Pattern | New Name | Kind | Subsystem | Confidence | Evidence Summary | Sources | Subagent Evidence |
|---|---|---|---|---|---|---|---|
| `Map_DestroyTile` | `Map_GetTileSurfaceClassOrUnexplored` | function | Map / Terrain Classification | High | The helper never mutates map state: it reads the tile terrain id and overlay word, collapses them through the canonical terrain-class table, forces the road class when a road/bridge overlay is present, and returns `1` for tiles outside current-player visibility. The world-map hover panel decodes its outputs as `Plain`, `Forest`, `Desert`, `Swamp`, `Water`, `Hills`, `Mountains`, and `Road`, while placement, trap, pathing, and move-sound callers all use it as a pure surface classifier. | c, asm, map | yes: Agent D |

## Batch 113 - Prisoner Text Table Rename Wave
| Old Name / Pattern | New Name | Kind | Subsystem | Confidence | Evidence Summary | Sources | Subagent Evidence |
|---|---|---|---|---|---|---|---|
| `off_518D08` | `g_PrisonerCastleIntakeTexts` | global | Prisoners / UI Text | High | Used only by `Prisoner_SetInCastles` after a captured `33/34` entry is placed into a castle prison; the localized strings report that the enemy captive has been brought to castle `%s`. | c, asm | yes: Agent B |
| `off_518D14` | `g_PrisonerBeheadingTexts` | global | Prisoners / UI Text | High | Used only by `Prisoner_Behead`, and every localized string is the beheading outcome text for a prisoner in castle `%s`. | c, asm | yes: Agent B |
| `off_518D20` | `g_PrisonerTortureRichestCastleRevealTexts` | global | Prisoners / UI Text | High | Selected only in torture cases `0/5`, which call `Prisoner_FindRichestHiddenEnemyCastle`; the strings report that the prisoner revealed the richest hidden enemy castle before dying. | c, asm | yes: Agent B |
| `off_518D2C` | `g_PrisonerTortureCastleRevealTexts` | global | Prisoners / UI Text | High | Selected only in torture cases `1/6`, which call `Prisoner_FindAnyHiddenEnemyCastle`; the strings report that the prisoner revealed one of the enemy king's castles. | c, asm | yes: Agent B |
| `off_518D38` | `g_PrisonerTortureEnemyStackRevealTexts` | global | Prisoners / UI Text | High | Selected only in torture cases `2/7`, which call `Prisoner_FindAnyHiddenEnemyUnitStack`; the strings report that the prisoner gave up the location of an enemy field stack. | c, asm | yes: Agent B |
| `off_518D44` | `g_PrisonerTortureNoConfessionDeathTexts` | global | Prisoners / UI Text | High | Selected only in torture case `3`, where the prisoner is killed and the strings say he died without revealing anything. | c, asm | yes: Agent B |
| `off_518D50` | `g_PrisonerTortureResistanceTexts` | global | Prisoners / UI Text | High | Selected only in torture case `4`, where the prison action is reset instead of killing the prisoner; the strings stress resistance to torture rather than a confession. | c, asm | yes: Agent B |
| `off_518D8C` | `g_PrisonerBriberyDefectionTexts` | global | Prisoners / UI Text | High | Used only by `Prisoner_Pay`, which spends the ransom/bribe, recreates a friendly `33/34` garrison unit, and shows text that the prisoner joined your side. | c, asm | yes: Agent B |

## Batch 117 - Battle AI Deployment Wave
| Old Name / Pattern | New Name | Kind | Subsystem | Confidence | Evidence Summary | Sources | Subagent Evidence |
|---|---|---|---|---|---|---|---|
| `sub_43C1E0` | `UnitBattle_BuildAiUnitQueueForCurrentMode` | function | Battle / AI | High | The helper scans all active slots on one side, orders them according to the current tactical AI mode in `dword_515A10`, and pushes the resulting unit ids into the action queue at `dword_5437C0` before the turn loop consumes them. | c, asm | yes: Agent A |
| `sub_43CC50` | `UnitBattle_SelectAiPlanMode` | function | Battle / AI | High | Both callers use this helper only to choose the small mode code later stored in `dword_515A10`, and the decision branches depend on wall presence, side ownership, and the current battle cycle state. | c, asm | yes: Agent A |
| `sub_43D2D0` | `Battle_DeploySideUnitsByRoleBuckets` | function | Battle / Deployment | High | Called exactly once per side during tactical setup; it compares side-strength ratios, chooses a role-order preset, and immediately hands off to the bucket builder plus placement helpers to deploy that side's units. | c, asm | yes: Agent A |
| `sub_43D430` | `Battle_BuildRoleDeploymentBuckets` | function | Battle / Deployment | High | The body clears the deployment scratch arrays and groups unit pointers into three bucket tables by combining `g_UnitTypeRole` with the role-order presets in `byte_515A70`. | c, asm | yes: Agent A |
| `sub_43D6E0` | `Battle_PlaceRoleDeploymentBuckets` | function | Battle / Deployment | High | The helper walks the three deployment bucket tables and repeatedly calls `sub_43D560` to place units on valid battle tiles in role-priority order. | c, asm | yes: Agent A |
| `byte_5125B6` | `g_UnitTypeCorpseSpriteBaseIndex` | global | Unit Metadata / Rendering | High | `UnitBattle_GetCorpseSpriteIndex` indexes this byte by `88 * unitType` and adds the facing-derived frame offset, proving the table stores the per-type corpse sprite base frame rather than a generic animation byte. | c | yes: Agent A |
| `JUMPOUT(0x40D410) in MiniMap_RedrawAllTiles` | `direct return after full redraw loop` | compile_fix | Minimap / Control Flow | High | The full redraw loop has already finished when the decompiler jumps to `0x40D410`; the shared asm epilogue only returns, so collapsing the scar to `return;` preserves behavior exactly. | c, asm | yes: Agent D |
| `JUMPOUT(0x48671B) in CRT_InitializeThreadAndFileHandleHooks` | `direct return after hook/TLS initialization` | compile_fix | Runtime / CRT | High | The asm shows `0x48671B` as the shared epilogue immediately after the function installs the CRT hook pointers, initializes TLS state, and stores `g_CrtThreadDataAccessor`, so a direct `return;` is the precise repair. | c, asm | yes: Agent F+G |

## Batch 118 - Port Reinforcement Runtime Wave
| Old Name / Pattern | New Name | Kind | Subsystem | Confidence | Evidence Summary | Sources | Subagent Evidence |
|---|---|---|---|---|---|---|---|
| `Port_IsSupplyReady` | `Port_IsReinforcementReady` | function | Port / Runtime State | High | The getter is only queried by the shipment-collection path and the port-status panel, both of which treat the flag as “reinforcements waiting at the port” rather than generic supply state. | c, asm | yes: Agent C+H |
| `g_PortSupplyUnitTypePool` | `g_PortReinforcementUnitTypePool` | table | Port / Reinforcements | High | The fixed 12-entry table is sampled only by `Port_CollectReinforcementShipment` when it seeds each arriving unit slot, so it is the reinforcement roster rather than generic supply metadata. | c, asm, prior-artifact | yes: Agent C+H |
| `g_PortSupplySpawnRingOffsets` | `g_PortReinforcementSpawnRingOffsets` | table | Port / Reinforcements | High | The 12 offset pairs are only used to probe empty/passable spawn tiles around the 2x2 port footprint for the arriving reinforcement stack. | c, asm, prior-artifact | yes: Agent C+H |
| `PortRuntimeState.next_supply_turn` | `PortRuntimeState.next_reinforcement_turn` | recovered_struct | Port / Runtime State | High | `Port_NewTurn` compares this field against the absolute game turn and raises the ready flag only when the next reinforcement shipment is due. | c, asm, prior-artifact | yes: Agent C+H |
| `PortRuntimeState.supply_ready_flag` | `PortRuntimeState.reinforcement_ready_flag` | recovered_struct | Port / Runtime State | High | The field is set when the shipment arrives, cleared after collection, and consumed only by reinforcement-ready UI plus pickup logic. | c, asm, prior-artifact | yes: Agent C+H |
| `PortRuntimeState.pending_supply_unit_count` | `PortRuntimeState.pending_reinforcement_unit_count` | recovered_struct | Port / Runtime State | High | The field is only used to decide how many random units the port shipment should seed into the spawned reinforcement stack. | c, asm, prior-artifact | yes: Agent C+H |
| `clash95.c-local Sleep prototype` | `platform_sdl.h-owned Sleep declaration only` | sdl_port_fix | SDL / Platform Seam | High | `platform_sdl.h` already owns the declaration and `platform_sdl_runtime.c` owns the SDL-backed implementation, so the duplicate local prototype was stale platform glue leaking into gameplay code. | c, prior-artifact | yes: Agent C+H |

## Batch 119 - Construction Throughput And Licence Gate Wave
| Old Name / Pattern | New Name | Kind | Subsystem | Confidence | Evidence Summary | Sources | Subagent Evidence |
|---|---|---|---|---|---|---|---|
| `Building_AddonWhitelist` | `g_ProductionLicenceSmithsRequiredUnitTypes` | table | Buildings / Production Licences | High | `Building_IsUnitLicenceEligible` scans this unit-id table only when smiths bit `0x10` is absent and rejects matching types, proving it is the smiths prerequisite roster rather than a generic whitelist. | c | yes: Agent E |
| `Building_AddonBlacklist` | `g_ProductionLicenceWorkshopRequiredUnitTypes` | table | Buildings / Production Licences | High | `Building_IsUnitLicenceEligible` returns early once workshop bit `0x04` is present and otherwise rejects matching types from this table, proving it is the workshop prerequisite roster rather than a generic blacklist. | c | yes: Agent E |
| `byte_512B58` | `g_BuilderConstructionProgressPerTurn` | global | Buildings / Construction | High | `Building_NewTurn` adds the literal byte once per garrisoned builder while reducing `construction_turns_remaining`, and the construction-progress dialog divides the same remaining timer by that constant to estimate turns left. | c | yes: Agent E |
| `Unit_CalcRestTurns` | `Building_CalcRemainingConstructionTurns` | function | Buildings / Construction | High | The helper takes a building record, sums builder throughput from garrisoned type-17 slots, and returns remaining construction turns; it never operates on resting units. | c | yes: Agent E |
| `Building_BuyAddon` stale alias callsites | `Building_BuyUnitLicence` | helper | Buildings / Production Licences | High | The implementation body is already named `Building_BuyUnitLicence`; only two callsites and one production-panel state note still carried the obsolete add-on alias despite operating purely on unit-licence purchases. | c, prior-artifact | yes: Agent E |

## Batch 120 - Road Build Bridge Overlay Rename Wave
| Old Name / Pattern | New Name | Kind | Subsystem | Confidence | Evidence Summary | Sources | Subagent Evidence |
|---|---|---|---|---|---|---|---|
| `sub_424020` | `MapTile_HasAlignedBridgeApproachRoadOverlay` | function | Map / Bridge Roads | High | The helper linearly scans the 48-entry `g_BridgeApproachRoadOverlayTileIds` table for the tile overlay id, then accepts only row- or column-aligned matches depending on the table band and finally requires bridge terrain ids `603..610`. | c, asm, prior-artifact | no |
| `sub_424120` | `MapTile_IsBareBridgeCrossingRoadOverlayCandidate` | function | Map / Bridge Roads | High | The helper rejects border tiles, normalizes neighboring village-road overlay families into six orientation classes, requires the center overlay word to be empty, and accepts only bridge terrain ids `603..610` with compatible adjacent road approaches. | c, asm | no |
| `sub_425120` | `RoadBuildMode_HighlightBuildableAdjacentTile` | function | UI / Builder Road Mode | High | The helper only handles the four tiles adjacent to the selected stack, computes the direction-widget screen position for that neighbor, and highlights the widget only when either bridge-road placement or a normal road step is legal. | c, asm | no |
| `sub_4254E0` | `RoadBuildMode_BuildInSelectedDirection` | function | UI / Builder Road Mode | High | The helper maps widget ids `0x1B..0x1E` to compass directions, temporarily clears the modal callback, calls `Road_Build`, restores the highlight callback, and returns to the builder road-mode UI loop. | c, asm | no |
| `sub_425540` | `Builder_StartRoadBuildMode` | function | UI / Builder Road Mode | High | The helper rejects water-surface origin tiles, switches the modal UI state to the road-building panel, installs the adjacent-tile highlight callback, animates the four direction widgets, and dispatches the chosen widget into the direction-build helper. | c, asm | no |

## Batch 121 - Road Build Exit Latch And AI Turn Loop Wave
| Old Name / Pattern | New Name | Kind | Subsystem | Confidence | Evidence Summary | Sources | Subagent Evidence |
|---|---|---|---|---|---|---|---|
| `sub_4250F0` | `RoadBuildMode_RequestExitAfterWidgetPress` | function | UI / Builder Road Mode | High | The asm forces `edx = 1`, calls the pressed-widget animation helper `sub_419E60`, and then stores that `1` into the road-build exit latch, proving the helper is the “flash button, then exit mode” callback. | c, asm | no |
| `sub_425110` | `RoadBuildMode_RequestExit` | function | UI / Builder Road Mode | High | The helper is a single `g_RoadBuildModeExitRequested = 1` store, and the surrounding road-build modal loop exits as soon as that latch becomes non-zero. | c, asm | no |
| `sub_43CD00` | `UnitBattle_RunAiTurnForSide` | function | Battle / AI | High | The only caller is the tactical battle loop that hands it `g_CurrentPlayerIndex` when AI control is active, and the helper advances the battle cycle, selects an AI plan mode, builds the action queue, and executes one side's queued AI actions until the side turn is exhausted or combat resolves. | c, asm | no |
| `dword_514294` | `g_RoadBuildModeMarkerBounceOffsets` | table | UI / Builder Road Mode | High | `RoadBuildMode_HighlightBuildableAdjacentTile` indexes this nine-entry signed sequence with `g_RoadBuildModeAnimationFrameIndex` to offset each directional marker's screen position during the active road-build animation cycle. | c | no |
| `dword_51438C` | `g_RoadBuildModeControlWidgets` | global | UI / Builder Road Mode | High | The symbol is the root address passed into `sub_419DC0`/`sub_419D60` for the road-build modal controls, and the adjacent DGROUP records in asm contain the corresponding road-build button callbacks. | c, asm | no |
| `dword_527C28` | `g_RoadBuildModeLastAnimationTick` | global | UI / Builder Road Mode | High | `Builder_StartRoadBuildMode` compares this global against `Time_Now() - 10` and refreshes it with the current tick each time the marker bounce animation advances. | c | no |
| `dword_527C30` | `g_RoadBuildModeExitRequested` | global | UI / Builder Road Mode | High | Both road-build exit callbacks write this latch, the road-build modal loop runs only while it is zero, and the mode forces it back to `1` before tearing the control widgets down. | c, asm | no |
| `dword_527C38` | `g_RoadBuildModeAnimationFrameIndex` | global | UI / Builder Road Mode | High | The road-build loop advances this global with `((index + 1) & 7)` every 10 ticks and uses it to index the directional marker bounce-offset table. | c | no |
| `g_RoadBuildModeMarkerBounceOffsets[9]` | `RoadBuildModeMarkerBounceOffsetTable` | recovered_struct | UI / Builder Road Mode | High | The signed offset array is indexed by `g_RoadBuildModeAnimationFrameIndex` when the highlight helper positions each of the four directional road-build markers, so it is a dedicated animation-offset table rather than generic UI padding. | c, prior-artifact | no |
| `0x527C28..0x527C38 road-build modal globals` | `RoadBuildModeUiState` | recovered_struct | UI / Builder Road Mode | High | `Builder_StartRoadBuildMode` zeroes the exit latch, advances the last-tick/frame-index pair inside the modal loop, and the highlight helper consumes the same frame-indexed bounce table, proving one coherent road-mode UI state family. | c, prior-artifact | no |
| `JUMPOUT(0x43C853) in sub_43CB80` | `direct return after the candidate-slot scan` | compile_fix | Battle / AI | High | The asm shows `sub_43CB80` jumping to `loc_43C853`, which is only the shared epilogue for the same push/pop set. Replacing the scar with `return;` preserves the exact control flow after the scan finishes. | c, asm | no |

## Batch 122 - Executable Bootstrap Smoke And Render Export Wave
| Old Name / Pattern | New Name | Kind | Subsystem | Confidence | Evidence Summary | Sources | Subagent Evidence |
|---|---|---|---|---|---|---|---|
| `Bootstrap_RunWindowSmokeLoop -> Platform_MainWindowProc root` | `Bootstrap_SmokeWindowProc containment` | boot_fix | Bootstrap / SDL | High | The executable-link containment pass showed the remaining unresolved surface was genuinely live through `Platform_MainWindowProc`. Replacing the smoke target's root window proc with a narrow proc that preserves the recovered `WM_DESTROY` / `WM_PAINT` / `WM_ACTIVATEAPP` message semantics removes that render/runtime band from the first runnable executable without claiming the authentic boot path is rebuilt. | c, prior-artifact | yes: Agent G |
| `missing QueryPerformanceCounter / QueryPerformanceFrequency seam` | `platform_sdl_runtime.c timing wrappers` | sdl_port_fix | SDL / Timing | High | `Time_Now` and the early timing helpers only need a stable performance counter/frequency pair. Adding microsecond-backed host wrappers to the SDL seam keeps those probes owned by the platform layer instead of the recovered gameplay translation unit. | c | no |
| `missing Render_SetResourceHandle body` | `Render_SetResourceHandle` | function | Render / Bootstrap | High | The map exposes the exact symbol and the asm body is a five-instruction leaf: swap `[render_state + 0xCC]` with the requested handle and return the previous value. This was the only truly absent function in the early render-helper family. | c, asm, map | yes: Agent B |
| `missing Render_DrawSprite symbol` | `Render_DrawSprite -> sub_406740` | helper | Render / Bootstrap | High | The map/asm/render-helper pass showed `sub_406740` already contains the body for the exported draw helper while `clash95.c.o` still imports `Render_DrawSprite`. Binding the exported name to the recovered body is a symbol-reconciliation repair, not new behavior. | c, asm, map | yes: Agent B |
| `missing Render_LoadResourceSprite_v2 symbol` | `Render_LoadResourceSprite_v2 -> sub_40AEC0` | helper | Render / Bootstrap | High | `sub_40AEC0` is the exact map-matched body for the exported symbol and only clears `dword_520310`; the missing piece was export ownership, not implementation. | c, asm, map | yes: Agent B |
| `missing Render_LoadResourceSprite_v3 symbol` | `Render_LoadResourceSprite_v3 -> sub_40BD40` | helper | Render / Bootstrap | High | The exported symbol was declaration-only, but the map/asm match shows `sub_40BD40` is the retained body the linker still needs. Binding it under the real symbol preserves the recovered text-height calculation path already present in `clash95.c`. | c, asm, map | yes: Agent B |
| `missing Render_LoadResourceSprite_v4 symbol` | `Render_LoadResourceSprite_v4 -> sub_40C1F0` | helper | Render / Bootstrap | High | The map/asm/render-helper pass matched the full cached font-sprite build/load body to `sub_40C1F0`; the repair here is export binding so later menu/bootstrap code can call the recovered implementation. | c, asm, map | yes: Agent B |
| `missing Render_CreateSprite symbol` | `Render_CreateSprite -> sub_40C5E0` | helper | Render / Bootstrap | High | The map exposes `Render_CreateSprite`, the asm starts at the already recovered `sub_40C5E0`, and `_WinMain@16` calls it on the authentic boot path. Re-exporting that body is the smallest safe step toward the real render bootstrap. | c, asm, map | yes: Agent B |
| `missing Unit_GetSquadCount body` | `Unit_GetSquadCount` | function | Units / Shared Runtime | High | The asm is a tiny leaf that starts at `unit + 6`, scans ten 31-byte squad slots, and stops on the first `-1` type id. Re-emitting that body recovers a high-fan-in unit helper the menu/world/battle code already expects by name. | c, asm, map | yes: Agent B |
| `smoke-bootstrap unresolved helper band` | `compat smoke-launch quarantine helpers` | runtime_fix | Runtime / Bootstrap | Medium | After section GC, the remaining undefineds were still retained through live sections, but the smoke executable never executes that runtime band. Isolating minimal placeholder definitions in `compat/decomp_runtime_stubs.c` makes the first runnable binary possible while keeping the unsafe usercall/hidden-register families explicitly quarantined for later asm-backed recovery. | c, asm, prior-artifact | yes: Agent A, Agent G |

## Batch 123 - Authentic Startup Prelude Repair Wave
| Old Name / Pattern | New Name | Kind | Subsystem | Confidence | Evidence Summary | Sources | Subagent Evidence |
|---|---|---|---|---|---|---|---|
| `Platform_IsWindowsNt4` | `Input_MousePresent` | function | Bootstrap / Input | High | The original map names `0x461740` as `Input_MousePresent`, `_WinMain@16` calls it immediately after `Win_CreateMainWindow`, and the result is stored in `dword_51D018` before the rest of early startup continues. The decompiled NT4-check body is the implementation detail behind that gate, not the semantic role the boot path relies on. | c, asm, map | yes: Agent A |
| `Platform_IsWindows9x` | `Input_MouseAcquire` | function | Bootstrap / Input | High | The map names `0x461770` as `Input_MouseAcquire`, and `_WinMain@16` calls it directly after `Input_MousePresent` before `CSS_SetDirectSoundHWnd`, `DetectGameCDPath`, and `Game_Init`. Keeping the original input-facing name is more accurate than the stale Win9x platform label. | c, asm, map | yes: Agent A |
| `Game_Init` undefined constructor base `v5` | `Game_Init` uses `result + 147174` for `_wcpp_4_ctor_array__` | boot_fix | Bootstrap / Runtime | High | The asm for `Game_Init` allocates `0x8F29E` bytes, then adds `0x23EE6` to the returned base before calling `__wcpp_4_ctor_array__`, and subtracts the same offset afterward when storing `gameData`. Using the decompiler's undefined `v5` was a real startup crash source. | c, asm | no |
| `Mem_Alloc -> nmalloc_(a2, a1)` | `Mem_Alloc -> nmalloc_(a1, 0)` | runtime_fix | Bootstrap / Allocator | High | The asm for `Mem_Alloc` preserves the requested byte count in `eax`, copies it into `edx` only for the later OOM log path, and then calls `_nmalloc_`. The decompiled argument order inverted the semantic size operand and made `Game_Init` fail immediately when `a2 == 0`. | c, asm | yes: Agent B |
| `compat _nmalloc_` zeroing allocation stub | `compat _nmalloc_` raw size-driven `malloc` shim | runtime_fix | Bootstrap / Allocator | High | Agent-B corroboration and the allocator asm both show `_nmalloc_` is a raw custom allocator, not a zeroing helper. Replacing `calloc` with a size-driven `malloc` shim keeps the quarantine closer to the original semantics while the hidden-register allocator family remains unrecovered. | c, asm | yes: Agent B |
| `ShowWindow` lacking activation side effect | `ShowWindow` enqueues `WM_ACTIVATEAPP(1)` | sdl_port_fix | SDL / Window Activation | High | `Platform_MainWindowProc` expects `WM_ACTIVATEAPP` to acquire input, restore lost surfaces, and resume sound. SDL window creation under WSL does not naturally reproduce that Win32 side effect, so the SDL seam now synthesizes the activation event on first show. | c, prior-artifact | yes: Agent C |
| `missing DirectInputCreateA seam` | `DirectInputCreateA` inert failure wrapper in SDL seam | sdl_port_fix | SDL / Input Containment | Medium | `Platform_CreateMainWindow` always calls `InputBackend_Initialize`, but the WSL/SDL runtime should keep legacy DirectInput setup inert instead of pretending to provide native DirectInput devices. Returning failure lets the window boot continue while the recovered input backend remains quarantined behind SDL. | c, prior-artifact | yes: Agent A, Agent C |
| `PIE bootstrap addresses above 32-bit int assumptions` | `clash95_bootstrap` built with `-fno-pie` / `-no-pie` | link_fix | Bootstrap / Build | High | The recovered startup code stores live pointers in 32-bit ints such as `dword_51D020`. For the bootstrap executable, disabling PIE keeps runtime addresses inside the legacy 32-bit assumptions instead of introducing high randomized addresses that would truncate incorrectly. | c, prior-artifact | no |
